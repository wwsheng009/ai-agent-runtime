// Package configwriteguard 承载「配置文件写点必须走共享写锁」的**静态守卫**测试
// （方案 §9.1 U-2 / §12 R4，2026-09-22 第四次增补后的防御性加强）。
//
// 为什么单独成包、且不引用仓库内任何其它包：
//
//  1. 守卫只做源码级 AST 分析，不需要被测代码的类型或符号——独立成包就从结构上保证
//     它不会因为「被测代码怎么组织」而失效；
//  2. 只依赖标准库，使守卫在仓库其它包处于编译失败（并行在途改动）时仍可运行，
//     也让 `go test ./internal/configwriteguard` 成为一条廉价、稳定的独立检查。
//
// 判据与豁免规则见 guard_test.go 的用例注释。
package configwriteguard
