# Multi-Agent Docs

This directory contains the multi-agent documentation migrated from `ai-gateway`.

Migration notes:

- These docs now live with the runtime implementation in `ai-agent-runtime`.
- Historical documents may still mention old paths such as `internal/runtime/*`, `cmd/aicli/*`, or `internal/api/skills/*`.
- In this repository, interpret those references as the matching paths under `backend/` unless the document explicitly marks them as historical.

Suggested entry points:

- `profile/implementation-status.md`
- `profile/aicli_profile_loading_flow.md`
- `agents/plan/refactor_roadmap.md`
- `teams/plan/multi-agent-processing-chain.md`
