# frontend 前端功能与样式优化方案（参考 deepseek-harness）

状态：**执行中**（P0 已完成，其中 P0-1/P0-2 验收在 2026-09-13 复核中发现回退：P0-1 已清理并加门禁，P0-2 已于 2026-09-13 两轮修复并新增行数门禁（> 500 行文件 3 → 0；后随 P2-1A 接线回退 2 个 → 二次拆分归零，口径自此由 `scripts/verify-max-lines.mjs` 看护并并入 `npm run lint`）；P1 进行中：9/10 完成、1 项未开始（P1-5 依赖后端队列/Steering API，硬阻塞）；P2 进行中：P2-1A 已交付 8 项子能力、P2-9 已交付四个子片（子片 1 后台任务常驻状态条、子片 2 子代理树「可折叠到末级 + 只读原因 + 记录跨度」、子片 3 会话目标四相指示 MVP、子片 4 composer 上方浮动「当前任务」Todo 面板）、其余 P2 项未启动）；实施记录见 §9.3，复核与缺口登记见 §9.4，缺口清单见 `docs/plan/frontend-deepseek-harness-gap-list.md`。

日期：2026-09-11

负责范围：`frontend/`（样式体系、工作台交互、设置域、i18n、测试基建）

参考材料：

- 目标项目特性分析：`E:\projects\ai\deepseek-harness\packages\client\*`（22 个 UI 包 + `apps/web` 入口）
- 目标项目设计体系 / 外壳 / 设置 / i18n / 质量保障分析（只读调研）
- 当前项目盘点：`frontend/`（只读调研，行号基于 2026-09-11 工作区快照）

---

## 1. 背景

当前 `frontend/` 已经具备可用的工作台闭环：会话列表 + 消息流（Markdown / 工具行 / 推理行 / 流式）+ artifact / checkpoint / plan 面板 + 轨迹视图 + runtime teams + 15 个后端配置域 + harness 设置。但整体「粗糙」体现在三个层面：

1. **功能域缺件**：缺少工具审批交互、运行时队列、附件与多模态输入、斜杠命令与 `@` 引用、deliverables 行、jobs 弹层、子代理目录、会话搜索、模型采样参数等 Agent UI 的常规能力（当前盘点 §6.3 已逐条确认「无 API、无 UI」）。
2. **样式体系未成型**：`globals.css` 是暗色优先的 690 行变量堆叠（220 条自定义属性），消费侧大量 `text-[var(--foreground)]` 式任意值；`landing.css` 是第二套独立 token 体系；设置卡片家族存在约 16 个近重复组件；硬编码圆角/间距散落。
3. **工程结构老化**：`backend-config-settings-page.tsx`(3932 行)、`workspace-sidebar.tsx`(1560 行)、`lib/workspace-thread-state.ts`(1815 行)、`types/runtime.ts`(1692 行)、i18n 双语资源各 2.5k 行手工同步；`src/**/.backups/` 残留；测试分布不均，无 a11y / 视觉 / 移动端验证，SSE 无自动重连。

`deepseek-harness` 的 Web 客户端（`packages/client/*`）在**同类问题上给出了成体系且可机械校验的答案**：三层设计 token、事件投影式消息管线、折叠谓词化、虚拟化与分页解耦、keyed 插槽式工具视图、schema 化设置与 staging 卡片、类型化 i18n 字典 + AST 硬编码门禁、以语义几何断言为主的 Playwright 质量基建。本方案的目标是把其中**高价值、低耦合**的部分移植到当前项目，而不是照搬其 cordis 插件架构。

---

## 2. 目标与非目标

### 2.1 目标

1. 建立**三层设计 token 体系**（primitive → semantic → 组合/皮肤），让主题/强调色/字号切换只依赖属性翻转与变量覆盖，消灭 `landing.css` 第二套体系与任意值扩散。
2. 把**消息渲染管线**从「行组件里散落 UI 状态」升级为「事件 → 中性节点 → 视图快照」的投影模型，落地可折叠 transcript、折叠谓词、system prompt 行、turn usage 行。
3. 补齐**输入面能力**：附件（拖放 / 粘贴 / 草稿轨 / 灯箱）、斜杠命令、`@` 引用、busy 队列坞与 steering 语义。
4. 补齐**阻塞式交互**：工具审批面板（含取消信号）与提问/计划评审卡，统一走可取消 pending 生命周期。
5. 对**设置域做结构治理**：超长页面拆分为注册表 + 按域懒加载；卡片家族收敛为统一 Card/Panel；引入「暂存 / 丢弃 / 保存」的草稿模型。
6. 建立**可机械执行的样式与 i18n 纪律**：语义 token 白名单、硬编码文案 AST 扫描、双语字典类型对齐。
7. 升级**质量基建**：dist fail-fast、空闲端口探测、失败截图、固定 locale/timezone、语义锚点 + 容差几何断言、golden 往返校验。
8. 以最小 API 增量补齐后端能力对接：**优先「前端接已有端点」**（就绪度矩阵见 §6.3 P2-1）；仅「队列/steering、附件上传、MCP 目录、Artifacts 统一接口、货币成本」五项保留后端前置依赖，其余能力不得以后端缺失为由延后。

### 2.2 非目标

- 不引入 cordis / 插件热插拔 / `ctx.slots` 体系，不复制 boot manifest、`__DSH_BOOT__`、profile 依赖镜像等宿主机制。
- 不引入 `ui-dockkit` 的 split-tree / float / dock 全量操作代数；右栏按当前需求做轻量停靠/全屏。
- 不重写数据层与轨迹 reducer（其乱序缓冲、幂等、终态冻结、序列化回放已有强测试，属于优质资产）。
- 不改变 Runtime HTTP/SSE 协议语义；新增 API 以前端消费为准，服务端缺失的能力进入「依赖项」而非强行前端模拟。
- 不回退、不重复建设既有资产：SSE 常驻重连/seq 续传、桌面通知、会话重命名/侧栏搜索、外观字体字号、轨迹搜索与导出、密度模式等已在库能力（清单见 §5.5）只允许增强或复用，禁止另起一套。
- 不引入账号体系、遥测上报、多标签状态广播、PWA 安装、自研 HMR 帧协议、全局命令面板、通用虚拟滚动基建与本地全文检索引擎（理由见 §8、§6.4）。
- 不照抄平台特定实现：`corner-shape: superellipse`、`0.5px` 发丝在 Chromium 的绘制行为、`::-webkit-scrollbar` 互斥门控需按目标浏览器重新测量。
- 不引入双语文档配对门禁（成本高、收益仅覆盖文档读者），只保留 UI 文案的 i18n 门禁。

---

## 3. 分析方法与证据口径

| 材料 | 范围 | 证据口径 |
|---|---|---|
| 目标项目功能域分析 | `packages/client/*` 22 个包 + 7 条优先交互管线 | 源码直读（标注路径:行），README 契约标注 `[README]` |
| 目标项目设计体系分析 | `ui-theme` / `ui-layout` / `ui-sidebar*` / `ui-dockkit` / `ui-settings*` / `locale` / `apps/web/tests` | 源码与 README 直读；含 e2e 契约清单 |
| 当前项目盘点 | `frontend/src/**`（排除 `.backups/`、`dist/`） | 源码直读；83 个 Vitest 文件 + 5 个 Playwright spec 统计 |
| 当前项目复核（Round 2） | 方案 §5/§6 的全部量化与定性断言 + 既有资产清单 | 逐项 grep/view 复核，产出 `.tmp/frontend-opt-analysis/round2-current-verification.md`；该报告第六节列出未取证项，未取证内容不得当作结论 |
| 目标项目功能点穷举（Round 2） | `apps/web` + `packages/client/*`：启动链路 / 布局侧栏 / 会话列表 / 设置 / 功能包 / 横向交互 | 源码直读 + 负向 grep，产出 `.tmp/frontend-opt-analysis/round2-target-shell-features.md`（含 TOP15、不采纳清单、证据缺口） |
| 后端就绪度审计（Round 2） | 路由全集（`handler.go:637-873`）× 事件常量 × 前端消费面 | 静态审计，产出 `.tmp/frontend-opt-analysis/round2-backend-readiness.md`；14 项能力逐条判定，未做运行时验证 |
| 证据缺口复核（Round 3） | Round 2 报告 C 节 C-1~C-7：侧栏装配 / 设置壳 / 负向结论 / `/export` / `apps/web` 构建 / 全局通知位 / 审批与运行时状态 | 源码直读 + 目录级 grep，产出 `.tmp/frontend-opt-analysis/round3-gap-a.md`、`round3-gap-b.md`；结论与未核验子项见 §11.6 |

> 行数与行号口径：本方案所有「N 行」均按含空行的编辑器口径（等价 `ReadAllLines`/`wc -l`），与 `view` 工具显示行号一致；用 `Measure-Object -Line` 等跳过空行的口径会偏小。

### 3.1 Round 2 复核修正记录（本版相对初版的实质变化）

| # | 初版断言 | 复核结论 | 本版处置 |
|---|---|---|---|
| R1 | P1-8「SSE 自动重连」需从零实现退避与游标回放 | 会话流**已有**常驻重连 + `seq` 游标续传 + 阈值退避；日志流亦有重连状态机 | 改写为 P1-8「连接状态统一与断线恢复收口」，禁止第二套退避（资产 A1/A2，§5.5） |
| R2 | 文件树 / 终端面板一并「不采纳」 | `fs/*` 端点**已就绪**且前端零消费；终端确无 PTY 接口 | 文件树改为采纳（新增 P2-10，只读 + 预览）；终端仍在 §8 不采纳 |
| R3 | 超长文件拆分清单仅列 6 个文件 | 实测 **41 个文件 > 500 行**；最大者 `backend-config-settings-page.tsx`(3932) 与 `workspace-sidebar.tsx`(1560) 未入清单，原「每文件 < 500」验收不可达 | P0-2 重写：首批 15 项 + 四批滚动 + 测试随源；§11.4 给出 41 项完整清单与批次归属 |
| R4 | 后端能力「大面积缺失」（审批/搜索/用量等） | 审批闭环、SSE 重放、子代理控制面、Skills 全族、会话元数据搜索、用量、文件读写**均已就绪**；硬前置收窄为 5 项 | §6.3 P2-1 重写为 A/B/C/D 四档就绪度矩阵；§10 风险表首行同步更新 |
| R5 | 卡片枚举数量与 P0-3 列举不一致 | 以源码枚举为准 | P0-3 固定为 16 个具名枚举 |
| R6 | 侧栏「无会话搜索、无重命名」 | 实为**已有**（会话名搜索 + 重命名） | 记为资产 A13；P1-9/P2-6 明确为「扩展」而非新建 |
| R7 | 「消息全量平铺」等定性描述 | 部分准确（context 卡已有折叠） | 表述收敛为「无 Turn 级折叠谓词」，避免以偏概全 |

> 未取证项（11 项，如 `maxSteps≤20` 具体 clamp、桌面通知触发条件等）仍以 `round2-current-verification.md` 第六节为准，未取证内容不得作为结论引用。

> 目标项目侧证据缺口：**Round 3 已 7/7 复核完毕**（不影响任务划分，仅影响实现细节取值）。关键修正：C-1 侧栏几何实际归 `ui-layout`（`columns.ts:13-19`），列表入口在 `ui-workspace` `WorkspaceBrowser`；C-2 设置 SHELL 属 `ui-settings-general`，契约槽位实为 8 个（非六类）；C-3 目录级复核确认多选/未读/置顶均不存在；C-4 `/export` 执行在 `session-log-export` 包（含子会话与附件、无内容级脱敏）；C-5 `apps/web` 为 `base:'./'`、无 proxy/HMR 配置、PWA 静态元数据齐备；C-6 全局通知位仅槽位声明、**零实现**；C-7 审批卡与 pending 注册表已核验，目标项目无审批超时态。完整证据与未核验子项见 **§11.6**。

---

## 4. 目标项目剖析（deepseek-harness）

### 4.1 架构分层（可作为「边界思想」参考）

- **22 个 UI 包按功能域切分**：`ui-chat`（transcript 投影 + 滚动）、`ui-conversation`（输入机、队列、装配管线）、`ui-trajectory`、`ui-tool`、`ui-primitives`、`ui-commands`、`ui-input-trigger`、`ui-attachment`、`ui-user-questions`、`ui-approval`、`ui-goal`、`ui-jobs`、`ui-subagent`、`ui-model-selection`、`ui-agent-preset`、`ui-skill`、`ui-message-feedback`、`ui-deliverables`、`ui-directory-picker-{browse,native}`、`ui-workflow-run`、`store`/`resources`。
- **契约层独立**：`src/client/contract/*.ts` 承担声明合并 map、Session Controller 类型派生（如 `QueueItemId`）、slots 契约；实现集中在 `{conversation,input,queue,skeleton}`。
- **权威边界原则**：Runtime/Host/Agent 拥有配对、生命周期、delivery window；客户端只做投影与乐观显示，权威记录到达时**原子替换**（本地输入立即显示、权威到达消失）。
- **不采纳部分**：`ctx.slots.register/provide/inject` 四 shares 体系、`ctx` 不进组件、业务包零 React context 等约束，仅在「多插件动态装配 + HMR」场景才划算；当前项目用路由 + 组合式组件 + 常规状态库即可。

### 4.2 核心交互管线（本方案主要移植来源）

**① 消息渲染（ui-chat / ui-conversation）**

- 位置层级 `ConversationLocation = session | turn | step | unresolved`，`Turn/Step.status ∈ open|closed|unknown`，各带 `start/end` 事件。
- Definition 把事件匹配成中性节点 `ConversationViewNode {key, kind, id, target, data}`，Chat/Trajectory 各自取快照。
- **折叠策略**（Compact 默认）：开启的 Turn 内 Context injection / reasoning / Assistant / Tool / Retry 保持展开，`turn/end` 后折叠；final-answer 边界 = 最新 Step 含非空文本/图片且**不含工具调用**；无 final answer 的 closed Turn 保全过程证据可见；折叠控件汇总 Turn 级持久计数（工具、带回复消息、子代理，零值省略、工具与子代理互斥），全零显示 `Thought for a while`。
- `system/message` 永不渲染为普通消息，而是请求前折叠的 `System prompt` 行；Turn token usage 行不完整即整行隐藏。
- **滚动所有权**：跨 prepend/remount 恢复语义锚点；贴底时 `ResizeObserver` 跟随新底线；离开底部后流高变化保顶、以 reading-line 几何选 active Turn。

**② Markdown 增量渲染（ui-primitives）**

- 前缀冻结：除尾部 `UNSTABLE_TAIL_BLOCKS = 2` 块外全部冻结，切割点取最后一个冻结块的 end offset，切片逐字一致；未闭合顶层 fence 走「最后完整行 + partial 行」第二前沿。
- 渲染 key = 块在全文中的绝对起始 offset（跨冻结边界稳定，React reconcile 而非 remount）；非追加输入使 `generation` 递增，调用方按代丢弃缓存。
- 引用式链接/脚注有已知偏差，最终 settled 全量解析自愈——**把偏差与自愈路径写进注释**是该项目的工程习惯。
- 高亮：单例 `IntersectionObserver` 文档级复用，元素首次相交即激活并永久离开观察集；`supportsHighlighting(lang)` 前置门。
- 安全默认：GFM + KaTeX，禁用 raw HTML、相对链接与不安全协议，仅绝对 HTTP(S) 图片直出。

**③ 工具行与工具专属视图（ui-tool）**

- `tool.call.toolview` 按 wire tool 名 keyed 注册；业务包只注册「工具名 → 组件」，**不配对事件、不重建 transcript、不拥有 root/subcall 拓扑**。
- 行状态机：`expandable = bodyRaw != null || outputText !== null || card !== null`；失败态**替换**摘要而非追加（`errorSummary`）；diff 折叠行携带 `+A -R`；失败时禁用文件链接。
- 嵌套可交互元素（文件链接）必须处理点击与键盘冒泡，否则行级展开吞掉链接；行根带 `data-variant/data-tool/data-state`，并以 visually-hidden 文本播报状态。

**④ Composer：触发器 / 命令 / 附件（ui-input-trigger / ui-commands / ui-attachment）**

- 光标处 `/`、`@` 打开分组候选菜单，支持键盘与指针、drill-down、单组 launcher；`aria-activedescendant` 高亮；Tab 作用于补全（`drill` vs `pick`），无高亮时原样放行保原生焦点遍历。
- 命令解析显式四分类：`leadingInput | popupSelect | action | execute`，**命令行永不静默降级为普通 prompt**；客户端与宿主命令同名「fail loudly」。
- 附件：有序草稿轨 + 全视口拖放邀请 + 消息图片按数量定尺寸 + 工具画廊 + 原图灯箱；数据/上传状态全部由 slot owner 提供，组件只渲染。

**⑤ 队列与 Steering（ui-conversation / ui-chat）**

- `resolveSubmitMode(preferred, running, gesture, steeringAvailable)`：非运行或不支持 steering → queue；否则 plain Enter/主发送按钮走用户偏好，Cmd/Ctrl chord 取反。
- steering 身份从事件溯源重建：重放 `agent/inbox/spliced`，只有 `next-step` 且 `source.kind === 'user'` 的 `user/message` 才算持久人类 steering。
- 队列帧是 wire 数据，**无 ref 的 image block 直接跳过**；缩略图加载失败保留空占位（读错误由耐久 transcript 呈现）。

**⑥ 审批 / 提问（ui-approval / ui-user-questions）**

- 统一生命周期：Remote Event waterfall 消费 → 注册 pending interaction → `await pending.result`；`PendingApproval(sessionId, {toolName, callId?, reason?, signal?})`，`delegate-on-remove` 保证会话取消/断开时安全收敛。
- 提问采用「单 entry 两 shape」：声明 `presentation intent` 的请求走自身 surface（如 `plan-review` 计划决策卡），其余走 generic question flow——刻意避免多 entry 竞争同一 carrier。

**⑦ 轨迹（ui-trajectory）**

- 纯投影：自有 Definition 从共享 Session 窗口组装业务记录，**既不读也不改 Chat 快照**。
- 分页 + 虚拟化解耦：初始取「挂载时 tail 结尾的 50 个目标 Node」，后续扩窗不驱逐前缀；虚拟化只挂载可见行 + 小 overscan；request-only 分隔符共享下一个可测量虚拟项；语义 key 与 ARIA index 跨 prepend 存活。
- 时间线总览：固定 Overview 投影真实 start/duration，Assistant span 拆 TTFT/decoding，hover 显示精确时钟，拖拽选中区间聚焦账本，wheel 缩放、右键清除/平移。

**⑧ 其余功能域速览（完整清单见目标项目分析报告 §2）**

| 域 | 关键契约 |
|---|---|
| ui-goal | composer 上下文栈第二卡；armed→pause、paused→resume；每个耐久 `/goal` run 渲染为右对齐命令气泡，重载可由 run 重建 |
| ui-jobs | 会话头动作 + 弹层；live 行按 `startedAt` 升序、settled 按 `finishedAt` 降序；elapsed 每秒 tick；只读投影、零 RPC |
| ui-subagent | 会话头 lineage 面包屑 + 后代目录；`{parentSessionId, childSessionId, mode}` 精确地址；独立 Stop；损坏行可读但 disabled |
| ui-model-selection | `/model` 弹窗与 composer 常驻座位共享同一「按 provider 分组」的每会话目录 |
| ui-agent-preset | 预设「创建即固定」，选择/默认只影响后续会话；broken 行保留定位/删除用于修复 |
| ui-skill | `/` 技能源与宿主命令冲突时宿主优先；落定 `Instructions` 卡内容不随目录漂移 |
| ui-message-feedback | Like/Dislike 为 log-only Session 事件，永不进入模型上下文；每 Session 单实例反馈面 |
| ui-deliverables | 回合结束 Files changed 行来自**成功文件变更工具结果**而非模型散文；CSS 容器查询实现响应式，无 JS 布局观测 |
| ui-workflow-run | run→phase→member 逐级展开；层级展开默认值按状态区分；可导航子会话需多重前置校验，宁 disabled 不误开 |
| store / resources | 无 React 快照存储；`dsh-resource://` 地址化读取，使「谁拥有数据」与「谁展示数据」分离 |

### 4.3 设计体系（ui-theme / ui-layout）

**三层 token**

| 层 | 内容 | 说明 |
|---|---|---|
| L0 基座 | `--dsw-font-family`、`--ds-font-family-code`、缓动与三档时长 | 组合型字体变量需要可解析；代码字体栈刻意不带裸 `monospace` 尾巴，避免 Windows CJK 回退 SimSun |
| L1 static 原色阶 | `--dsw-static-*`（amber/blue/deepseek/green/neutral/neutral-bluish/red，50–1000 步进） | 与主题无关；暗色段重复声明属导出噪音 |
| L2 semantic alias | `--dsw-alias-*`（bg/label/border/interactive/brand/button/state/markdown/code/tooltip/specific-*） | 亮色与暗色各一份；feature 组件**只允许消费这一层** |
| L3 组合与皮肤 | 字体五元组与简写、`--dsw-shadow-lv*`、`--dsw-elevation-*`、滚动条、高亮 | 每个字体角色同时给 `font-family/weight/line-height/font-size` 与 `font` 简写；字号与行高成对 |

**关键机制**

- **主题解析与应用分离**：解析发生在 `ui-theme`（light/dark/system → 不可变 `ThemeSnapshot`）；应用在 `ThemePresenter`：`colorScheme` → `documentElement.style.colorScheme`、`dark` → `body[data-ds-dark-theme]`、字号 → `body` 的 `--dsh-content-font-size`，其余 token 逐个写成 body 内联变量，并且**只回收自己写过的变量名**，`dispose()` 逆向撤销。绝不 `body.className = ...` 式覆写。
- **首屏防闪烁**：host 把已注册主题设置嵌进 index 响应，loading 页渲染前浏览器即设置 `color-scheme` / 深色属性 / 字号。
- **消费纪律**：feature 组件禁止复制 static 值、禁止写死颜色、禁止在 feature CSS 写主题选择器；新增共享 token 必须回到 owner sheet，且「primitive 步进 + semantic 别名」同批落地。
- **运行期约束**：内置 token 只覆盖一个白名单集合，超过即报错训练消费纪律；feature 样式表禁止 import token 实现文件。

**组件表现契约（可直接写进规范与 lint）**

- 浮层（菜单/popover/modal/panel/浮动按钮/composer）一律 `border:0` + 单条 elevation shadow；0.5px 发丝是 shadow 第一层，可用 `--dsw-elevation-stroke-color` 每表面重绑或抑制；**禁止** alias-border 与 elevation shadow 并用；状态色边框（如 warn 面板）保留真 border。
- 中性 border 一律 0.5px 发丝；满圆（50%/100%/pill）必须配对 `corner-shape: round`。
- 链接：`font-weight:500`、静止无下划线、hover 点状下划线。
- 滚动条全局单点拥有，暴露 `--scrollbar-thumb*` 变量供提升表面重绑；若同时支持 `scrollbar-width/color` 与 `::-webkit-scrollbar`，必须互斥门控。
- 外壳：`sidebar | center | rightbar` 三轨 grid；右栏是**轨道（track）不是盒子**，占位者上报 `shown/track/fullscreen`；列宽求解顺序为「右栏先缩 → 丢掉整条右轨 → 中间列才可跌破 400px」，左栏永不退让（由响应式折叠的有效偏好决定）；拖拽手柄用 pointer capture + 按帧合并 dx。

### 4.4 设置、表单与 i18n

- **schema 化设置服务**：`rehydrate()/validate()/nodeAtPath()/getPath()` + 不可变草稿克隆；设置 UI 本身也是槽位体系的一部分。
- **卡片模型（staging / discard / save）**：编辑先 staging，只有 save 写文档；discard 不触碰文档；reset 回落组合默认值；非数字草稿拒绝保存；错误在写入前给出。
- **Onboarding**：key 只写不读；configured 状态无需重启即可观测；已配置的重载绝不再画 takeover chrome。
- **i18n**：中文 key 集是**真源**，其它语言 `satisfies Record<Key,string>` 编译期对齐；namespace 按 feature 归属并做类型合并；`verify-client-ui-i18n.ts` 用 TS AST 扫描 JSX 文本与 copy 型属性（`alt/aria-*/label/placeholder/title/...`）拒绝硬编码，并设最小扫描文件数防止口径缩水；内部匹配用 discriminant / 稳定 id，绝不用本地化文本。

### 4.5 质量保障（apps/web/tests，约 108 个 e2e 文件）

- **组织形式**：vitest 组织 + Playwright(chromium) **直打构建产物**；`DIST_INDEX` 缺失即 fail-fast 提示先 build；`probeFreePort()` 申请空闲端口给真实服务；`scaffold.ts` 负责 seed session / host 配置 / 页面启动。
- **断言方法**：优先 `getByRole` + accessible name；状态锚点用 data 属性；几何断言带容差（原生 scroll 距离/锚点 top ±2px，响应式回流 ±32px）；断言「语义行位置、底部归属、真实滚动宿主」而非 DOM 基数。
- **golden 往返**：几何/可见性写成 `expected/**/*.expected.md`；测试同时校验「运行读取的 fixture 集合 == 提交的 fixture 集合」。
- **失败证据与环境控制**：失败写全页截图（gitignored `.artifacts/`）；固定视口、locale、`timezoneId`；页面已死也不能吞掉真正的断言错误。
- **契约清单覆盖面**：设置 chrome/主题/字号/语言持久化、右栏与 dock（含指针链、全屏、per-session 表面隔离）、轨迹虚拟化、长会话滚动契约（分页/流式/恢复/非滚轮/fling）、composer 几何与草稿滚动、模型设置、插件配置、Onboarding。

### 4.6 外壳与功能包（Round 2 补充，为本版新增任务的直接参照）

§4.1–§4.5 覆盖核心交互与设计/设置/质量体系；以下链路是本版新增任务（P1-9/P1-10、P2-6~P2-11）的参照，证据取自 `round2-target-shell-features.md`（TOP15 + 功能点表）。

| 领域 | 目标项目实现（证据） | 可移植要点 | 本方案任务 |
|---|---|---|---|
| 启动链路 | `web/src/boot-page.ts:17-45,68-70`（框架无关加载页 + 失败报告）、`boot-client.ts:41-60`（逐插件加载状态 + 完整性审计） | 失败可见性不依赖框架；「未完整加载不得进入可用态」 | P1-10 |
| 错误边界 | `ui-renderer/src/client/scoped-slots.tsx:325-333,672-682`（槽位级边界） | 全局/路由/面板三层；单面板崩溃不拖垮外壳 | P1-10 |
| 会话列表 | `ui-workspace/src/client/tree.ts:108,173,231`、`rows/WorkspaceBrowser.tsx:379-424,557-562`、`locales.ts:43-102`（状态/归档/删除/Fork/双排序/拖拽写回/搜索降级） | 排序模式正交切换；乐观更新 + 失败恢复；等待态指示 | P1-9、P2-6 |
| 命令系统 | `ui-commands/src/client/{PopupSelectView.tsx:50,service.ts:411-412}`（输入触发 + 命令服务 + 错误隔离） | 命令是「输入触发」而非全局面板；单命令失败不影响输入 | P2-7 |
| 交付物 | `ui-deliverables/src/client/locales.ts:7-45`（宿主动作 + 能力降级 + opening/error/retry） | 浏览器能力与宿主能力分层；无宿主则隐藏而非报错 | P2-8 |
| 任务与子代理 | `ui-jobs/src/client/locales.ts:4-8`（状态条）、`ui-subagent/src/client/locales.ts:7-60`（会话树 + 只读态解释） | 只读投影；「为何不能继续输入」必须说清 | P2-9 |
| 会话目标 | `ui-goal/src/client/locales.ts:4-16`（四相：进行中/暂停/完成/受限） | 状态机四相 + 目标文本；暂停/恢复/完成入口 | P2-9（后端暴露见 §6.3 P2-1B） |
| 文件树 | `ui-sidebar-files/src/client/locales.ts:22-50`（错误分类/截断/noWorkspace/reload） | 错误分类各有可行动文案；截断可见 | P2-10 |
| 右栏与资源 | `ui-sidebar-right/src/client/index.ts:17-21,41-44`（两段式注册）、`resources/src/client/contract.ts:35-70`（资源模型） | 标签注册表 + 统一「打开资源」 | P2-11 |
| 三栏几何 | `ui-layout/src/client/columns.ts:11-29,43-44`（1024 自动折叠 + 手动覆盖双状态） | 自动与手动两态分离、可记忆 | P2-3 |

> 未读/置顶/多选、全局命令面板、多窗口、桌面原生账号与遥测等按 §8 处理；目标项目侧证据缺口见 §3.1 注。

---

## 5. 当前项目 frontend 现状与差距

### 5.1 现状摘要（详见当前项目盘点报告）

- **技术栈**：React 19.2 + react-router-dom 7.13 + TS 5.9 + Vite 8 + Tailwind v4（`@tailwindcss/vite`）；Vitest 4.1 + jsdom；Playwright 1.62；react-markdown 10 + remark-gfm/breaks；PrismJS；recharts；i18next 26。
- **路由**：`/`（落地页）、`/logs`、`/usage*`、`/analytics`、`/runtime/config`、`/workspace/*`，全部 lazy + Suspense。
- **工作台**：`pages/workspace-page.tsx`(327) 纯组合层，向 `WorkspaceShell` 传 **68 个 props**（唯一属性名去重后仍为 68）；会话/线程/回溯/流式/轨迹恢复/团队/目录各有独立 hook。
- **数据层**：`api/runtime/*`（sessions/teams/config/agent-chat/harness/analytics/cache/logs/siteaccount）+ `types/runtime.ts`(1692)；SSE `consumeSseResponse` 带 AbortController 与 15s 软超时；错误统一 `RuntimeApiError` 与租约冲突映射；轨迹 `trajectory-reducer.ts`(831) 有乱序缓冲/幂等/终态冻结/seq 空洞推进与 golden 对拍。
- **样式**：`globals.css`(690 行、220 变量、暗色优先) + `landing.css`(26 变量、独立动效) 双体系；`src/components/ui/*` 基础件（cva/clsx/tailwind-merge）；设置卡片家族约 16 个近重复组件。
- **测试**：Vitest 83 个文件（设置 27、workspace 组件 13、hooks 12、lib/trajectory 8、lib 5、pages 5、api 3、其余 4）；Playwright 仅 5 个 spec / 17 个 test。
- **已具备、但此前方案未计入的资产**（Round 2 复核新增）：桌面通知（`use-workspace-agent-chat-turn.ts:674,1054`）、会话重命名与会话名搜索（`workspace-sidebar.tsx:620,724`）、外观字体/字号（`appearance-settings-page.tsx:495-519`）、轨迹搜索（`lib/trajectory/trajectory-search-index.ts`）、会话运行时流常驻重连（`hooks/workspace/use-session-runtime-stream.ts`）、日志流连接状态机（`hooks/use-runtime-logs.ts`）。保留要求见 §5.5。

### 5.2 功能域差距矩阵

| 功能域 | 当前 frontend | 参考做法（deepseek-harness） | 优先级 |
|---|---|---|---|
| 工具审批（approve/deny） | 无 UI；`approval_requested/resolved` 仅作轨迹单行文本（后端应答通道 `runtime/commands{approve_tool}` 与 pending 查询已就绪，见 §6.3） | 审批面板经 Remote waterfall 注册 pending interaction，带 `signal` 与 delegate-on-remove | P1 |
| 提问 / 计划评审 | 仅 plan mode 三选一面板（approve/request_changes/quit） | 单 entry 两 shape：`plan-review` 计划决策卡 + generic question flow，草稿存储与租约类型 | P1 |
| 消息折叠 | Turn 级无折叠（仅 context 卡内部可折叠）；无折叠谓词、无折叠摘要、无 System prompt 行、无 turn usage 行 | 终局边界谓词 + 计数摘要 + 稳定 seat；`system/message` 折叠行 | P1 |
| 流式 Markdown | `splitStreamingMarkdown` / 打字机逐字显示；每 chunk 语义未冻结 | 前缀冻结 + 尾块不稳 + fence 双前沿 + 绝对 offset key + 视口懒高亮 | P1 |
| 滚动所有权 | `useTypewriter` + 容器滚动；无锚点恢复契约 | 语义锚点恢复 + ResizeObserver 跟随 + reading-line 选 active Turn | P1 |
| Composer 输入面 | textarea + provider/model/reasoning 选择；无附件、无命令、无引用 | 输入机 + 块/装饰模型 + `/`、`@` 触发器 + 四分类命令派发 + 附件轨 | P1 |
| 队列 / steering | 无（后端亦无排队/插话实现，需新接口） | QueueDock + busy Enter 偏好 + 事件溯源重建 steering 身份 | P1 |
| 工具行 | `message-tool-row.tsx` 单组件；无工具专属卡、无失败替换摘要规则 | keyed `tool.call.toolview` + 状态机 + diff 统计 + visually-hidden 播报 | P1 |
| 轨迹 | 已有虚拟行/搜索/详情/reducer（较厚） | 纯投影 + tail 50 节点分页 + 扩窗不驱逐 + 时间线总览双向联动 | P2 |
| Artifacts | 面板数据来自消息富内容与 checkpoint diff；无 `artifacts` API（后端无 `/artifacts` 路由；MVP 可由 checkpoint list/files/preview + `fs/read-file` + metadata 聚合） | deliverables 行来自成功变更工具结果；正文内联文件链接 | P2 |
| 后台任务 | 无（后端 `/background/jobs` 五端点 + 四类事件已就绪，前端零消费） | 会话头动作 + jobs 弹层（只读投影、零 RPC） | P2 |
| 子代理 | runtime-teams 有队友/任务视图（后端 agents spawn/wait/events/input/close/resume + mailbox 已就绪） | 会话头 lineage 面包屑 + 后代目录 + 独立 Stop + `@` 引用源 | P2 |
| 技能 / MCP | 无（后端 skills 全族路由已就绪；MCP 仅 reload，无目录/schema 接口） | `/` 技能源；插件配置卡片 + 花名册 | P2（技能市场在 P2-1A 内交付；MCP 目录属 P2-1C） |
| 消息反馈 | 无 | Like/Dislike → 反馈弹窗；log-only 事件；本期只做命令入口（P2-7），不做独立反馈面板 | P2（收敛） |
| 模型选择 | provider/model/reasoning-effort 选择器 | `/model` 弹窗 + composer 常驻座位共享同一目录 | P2 |
| 模型采样参数 | 仅有 reasoningEffort + maxSteps（0=不限、clamp ≤20）；temperature/max_tokens 存在于配置文档但 `/models` 不暴露，`top_p` 后端无字段 | schema 化设置 + 暂存/校验后再写 | P2 |
| 会话搜索 / 分享 | 侧栏有会话名搜索 + 重命名；轨迹有增量倒排搜索与 JSONL/golden 导出；无跨会话内容检索（后端 `/sessions/search` 仅元数据过滤） | 选择/搜索覆盖可见窗口（轨迹面） | P2 |
| 成本 / 预算 | analytics 页有 token/缓存统计；后端 `/usage/*` 未消费；账本无 cost/价格字段 | turn usage 行 + inspector 用时/用量 | P2 |
| 文件树 / 终端 | workspace 目录注册/增删已具备（含目录/会话对话框）；后端 `fs/read-file|write-file|append-file` 未消费；无终端 | 目标项目有文件树（含错误分类/截断提示）与文档预览（PDF），无终端 | 文件树 P2（`fs/*` 已就绪）；终端 不采纳 |
| 键盘命令面板 | 无（目标项目亦无全局面板） | `/` 命令 + `@` 触发；内置 `/export` 等命令可作首批 | P2 |
| 连接状态 / 断线恢复 | 会话运行时流与日志流各自内建重连（§5.4），**缺**统一状态呈现、手动重试、直连 `/api/agent/chat` 流恢复 | 连接状态呈现件 + 重试入口 + 恢复幂等 | P1 |
| 会话列表组织与状态 | 侧栏有目录分组/重命名/会话名搜索；**无**运行/等待状态指示、无排序模式切换、无拖拽重排/跨组、无归档与 Fork | 行状态指示 / 归档 / 非破坏删除 / Fork → **P1-9**；分组视图 / 双排序 / 拖拽写回 / 组内折叠 → **P2-6** | P1-9 + P2-6 |
| 全局错误边界与启动失败面 | 无 ErrorBoundary/`componentDidCatch`；lazy chunk 失败白屏；`#root` 缺失无提示 | 全局 + 路由级 + 面板级三层边界；boot 失败报告 | P1 |
| 交付物（deliverables）呈现 | 无独立视图；变更文件信息散落在工具行（且行级字段未进事件白名单） | 回合结束 Files changed 行 + 宿主能力降级（预览/下载，无宿主则隐藏原生动作） | P2 |
| 可观测增补（子代理树 / 任务状态条 / traces） | runtime-teams 已有队友/任务视图；后台任务与 traces/diagnostics 端点前端零消费 | 子代理树 + jobs 状态条与弹层 + （可选）traces 查看器 | P2 |
| 会话目标（goal） | 无 UI；后端已有 goal 运行时工具（`aicli_goal` Get/Update）与四相状态（active/paused/complete/budget-limited），但未暴露 REST/快照；CLI 有 `/goal` | 目标指示（四相 + 目标文本）与暂停/恢复/完成入口；快照字段就绪前以轨迹只读派生 | P2 |
| 右栏面板架构 | 散装面板（artifact/checkpoint/详情直接挂载，资产 A7）；无标签注册表、无统一「打开资源」入口 | 标签注册表（显示/切换/全屏/tab 记忆）+ 统一资源打开入口 | P2 |

### 5.3 样式与设计系统差距

| 维度 | 当前 | 目标 | 优先级 |
|---|---|---|---|
| token 分层 | 单层变量堆叠（`:root` 暗色默认 + `html[data-theme=light]` 覆盖 + accent-tone 覆盖） | L1 static / L2 semantic / L3 组合三层，feature 只消费 semantic | P0 |
| 消费方式 | Tailwind 任意值 `text-[var(--foreground)]`、`[background:var(--workspace-shell-bg)]` | 语义主题类（`@theme` 内 `--color-*` 映射）或受限语义工具类 | P0 |
| 第二套体系 | `landing.css` 26 变量 + 独立 keyframes/类名 | 并入同一 token 体系，仅保留页面级布局类 | P0 |
| 组件复用 | 16 个近重复卡片组件；artifact/checkpoint 各自实现 | 统一 Card/Panel/Section 抽象 + 变体枚举 | P0 |
| 表现契约 | 无浮层/边框/链接/滚动条规范 | elevation-only 浮层、0.5px 发丝、滚动条单点拥有 | P1 |
| 主题应用 | `applyDocumentSettings` 写 `<html>` 属性，逻辑与主题解析耦合在 bootstrap | 解析/应用分离 + 幂等写入 + retraction set + dispose 逆操作 | P1 |
| 密度模式 | 已有 comfortable/compact（composer 与 shell 已接线） | 保留密度开关，token/字号治理不得写死单一密度 | P0（治理前置） |
| 硬编码值 | `rounded-[0.7rem]`、`rounded-[0.9rem]`、字号默认散落 | 圆角/字号收敛到 token 或局部组件契约 | P0 |
| a11y | 零散 `ariaLabel`；无 focus-trap 断言；`index.html lang="en"` | visually-hidden 播报、`aria-activedescendant`、ARIA index 跨 prepend 存活、lang 与语言同步 | P1 |

### 5.4 工程与质量差距

| 维度 | 当前 | 目标 | 优先级 |
|---|---|---|---|
| 超长文件 | **41 个文件 > 500 行**；Top：3932（backend-config-settings-page）/ 2595 / 2536（i18n 双语字典）/ 1815（workspace-thread-state）/ 1692（types/runtime）/ 1560（workspace-sidebar）/ 1358（provider-groups 域编辑器）/ 1337（agent-chat-turn hook）；完整清单见 §11.4 | 按域拆分 + 注册表 + 懒加载，分批推进至单文件 < 500 行 | P0（首批）→ P1/P2（后续批次） |
| 目录卫生 | **8 个 `.backups/` 目录、20 个 `.bak` 文件**（pages 6、components/workspace 5、i18n/resources 3、api/runtime 2、src 根与 components/ui、hooks/workspace、styles 各 1） | 清理并加 `.gitignore` 规则 / 禁止提交 | P0 |
| i18n | 双语文案单文件 2.5k 行手工同步 | zh 为 key 真源 + en 编译期 `satisfies`；AST 硬编码扫描 | P0 |
| 设置域测试 | 15 个域编辑器里 14 个无组件测试 | 每域至少 1 个组件/契约测试 + staging/save 行为测试 | P1 |
| e2e 覆盖 | 5 spec / 17 test，含调试用 `diag.spec.ts`；无 a11y/视觉/移动端 | 语义锚点 + 容差几何 + golden 往返；覆盖滚动/主题/composer/审批 | P1 |
| 测试基建 | 直接 launch dev/preview；无 dist fail-fast、无空闲端口探测、无失败截图 | 三件套 + 固定 locale/timezone | P0 |
| 断线恢复 | 会话流已有常驻重连 + seq 游标续传与阈值退避（`use-session-runtime-stream.ts:91-242`）；日志流有重连状态机与状态标签；**缺**统一连接状态 UI、手动重试入口、直连 `/api/agent/chat` 流恢复 | 连接状态可见化 + 手动重试 + 恢复幂等；不重复实现退避 | P1 |
| 类型组织 | 单一 `types/runtime.ts`(1692) | 按域拆分 + barrel re-export，保持对外 import 稳定 | P0 |
| 性能预算 | 无 | 长会话滚动 e2e + 增量渲染基准（可选） | P2 |

### 5.5 既有资产保留清单（不得重复建设或回退）

> Round 2 复核结论。P0/P1/P2 任何任务若与下表重叠，必须**复用/增强**现有实现，补幂等与回归断言；禁止并行实现第二套机制。未取证项（个别设置页交互细节）不在此列，见 `round2-current-verification.md` 第六节。

| # | 资产 | 证据（当前项目） | 对方案任务的约束 |
|---|---|---|---|
| A1 | 会话运行时流常驻重连 + seq 游标续传 + 退避阈值 | `hooks/workspace/use-session-runtime-stream.ts:91-242` | P1-8 仅做状态 UI/手动重试/直连 chat 流恢复；禁止另写退避循环（幂等断言：恢复不重复渲染已消费 delta） |
| A2 | 日志流重连状态机 + 连接状态标签 | `hooks/use-runtime-logs.ts`、`pages/logs-page/connection.tsx` / `logs-header.tsx`（P0-2 拆分后；原 `logs-page.tsx:132-166,301-303` 引用已失效） | P1-8 复用同一状态呈现与文案规范 |
| A3 | 轨迹搜索：增量倒排索引、AND 语义、节流重建、与筛选叠加 | `lib/trajectory/trajectory-search-index.ts`、`trajectory-view*.tsx` | P2-1「会话搜索」限定为**侧栏会话级 + 服务端内容检索**；不重写轨迹搜索 |
| A4 | 轨迹导出 / golden 对拍基建 | `lib/trajectory/{export,golden}.ts` 及测试 | P2-5 视觉 golden 前先盘点既有往返测试，避免重复建设 |
| A5 | 会话级 reasoning effort 选择（composer + 会话 context 投影） | `lib/reasoning-effort.ts`、composer 选择器（HEAD commit 即此功能） | P2-2 采样参数草稿模型不得回退该交互 |
| A6 | provider/model 目录同步与运行时模型目录 | `hooks/workspace/use-runtime-model-catalog.ts`、`lib/runtime-model-catalog-sync.ts`、`api/runtime/models.ts` | P2-1「模型目录扩展字段」在此基础增量，不新建目录源 |
| A7 | Artifact 三 surface（artifacts/checkpoints/plan）+ 详情对话框 + 消息元数据派生 | `components/workspace/artifact-panel*.tsx`、`lib/workspace-artifacts.ts`、`lib/workspace-thread-state.ts:1623-1696` | P2-1 新接口就绪前保留派生路径为降级/兼容层 |
| A8 | 回溯（message-backtrack + checkpoint 恢复） | `hooks/workspace/use-session-backtrack.ts`、checkpoint 恢复链路 | 折叠/投影改造须在回溯后强制重算（§10 风险同源） |
| A9 | Runtime teams 派发控制台/详情/console | `components/workspace/runtime-teams*`、`use-runtime-team-dispatch.ts` | P2-1 子代理 lineage **扩展**该视图而非替换 |
| A10 | workspace 目录注册/增删对话框 | `api/runtime/workspace-directories.ts`、目录对话框组件 | P2-3 窄屏改造须覆盖这两个对话框 |
| A11 | 设置页家族：外观/聊天/本地化/通知/关于 + harness | `components/workspace/settings/*-settings-page.tsx` | P2-2 治理范围覆盖**全部设置页**，不止 15 个 runtime 域 |
| A12 | 桌面通知（会话完成/失败，含可见性判断与 tag） | `hooks/workspace/use-workspace-agent-chat-turn.ts:674,1054,1187-1205` | 通知相关改造复用现有 `settings.notification` 通道与 `maybeShowDesktopNotification` |
| A13 | 会话重命名 + 侧栏会话名搜索 | `components/workspace/workspace-sidebar.tsx:620-637,724-737` | P1-9/P2-6 复用，不另做搜索框或重命名入口 |
| A14 | 外观字体族/字号（含代码字体、预览卡） | `components/workspace/settings/appearance-settings-page.tsx:436-519` | P0-4 token 治理须保留该能力与「字号仅影响内容」的语义 |
| A15 | 密度模式（comfortable/compact） | composer 与 shell 接线、`core/settings/local.ts` | P0-4 字号/间距治理不得写死单一密度 |
| A16 | 轨迹 reducer（乱序缓冲/幂等/终态冻结/seq 空洞） | `lib/trajectory/trajectory-reducer.ts` + 8 个相关测试 | 已列为非目标；P0-2 拆分清单中 `lib/trajectory/*` 不得拆除 |
| A17 | 会话历史同步/检查点/计划模式 hook 与 UI | `hooks/workspace/use-runtime-checkpoints.ts`、`use-workspace-history-sync.ts`、plan mode 面板 | P1-7 统一 pending 生命周期须兼容现有 plan 决策入口 |
| A18 | ui 基础件体系（cva/clsx/tailwind-merge） | `components/ui/*` | P0-3 卡片收敛基于该体系扩展 Card/Panel，不另起炉灶 |

> 资产对应关系：A1/A2 ↔ P1-8；A3/A4 ↔ P2-1/P2-5；A5/A6 ↔ P2-2/P2-1；A7/A8/A9 ↔ P2-1/§7.2；A10/A11 ↔ P2-3/P2-2；A12–A15 ↔ P0-4/P1-9；A16–A18 ↔ P0-2/P0-3。

### 5.6 Round 2 功能点处置总表（完整性口径）

> 口径：`.tmp/frontend-opt-analysis/round2-target-shell-features.md` 六节共 **89 个功能点**（1-x 外壳 15、2-x 布局/侧栏 15、3-x 会话列表 19、4-x 设置 12、5-x 运行时包 14、6-x 横向交互 14），本方案逐项给出处置，完整矩阵见 **§11.5**。

| 处置 | 数量 | 说明 |
|---|---|---|
| 落地（绑定任务 ID） | 63 | 每项都有 P0/P1/P2 任务承接，见 §11.5「本方案处置」列 |
| 部分采纳 | 2 | 2-13（文本预览走 P2-10；PDF/加密失败面暂缓）、5-14（只采纳订阅者错误隔离与浅比较纪律，见 §7.9） |
| 已具备（列入 §5.5 保留清单） | 8 | HTML 壳 `#root`（1-1）、应用内目录浏览对话框（2-14）、新建会话/新增工作区（3-6/3-7）、重命名（3-8）、设置触发入口（4-4）、模型设置分区（4-8）、Composer 模型选择器（5-10）；个别行附带的 P0/P2 任务仅作增量约束或「不得回退」声明，不改变归类 |
| 暂缓（本期不做，原因明确） | 6 | 1-14 浏览器鉴权、1-15 明文 HTTP 信任提示、3-14 定时任务指示、4-5 Onboarding 槽、5-5 工作流运行视图、5-11 会话级 Agent 预设 |
| 不采纳（进 §8） | 10 | 1-2 PWA、1-10 HMR 帧协议、1-11 fixture 开关、1-12 单页单视图、1-13 多标签同步、2-15 原生目录选择器、3-19 多选/未读/置顶、4-2 设置壳独立包、6-3 全局命令面板、6-12 遥测 |

> 结论：目标项目功能点**无悬空项**。新增任务的必要性来自「处置=落地」的 63 项与 §5.4 工程差距；「暂缓/不采纳」均有理由与替代方案，防止范围蔓延。

---

## 6. 优化实施方案

### 6.0 总体路线

分三阶段推进，每阶段可独立交付、独立验收：

| 阶段 | 主题 | 目标 | 预计周期 |
|---|---|---|---|
| P0 | 治理与底座 | 目录卫生、超长文件拆分、组件收敛、三层 token、i18n 真源、测试基建 | 1–2 周 |
| P1 | 核心交互对齐 | 消息渲染（折叠/增量/滚动）、Composer（附件/命令/引用）、队列与审批、工具行状态机、连接状态与断线恢复、会话列表状态、错误边界 | 2–3 周 |
| P2 | 功能补齐与工程化 | 后端就绪能力接入（jobs/subagent/usage/fs/skills）、列表组织与命令系统、交付物呈现、可观测增补、设置 schema 化、a11y 与移动端、性能与可靠性 | 3–6 周 |

实施约束：

- 每个任务先加测试再改行为；涉及样式的任务必须在 e2e 或组件测试中留下可验证证据。
- 新样式一律只消费 L2 semantic token；新增 token 必须按「primitive 步进 + semantic 别名」成对添加。
- 新增 UI 文案必须先落 `zh-CN` 真源并同步 `en-US`，否则不允许合入。

### 6.1 P0：治理与底座

#### P0-1 目录与构建卫生

- **内容**：删除全部 **8 个 `.backups/` 目录（20 个 `.bak` 文件）**；清理 `dist/` 外的构建残留；在 `.gitignore` 增加 `*.bak`、`.backups/` 规则；移除调试用 e2e（`diag.spec.ts` 改为按需运行的 `*.manual.ts`）。
- **涉及**：`frontend/src/.backups/`、`frontend/src/{api/runtime,components/ui,components/workspace,hooks/workspace,i18n/resources,pages,styles}/.backups/`（20 个 `.bak`：pages 6、components/workspace 5、i18n/resources 3、api/runtime 2，其余 4 个目录各 1）、`frontend/.gitignore`、`frontend/e2e/`。
- **验收**：`git status` 无 `.bak` 新增；`frontend/src` 下 `*.bak` 计数为 0（`Get-ChildItem -Recurse -Force -File frontend/src -Include *.bak | Measure-Object` = 0）；CI/本地 `npm run build`、`npm run test` 全绿。

> **状态**：已完成（2026-09-11）；清理与验证证据见 §9.3。复核：`.backups` 目录 0 / `.bak` 文件 0（清理前实测 7 / 11，且从未被 git 跟踪），`.gitignore` 已含 `*.bak`、`.backups/`，调试用 e2e 已由 `diag.spec.ts` 改名为 `diag.manual.ts`（`playwright.manual.config.ts` 承接）。 **2026-09-13 复检：** 该验收在复核时已被回退——`frontend/src` 下实有 52 个 `.bak` 与 17 个 `.backups/`（全部为 2026-09-13 生成；`.gitignore` 使其对 `git status` 隐形），`frontend/` 全域共 72 个 `.bak` / 19 个 `.backups/`。本次已全部清理（清理后实测 0 / 0），并新增 `frontend/scripts/verify-no-backups.mjs`（并入 `npm run lint`）把该约束变成可执行门禁；详见缺口清单 A1。

#### P0-2 超长文件拆分（按域注册表化）

- **内容**：
  - `components/workspace/settings/backend-config-settings-page.tsx`(3932) → 页面壳 + 域注册表 + 15 个域编辑器目录（每域改为 dynamic import + 独立测试入口），共享 `config-domain-table/config-form-field/config-domain-dialog`。
  - `components/workspace/workspace-sidebar.tsx`(1560) → 侧栏容器 + 列表/分组/目录对话框/会话行组件；纯逻辑继续留在 `workspace-sidebar-shared.ts` 并补齐边界测试。
  - `lib/workspace-thread-state.ts`(1815) → 按职责拆为 `thread-state/{messages,deltas,queue,projection}.ts`，对外 barrel 保持既有 import 路径。
  - `types/runtime.ts`(1692) → `types/runtime/{sessions,checkpoints,backtrack,plans,teams,analytics,cache,harness,config}.ts` + `index.ts` re-export。
  - `hooks/workspace/use-workspace-agent-chat-turn.ts`(1337) → 拆分为发送编排 / SSE 消费 / delta 协调 / 目录同步 4 个 hook，组合于原 hook。
  - `components/workspace/settings/runtime-provider-groups-domain-editor.tsx`(1358) → **（原方案漏列，必须纳入）**：若不拆，本任务「单文件 < 500 行」验收不可能达成；作为域编辑器注册表化的首个迁移样本。
  - 首批同批纳入（> 700 行，共 15 项）：`runtime-provider-domain-editor.tsx`(1187)、`pages/usage-analytics-page.tsx`(974)、`pages/logs-page.tsx`(893)、`runtime-team-dispatch-panel.tsx`(846)、`lib/trajectory/trajectory-reducer.ts`(831，仅机械整理不改语义)、`runtime-team-details-panel.tsx`(812)、`runtime-agent-routing-domain-editor.tsx`(807)、`runtime-rate-limit-domain-editor.tsx`(770)、`hooks/workspace/use-session-backtrack.ts`(744)、`use-runtime-team-dispatch.ts`(743)、`message-list.tsx`(719)。
- **分批策略**：当前 `frontend/src` 共 **41 个文件 > 500 行**（39 非测试 + 2 测试，完整清单与批次归属见 §11.4）。本任务承诺**首批 15 项拆完并达标**；其余按四批滚动推进，每批随对应功能任务交付，避免一次性大爆炸改造：
  - **第 2 批（8 项，随 P2-2 收口）**：设置域编辑器残余（retry 715 / routing 565 / transformer 508 / provider-queue 504 / agent-routing-utils 514）+ appearance 596 / harness 568 + `backend-config-settings-page.tsx`(3932，**全仓最大文件**，按 section 拆分并复用域编辑器注册表；P0-2 期间即启动设计）。
  - **第 3 批（10 项，随 P2-7/P2-8/P2-9）**：`workspace-sidebar.tsx`(1560，第 3 批首位)、`workspace-shell.tsx`(695)、`runtime-teams.tsx`(693)、team-details(683)/shared(628)/dispatch-console(575)、`message-markdown.tsx`(625)、`artifact-panel.tsx`(618)、`artifact-panel-checkpoint-surface.tsx`(613)、`artifact-detail-dialog.tsx`(534)。
  - **第 4 批（4 项，随 P2-4/P2-10）**：`use-runtime-sessions-data.ts`(523)、`use-runtime-checkpoints.ts`(509)、`data/mock.ts`(539)、`cache-analytics-page.tsx`(518)。
  - **P0-6 承担（2 项）**：i18n 双语字典（2595/2536）按 namespace 拆分，必须与 P0-6 同批完成，不留给后期。
  - **测试随源（2 项）**：`workspace-thread-state.test.ts`(800)、`trajectory-reducer.test.ts`(537) 随对应源拆分同步切分，禁止只拆源不拆测试。
- **验收**：拆分后对外 API 不变（`tsc -b` 通过）；**首批 15 项内**每个新文件 < 500 行；相关既有测试不改断言即通过；41 项总量在 M4 前降至 ≤ 10（且余项均有排期与责任批次）。

> **状态**：已完成（2026-09-11）；分批落地与复核证据见 §9.3。复核：`frontend/src` 内 > 500 行文件仅剩 i18n 双语字典 2 个（按方案归 P0-6 承担），41 项总量 **41 → 2**（≤ 10 达标），首批 15 项均在 < 500 行。**（2026-09-11 更新）** P0-6 已将双语字典按 namespace 拆分，> 500 行文件数降至 0（见 §9.3 P0-6 行）。 **2026-09-13 复检：** 该全局指标当前不成立——> 500 行文件为 3 个：`styles/globals.css`=1043、`components/workspace/message-markdown-streaming.ts`=525（P1-2 `c12e0cf4` 引入）、`components/workspace/trajectory/subagent-session-dialog.test.tsx`=516（`8c55411d`）；按计划 ts/tsx 口径为 2 个，M4 阈值（≤ 10）仍满足。§9.3 另有若干模块行数陈旧漂移（均在阈值内）。详见缺口清单 A2。 **2026-09-13 修复：** 三个文件已全部拆分达标——`styles/globals.css`(1043) → 入口 17 行 + `styles/globals/{theme 110, tokens 335, themes 150, base 435}`（切割脚本内置字节级重组校验，构建产物 CSS 与拆分前同尺寸同 SHA256）；`message-markdown-streaming.ts`(525) → `message-markdown-streaming/` 5 模块（barrel 保持 14 个导出不变）；`subagent-session-dialog.test.tsx`(516) → 3 个测试文件 + 1 个 helper（12 个 it 标题 1:1，用例数不变）。复测：**`frontend/src` 内 > 500 行文件 0 个，全局指标恢复成立**；证据见 §9.3「P0-2 回退修复」行与缺口清单 A2。

#### P0-3 卡片/面板组件收敛

- **内容**：以现有 16 个设置卡片（`settings-choice-card / field-card / info-card / mini-card / mini-toggle-card / notice-card / panel-card / section / subsection-card / toggle-card / inline-toggle-card / empty-state / badge-list / action-group / add-button / dialog / dialog-footer / panel-icon`）为基础，抽取统一 `Card`（表面/密度/交互三轴变体）、`PanelSection`、`EmptyState`；用 codemod 或逐个替换；artifact/checkpoint surface 复用同一抽象。
- **落地要点**：变体用 cva 显式枚举，禁止透传任意 `className` 覆盖表面颜色；保留现有语义命名以免大范围改动测试快照。
- **验收**：近重复组件数量从 16 降到 ≤ 6（Card/PanelSection/EmptyState/Dialog/FieldRow/ActionGroup）；无视觉回归（截图对比或现有组件测试通过）。

> **状态**：已完成（2026-09-11）；落地口径、抽象映射与验证证据见 §9.3。

#### P0-4 三层设计 token 体系

- **内容**：
  1. 在 `globals.css` 内建立三层：L1 `--primitive-*`（现有色板/hue 阶梯）、L2 `--color-*` 语义别名（bg/label/border/interactive/brand/button/state/markdown/code/tooltip/surface-*）、L3 组合（阴影 `--shadow-lv*`、浮层 `--elevation-*`、滚动条变量、字体角色 `--font-{s,xs,xxs}[-strong]`）。
  2. Tailwind v4 `@theme` 中把 L2 映射为语义颜色/圆角/字体工具类；feature 组件只允许语义类与语义工具。
  3. **`landing.css` 并入**：删除其 `:root` 26 变量与 light 覆盖，改用同层 token；仅保留页面布局类与 keyframes，动效时长引用统一 token。
  4. 收敛硬编码：`rounded-[0.7rem]`/`rounded-[0.9rem]`/字号默认值 → token 或组件内局部变量。
- **落地要点**：暗色仍可作为默认（保持现有 `applyDocumentSettings` 行为），但变量声明改为「亮色在 `:root`、暗色在 `html[data-theme="dark"]`」或反向保持一致；新增规则：feature 层禁止出现 `#hex`、`rgb()`、主题选择器。
- **验收**：`grep -c "var(--"` 的任意值用法下降 ≥ 70%；`landing.css` 变量数为 0；主题/强调色/字号切换后无颜色回归（e2e 覆盖三档主题 × 两种强调色）。

> **状态**：已完成（2026-09-11）；三层落地口径、收敛批次与验证证据见 §9.3。验收复核：`[var(--…)]` 任意值 **2008 → 20（-99%）**；`landing.css` 88 行内变量声明 0 / `:root` 0 / 颜色字面量 0；主题 × 强调色 × 字号的 e2e 覆盖见 §9.3 末行（提交 `7d42b79b`）。有意保留项：未命中阶梯的硬编码圆角/字号、mock 产物 iframe 内的颜色字面量、无对应 `@theme` 工具类的渐变/阴影 token，均逐条记录在 §9.3。 **2026-09-13 复检：** `@theme` 映射 89 条（原记 87）、残留 `[var(--…)]` 任意值 22 行/14 文件（原记 20），方向未变；详见缺口清单 A3。

#### P0-5 主题解析/应用分离与首屏防闪烁

- **内容**：抽 `theme/{resolve.ts,present.ts}`：`resolve` 负责 light/dark/system + accentTone + 字体 + reduced-motion → 不可变 snapshot；`present` 只做 DOM 写入（`documentElement.style.colorScheme`、`html[data-theme]`、`html[data-accent-tone]`、CSS 变量内联覆盖），维护 retraction set，`dispose()` 逆操作；`index.html` 内联一段与 present 同源的启动脚本，消除首屏闪白/闪黑；`main.tsx` 改为调用 resolve+present。
- **验收**：DevTools 硬刷新（禁用缓存）无主题闪变；present 重复调用幂等；单测覆盖 resolve 矩阵与 present retraction。

> **状态**：已完成（2026-09-11）；resolve/present 拆分口径、启动脚本同源保证与验收证据见 §9.3。验收复核：启动脚本在 React 挂载前即完成主题/字号/lang 落地（e2e 拦截入口模块后的 pre-bundle 断言），`present` 重复调用幂等、`dispose()` 只回收自有写入，单测覆盖 resolve 矩阵与 retraction。

#### P0-6 i18n 类型真源与硬编码门禁

- **内容**：把 `resources/zh-CN.ts` 拆为按 feature 的 namespace 模块并作为 key 真源；`en-US.ts` 用 `satisfies Record<ZhKey, string>` 编译期对齐；支持语言包按 namespace 动态 import；新增脚本 `scripts/verify-frontend-i18n.ts`（基于 TS AST + 现有 tsconfig）扫描 JSX 文本与 `aria-label/title/placeholder/alt/label/confirmLabel` 等 copy 型属性，白名单豁免 locale 文件与纯 token 常量，并设最小扫描文件数；**运行时回退链**：en 缺失键回退 zh、再回退 key 文本，dev 模式对回退发告警而非静默。
- **落地要点**：`index.html` 的 `lang` 改为与 `document.documentElement.lang` 同步（由 present 步骤写入）。
- **验收**：`npm run lint:i18n` 可执行且在存量清理后为 0 违规；删除 en 中任一 key 会导致 `tsc -b` 失败（编译期对齐生效）。

> **状态**：已完成（2026-09-11）；namespace 拆分、编译期对齐探针、运行时回退链与门禁扫描的证据见 §9.3。

#### P0-7 测试基建三件套

- **内容**：Playwright 配置改为「构建产物 + 空闲端口」：缺失 `dist/index.html` 直接 fail-fast 提示先 `npm run build`；`probeFreePort()` 为 mock/preview 服务申请端口；失败用例写全页截图到 `.artifacts/`（gitignored）；固定 `locale`、`timezoneId`、视口种子；`mock-server.mjs` 与 `global-setup.ts` 收敛为 scaffold 工具，提供 seed 会话/主题/语言/历史的能力。
- **验收**：故意让一条 e2e 失败时产出截图与可读错误；e2e 在无 dist 时给出明确提示而不是白屏超时。

> **状态**：已完成（2026-09-11）；构建产物/空闲端口/失败产物三件套与两项验收探针证据见 §9.3。

### 6.2 P1：核心交互对齐

#### P1-1 消息渲染投影模型与折叠策略

- **内容**：
  - 在 `lib/` 引入会话视图装配：把现有「消息 + delta + 工具 + 推理 + 富内容」的合并逻辑抽象为 Definition/Node 投影（`key/kind/id/target/data`），Chat 与 Trajectory 各自消费快照，**不新建第二份权威状态**。
  - 实现折叠谓词：Turn 结束后折叠过程证据；final-answer 边界 = 最新 Step 含非空文本/图片且不含工具调用；无 final answer 的 closed Turn 保全过程可见。
  - 折叠摘要行：汇总工具数/带回复消息数/子代理数，零值段省略，全零显示 `Thought for a while`。
  - `system/message` 渲染为请求前折叠的 `System prompt` 行；Turn token usage 行在用量不完整时整行隐藏。
  - 本地即时消息与权威记录**原子替换**（现有 `deltaCoordinator` 与轨迹投影的交接处需明确 ownership）。
- **涉及**：`components/workspace/message-list.tsx`、`message-*.tsx`、`lib/workspace-thread-state.ts`、`lib/trajectory/projection.ts`。
- **验收**：组件测试覆盖「工具调用后跟文本」「无 final answer」「纯工具 Turn」「重试行」四类折叠结果；同一 Turn 折叠后行数稳定，不因新消息到达重排。

> **状态**：已完成（2026-09-11，两步落地：投影内核 `534ca637` + 渲染接线 `deebd764`）；落地口径与验证证据见 §9.3「P1-1 消息渲染投影模型与折叠策略」行。复核：assistant / history-context 两类卡片已全部改为消费 `projectChatView`（`nodes` / `hiddenNodes` / `collapsed` / `summary` / `usage`），`user-message-bubble` 仍直读 `segments`（用户气泡的附件/引用渲染，不承载过程证据折叠）；Trajectory 沿用既有 `lib/trajectory/` Definition/Node 管线，未新建第二份权威状态；本地 delta 与权威记录的 ownership 由 `lib/thread-state/deltas.ts` 的 `RuntimeDeltaCoordinator` 承载（`claim()` 按 provider `stream_id` + `sequence` 判重、`beginTurn`/`endTurn` 划轮，注释即契约），本次未改其语义。

#### P1-2 Markdown 增量渲染与视口懒高亮

- **内容**：以 `splitStreamingMarkdown`/`parseStreamingCodeFence` 为基础实现前缀冻结（尾 2 块不稳定）、未闭合 fence 双前沿、绝对 offset key、非追加输入 generation 递增；流式期不渲染可能过期的引用/图片 handler（settled 后再生效）；代码高亮改为 `IntersectionObserver` 懒激活（激活后停止观察），`supportsHighlighting` 前置；安全面保持「去 raw HTML、限链接、仅绝对 HTTP(S) 图片」。
- **验收**：性能用例（1 万字流式追加 N chunk）下重解析次数为常数上界（可用计数器断言）；流式长回复首屏帧率无肉眼卡顿；DOM 结构在 settled 后与全量解析一致。

> **状态**：已完成（2026-09-11，三个子片：1/3「视口懒高亮」`29b336fd`、2/3「流式期引用/图片门控」`afb09986` + 白名单收紧 `a303161c`、3/3「前缀冻结 / 绝对 offset key / generation 递增 / fence 双前沿」`c12e0cf4`，均全门禁通过，证据见 §9.3「P1-2 …」行）：`supportsHighlighting` 前置门、单例 `IntersectionObserver` 文档级复用与「首次相交激活后永久离开观察集」已就位；流式期（稳定区与尾块两条路径）不再把 `href`/`src` 烘焙进 DOM，只渲染 `data-streaming-link` / `data-streaming-image` 占位，settled 后由同一 handler 按「绝对 `http(s)` / `mailto` / `tel` + 页内锚点」白名单重解析补齐（相对链接、协议相对 `//host` 与 `javascript:`/`data:`/`file:` 拦截），图片仅放行绝对 HTTP(S)，其余在 settled 后保持 `data-image-blocked` 占位；子片 3/3：`scanMarkdownBlocks` 按空行切顶层块并合并同容器（松散列表 / 多段引用跨空行仍是一块），除尾 2 块（`UNSTABLE_TAIL_BLOCKS = 2`）外全部冻结，切割点取最后冻结块的 `end`，`stableContent`/`tailContent` 逐字一致；渲染 key = 块绝对起始 offset 且冻结块与不稳定区首块同处一个 keyed 列表（块滑进冻结前缀时复用 fiber、不重解析），非追加输入使 `generation` 递增触发换代 remount；未闭合 fence 走「整行 `stableCode` 高亮 + `partialLine` 纯文本」第二前沿，fence 中断段落时按 fence 起点截断该块；验收用例已补齐（24 块 × 4/48 chunk 重解析计数同为 23 次 ≤ 块数；流式增量 settled 后与一次性全量解析逐字节一致）。

#### P1-3 滚动所有权契约

- **内容**：会话滚动宿主改造为「语义锚点恢复 + 贴底跟随 + 离开底部保顶」：`ResizeObserver` 观察内容与输入座位高度（composer 悬浮时预留实时高度变量）；prepend 历史时按锚点行恢复位置；离开底部时以 reading-line 几何选 active Turn；为滚动契约写虚拟化中立的 e2e（断言语义行位置、底部归属、真实滚动宿主，±2px / ±32px 容差）。
- **验收**：e2e 覆盖「流式期间向上滚动不打断阅读」「加载历史前插后视口不跳」「工具流式增长时贴底跟随」三类场景。

> **状态**：已完成（2026-09-11，`066fcd69`，全门禁通过，证据见 §9.3「P1-3 滚动所有权契约」行）：会话滚动收口到唯一宿主 `useConversationScroll`——`ResizeObserver` 持续贴底跟随、离开底部后按语义锚点（消息 id + 视口偏移）保顶、锚点按线程记忆（remount / 会话切回自愈），reading-line（视口 1/3）几何命中 active Turn 并以 `data-active-turn` 暴露；宿主禁用原生 `overflow-anchor`，回溯导航期间让出滚动所有权。e2e 新增三类场景（阅读位置 ±2px、前插保顶 ±2px、工具卡增长贴底 ±32px）全绿。

#### P1-4 Composer 输入面升级

- **内容**：
  1. **附件**：file input + 粘贴 + 全视口拖放邀请；草稿附件轨（composer 下方）；消息图片按数量决定尺寸；图片灯箱（Escape/遮罩/关闭）；上传状态与数据 owner 化（hook 提供，组件只渲染）。
  2. **斜杠命令（输入面机制）**：`/` 菜单与 `+` 按钮同源；命令解析四分类（宿主 `leadingInput` / `popupSelect` / `action` / 默认 `execute`），**命令行永不静默降级为 prompt**；冲突 fail loudly。**边界**：触发/菜单/派发机制属本任务；具体内置命令清单与执行器属 P2-7，不在此重复定义。
  3. **`@` 引用**：光标处检测；分组候选（文件/会话/子代理）；键盘 + 指针 + drill-down；`aria-activedescendant`；Tab 在无高亮时原样放行。
  4. **草稿持久化**：按会话保存草稿到 localStorage；14 行封顶滚动 + 输入回焦行为对齐目标项目 e2e 契约。
- **涉及**：`components/workspace/message-composer.tsx`、新增 `composer/{menu,attachments,draft}.tsx`、`hooks/workspace/*`、`api/runtime/*`（附件上传依赖后端 `POST /runtime/uploads`，见 §6.3 P2-1C；接口就绪前仅本地预览 + 「待发送」标记，**不伪造已上传状态**）。
- **验收**：附件三段式（选择/粘贴/拖放）可用；命令不匹配时不发送原文；`/` 与 `@` 菜单键盘可达；草稿切换会话后恢复。

> **状态**：已完成（2026-09-11，三个子片：1/3「草稿按会话持久化 / 14 行封顶 / 输入回焦」`13cfeba2`、2/3「附件输入面」`3873dc24`、3/3「触发器 / 命令 / 引用菜单」`51970e84`，均全门禁通过，证据见 §9.3「P1-4 Composer 输入面升级（子片 …）」三行；内置命令清单与执行器按边界归 P2-7）：`lib/composer-draft.ts` + `hooks/workspace/composer/use-composer-draft.ts` 已把草稿键收口到会话（sessionId 优先、线程 id 兜底，空白即删条目，存储异常降级内存态）；`lib/composer-textarea.ts` + `message-composer.tsx` 落地 14 行封顶内部滚动（resize 重度量）与挂载/切会话/提交后回焦；`lib/composer-attachments.ts` + `hooks/workspace/composer/use-composer-attachments.ts` + `composer-attachment-rail.tsx` 落地选择/粘贴/全视口拖放三入口与 pending 草稿轨（上传接口未就绪，不伪造已上传态）；**子片 3** `lib/composer-trigger.ts`（行首 `/` 与词边界 `@` 的光标处触发、token 内 caret/click 重定位、补全插入不吞后续文本）+ `lib/composer-menu.ts`（分组候选 / root launcher + drill-down / 前缀优先排序裁剪 / 空态剔除）+ `lib/composer-commands.ts`（`leadingInput`/`popupSelect`/`action`/`execute` 四分类、非法名与同名冲突 fail loudly、命令行永不降级为 prompt）+ `hooks/workspace/composer/use-composer-menu.ts`（`+` / `/` / `@` 三入口同源、菜单打开时接管 Esc/↑/↓/Tab/Enter、阻塞与未接线提示）+ `components/workspace/composer-menu.tsx`（listbox / `aria-activedescendant` / 计数 / 空态）：`message-composer.tsx` 暴露 combobox 语义并按分类分流提交，`main-section.tsx` 用当前线程交付物构造 `@` 文件组（`composer-references.ts`；会话/子代理分组待数据源就绪）。**边界**：内置命令清单与执行器归 P2-7，宿主未接线时给「尚未接入执行器」提示，绝不降级为 prompt。

#### P1-5 队列与 Steering

- **内容**：`resolveSubmitMode(preferred, running, gesture, steeringAvailable)`：非运行或不支持 steering → queue；运行中 plain Enter/主发送按钮走用户偏好（设置项 `busyEnter`），Cmd/Ctrl chord 取反；队列坞位于 composer 上下文栈；队列帧无 ref 的图片直接跳过；缩略图加载失败保留空占位；steering 身份从事件流（splice → user/message）重建，刷新后可区分 queue 与 steer。
- **依赖**：需要运行时队列 API 与 `updateQueue` 能力（当前无，列入后端依赖）。
- **验收**：busy 状态下 Enter 行为可配置且持久化；队列项可删除/立即发送；断线重连后队列状态与权威一致。

> **状态**：未开始（2026-09-13 复核；依赖后端队列/steering API，当前未就绪）。注意：`frontend/src` 中形如「P1-5 方案 1/2/4」的注释指 `docs/plan/multi-agent-execution-optimization-plan.md` 的 P1-5（子代理下钻），与本任务无关。

#### P1-6 工具行状态机与工具专属视图

- **内容**：`message-tool-row.tsx` 重构为状态机：`expandable` 谓词、失败态替换摘要（不追加）、diff 折叠行携带 `+A -R`、失败禁用文件链接、嵌套链接处理点击与键盘冒泡、行根 `data-*` 属性 + visually-hidden 状态播报；注册表支持「工具名 → 专属卡」（read/diff/terminal/search/web/image/JSON），未注册走 generic 卡。
- **边界**：工具行内的单次 diff 统计属本任务（数据来自工具事件白名单，§6.3 P2-1B）；**回合级** Files changed 汇总行属 P2-8，两者不共用聚合逻辑。
- **验收**：工具行组件测试覆盖成功/失败/流式中/长输出四态；键盘 Tab 到文件链接可聚焦且不触发行展开。

#### P1-7 审批与提问交互

- **内容**：审批面板（approve/deny + 理由）与提问/计划评审卡统一为「pending interaction」生命周期：注册 → 可取消（`signal`）→ 结果回填；plan mode 现有三选一面板收敛为同一呈现位（计划评审 shape）；会话取消/断开时 delegate-on-remove 优雅收敛。
- **依赖**：后端**已就绪**（`approval_requested/resolved` 事件 + `runtime/commands{approve_tool}` + `GET /sessions/{id}/runtime` pending 查询，见 §6.3 P2-1A）；前端待补 UI。
- **验收**：审批卡出现/消失与会话生命周期一致；取消会话后 pending 卡不悬挂；拒绝审批后工具结果在轨迹与消息中一致呈现。

#### P1-8 连接状态统一与断线恢复收口（复用既有重连）

- **背景修正**：会话运行时流**已有**常驻重连 + `seq` 游标续传 + 阈值退避（`use-session-runtime-stream.ts:91-242`），日志流已有重连状态机与状态标签（`use-runtime-logs.ts`、`pages/logs-page/connection.tsx` / `logs-header.tsx`）。原「从零实现退避」的表述作废。
- **内容**：① 抽取统一「连接状态」呈现件（connecting/reconnecting/online/offline + 手动重试），会话流与日志流共用；② 状态条接入工作台顶栏与消息流尾；③ 补齐**直连 `/api/agent/chat` 流**的失败恢复路径（本轮只核验了 thread 运行时流与日志流）；④ 手动重试与自动重连共用入口，重试前以本地 last seq 拉齐。
- **约束**：不得新增第二套退避循环；复用现有 `settings.notification` 与错误文案映射（§5.5 A1/A2）。
- **验收**：e2e/单测模拟断流 → 自动恢复；恢复期间不重复渲染已消费 delta（幂等断言）；手动重试不产生重复请求；直连 chat 流断线后可见状态并可恢复。

> **状态**：**已完成**（2026-09-13，批次 2 第二项）。交付：`lib/connection-status.ts`（统一状态词汇/配色/文案 + `connectionStatusFromLogsState` / `withTransportDegradation`）、`components/ui/connection-status-badge.tsx`（pill/header 双变体，非在线态挂手动重试）、`hooks/workspace/use-connection-status-labels.ts`；`use-session-runtime-stream.ts` 暴露 `connectionStatus/retryConnection`（复用既有常驻重连与 last seq 拉齐，**未新增第二套退避**；状态按「会话键」存储，避免 effect 同步 setState）；状态条接入顶栏与消息流尾；`workspace-page.tsx` 以 `withTransportDegradation(streamStatus, thread.transport)` 收口直连 `/api/agent/chat` 断线；日志页头改挂共享呈现件（`variant="header"`，视觉不变）。验证：`pnpm lint` 0 error / 3 基线 warning（i18n scanned=558 / violations=0、备份门禁 784 文件 0 残留）；`vitest` **131 文件 / 793 用例**；`build` exit 0；`test:e2e` **36 passed**（含新增 `e2e/connection-recovery.spec.ts`：断线状态可见 + 重试不重发 chat）。

> **状态（P1-8 复核补丁，2026-09-14）**：针对用户报「连接中… 重试」常驻 / 闪烁复核出两处机制缺陷并修复（仍复用既有常驻重连，**未新增第二套退避**）：① **正常收流被误判成降级**——`use-session-runtime-stream.ts` 原按「本轮是否收到事件」在循环尾把状态回落为 `connecting`，而该流是常驻长轮询：服务端 / 代理按空闲超时正常关闭长连接时本轮本就没有事件，徽标于是在每个重连窗口闪回「连接中… 重试」，把健康连接报成假故障。改为**循环尾不回落状态**：正常收流只说明本轮结束，可用性交由下一轮 `onOpen` 验证；真断连仍在 `onErrorEvent` / `catch` 分支按阈值收敛为 `reconnecting` / `offline`。② **「重试」对非会话流来源的降级无效**——`shouldSyncSessionHistory` 在 `transport === "error"` 时跳过自动同步，而直连 `/api/agent/chat`、轨迹恢复、历史同步失败都会置 `transport="error"`；空闲会话上点「重试」只重启会话流，`withTransportDegradation` 仍把 UI 收敛为 offline，按钮形同失效。`use-session-history-sync.ts` 新增 `recoverSessionHistory()`（权威历史探活：成功 → 应用投影 + `transport:"live"` + `lastError:null`；失败 → 保留降级并写入 `Session history recovery failed: <msg>`），`workspace-page.tsx` 的 `handleRetryConnection` 在「非回合进行中且已降级」时追加调用。验证：`npm test` **193 文件 / 1449 用例全绿**（109.3s；加固用例：健康空流结束保持 `online`、手动恢复成功清除降级 / 失败保留降级并刷新原因、重试复用 last seq 且不重复渲染已消费 delta）；`npm run lint` **0 error / 3 基线 warning**（i18n scanned=656 / violations=0、备份门禁 970 文件 0 残留、行数门禁 919 个 `.ts/.tsx` 0 超限、字面量门禁 132 文件 0 违规）；`npm run build` exit 0；`npm run test:e2e` **75 passed / 1 failed**——唯一失败为 `workspace-chat` P1-3a 读数锚点漂移（1084px），系**已登记的既有滚动用例抖动**（同症状 1032px 及更早失败产物见 `docs/plan/frontend-message-rendering-deepseek-alignment-plan.md` §门禁与 `docs/plan/frontend-session-branch-fork-plan.md:575`；本机失败截图时间戳 18:24 / 21:11 早于本补丁），与本补丁无交互面；`e2e/connection-recovery.spec.ts` 通过并新增断言（重试后 `online` 徽标可见、`offline` 计数为 0、`chatRequests === 1`）。

#### P1-9 会话列表状态与整理能力

- **内容**：侧栏会话行状态指示（运行中/子代理运行数/等待审批/计划待审/等待回答/已完成，等待类视觉最醒目）；归档与**非破坏删除**（移除引用/保留数据；删除工作区后其会话回落「未分组」而非连带删除）；归档恢复入口；会话 Fork；列表操作菜单补齐 Esc/方向键与 aria；行内相对时间（含「创建于」浮层）；空态区分「无会话/无匹配」。
- **依据**：目标项目 `ui-workspace/locales.ts:43-61`（状态/归档/删除语义、Fork）；当前侧栏已有重命名/会话名搜索/目录分组（§5.5 A13），本任务在其上扩展，不重做。
- **依赖**：状态数据源为会话快照 + 本地流状态合并；后端 `/sessions/{id}/archive|activate|close` 与 `/sessions/stats` 已就绪但未消费（§6.3）。
- **验收**：多会话并发时「哪个在等我」可一眼识别；归档后列表不出现且可恢复；Fork 后新会话命名与分组归属可预期。

> **状态**：**已完成**（2026-09-13，批次 3 合入主树）。交付：① 行状态指示——`workspace-sidebar/session-row-status.ts`（快照 + 本地流状态合并，等待类最醒目：等待审批 > 计划待审 > 等待回答 > 运行中 > 子代理运行数 > 归档/关闭 > 空闲）、`state-icon-utils.ts`、`labels.ts`；② `session-item.tsx` 操作菜单补齐 Fork / 归档 / 归档恢复 / 非破坏删除，键盘 Esc / ↑↓ / Home/End / Tab 收束与 `aria-haspopup`/`aria-expanded` 齐备，行内相对时间 + 「创建于」浮层；③ `sessions-section.tsx` 空态三态（无会话 / 无匹配 / 全部归档）；④ `session-row-actions.ts` 纯函数（Fork 标题与建会话请求、删除后选中回落）+ 单测 4 例；⑤ `workspace-page.tsx` 三个处理器 + `workspace-shell/` 与 `workspace-sidebar/` 全链路传参。**Fork 语义**：后端无克隆 API，以「同标题 + 本地化后缀 + 继承工作目录」新开独立会话，不复制消息历史（不伪造分支历史）；源会话未绑定目录时新会话回落「未分组」。**删除语义**：仅移除会话引用（不连带目录/磁盘数据），删除的是当前会话时清空轨迹并回落 `/workspace/chats/new`。验证：`pnpm lint` 0 error / 3 基线 warning（i18n scanned=560 / violations=0、备份门禁 789 文件 0 残留）；`pnpm test` **133 文件 / 803 用例全绿**（73.1s）；`pnpm build` exit 0；`pnpm test:e2e` **38 passed**（1.6m，含新增 `e2e/sidebar-session-actions.spec.ts`：Fork 请求体带分支后缀 + 删除后该行从侧栏消失；mock 侧补齐 POST 建会话确定性 id 与单会话 DELETE）。

#### P1-10 全局错误边界与加载失败面

- **内容**：全局 ErrorBoundary（当前 `frontend/src` 内无 ErrorBoundary/componentDidCatch，已核验）；lazy 路由 chunk 加载失败的重试面（**有次数上限与退避**）；`#root` 缺失/启动失败的可见提示（非白屏）；**启动完整性检查**——React 挂载后确认关键 provider/路由就绪，未就绪进入可见错误面而非「半加载」状态（目标项目 `boot-client.ts:54-60` 的单应用等价物）；错误统一走向 logger 出口。
- **依据**：目标项目 `web/src/boot-page.ts:68-70`（启动失败报告）、`web/src/boot-client.ts:54-60`（启动完整性审计）、`ui-renderer/src/client/scoped-slots.tsx:325-333`（槽位级错误边界）；当前项目为单应用，等价物是「全局 + 路由级 + 面板级」三层边界。
- **验收**：人为抛错不白屏且可恢复；chunk 404 显示重试而非死循环；错误边界有测试覆盖。

> **状态**：**已完成（2026-09-13 合入主树）**。实现：`components/errors/`（全局/路由/面板三层边界 + 统一错误面 + chunk 可重试懒加载路由壳）、`lib/lazy-retry.ts`（尝试上限 + 400/1200ms 递增退避 + `ChunkLoadError`）、`core/bootstrap/`（`#root` 解析、启动失败可见面、启动完整性检查 `data-app-ready` + i18n 就绪）、`core/logger.ts`；`main.tsx` 启动路径全 try/catch，失败落可见错误面且不覆盖已有 `role="alert"`；`App.tsx` loader 提升为模块级稳定引用；右栏两面板挂 `PanelErrorBoundary`；双语 `errors.*` 10 键对齐。证据：新增/改动 20 个文件（含 5 个测试文件），`lint` 0 error、`vitest` 126 文件 / 778 用例、`build` exit 0 全绿。缺口清单 B4 关闭。

### 6.3 P2：功能补齐与工程化

> **状态**：2026-09-13 起分批启动；**P2-1A 已交付八项：后台任务（Jobs）、运行时状态快照、用量 / 配额（token 口径）、会话元数据搜索、侧栏会话统计摘要（`GET /sessions/stats`）、运行时文件读取与预览、技能市场与热重载、子代理控制面（AgentControl 身份图）**（证据块见 A 表下方「状态（P2-1A 逐项）」及其续块），**P2-7 命令系统已于批次 12–14 收口（机制 + 执行器 + `/model` 弹窗 / `/feedback` + 命令专属 popupSelect 候选面，四个子片全部交付）**，其余 P2 各项未启动（P2-1 除 A 类八项外仍未接，P2 其余各项仅有前序任务 P1-7/P1-8/P1-4 遗留的部分能力）。逐项状态登记随交付更新，缺口清单见 `docs/plan/frontend-deepseek-harness-gap-list.md` B5/C2/C3。

#### P2-1 后端能力对接（就绪度驱动）

> 结论（Round 2 后端审计）：14 项能力中 **6 项直接可用、7 项需小改、4 项需新接口/新字段**（另 `top_p` 与货币成本单列）；同时有 **17 组端点**后端已暴露、前端零消费（免费收益）。原则：**先接既有端点，再谈新接口**。

**A. 直接可用（前端接线即可，优先做）**

| 能力 | 现有端点/事件（证据） | 前端交付 |
|---|---|---|
| 后台任务（Jobs） | `/background/jobs` 五端点 + `job_started/output/cancelled/finished` 事件（`background_handlers.go:15-210`；`handler.go:713-717`；输出走 REST `/output` 分页） | 会话头动作 + 弹层（live/settled、elapsed tick、Escape 回焦） |
| 子代理控制面 | `/sessions/{id}/agents`（spawn/wait/events/input/close/resume）+ mailbox（`handler.go:743-751`） | 会话头 lineage 面包屑 + 后代目录 + 独立 Stop（扩展既有 runtime-teams，§5.5 A9） |
| 审批闭环 | `approval_requested/resolved` 事件 + `runtime/commands{approve_tool}` + `GET /sessions/{id}/runtime` pending（`actor.go:3749-3825`；`session_runtime_handlers.go:770-783`） | P1-7 审批卡；处理 30min 超时的 `expired` 终态 |
| SSE 断线重放 | `runtime/stream?after=&poll_ms=` + `runtime/events?after_seq=&wait_ms=`（`session_runtime_stream.go:37-131`） | P1-8 复用；如需浏览器原生 `Last-Event-ID` 再小改 |
| Skills 全族 | list/get/create/update/delete/execute/search/stats/reload/validate/export/import/hot-reload（`handler.go:647-664,852-861`） | `/` 技能源、技能市场/热重载面板 |
| 会话元数据搜索 | `POST /sessions/search`（user/tags/state）+ `/sessions/stats`（`handler.go:725-729`） | P1-9 筛选搜索 + **空态降级与结果上限提示**（对齐 `ui-workspace/locales.ts:95-102`）；内容全文检索另议 |
| 用量 / 配额 | `/usage/stats|ledger`、`/usage/policy`、`/analytics/sessions/{id}/usage`（`:670-675,706-708`） | 成本/预算面板（token 口径，无货币成本） |
| 文件读写 | `POST /fs/read-file|write-file|append-file`（`:689-691`） | 文件树只读浏览 / Artifact 内容查看 |

> **状态（P2-1A 逐项）**：后台任务（Jobs）**已完成（2026-09-13 合入主树）**。交付：`api/runtime/jobs.ts`（五端点归一化，兼容 PascalCase/snake_case）+ `hooks/workspace/use-background-jobs.ts`（打开拉取一次、`job_*` runtime 事件后 800ms 合并刷新、live 任务 5s 轮询、取消后即时刷新）+ `hooks/workspace/use-elapsed-tick.ts`（仅存在 live 任务时启动 1s 计时）+ `components/workspace/jobs-panel.tsx` / `jobs-panel-shared.ts`（会话头顶栏入口、live/settled 分区、elapsed 与退出码、输出分页展开、Esc/遮罩关闭 + 焦点恢复）+ 双语 `panels.jobs.*`。验证：新增 7 文件（含 3 个单测文件 + `e2e/jobs-panel.spec.ts` 2 用例）；`pnpm lint` 0 error / 3 基线 warning（i18n scanned=567 / violations=0、备份门禁 802 文件 0 残留）；`pnpm test` **136 文件 / 837 用例**；`pnpm build` exit 0；`pnpm test:e2e` **40 passed**。缺口清单 C2 中「jobs」项关闭，其余 A 层各行未启动。

> **状态（P2-1A 逐项 · 续）**：运行时状态快照（`GET /sessions/{id}/runtime`）**已完成（2026-09-13 合入主树）**，关闭 P1-7 遗留的「重载 / 重连后未决审批与提问丢失」缺口（A 表「审批闭环」行由此整行闭环：卡片 + `approve_tool`/`answer_question` 投递 + 30min `expired` 终态 + 快照重建）。交付：`types/runtime/session-runtime.ts`（`RuntimeSessionState` / `RuntimeSessionSnapshot` + barrel 导出）+ `api/runtime/session-runtime.ts`（`getSessionRuntimeState`：404 按空态、结构不满足抛错不伪造空态、snake_case/camelCase 双兼容、id 编码）+ `lib/pending-interaction/snapshot.ts`（`pendingInteractionsFromRuntimeState` / `hydratePendingInteractions`：幂等重建、不回退 `resolving`/终态、缺席结算收敛 `snapshot_absent`、`expires_at` 超时兜底）+ `hooks/workspace/use-session-runtime-state.ts`（按会话键存、切换/卸载 abort 在途请求、effect 同步段零 setState、暴露 `refresh`）+ `hooks/workspace/use-pending-interactions.ts` 水合接线 + `workspace-page.tsx` 传参。验证：新增 3 个单测文件（`api/runtime/session-runtime.test.ts` 9 例、`lib/pending-interaction/snapshot.test.ts` 10 例、`hooks/workspace/use-session-runtime-state.test.tsx` 6 例）+ `e2e/mock-server.mjs` 新增「由事件存储推导未决项」的 `GET /sessions/{id}/runtime` + `e2e/pending-interaction.spec.ts` P1-7d「重载后由快照恢复」（反向对照：临时摘除该端点后用例失败，证明卡片确由快照水合而非事件流）。门禁见 §9.3 复测行。


> **状态（P2-1A 逐项 · 续 2）**：用量 / 配额（`GET /usage/stats|ledger|policy`）**已完成（2026-09-13 合入主树）**，A 表「用量 / 配额」行闭环。**口径硬约束：只呈现 token / 请求计数，不推算任何货币成本**（后端账本无价格字段，`token_usage_history.go:148-159`）。交付：① `types/runtime/usage.ts` + `api/runtime/usage.ts`——三端点归一化（键名固定 snake_case；缺 `usage` / `policy` / `records` 即判结构失败并抛错，**不伪造正常空态**；`scope` 缺席即全局视图，`quota` 缺席为显式降级），403 / 503 保留 `status` 供上层分类；② `hooks/use-usage-quota.ts`——三段请求独立发起与独立错误，任一段失败不遮蔽其余段，按作用域重取，筛选后重取账本，**已知作用域在切到具体作用域（响应不再带 `scopes`）后仍保留供回选**；③ `pages/usage-analytics/quota.tsx`（`UsageQuotaPanel`）——策略摘要、全局 / 作用域余量与生效来源、账本筛选（入口 / 技能 / 成功与否 / limit 上限 200）与 `N matched (limit M)` 计数、403 提示补 admin token（不重复渲染三行错误横幅）、503 说明账本未配置（未配置账本时不假装正常空态）、未配置上限的作用域显示「无配额快照」而非伪造余量；④ `pages/usage-analytics/overview.tsx` 挂载 + 双语 `usageAnalytics.quota.*`。验证：新增 3 个单测文件（`api/runtime/usage.test.ts`、`hooks/use-usage-quota.test.tsx`、`pages/usage-analytics/quota.test.tsx`，共 29 例）；`e2e/mock-server.mjs` 新增 `/usage/stats|ledger|policy` 夹具（全局带 `scopes`、指定作用域带 `scope + quota` 且不再带 `scopes`、`tenant-a` 未配置上限如实返回 `quota: null`、账本支持 entrypoint / skill / success / limit 过滤）；`e2e/usage-quota.spec.ts` 2 用例（全局聚合 → 账本筛选 → 作用域余量；403 时提示补 admin token 且不显示伪造数据）。缺口清单 C2 中「usage / 配额」项关闭，其余 A 层各行未启动。门禁见 §9.3 复测行。

> **状态（P2-1A 逐项 · 续 3）**：会话元数据搜索（`POST /api/runtime/sessions/search`）**已完成（2026-09-13 合入主树）**，A 表「会话元数据搜索」行闭环（`/sessions/stats` 与内容全文检索不在本次范围，仍待排期）。**后端零改动、纯前端接线**。交付：① `types/runtime/sessions.ts` 增补 `RuntimeSessionRecord` / `RuntimeSessionSearchFilters`（前端入参 camelCase）/ `RuntimeSessionSearchEcho`（后端 filters 回显是 camelCase，与请求体的 snake_case 不同名）/ `RuntimeSessionSearchResponse`；② `api/runtime/session-search.ts`——`searchRuntimeSessions`：请求体 snake_case 且空 user/tags/state 一律省略（不让空串当筛选值）、`limit` 默认 50、offset 收口非负；响应 `sessions` 非数组即抛错（**不把结构异常伪装成空结果**），单条缺 `id` 只丢该条、不整页失败；`isSessionSearchUnavailable` 把 404/405/501/503（含存储层不支持 ListAll 的 `STORE_UNAVAILABLE`）归类为「服务端检索不可用」；③ `hooks/workspace/use-session-search.ts`——idle → loading → ready/error 状态机：请求序号丢弃过期响应、新检索与卸载都 abort 在途请求（调用方 signal 与超时 signal 合并，超时保护不丢）、主动取消不落错误态；④ `components/workspace/session-search-dialog.tsx` + `session-search-shared.ts`——`role="dialog"` 弹层（`data-testid="session-search-dialog"`）：用户 / 状态 / 标签（逗号或顿号分隔、AND 语义）三组筛选、结果行（标题 / 会话 id / 标签 / 状态徽标 / 相对时间，无有效时间戳显示「时间未知」而非 Invalid Date）、`N session(s) matched` 计数与触顶 limit 提示、空态与不可用降级各有独立呈现（不可用时给状态码并说明「本地标题搜索仍可用」，不渲染伪结果）、`useDialogLifecycle` + `useFocusRestore` 保证 Esc / 遮罩关闭与焦点回位；⑤ 入口接线：`workspace-sidebar/sidebar-header.tsx` 触发按钮（`data-testid="session-search-trigger"`）+ `workspace-sidebar.tsx` 打开即重挂载（不复用过期检索）、选中结果关闭弹层并切到该会话；⑥ 双语 `panels.sessionSearch.*`。验证：新增 2 个单测文件 / 17 例（`api/runtime/session-search.test.ts` 12、`components/workspace/session-search-dialog.test.tsx` 5）；`e2e/mock-server.mjs` 新增 `POST /api/runtime/sessions/search`（tags 为 AND、state 全等、无 user_id 即不限用户，filters 以 camelCase 回显）、seed 支持 `state/user_id/tags/metadata`、`/sessions/users` 由 seed 的会话派生（原为硬编码空列表）；新增 `e2e/session-search.spec.ts` 3 例（服务端 AND + 状态过滤与请求体口径、按用户过滤后选中结果跳转、503 时如实降级且不出现结果行/空态）。门禁见 §9.3 复测行。

> **状态（P2-1A 逐项 · 续 4）**：运行时文件读取与预览（`POST /api/runtime/fs/read-file`）**已完成（2026-09-13 合入主树）**，A 表「文件读写」行的**读路径**接入（写入类 `/fs/write-file|append-file` 不进 UI；文件树浏览本体仍归 P2-10）。**后端零改动、纯前端接线**；内容唯一来源是运行时进程返回的字节，**不做本地兜底缓存或占位文本**。交付：① `lib/file-preview/decode.ts`——base64 解码、含 NUL 字节 / UTF-8 解码失败 → binary、空内容 → empty、行数统计与 `formatByteSize`；② `types/runtime/files.ts`（`RuntimeFileReadResult` + barrel）+ `api/runtime/files.ts`——`FILE_READ_PATH`、`FILE_PREVIEW_MAX_BYTES = 1_000_000`、`normalizeFileReadPayload`（缺 `file` / `path` / `data_base64` 即抛错，`byte_count` 缺失时按解码字节数收口）、`readRuntimeFile`、`isFileReadUnavailable`（404/405/501/503）；③ `hooks/workspace/use-file-preview.ts`——closed → loading → ready/error 状态机：请求序号丢弃过期响应、open / retry / close 均 abort 在途请求、AbortError 不落错误态、超限时 `tooLarge=true` 且 `body=null`（**宁可明说超限，不截断伪全文**）；④ `hooks/workspace/use-focus-restore.ts`——打开记录 `activeElement`、关闭还焦；⑤ `components/workspace/file-preview-dialog.tsx`——portal + `DialogOverlay` / `DialogPanel`，`data-testid` 家族 `file-preview-dialog|resolved-path|bytes|lines|loading|error|too-large|empty|binary|text`；错误分「端点不可用（带状态码）」与「真实失败（展示后端 error 文本）」两类，`errorMessage` 用 `TFunction<"workspace">`（i18next 类型契约）且 `status: String(status)`；面板 `aria-modal` + `tabIndex={-1}` + 打开即聚焦，绕开工具行按钮吞掉 Escape 冒泡的既有行为（关闭仍由 `useFocusRestore` 还焦给触发元素）；⑥ 工具行接线：`components/workspace/tool-row/*` 的文件链接（`data-tool-row-file-link="true"`）→ 打开预览；⑦ 双语 `panels.filePreview.*`。验证：新增 3 个单测文件 / 26 例（`lib/file-preview/decode.test.ts` 8、`api/runtime/files.test.ts` 9、`components/workspace/file-preview-dialog.test.tsx` 9）；`e2e/mock-server.mjs` 新增 `POST /api/runtime/fs/read-file`（**未登记路径返回 500 + `no such file`**，与后端「读盘失败即 500」口径一致，不伪装 404）+ `read-file` 会话脚本；`e2e/support.ts` 新增 `seedRuntimeFiles`；新增 `e2e/file-preview.spec.ts` 3 例（工具行链接 → 预览字节 / 行数 / 文本 + `Escape` 关闭；空文件与含 NUL 二进制各自如实呈现且不渲染文本；读失败显示后端 `no such file` 文本、无兜底内容、可重试）。缺口清单 C2 中「fs/read-file」项关闭，其余 A 层各行未启动。门禁见 §9.3 复测行。

> **状态（P2-1A 逐项 · 续 5）**：技能市场 / 热重载（`GET /api/runtime/skills`、`/skills/{name}`、`/skills/search`、`/skills/stats`、`/skills/hot-reload/stats`、`POST /skills/hot-reload/{start,stop,reload}`）**已完成（2026-09-13 合入主树）**，A 表「技能」行闭环（技能**市场 / 目录 / 检索 / 统计 / 热重载**接入；MCP 目录仍属 P2-1C，不在本次范围）。**后端零改动、纯前端接线**。交付：① `types/runtime/skills.ts` + barrel——`RuntimeSkill` / `RuntimeSkillCatalog` / `RuntimeSkillSearchResult` / `RuntimeSkillStats` / `RuntimeHotReloadStats` / `RuntimeSkillMutationPolicy` 等契约类型；② `api/runtime/skills.ts`——八端点客户端与归一化：`skills` / `stats` 非数组即抛错（**不把结构异常伪装成空市场**）、单条技能缺 `name` 只丢该条（不整页失败）、`count` / `total_skills` 保留后端上报值（不按数组长度改写）、`mutation_policy` / `embedding` 缺席置 `null`（UI 显示未知，不按 false 渲染）、检索 `limit` 默认 20 / 上限 200、`isSkillsUnavailable`（404/405/501/503）与 `isSkillsForbidden`（403 策略 / 鉴权拒绝）**分类不混用**、写操作管理令牌头 `X-Skills-Admin-Token`（空串即不发头）；③ `hooks/use-skills-market.ts`——目录 / 统计 / 热重载三段独立请求与独立错误（任一段失败不遮蔽其余）、检索「一份在途」+ 请求序号丢弃过期响应、主动 abort 不落错误态、写操作成功即以响应里的 stats 覆盖本地快照（**不以本地状态臆测 watching**）；④ `pages/skills-page.tsx` + `pages/skills/{catalog,detail,stats,hot-reload,shared}.tsx`——目录行（名称 / 分类与版本 / 描述 / 来源 / 标签）+ 检索（`q` / `category` / `auto|lexical|semantic` 三档模式，**`resolved_mode` 与 `used_embedding` 原样展示**：embedding 关闭时 semantic 请求降级为 lexical 也如实标注，不假装语义命中；触顶提示用后端 `limit` / `count` 计算）、详情面板（选中后重新拉取 `GET /skills/{name}`；失败如实报错并保留列表行数据，**不回退本地缓存假装成功**）、统计面板（`total_skills` / `skill_dirs` / `source_summary` / policy 标志三态 on/off/unknown / 按 `call_count` 排序的调用行）、热重载面板（未配置 503 → 说明端点不可用且**不渲染任何 watching 卡片**；403 → 提示补管理令牌并给出日志页链接；mutation policy 明确 `disable_hot_reload` → 按钮禁用并说明原因；启动前目录去空去重）；⑤ 入口接线：`App.tsx` 路由 `/runtime/skills`（`RetryableLazyRoute` + `AppRouteFallback`）、runtime-config 页头部导航入口、侧栏 runtime 区块入口；⑥ 双语 `skills.*`（zh-CN / en-US 逐键对齐）。验证：新增 `api/runtime/skills.test.ts` 14 例；`e2e/mock-server.mjs` 新增 skills 全族夹具（目录**刻意含一条缺 `name` 的坏条目且 `count` 上报 3**，用于断言「丢坏行但保留后端计数」；检索支持 `q` / `category` / `mode` 且 semantic 在 embedding 关闭时降级 `lexical` 并原样上报 `resolved_mode`；stats 带 policy / embedding / source_summary；热重载未配置时 503、写操作校验 `X-Skills-Admin-Token` 或 Bearer、start 校验 `dirs` 非空）；新增 `e2e/skills-market.spec.ts` 4 例（目录 / 检索 / 详情 / 统计全链路与模式降级、热重载 503 → 启动 / 停止 / 重载三个真实 POST 与请求体口径、写操作 403 提示补令牌且无伪状态、目录端点 404 不伪装空市场 + policy 禁用时按钮不可点）。缺口清单 C2 中「skills 市场 / 热重载」项关闭，余 11 行待排期。门禁见 §9.3 复测行。

> **状态（P2-1A 逐项 · 续 6）**：子代理控制面（AgentControl 身份图：`GET /api/runtime/agent-control/agents` + `POST /api/runtime/sessions/{id}/agents/{agent_id}/close|resume`）**已完成（2026-09-13 合入主树）**，A 表「子代理控制面」行交付（会话头 lineage 面包屑 + 后代目录 + 独立 Stop / Resume）。**后端零改动、纯前端接线**；身份图是控制面事实源，动作面只做「看 + 停 + 恢复」——spawn / wait / events / input 仍由 agent 侧工具调用完成，UI 不代发。交付：① `types/runtime/agents.ts`——`RuntimeAgentStatus`（`active` / `stale` / `closed` / `unknown`）、`RuntimeAgentRecord`（身份路径 / 深度 / 类型 / workflow / team / 生效 provider / model / 时间戳 / `route_warnings`）、`RuntimeAgentCatalog`（`count` 保留后端上报值、`source` 仅作诊断）、`RuntimeAgentMutation`；② `api/runtime/agents.ts`——`listRuntimeAgents`（`root_session_id` / `session_id` / `path_prefix` 三类过滤任意组合 + `include_closed`；`limit` 由前端收口默认 200 / 上限 500：后端不设上限且 `0` 表示不限，避免一次拉回全量身份图）、`closeRuntimeAgent` / `resumeRuntimeAgent`（`{agent}` 响应缺 `agent` 即抛错）、`normalizeRuntimeAgentStatus`（空值 / 未知值一律 `unknown`，**不按 active 渲染**）、`isAgentControlUnavailable`（404/405/501/503 判「控制面未启用」；500 等按真实失败呈现）；`agents` 非数组即抛错（**不把结构异常伪装成「无子代理」**），单条缺 `agent_id` 只丢该条；③ `hooks/use-session-agents.ts`——两段式加载（先按 `session_id` 定位本会话身份行拿 `root_session_id` / `agent_path`，再按 root 拉整棵树）+ 纯派生 `pickCurrentAgent` / `buildAgentLineage` / `listAgentDescendants` / `deriveSessionAgentTree`：一次加载 = 一个 AbortController + 序号（切换 / 卸载 abort、过期响应丢弃、主动 abort 不落错误态）、**身份行缺失 ≠ 错误**（`hasIdentity=false` 如实呈现「未登记身份」，不伪造 root 行）、`truncated` 用后端 `count` 与返回行数比较（不按数组长度改写）、close / resume 成功只以响应里的身份行覆盖本地该行、失败保留错误交给 UI 分类（`pendingAgentId` / `actionErrorAgentId` 定位到行）；④ `components/workspace/session-agents-panel.tsx` + `session-agents-panel-shared.ts`——`role="dialog"` 弹层（portal 到 `document.body`；`data-testid` 家族 `session-agents-panel` / `agents-unavailable` / `agents-error` / `agents-no-identity` / `agents-lineage(-node)` / `agents-descendants` / `agents-truncated` / `agents-empty` / `agents-running` / `agents-settled` / `agent-row` / `agent-no-action` / `agent-action-error`）：lineage 链（当前节点 `data-current`）+ 后代目录（运行 / 已结算分区、昵称→类型→id 回退命名、模型 / provider / workflow / team 元数据、触顶截断提示）、**动作面只对 `active` / `stale` 提供停止、只对 `closed` 提供恢复，`unknown` 不提供任何动作**（显示「状态未知，不提供操作」）；⑤ 会话头接线：`workspace-shell-topbar.tsx` lineage 面包屑（仅非新会话且链长 > 1 时渲染）+ 控制面入口按钮（含后代计数）、`main-section.tsx` 按当前会话 id 装载 hook 与弹层；⑥ 双语 `panels.agents.*` + `workspace.base.topbar.agents` / `agentsBreadcrumb`。验证：新增 4 个单测文件 / 49 例（`api/runtime/agents.test.ts` 17、`hooks/use-session-agents.test.tsx` 12、`components/workspace/session-agents-panel-shared.test.ts` 9、`components/workspace/session-agents-panel.test.tsx` 11）；本批未新增 e2e 用例（mock 环境无控制面夹具，面板对「服务端不可用 / 未登记身份」如实降级，**不伪造身份行**）。缺口清单 C2 中「subagent 控制面」项关闭，余 10 行待排期。门禁见 §9.3 复测行。

> **状态（P2-1A 逐项 · 续 7）**：侧栏会话统计摘要（`GET /api/runtime/sessions/stats`）**已完成（2026-09-13 合入主树）**，补齐续 3 中「`/sessions/stats` 不在本次范围、仍待排期」的欠账（A 表「会话元数据搜索」行的统计侧由此闭环；内容全文检索仍另议）。**后端零改动、纯前端接线**；口径硬约束：**只呈现后端上报计数，不推算、不补零**。交付：① `types/runtime/sessions.ts` 增补 `RuntimeSessionStats`（total / active / idle / closed / archived / totalMessages / tags）与 `RuntimeSessionStatsResponse`（`user_id` + `stats`）；② `api/runtime/session-stats.ts`——`fetchRuntimeSessionStats`（`user_id` 空串即不带查询参数，交服务端取默认用户）、`normalizeSessionStats`（`stats` 非对象即抛错，**不把结构异常伪装成「0 个会话」**；计数字段只接受有限非负数并取整，缺失 / 非法按 0 计；`tags` 只保留「字符串键 → 有限数」条目，不构造占位标签）、`isSessionStatsUnavailable`（404/405/501/503 判「统计不可用」，与真实失败分列不混同）；③ `hooks/workspace/use-session-stats.ts`——idle → loading → ready/error 状态机 + `refresh()`：AbortController + 请求序号（userId 变化 / 重跑 / 卸载均在途中止、过期响应丢弃）、主动 abort 不落错误态、失败保留上一次成功数据且错误标记不清除（呈现口径交 UI）、`unavailable` 单列；④ `components/workspace/workspace-sidebar/session-stats-summary.tsx` + `session-stats-summary-shared.ts`——摘要行（`data-testid` 家族 `session-stats-summary` / `session-stats-chip-{total,active,idle,closed,archived,totalMessages}` / `session-stats-loading` / `session-stats-unavailable` / `session-stats-error` / `session-stats-retry`）：total 恒显、**其余计数非零才渲染 chip**（0 计数不制造噪声，也不伪造不存在的数据）；加载中且无历史数据只说「统计加载中」、不渲染骨架数值；不可用给状态信息并显式说明，**绝不回落成 0 计数**；其他失败显示原因并可手动重试；⑤ 接线：`workspace-sidebar.tsx` 以侧栏用户筛选的**同一 `userId`** 调 hook（统计与列表同口径）、`sessions-section.tsx` 渲染摘要并透传 `refresh`；⑥ 双语 `workspace.base.sessionStats.*`（zh-CN / en-US 逐键对齐）。验证：新增 4 个单测文件 / 29 例（`api/runtime/session-stats.test.ts`、`hooks/workspace/use-session-stats.test.tsx`、`session-stats-summary-shared.test.ts`、`session-stats-summary.test.tsx`）；`e2e/mock-server.mjs` 新增 `GET /api/runtime/sessions/stats` 夹具（按 `user_id` 聚合、camelCase `totalMessages`）、`e2e/support.ts` 的 `seedSession({ userId, state })` 维度；`e2e/session-stats.spec.ts` 4 用例（默认用户聚合与 seed 一致 → 切侧栏用户重取 → 503 如实提示且不可用时不渲染任何 chip、恢复后重试可见真实计数 → 500 按真实失败呈现、不与降级混同）。缺口清单 C2 中「`/sessions/stats`」项关闭，余 9 行待排期。门禁见 §9.3 复测行与 §9.4 门禁实测。

**B. 需小改（后端字段/白名单，可并行排期）**

| 能力 | 缺口（证据） | 前端交付 |
|---|---|---|
| Deliverables 行级数据 | 工具事件白名单不含 `additions/removals/patch/mutated_paths/created_files/...`（`tool_runtime_events.go:478-532`，仅 `file_path`）；`apply_patch` 无行级统计 | 回合结束 Files changed 行（≤6 chip、`+N files`） |
| 插件配置 schema | 插件 DTO 无 `config_schema`（`harness_handlers.go:86-96`） | 设置花名册 + 配置卡片 |
| 模型采样参数 | `/models` 快照无采样字段；temperature/max_tokens 在配置文档内；`top_p` 无字段 | 设置域编辑器（草稿/校验/保存） |
| 设置域 schema | 无域级 typed schema（document sections 动态，`config_document_handlers.go:13-46`） | P2-2 草稿模型的前端映射层 |
| SSE 原生重连 | 无 `Last-Event-ID` 解析 | 仅当要求 EventSource 原生语义时补 |
| 会话目标（goal）暴露 | 后端有 goal 运行时工具（`aicli_goal`：Get/Update）与状态机（active/paused/complete/budget-limited，`session_goal_tools.go:23-52` + `internal/goal`），但**无 REST / runtime 快照字段** | 优先在 runtime 快照补 goal 字段；前端 MVP 可先解析轨迹中的 goal 工具结果做只读指示（P2-9） |

**C. 需新接口（保留后端前置依赖，UI 先以 pending/占位，不做假数据）**

| 能力 | 现状 | 建议接口形态 |
|---|---|---|
| 队列 / steering | 命令仅 submit/continue/approve/answer/interrupt/rewind；无排队实现 | `/runtime/queue` CRUD + `send-now` + `queue_updated` 事件 |
| 附件 / 多模态上传 | 无 upload 路由；chat messages 为 `[]map[string]string`；已有本地路径自动内联（`multimodal_input.go:150-171`） | `POST /api/runtime/uploads`（multipart）+ 消息 attachments/image block |
| MCP 目录 / schema | 仅 `POST /mcps/reload` | `GET /mcps`、`GET /mcps/{name}/schema` |
| Artifacts 一等公民 | 无 `/artifacts` 路由；MVP 可聚合（checkpoint + `fs/read-file` + metadata + generated-images） | `GET /sessions/{id}/artifacts`、`.../artifacts/{id}/content`、`artifact_created/updated` 事件 |
| 货币成本 | 账本无 cost/价格字段（`token_usage_history.go:148-159`） | 价格表 + cost 字段（或明确不做，仅 token 口径） |

**D. 免费收益（后端已暴露、前端零消费，17 组）**：`/capabilities`、`/status|health|events`、`/debug/prompt-layout`、`/traces/*`、`/mutation|auth|governance/policy`、`/sessions/search|stats|batch/*`、`/sessions/{id}/archive|activate|close`、`/runtime/tools`、`/runtime/tool-receipts`、`/mcps/reload`、`/teams/reload`、`/validate`、`/skills/*`、`/background/jobs/*`、`/fs/*`、`/agent-control/*`、`/supervision/*`、`/teams/{id}` 运维族。优先接：**Jobs、runtime/commands + runtime 状态、usage、sessions/search、fs/read-file、traces**。

**注意**：`/usage/*` 受 `authorizeUsageAdmin` 限制、`/observe/v1/*` 为条件注册，接入前先确认权限模型。

**落地拆包**：A 类各行前端交付可独立成 PR，建议顺序 **jobs → runtime 状态/commands → usage → sessions/search → fs/read-file → skills**；B 类后端小改与其行前端并行推进。本节 A/B/C 三档在全文（含 §4.6、§11.5）简记为 **P2-1A / P2-1B / P2-1C**。

#### P2-2 设置体系 schema 化

- **内容**：① 为 15 个后端配置域引入统一「staging 草稿 → 校验 → save/discard」模型（不可变草稿 + `validate()` 可读错误 + `discard` 不触文档 + `reset` 回落组合默认值）；② 设置读取收敛为**单一 describe 镜像 + 失效订阅**（文档一处缓存，保存后按域失效；禁止每页各自拉取/各自缓存）；③ 表单状态机显式化：`dirty / saving / readonly / overridden / invalid / reset` 均有可见表达（对齐目标项目 `ui-settings-plugins/src/client/locales.ts:4-39`）；④ **域编辑器注册表**：新增设置项只注册域描述符，不改设置壳（吸收目标项目槽位组思想，不照搬其槽位体系——Round 3 复核实为 8 个契约座位，见 §11.6 C-2）；⑤ 卡片基于统一 Card 抽象；每域至少一条组件/契约测试；⑥ 敏感输入（API Key/token）统一 `type="password"`，保存失败保留用户输入且不清空；General 增补语言切换行（回退链由 P0-6 提供）。
- **验收**：编辑不保存切走再回来不丢草稿或明确丢弃；非法值在写入前给出字段级错误；`runtime-config-diff` 继续可用；同一设置页多次进入只产生一次 describe 请求（失效订阅断言）；只读/被覆盖项有 readonly/overridden 表达而非静默失败。

#### P2-3 可访问性与响应式

- **内容**：对话框 focus-trap 与 Esc 语义；图标按钮补 `aria-label`/`title`；visually-hidden 状态播报；`aria-activedescendant` 菜单；`lang` 动态同步；**三栏几何双状态**：自动折叠（<1024px）与用户手动覆盖分离，手动展开不被 resize 重置且可记忆；窄屏导航抽屉（已有 responsive 测试）扩展为 sidebar/rightbar 双轨；长面板（teams/trajectory 详情）在窄屏转为全屏覆盖；触控目标 ≥ 32px。
- **验收**：新增 a11y e2e（键盘走查主流程 + focus 不逃逸对话框）；窄屏（<768px）可完成新建会话 → 发送 → 查看轨迹；手动覆盖状态在 resize 后保持（e2e 断言）。

#### P2-4 性能预算与长会话

- **内容**：长会话（≥2000 事件）滚动/切换性能基准；消息列表虚拟化或窗口化（如仍卡顿则引入）；轨迹已具备虚拟行，补齐「扩窗不驱逐 + ARIA index 跨 prepend」；Markdown 缓存命中率指标。
- **验收**：2000 事件会话下首次进入 ≤ 1.5s（本地构建产物）、滚动无 >100ms 长任务。

#### P2-5 视觉回归与 golden

- **内容**：选取 5–8 个高价值场景（工作台空态/消息流/折叠 Turn/工具行/审批卡/设置卡片）建立截图 golden 与 fixture 集合自校验。
- **验收**：golden 变更需显式更新并在 PR 中可见；fixture 集合不一致时测试失败。

#### P2-6 会话列表组织（分组 / 排序 / 拖拽）

- **内容**：分组视图（工作区分组 / 平铺）与排序双模式（手动 / 最近更新）正交切换；组内拖拽重排 + 跨组移动（先本地乐观、再 Host 写回、失败可见可恢复）；组内展开/折叠（Show more/less）；空白新会话在获得首条消息后自动提升。
- **依据**：目标项目 `ui-workspace/src/client/tree.ts:108,173,231`、`rows/WorkspaceBrowser.tsx:379-424,557-562,725,886-898`、`locales.ts:84-91`；当前侧栏仅有目录分组与固定排序（§5.2）。
- **依赖**：排序偏好本地持久化（`core/settings/local.ts`）；跨组移动复用 `workspace-directories` 已有归属字段。
- **验收**：两种排序模式切换不丢手动顺序；跨组拖拽失败回滚且提示；空态区分「无会话/无匹配」。

> **状态（P2-6 逐项）**：**子片 1「排序双模式 + 手动顺序账目 + 组内拖拽重排」已完成并提交（2026-09-13，`10627d34`）**；**子片 2「会话分组视图（按目录 / 平铺）+ 跨组移动与 Host 写回」已完成并提交（2026-09-13，`8520de33`；提交只含本批 27 个文件——23 个独占文件 + 4 个与并行工作流共享的文件（`workspace-shell.tsx` / `workspace-shell/types.ts` / 双语词典 `workspace/base.ts`）按「HEAD + 本批 hunk」入索引，未把在飞改动带入；缺口清单 §3 批次 15 / 批次 16 同批回填）**——**后端零改动、纯前端**；**分组口径（按会话元数据的工作目录归属）沿用批次 3 / P2-1A 既有能力，本批只加「呈现形状」与「跨组移动」，不改分组判定、不新建第二套顺序账目**。子片 2 明细：① 新增 `lib/workspace/session-grouping.ts`（`SessionGroupingMode` = `directory | flat`；`flattenSessionDirectoryGroups` 按「分组顺序 → 组内顺序」拼接平铺序列——两视图共用同一份组内账目，切换不让手动顺序分叉）；② 新增 `hooks/workspace/use-session-grouping.ts`（视图走设置域 `workspace.sessionGrouping`，渲染期只读、切换即写入；兜 `isSessionGroupingMode` 防脏存储透传到渲染层）；③ 新增 `hooks/workspace/use-session-group-view.ts`（跨组移动编排：先本地乐观覆盖 `sessionId → 目标路径`，Host 快照已反映目标路径 / 会话消失时在渲染期归约回收覆盖（不在 effect 里 setState），失败即撤销覆盖并就地提示）；④ 新增 `components/workspace/workspace-sidebar/session-grouping-control.tsx`（`role="group"` + `aria-pressed` 双档控件，与排序控件同形，两个选项都是真实行为）；⑤ 扩展 `use-session-drag-reorder.ts`（跨组落点 = 目标目录组头：写回 `PATCH /api/runtime/sessions/:id` 的 `metadata.context.workspace_path`（用注册表原始路径，组头 title 仍为规范化路径），失败回滚原组、不写假落点）；⑥ 下沉 `session-row-view-model.ts` / `session-user-menu.ts`（行为不变，为过行数门禁）并接线 `workspace-sidebar` / `sessions-section` / `workspace-shell` / `workspace-page`（`onMoveRuntimeSession` 自页面透传；平铺视图不渲染目录组头——跨组移动不可表达），`workspace-sidebar-shared.ts` 把乐观覆盖的套用 / 回收做成纯函数（无覆盖返回入参同一引用）；⑦ 双语 `workspace.base.sidebar.sessionGrouping.*`（label / groupLabel / directory / flat / directoryHint / flatHint）与 `sessionMove.*`（moved / failed）逐键对齐 + 设置域归一化（未知取值回落默认，不做隐式映射）。**子片 3「组内展开 / 折叠（Show N more）+ 空白会话提升」已完成并提交（2026-09-14，`f2c9cf98`）**：① `lib/workspace/session-grouping.ts` 新增 `SESSION_GROUP_VISIBLE_LIMIT`（5）与 `resolveSessionGroupVisibility`（纯判定：不超上限的组整体可见且不给控件；折叠态只渲染前 `limit` 个并给出「展开其余 N 个会话」；`pinnedId`（当前选中会话）落在折叠区时自动展开——不出现「已选中却看不见」的假态；只影响呈现，不改顺序、不读不写排序账目）；② `lib/workspace/session-order.ts` 新增 `SessionBlankCandidate` / `isBlankSession`（只认权威消息计数 `metadata.totalTurns === 0`；字段缺失 / 非数字一律按非空白处理——宁可不提升，也不猜）/ `promoteBlankSessions`（空白会话钉在组顶且彼此保持传入顺序，其余会话相对顺序不变；`anchoredIds`（该组手动账目里出现过的会话）不参与提升——显式意图优先于呈现层；无空白会话时返回入参同一引用）；③ 新增 `components/workspace/workspace-sidebar/use-session-group-visibility.ts`（展开态是会话期本地状态，渲染期零写存储，展开键按目录路径记忆）+ `session-group-toggle.tsx`（真实按钮：`aria-expanded` + 「展开其余 N 个会话」/「收起」）；④ `hooks/workspace/use-session-group-view.ts` 在排序结果之上套一层空白提升（`anchoredIds` 取该组手动账目）；⑤ `sessions-section.tsx` 组内渲染改走 `groupVisibility.visible`、当前选中会话作为 `pinnedId`；⑥ 双语 `workspace.base.sidebar.sessionGrouping.collapse` / `showMore` 逐键对齐。**P2-6 收口**：3-1 / 3-2 / 3-3（批次 15，`10627d34`）、分组视图 + 跨组移动与 Host 写回（批次 16，`8520de33`）、3-4 + 3-18（本批）、3-5 空态三态（批次 3，`c25a72e0`）全部落地，**P2-6 项下无未交付子项**。证据见 §9.3 对应行与缺口清单 §3 批次 15 / 批次 16 / 批次 17。

#### P2-7 命令系统（`/` 命令与内置动作）

- **内容**：composer 输入触发式 `/` 命令面板（焦点保持、方向键虚拟高亮、Enter 选择、Esc 关闭）；命令注册表 + 执行监听器错误隔离；首批内置命令：会话导出（复用轨迹 JSONL/golden 导出）、反馈入口、重命名会话。
- **依据**：目标项目 `ui-commands/src/client/{PopupSelectView.tsx:50,service.ts:411-412,locales.ts}`；当前无命令系统（§5.2）。
- **约束**：**不做**全局 Cmd+K 面板（§8）；与 P1-4 Composer 升级共用输入机。
- **验收**：命令面板键盘全流程可完成；单命令执行失败不影响其他命令与输入；导出产物可复现（golden 往返）。

> **状态（P2-7 逐项）**：**子片 2「内置命令清单与执行器」已完成并提交（2026-09-13，`bdb3ed76`；提交只含本批 15 个文件，并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底）的在飞改动未混入；本台账行与缺口清单 §3 批次 12 同批回填）**——机制层（P1-4 子片 3：`/` 触发菜单、四分类注册表、`classifyComposerSubmit`、`no-executor` 阻塞提示、键盘所有权）此前已在位，本子片补齐「真实可执行」的清单与执行器：① `lib/composer-builtin-commands.ts`（`export`=`action` / `rename`=`execute`，`parseExportCommandArgs` 只认 `--redact` 且未知 flag / 多余位置参数前置失败，`parseRenameCommandArgs` 空标题前置失败，注册表沿用「非法名 / 同名冲突构造即抛」）；② `hooks/workspace/composer/use-composer-command-executor.ts`（`run` 返回「是否认领」布尔契约、逐命令 `try/catch` 错误隔离、通知只持 i18n key + 插值、导出重入保护；`/export` 复用轨迹页同一 `exportSessionTrajectoryJsonl`，`/rename` 复用侧栏同一 `onRenameSession`——**单一事实源，不另写第二套导出 / 重命名**）；③ `components/workspace/composer-command-result-notice.tsx`（成功 `role="status"` / 失败 `role="alert"` 可关闭回执条）+ `components/workspace/composer-status-row.tsx`（状态条抽出以满足行数门禁）；④ 接线 `workspace-shell/main-section.tsx` 与 `message-composer.tsx`（已认领的命令提交后清空命令行、未认领保留草稿）；⑤ 双语 `composer.builtin.export.*`（9 键）/ `composer.builtin.rename.*`（6 键）；回执条关闭按钮复用既有 `composer.commands.dismiss`。**提交口径**：菜单打开时纯 Enter 归菜单所有，命令行提交走 Ctrl/Cmd+Enter 或发送按钮（e2e 依此断言）。**未交付（仍开放）**：命令专属 popupSelect 候选（§9.1 收敛表 5-12）。证据见 §9.3 对应行与缺口清单 §3 批次 12。

> **状态（P2-7 逐项 · 续）**：**子片 3「`/model` 会话模型切换与目录弹窗 + `/feedback` log-only 入口」已完成并提交（2026-09-13，`28546e71`；提交含本批 23 个文件——18 个独占文件 + 5 个与并行工作流共享的文件按「HEAD + 本批 hunk」入索引，并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底 / 右栏合并）的在飞改动未混入；提交态独立核验见 §9.3 行与缺口清单 §3 批次 13）**——① 新增 `lib/composer-model-options.ts`（`GET /api/runtime/models` 目录 → provider 分组（保序、同名跨 provider 不合并）、按名 / 按座位解析当前项；目录未就绪 / 为空如实返回空集与 not-ready，**不伪造模型名**）；② 新增 `components/workspace/composer-model-dialog.tsx`（+ 单测）——分组列表 + 当前项徽标 + loading / error / empty 三态 + Esc 关闭；选中走 composer 常驻座位同一个 `onModelChange`（**单一事实源**）；③ 新增 `hooks/workspace/composer/use-composer-command-surface.ts`——命令清单、`/model` 候选与弹窗开合、执行器与回执文案收口为一个命令面，`main-section.tsx` 退化为接线；④ 内置命令扩展 `/model`（缺参 / 目录未就绪前置失败、未知模型名如实报错并列出目录）与 `/feedback`（log-only：只写本地日志，回执明说本版本未接入上报渠道）；⑤ `workspace-shell.tsx` / `workspace-shell/types.ts` / `pages/workspace-page.tsx` 透传 `runtimeModels`（与常驻座位同源，不新建数据通道）；⑥ 双语 `workspace.base.modelDialog.*` 与 `composer.builtin.model` / `composer.builtin.feedback`。**如实降级**：目录 loading / 失败 / 空各自如实呈现，无目录不弹假候选，`/feedback` 不假装已上报。**未交付（仍开放）**：命令专属 popupSelect 候选（§9.1 收敛表 5-12）。证据见 §9.3 对应行与缺口清单 §3 批次 13。
> **状态（P2-7 逐项 · 续 2）**：**子片 4「命令专属 popupSelect 候选面」已完成并提交（2026-09-13，`82c81149`；提交只含本批 9 个文件，其中 2 个与并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底 / 右栏合并）共享的双语词典按「HEAD + 本批 hunk」入索引，未把在飞改动带入；提交态独立核验见 §9.3 行与缺口清单 §3 批次 14）**——把候选面从「切面清单」推进到目标项目 `PopupSelectView` 的键盘/检索语义：① 弹窗（`composer-model-dialog.tsx`）打开即把焦点交给检索框（**弹层自持焦点**，打字即检索，不必先点输入框）、↑/↓ 虚拟高亮 + Enter 应用（`preventDefault` 阻止行按钮默认激活，同一次按键不重复派发）、高亮行 `scrollIntoView({ block: "nearest" })` 滚入视口；② 新增 `filterComposerModelGroups`（`lib/composer-model-options.ts`，与目标项目 `filterOptions` 同口径：空 query 原样返回不排序不裁剪、大小写不敏感子串命中模型名或所属 provider 名、全部落空的 provider 分组不留空壳）；③ **「检索无匹配」与「目录为空」是两种状态**，各自如实呈现——不补占位模型、不伪造默认选中；④ 目录晚到（先 `loading` 后到数据）时检索框更晚挂载：以「待聚焦」标志把焦点接住，**修复打开瞬间焦点落在遮罩上的真实缺陷**；⑤ 候选面状态随弹层挂载——外壳只持对话框生命周期，检索词与高亮在关闭时随卸载归零，高亮改为派生值（默认跟住当前座位，用户 ↑/↓ 或悬停后接管），避免在 effect 里同步 `setState` 触发级联渲染（过 `react-hooks/set-state-in-effect` 门禁）；⑥ 双语 `workspace.base.modelDialog.noMatch` / `search.{aria,placeholder}`。**P2-7 至此四个子片全部落地**（机制 P1-4 子片 3 + 执行器批次 12 + `/model` 弹窗与 `/feedback` 批次 13 + 候选面本批），§6.3 验收（键盘全流程 / 单命令失败不影响其它命令与输入 / 导出可复现）自此全部有据。证据见 §9.3 对应行与缺口清单 §3 批次 14。

#### P2-8 交付物呈现与能力降级

- **内容**：回合结束 Files changed 行（依赖 P2-1B 工具事件字段）；交付物卡片四态（运行中/已交付/失败/中断）；打开动作按宿主能力降级：浏览器内「预览」（走右栏/对话框）与下载；「在文件管理器中显示/默认应用打开」仅在有宿主能力时出现，否则隐藏而非报错。
- **依据**：目标项目 `ui-deliverables/src/client/locales.ts:4-45`（含 opening/opened/error/retry 三态与 nativeUnavailable 降级）；当前无 deliverables 视图（§5.2）。
- **验收**：无宿主能力时不出死按钮；打开失败可重试；`+N files` 数据来自工具结果而非猜测。

#### P2-9 可观测面板增补（子代理树 / 任务状态条 / 目标指示）

- **内容**：子代理会话树（可折叠、运行/非运行、只读态解释为「一次性记录/父会话离线」、耗时与 token 格式化统一）；后台任务常驻状态条（"{count} 个后台任务运行中"）与 jobs 弹层整合，避免与 toast 重复通知位；**会话目标（goal）指示**——四相状态（进行中/暂停/完成/预算受限）与目标文本，MVP 数据源为轨迹中的 goal 工具结果，后端快照字段就绪后切换权威源（不双写）。
- **依据**：目标项目 `ui-subagent/src/client/locales.ts:7-60`、`ui-jobs/src/client/locales.ts:4-8`、`ui-goal/src/client/locales.ts:4-16`；后端 jobs/subagent 能力已就绪（§6.3 P2-1A），goal 状态机证据 `session_goal_tools.go:23-52` + `internal/goal`（暴露方式见 §6.3 P2-1B）。
- **验收**：子代理树可展开到末级并显示只读原因；任务状态条与弹层计数一致；格式化函数单测覆盖。

> **状态（P2-9 逐项）**：**子片 1「后台任务常驻状态条」已完成（2026-09-13；实现已入库，`frontend/` 工作树无未提交改动；本台账行尚未单独 commit）**——交付 `use-background-jobs.ts` 的 `liveCount` 派生与 `BackgroundJobsController` 导出、`jobs-panel.tsx` 改为 controller 注入、`main-section.tsx` 成为 shell owner 单例、`workspace-shell-topbar.tsx` 常驻状态条（`data-testid="topbar-jobs-running"`，仅 live > 0 渲染，点击打开弹层）+ 双语 `topbar.jobsRunning`；「任务状态条与弹层计数一致」由**同一 hook 同一判据**结构性保证（`jobs-status-bar.test.tsx` 断言 chip 与 live 分区同源），通知位按 §10 风险表约束只做计数与入口，不新增通知源。证据见 §9.3 对应行与 `docs/plan/frontend-deepseek-harness-gap-list.md` §3 批次 9。**子片 2「子代理树（可折叠到末级 + 只读原因 + 记录跨度）」已完成（2026-09-13）**——`session-agents-panel-shared.ts` 新增 `buildAgentForest`（`parentAgentId` → `agent_path` 最长前缀祖先回退、解析不到的孤儿行保留为顶层、环数据断边）/ `flattenAgentTree`（折叠感知 + `depth` / `childCount` / `hiddenDescendantCount`）/ `agentDurationSpan`（`createdAt` → `closedAt ?? updatedAt`，端点缺失 / 非法 / 倒挂即 `null`）/ `formatAgentDuration` + `formatAgentDurationExact`（判别联合九档，精确档 ≥ 一天补零到秒）/ `agentReadOnlyReason`（只答「已关闭记录」「父代理不在线」）；新增 `session-agents-tree.tsx` 承载层级渲染（`role="treeitem"` + `aria-level` + 分支 `aria-expanded` + `agent-branch-toggle` + `agent-duration` chip + `agent-readonly`），`session-agents-panel.tsx` 行渲染整体下沉（分区改为计数摘要 + 单棵层级树）；双语 `panels.agents.*`（`treeAria` / `branchExpand` / `branchCollapse` / `durationLabel` / `durationExactTitle` / `duration.*` / `readonly.*`）逐键对齐。验收「子代理树可展开到末级并显示只读原因」由 `session-agents-tree.test.tsx`（4 例）与 `session-agents-panel-shared.test.ts`（+14 例）覆盖，证据见 §9.3 对应行与缺口清单 §3 批次 10。**子片 3「会话目标（goal）四相指示（MVP）」已完成（2026-09-13）**——数据面复核确认 goal 只在 chat SSE `tool_end.payload.tool.content` 完整到达（线程层 `getToolResultSummary` 截断 240 字符、轨迹层 `toolResultSummaryOf` 不读 `tool.content`），故唯一写入口放在 SSE 边界：新增 `lib/session-goal/derive.ts`（`parseGoalToolResult` 对截断 / 非法 JSON / 无 goal 字段一律 `unknown`；`parseSessionGoal` 四相映射且未知 status 不映射但 `statusRaw` 保真；`deriveSessionGoal` 的 `unknown` 不覆盖 / `absent` 如实清空；`formatGoalTokens` 不补零）+ `lib/session-goal/store.ts`（按 `session_id` 的只读投影，`useSyncExternalStore`，无法判定的结果不写入）+ `components/workspace/session-goal-indicator.tsx`（`topbar-session-goal`，无数据不渲染、token 两侧都有才显示），由 `stream-handlers.ts` 的 `onToolEnd` 捕获、`workspace-shell-topbar.tsx` 以 `selectedThread.sessionId` 订阅；暂停 / 恢复 / 完成入口不渲染（REST 未暴露，不放置死按钮）。验收由 `session-goal.test.ts`（14 例）与 `session-goal-indicator.test.tsx`（7 例）覆盖，证据见 §9.3 对应行与缺口清单 §3 批次 11。**剩余**：① 子代理 token 口径——`RuntimeAgentRecord` 无 token 字段（后端未上报），按「缺啥不显示啥」不渲染，待后端补字段后接入（禁止用会话级 usage 均摊推算）；② 状态词汇表统一（§11.6 C-6：全局通知位需自建，不得引用目标实现作参照）。

#### P2-10 文件树只读浏览（基于已就绪的 `fs/*`）

- **内容**：按会话工作区浏览目录（loading/empty/truncated/noWorkspace 状态 + reload）；错误分类（不存在/越出工作区/非目录/暂不可用）各有可行动文案；文件内容预览接入右栏；路径显示统一家目录 `~` 缩写（对齐 6-14）。
- **依据**：后端 `POST /fs/read-file` 等已就绪（§6.3 P2-1A）；目标项目 `ui-sidebar-files/src/client/locales.ts:22-50`（错误分类/截断提示）。
- **约束**：**不采纳**终端面板（无 PTY 接口，§8）；目录越界属安全语义，文案不可弱化为普通失败。
- **验收**：大目录截断有提示；越界错误不可重试为普通失败；预览不阻塞侧栏。

#### P2-11 右栏标签注册表与资源打开

- **内容**：把右栏（artifact / checkpoint / 文件预览 / 交付物 / teams 详情）收敛为**标签注册表**：每个面板注册 `id / label / icon / 打开谓词 / 渲染器`，右栏壳统一处理显示、切换、全屏、「tab 记忆」与打开历史（后退/历史列表）；「打开资源」统一入口（消息内文件链接 / Files changed 行 / 文件树 → 右栏预览，必要时回退对话框）。
- **依据**：目标项目 `ui-sidebar-right/src/client/index.ts:17-21,41-44`（两段式注册）、`resources/src/client/contract.ts:35-70`（资源模型）；当前右栏为散装面板（资产 A7）。
- **约束**：不引入 dockkit 停靠代数（§8），只做注册表 + 轻量显示状态；不改变现有面板对外 props。
- **验收**：新增面板不改右栏壳代码（注册即生效）；同一资源重复打开不重复挂载；右栏宽度/折叠状态可记忆。

---

## 7. 关键设计决策（落地方案要点）

### 7.1 消息是投影，不是状态

**决策**：会话渲染的唯一权威仍是 runtime 事件与会话历史；前端只维护「投影 + 本地乐观输入」。本地 optimistic 消息与权威记录之间用**原子替换**（同 key 替换而非增量补丁），禁止双写漂移。

**理由**：目标项目在 Chat/Trajectory/Goal/Steering 四处均采用同一模式；当前项目 `deltaCoordinator` 已接近该模型，改造是收敛而非重写。

**代价与缓解**：投影表需要在 `workspace-thread-state.ts` 拆分时显式定义 ownership；新增单测覆盖「乐观消息被权威替换」「重复 delta 幂等」「乱序事件缓冲」三类。

### 7.2 折叠用谓词表达，不用组件内状态

**决策**：Turn 折叠与否由纯函数谓词决定（终局边界 + 证据计数），折叠摘要由持久计数生成，组件只渲染结果。

**理由**：当前所有消息平铺在 `message-list.tsx`，折叠若散落在行组件里会导致「折叠状态 vs 流式到达」竞态。谓词化后可单测、可复用（Chat 与 Trajectory 共享同一窗口数据）。

### 7.3 增量渲染的边界

**决策**：只对**追加式**流式文本启用前缀冻结；输入被截断/编辑（如用户回溯、重试）时递增 generation 并丢弃缓存。流式期不烘焙可能过期的 handler（引用/图片/链接），settled 后再解析。

**理由**：目标项目已把「已知偏差 + 自愈路径」写进注释；本方案要求在实现处同样写明偏差（引用式链接/脚注在冻结边界另一侧时的短暂字面渲染）。

### 7.4 滚动与输入座位

**决策**：滚动宿主唯一，composer 悬浮高度通过 CSS 变量参与滚动区域计算；贴底跟随与阅读保顶互斥切换；prepend 历史按锚点恢复。

**理由**：当前 `useTypewriter` 逐字显示 + 容器滚动在长会话下会与历史加载互相干扰；把滚动契约写成可执行 e2e 是最低成本防回归手段。

### 7.5 阻塞式交互的统一生命周期

**决策**：审批与提问共用「注册 pending → 持有取消信号 → 结果回填 → delegate-on-remove」生命周期；plan mode 评审作为提问的一种 shape 复用同一呈现位。

**理由**：当前只有 plan mode 三选一面板且与 artifact 面板耦合；统一后新增审批只需新增 shape，不新增并行机制。

### 7.6 样式唯一权威与消费纪律

**决策**：L2 语义 token 是唯一颜色/阴影/字体来源；feature 层禁止 hex/rgb、禁止主题选择器、禁止引用 L1 原色阶；新增 token 必须成对（primitive 步进 + semantic 别名）。

**理由**：当前 `globals.css` 单层变量 + 任意值消费，把「主题/强调色/落地页」耦合在三处；三层化后主题切换退化为属性翻转，落地页并入同体系。

### 7.7 组件表面契约

**决策**：浮层 elevation-only（禁用 border + shadow 并用）、中性 border 0.5px 发丝、滚动条单点拥有、满圆配对 corner-shape（若启用）。这些作为规范写入 `frontend/docs/styling.md`（新增），并尽量用样式测试或 lint 校验（如 CSS 中禁止 `border` 与 `elevation` 同时出现于同一浮层选择器）。

### 7.8 i18n 真源与门禁

**决策**：`zh-CN` 为 key 真源，其它语言编译期对齐；UI 文案必须走 `t()`；内部匹配用稳定 id/discriminant。门禁脚本随 CI 或 `npm run lint` 执行。

**理由**：当前双语文案 2.5k 行手工同步 + 无门禁，任何新增文案都可能单语漂移；类型对齐加 AST 扫描是成本最低的机械保障。

### 7.9 外部订阅契约（store 的最小纪律）

**决策**：不引入目标项目的自研 store；但任何对外暴露订阅的状态层（含 `useSyncExternalStore` 包装、事件总线）必须满足两条：① 单个订阅者回调抛错被捕获并 `console.error`，不影响其余订阅者；② 选择器结果浅比较后才通知，避免无关重渲染。

**理由**：这两条是 `store/src/index.ts:52-58,67-69` 与功能点 6-10（订阅者错误隔离）的可移植内核，成本低，可避免「一个面板抛错拖垮全局刷新」类故障。

---

## 8. 明确不采纳清单

| 项 | 原因 | 替代方案 |
|---|---|---|
| cordis `ctx.slots.register/provide/inject` 四 shares 体系 | 收益仅存在于「多插件动态装配 + HMR」；照搬会在 React 之上再造一套框架 | 路由 + 组合式组件 + 显式 props/context；仅保留「keyed 注册表」思想用于工具视图与命令 |
| `dsh.client` 清单 / boot manifest / `__DSH_BOOT__` / profile 依赖镜像 | 宿主插件体系基础设施，普通 Web 应用不需要 | 维持 Vite 构建 + 路由懒加载 |
| `ui-dockkit` split-tree/float/dock 全量操作代数 | 只有在产品真需要可停靠工作台时才划算 | 轻量右栏（显示/隐藏/全屏 + tab 记忆），吸收「纯函数 planner + settled intent 上报」思想 |
| `schema-form` 包 | 目标仓库快照仅剩 `lib/` 构建产物，无可移植源码 | 自建 `settings-draft`（不可变草稿 + validate）小模块 |
| 平台特定样式细节 | 依赖 Chromium 具体行为，需按目标浏览器重新测量 | 项目内以 token 表达，测量后定值 |
| 双语文档配对门禁 | 成本高，仅覆盖文档读者 | 只保留 UI 文案 i18n 门禁 |
| 终端面板（PTY / xterm） | 后端无 PTY 接口，目标项目 Web 端亦未提供 | 不进入本期；若未来需要，另立专项 |
| **文件树（修正）** | 原判「无后端接口」已过时：`POST /fs/read-file|write-file|append-file` 早已就绪、前端零消费（§6.3 P2-1A/§6.3 P2-10） | **改为采纳**：本期做只读浏览 + 内容预览；写入类动作不进 UI |
| 全局 Cmd+K 命令面板 | 目标项目无此设计；与 composer `/` 命令、轨迹搜索、会话搜索功能重叠 | 只做输入触发式 `/` 命令（P2-7）；全局入口留给后续独立评估 |
| 多标签 / 多窗口工作台（1-13） | 当前路由与状态模型不支撑，收益不明 | 单窗口 + 右栏切换；如需并行会话用 Fork/多会话列表 |
| 桌面原生动作（在文件管理器中显示 / 默认应用打开） | 浏览器环境无宿主能力，硬做会产出死按钮 | 按宿主能力探测降级：有则显示，无则隐藏（P2-8）；只保留预览/下载 |
| 未读标记 / 置顶 / 批量多选（3-19） | 目标项目（Round 3 目录级逐文件复核，关键词零命中，§11.6 C-3）与当前均无；需求未验证 | 不采纳（需求未验证）；P1-9 先做「等待态」这类有明确任务的指示 |
| 货币成本展示 | 后端账本无 cost/价格字段（`token_usage_history.go:148-159`） | 本期以 token/缓存口径呈现（P2-1A）；不做伪成本 |

**次级清单（Round 2 B 节及暂缓项，编号对应目标项目功能点）**

> 类别口径：本清单中**未标注类别者为不采纳**；**标注「暂缓」者为暂缓**（与 §5.6 计数、§11.5 处置一致）。

| 项 | 原因 | 替代方案 |
|---|---|---|
| PWA manifest / 可安装应用（1-2） | 本地工作台以标签常驻，安装收益低（目标项目静态元数据齐备，属产品取向取舍，§11.6 C-5） | 如未来需要离线/角标，另立专项 |
| HMR 帧协议（1-10） | 依赖目标项目自研 loader | Vite HMR |
| fixture 查询开关（1-11） | 生产误开风险高于收益 | e2e 用 route mock / seed |
| 单页单视图（1-12） | 当前已有 react-router 多路由，属能力而非负担 | 保留现有路由 |
| 多标签状态广播（1-13） | 会引入双写流与重复 SSE | 单窗口守护（见主表「多标签/多窗口」行） |
| 原生 OS 目录选择器（2-15） | 纯 Web 部署无法依赖宿主能力 | 应用内浏览对话框（已具备）为主，原生仅作可选增强 |
| 设置壳独立包（4-2） | 当前设置页结构可用，拆包无收益 | P2-2 在原结构内 schema 化与收敛 |
| 遥测 / 埋点（6-12） | 本地优先产品无接收方 | 结构化日志 + 诊断导出 |
| 浏览器鉴权 / 登录 UI（1-14，暂缓） | 本地/局域网部署无账号语义 | 仅按权限模型接入后端授权策略端点（§6.3 P2-1）；如需要只做 token/一次性链接页 |
| 明文 HTTP 信任提示（1-15，暂缓） | 属文案级增强，非阻塞 | 后续在连接设置补安全提示文案 |
| 定时任务指示（3-14，暂缓） | 后端无 scheduled task 证据 | 待核验后再定 |
| Onboarding 设置槽（4-5，暂缓） | 目标项目亦无完整流程；当前落地页已承担首屏引导 | 按留存数据再决策 |
| 工作流运行视图（5-5，暂缓） | workflowRun 阶段/成员模型后端证据不足 | 先统一状态词汇表（P2-9） |
| 会话级 Agent 预设切换（5-11，暂缓） | 当前为设置页路由配置；会话级座位缺后端预设版本 | 待 §6.3 P2-1B/C 评估 |
| 加密 PDF 预览（2-13 子项，暂缓） | 源产品自身不支持；成本高、需求低频 | 降级为下载/外部打开 |
| 全局虚拟滚动 / 本地全文索引 | 消息面未到瓶颈；本地索引有数据规模与迁移成本 | 窗口化优先（P2-4）；检索走服务端（P2-1A） |

---

## 9. 里程碑与验收标准

### 9.1 里程碑

| 里程碑 | 内容 | 验收标准 |
|---|---|---|
| M0（≈2 周） | P0-1 ~ P0-7 全部完成（**已完成 2026-09-11**） | `npm run build` / `test` / `test:e2e` 全绿；无 `.bak`；`landing.css` 变量清零；`lint:i18n` 通过（复核证据见 §9.3「M0 门禁复核」） |
| M1（≈4 周） | P1-1 ~ P1-4（渲染/滚动/Composer）+ P1-10（错误边界，纯外壳可并行） | 折叠谓词与滚动契约 e2e 通过；附件/命令/引用可用；增量渲染基准达标；抛错不白屏 |
| M2（≈6 周） | P1-5 ~ P1-9（队列/工具行/审批/连接状态/列表状态） | busy 队列与审批卡端到端可用；断流自动恢复且手动重试幂等；工具行四态测试通过；等待态可识别 |
| M3（≈10 周） | P2 首批：P2-1 A 类 + P2-6 / P2-7 / P2-9 | 新面板数据来自权威接口（无假数据）；Files changed 行来源为工具结果；jobs/subagent 面板可用 |
| M4（≈12 周） | P2-2 / P2-3 / P2-4 / P2-5 + P2-8 / P2-10 / P2-11 | 每域测试齐备；窄屏主流程可完成；长会话性能预算达标；右栏注册即生效；**超长文件（>500 行）降至 ≤ 10 且余项有排期** |

### 9.2 全局完成定义（DoD）

1. 新增/修改的交互均有组件测试或 e2e 覆盖，且断言写在语义层（角色/可访问名/可见文本/锚点），不依赖 DOM 数量与实现细节。
2. 新样式只消费 L2 semantic token；新增 token 有 primitive + semantic 两条。
3. 新文案有 zh/en 双份且通过类型对齐与门禁扫描。
4. 不引入新的重复卡片/重复对话框实现。
5. 相关文档（`frontend/docs/styling.md`、本方案）随实现同步更新。

### 9.3 实施记录

| 任务 | 状态 | 落地内容 | 验证证据 |
|---|---|---|---|
| P0-1 目录与构建卫生 | 已完成（2026-09-11） | 删除 `frontend/src/**/.backups/` 全部 8 个目录（20 个 `.bak`）；清理 `frontend/tmp/`（孤儿 `check-usage-page.mjs` 备份）与 `frontend/mock-out.log`/`mock-err.log` 残留；`frontend/.gitignore` 增补 `*.bak`、`.backups/`；`e2e/diag.spec.ts` → `e2e/diag.manual.ts`，新增 `playwright.manual.config.ts` 与 `npm run test:manual` 按需运行 | `frontend/src` 下 `*.bak`=0、`.backups/`=0；`git check-ignore` 命中新增规则；`npm run build` + `npm run test` 全绿（exit 0）；`npx playwright test --list` = 16 tests / 4 files（不含 diag），`npm run test:manual` 单跑 diag 通过 |
| P0-2 超长文件拆分（首批·第 1 项 `types/runtime.ts`） | 已完成（2026-09-11） | `types/runtime.ts`(1692) → `types/runtime/` 14 个域模块：`chat`149 / `sessions`102 / `checkpoints`82 / `backtrack`96 / `plans`50 / `events`10 / `logs`53 / `analytics`232 / `teams`354 / `config`99 / `service`26 / `harness`108 / `siteaccount`166 / `cache`152 + `index.ts` barrel（`export *`）。机械切片不改字段与导出名，`@/types/runtime` 对外路径不变；切片脚本留档 `.tmp/split-runtime-types.mjs` | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm run test` 83 文件 / 459 用例全绿；14 个新模块 + barrel 全部 < 500 行（最大 `teams.ts` 354）；`frontend/src` 内 > 500 行文件数 41 → 40 |
| P0-2 超长文件拆分（首批·第 4 项 `lib/workspace-thread-state.ts` + 随源第 16 项测试） | 已完成（2026-09-11） | `workspace-thread-state.ts`(1815) → `lib/thread-state/` 10 个职责模块：`deltas`130 / `messages`111 / `generated-images`241 / `history-mapping`235 / `history-artifacts`296 / `events`411 / `tools`191 / `sessions`141 / `shared`60 / `text-utils`54 + `index.ts` barrel。跨模块复用的原私有 helper 就地补 `export` 但**不上浮**：`index.ts` 按拆分前导出名逐个 `export` / `export type`（新增对外导出 0）；`workspace-thread-state.ts` 退化为 2 行 barrel（`export * from "./thread-state"`），13 个消费方 import 路径零改动。随源测试 `workspace-thread-state.test.ts`(800) 按关注点切为 4 个同源测试：`history-projection`394 / `assistant-segments`215 / `runtime-events`99 / `deltas`85 + 共享 `test-fixtures.ts`(35)，断言零改动。模块划分与 §6.1 草拟名 `{messages,deltas,queue,projection}` 不一致——`queue` 在该文件中无对应实现，按实际职责落为上述 10 模块，验收仍按「职责拆分 + barrel 保接口」。脚本留档 `.tmp/split-thread-state.mjs`、`.tmp/split-thread-state-test.mjs` | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm run test` 86 文件 / 459 用例全绿（切分前 83/459，仅文件数因测试切分 +3）；对外导出面对拍：拆分前 42 → 拆分后 42，missing=0 / extra=0；新模块与测试文件全部 < 500 行（最大 `events.ts` 411）；`frontend/src` 内 > 500 行文件数 40 → 38 |
| P0-2 超长文件拆分（首批·第 13 项 `lib/trajectory/trajectory-reducer.ts` + 随源第 34 项测试） | 已完成（2026-09-11） | `trajectory-reducer.ts`(831) → `lib/trajectory/trajectory-reducer/` 4 个层次模块 + `index.ts` barrel：`event-readers`230（载荷/seq/描述解析）/ `snapshot-ops`120（`TERMINAL_STATUSES` + clone/append/find/upsert）/ `apply`330（`applySequencedEvent`/`applyReasoningEvent`/`applyToolEvent`/finalize/freeze）/ `api`147（`removeItem`/`applyEvent`/`advanceSeqCursor`/`applyEvents`）。沿用「跨模块复用的原私有 helper 就地补 `export` 但不上浮」策略：`index.ts` 逐个转出拆分前 11 个公共导出（新增导出 0），`trajectory-reducer.ts` 退化为 2 行 barrel，消费方 import 路径零改动；语义零改动——乱序缓冲/幂等/终态冻结/seq 空洞推进按 A16 原样搬迁。随源测试 `trajectory-reducer.test.ts`(537) 按关注点切为 3 个同源测试：`sequence`174（基础序列/乱序缓冲/空洞续接）/ `state`212（幂等/终态/工具状态机/G7/error）/ `events`144（未知事件/批量重放/runtime 摘要）+ 共享 `trajectory-reducer.test-fixtures.ts`(29)，12 个 describe 整块搬迁、断言零改动。脚本留档 `.tmp/split-trajectory-reducer.mjs`、`.tmp/split-trajectory-reducer-test.mjs` | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm run test` 88 文件 / 459 用例全绿（切分前 86/459，文件数 +2 因测试切分）；对外导出面对拍：拆分前 11 → 拆分后 11，missing=0 / extra=0；trajectory 目录定向 `npx vitest run src/lib/trajectory` 10 文件 / 86 用例全绿；拆分等价性校验 `.tmp/verify-trajectory-split.mjs`：28 个声明块 + 12 个 describe 块逐字节命中（仅允许 `export` 提升）→ EQUIVALENCE OK；新模块与测试文件全部 < 500 行（最大 `apply.ts` 330）；`frontend/src` 内 > 500 行文件数 38 → 36 |
| P0-2 超长文件拆分（首批·第 10 项 `pages/usage-analytics-page.tsx`） | 已完成（2026-09-11） | `usage-analytics-page.tsx`(974) → `pages/usage-analytics/` 4 个模块 + 40 行入口保留：`format`160（数字/百分比/时长/时间戳格式化、`readAdminToken`、`analyticsFilterKeys`、全部 i18n key/tone 映射器）/ `primitives`287（`emptyTotals`/`emptyCoverage`/`emptyDimensions` + `AnalyticsHeader`/`Metric`/`QualityNotice`/`FilterInput`/`FilterSelect`/`QualityBadge`/`TabButton`/`UsageViewTabs` + 图表 fallback）/ `sessions`294（`SessionTable`/`SessionDetail`/`SessionOverview`/`SessionTokens`/`Diagnostics`/`TurnTable`）/ `overview`201（`UsageOverview` + 懒加载 `UsageAnalyticsCharts`）；入口保留 `UsageAnalyticsPage` + 懒加载 `CacheAnalyticsView` + `CacheViewFallback`。沿用「跨模块复用的原私有 helper 就地补 `export` 但不上浮」：对外公共面仍是 `UsageAnalyticsPage` 单一导出（missing=0 / extra=0），3 个消费方（`App.tsx` 路由 + 2 个测试）import 路径与具名导入零改动；模块依赖单向无环（format ← primitives ← sessions ← overview ← page）。脚本留档 `.tmp/split-usage-analytics.mjs` | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/pages/{usage-analytics-page,cache-analytics-page}.test.tsx` 2 文件 / 8 用例全绿；拆分等价性校验 `.tmp/verify-split.mjs`：44 个声明块逐字节命中 → EQUIVALENCE OK；新模块全部 < 500 行（最大 `sessions.tsx` 294）；`frontend/src` 内 > 500 行文件数 36 → 35 |
| P0-2 超长文件拆分（首批·第 8 项 `hooks/workspace/use-workspace-agent-chat-turn.ts`） | 已完成（2026-09-11） | `use-workspace-agent-chat-turn.ts`(1337) → 入口 446 行 + `hooks/workspace/agent-chat-turn/` 10 个模块：`stream-handlers`394（SSE 消费：meta/chunk/reasoning/tool/planning/orchestration/route/observation/subagent/result/done/error 回调聚合为 `createAgentChatStreamHandlers`）/ `finalize-turn`249（终态收敛：文本与推理合并、最终产物生成、线程消息段写回、DEV 双跑校验、完成通知）/ `use-reasoning-effort`152（档位子 hook）/ `streaming-writers`144（工具段 upsert / 工具结束 / 阶段切换 / 流式错误落盘）/ `final-artifacts`113 / `streaming-frame`103（rAF 批处理 + 可见性监听）/ `turn-state`51（可变状态容器 `ChatTurnRuntimeState`）/ `notifications`37 / `thread-factory`29 / `shared`23。沿用「机械搬迁、语义零改动」：handler 闭包按原顺序逐字节搬迁，仅以 `turnState.X` 前缀与 deps 注入改写；入口保留发送编排（`submitPrompt`、连接超时、abort/finally 收口）与各工厂装配；跨模块复用的原私有 helper 就地补 `export` 但不上浮，对外导出面 3 → 3（missing=0 / extra=0），`workspace-page` 等消费方 import 路径零改动。脚本留档 `.tmp/split-agent-chat-turn-{4,5,6}.mjs`；等价性校验 `.tmp/verify-agent-chat-turn-step{4,5,6}.mjs`：回调块逐字节（14/14）、入口非 import 行逐行（changed=0）、字符串字面量多重集（missing=0 / extra=0）→ 三步 EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/hooks/workspace/use-workspace-agent-chat-turn.test.ts src/pages/workspace-page.test.tsx` 全绿；新模块全部 < 500 行（最大 `stream-handlers.ts` 394），入口 1337 → 446；`frontend/src` 内 > 500 行文件数 35 → 34 |
| P0-2 超长文件拆分（首批·第 7 项 `components/workspace/settings/runtime-provider-groups-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-provider-groups-domain-editor.tsx`(1358) → 入口 149 行 + `runtime-provider-groups-domain-editor/` 6 个模块：`provider-group-members-section`423（members 区块：memberSummary/missingRoleCount/weightedMissingWeightCount 记忆化 + 自动补角色/权重 + 成员表）/ `draft-utils`309（KNOWN keys、5 组 option 常量 + 草稿构造/校验定位/选项构建/成员汇总 13 个纯函数）/ `provider-group-basic-fields`271（基础字段网格 + failover/truncation 面板）/ `provider-groups-table`267（域表：摘要徽标、成员引用徽标、行内复制/编辑/删除）/ `provider-group-dialog`127（对话框外壳 + 校验提示 + 组合）/ `field-parts`48（ProviderReferenceBadge / FieldIssueText）。沿用「机械搬迁 + props 化外壳」：15 个区间按行搬迁（含 state/memo/handler），入口仅保留 state、2 个 memo、4 个 handler 与两个子组件装配；跨模块复用的原私有 helper 就地补 `export` 但不上浮（入口对外仍只导出 `RuntimeProviderGroupsDomainEditor`，before=after）；表格块 3 个回调名（handleCopyGroup/openEditDialog/openCreateDialog）随 props 改名，dialog 外壳 4 处（open/onClose×2/onConfirm）props 化；`setDraft` 直接下传以保证原 JSX 字节不动。脚本留档 `.tmp/split-provider-groups-1.mjs`；等价性校验 `.tmp/verify-provider-groups-split.mjs`：15 个区间逐行命中（仅允许 `export` 提升与上述改名）→ EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npm run build` exit 0（2841 modules transformed）；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/components/workspace/settings/runtime-provider-groups-domain-editor.test.ts` 6 用例全绿；新模块全部 < 500 行（最大 `provider-group-members-section.tsx` 423），入口 1358 → 149；`frontend/src` 内 > 500 行文件数 34 → 33 |
| P0-2 超长文件拆分（首批·第 9 项 `components/workspace/settings/runtime-provider-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-provider-domain-editor.tsx`(1187) → 入口 349 行 + `runtime-provider-domain-editor/` 6 个模块：`provider-table`243（域表：摘要徽标 + 6 列（名称/协议/默认模型/站点余额/状态/行操作）+ 搜索与分页）/ `provider-account-section`235（账号面板：site_type/account_auth_ref/凭据字段 + 探测/抓取/刷新按钮 + 结果提示）/ `draft-utils`164（KNOWN keys、`AccountAction`、协议选项、搜索文本、草稿构造、错误描述）/ `provider-basic-fields`162（基础字段网格 + supported_models/support_types + api_key）/ `provider-dialog`141（对话框外壳 + 错误提示 + 三个区块组合 + extraJson + enable/setAsDefault 开关）/ `provider-network-fields`133（headers/model_mappings + proxy 面板）。沿用「机械搬迁 + props 化外壳」：14 个区间按行搬迁；跨模块复用的原私有 helper 就地补 `export` 但不上浮（入口对外仍只导出 `RuntimeProviderDomainEditor`，before=after）；表格 4 个回调名（openCreateDialog/openEditDialog/handleCopyProvider/handleRefreshProviderAccount）与账号区块 3 个回调名随 props 改名，dialog 外壳 4 处（open/onClose×2/onConfirm）props 化；`draft`/`setDraft` 直接下传以保证原 JSX 字节不动。脚本留档 `.tmp/split-provider-domain-1.mjs`；等价性校验 `.tmp/verify-provider-split.mjs`：14 个区间逐行命中（仅允许 `export` 提升与上述改名）→ EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npm run build` exit 0（813ms）；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/components/workspace/settings/runtime-provider-domain-editor.test.ts` 6 用例全绿；新模块全部 < 500 行（最大 `provider-table.tsx` 243），入口 1187 → 349；`frontend/src` 内 > 500 行文件数 33 → 32 |
| P0-2 超长文件拆分（首批·第 11 项 `pages/logs-page.tsx`） | 已完成（2026-09-11） | `logs-page.tsx`(893) → 入口 365 行 + `pages/logs-page/` 5 个模块：`logs-header`277（页头：状态徽标/导航/搜索+级别+口令过滤栏/刷新/复制视图链接/跟随开关/文件状态行/活动 chips）/ `logs-list-panel`179（左列表面板：级别统计筛选徽标 + 列头 + loading/error/empty/列表四态）/ `format`136（时间戳、详情值、级别色调与短标签、条目副标题/元信息/上下文、详情行等 9 个纯函数）/ `primitives`54（`LogHeaderBadge` / `CopyActionButton` / `LogsPageDetailPanelFallback`，拆出以满足 `react-refresh/only-export-components`）/ `connection`49（`connectionTone`：连接态徽标与图标）。沿用「机械搬迁 + props 化外壳」：原 49-268 行 helper 按纯函数/组件/连接态三类原样落位（仅补 `export`）；header 块（原 543-727）去 4 空格缩进、9 处回调 props 化（setQuery/setLevel/setAdminToken/setFollow/refresh/clearActiveChip/clearAllActiveState/handleCopy(view_link)/copiedSection）；列表面板块（原 730-853）去 6 空格缩进、2 处回调 props 化（setLevel/setSelectedCursor）；`subtitleLabels` 与 `levelFilterOptions` 在子组件内以同一 t 键重建，入口 `buildEntrySubtitle` 复用等值三键的 `detailLabels`；入口保留状态编排、URL 同步、详情面板 labels 与装配。等价性校验 `.tmp/verify-logs-split.mjs`：helper 三类区间逐行、header/列表面板块逐行（仅允许上述改名与缩进）、入口保留区间逐行、被移出符号零残留 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint src/pages/logs-page.tsx src/pages/logs-page` 0 问题；`npm run build` exit 0（807ms，2852 modules transformed）；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/pages/logs-page-shared.test.ts src/pages/logs-page-detail-panel.test.ts` 9 用例全绿；新模块全部 < 500 行（最大 `logs-header.tsx` 277），入口 893 → 365；`frontend/src` 内 > 500 行文件数 32 → 31 |
| P0-2 超长文件拆分（首批·第 12 项 `components/workspace/runtime-teams/runtime-team-dispatch-panel.tsx`） | 已完成（2026-09-11） | `runtime-team-dispatch-panel.tsx`(846) → 入口 180 行 + `runtime-teams/runtime-team-dispatch-panel/` 8 个模块：`provision-section`181（创建可运行团队表单 + fan-out 模板切换）/ `task-composer`177（任务表单：标题/目标/输入/交付物/优先级 + 派发按钮 + 错误 + 结果列表）/ `monitor-outcome-compare`177（结果对比：覆盖率卡片 + 对比行 + 终态摘要/缺口列表）/ `monitor-summary`138（批次统计 10 卡片 + 状态 pill + 自动刷新/最新更新/监控计数）/ `team-select-list`96（团队复选列表 + readiness 徽标）/ `monitor-entries`91（监控条目：摘要/邮箱预览/错误）/ `monitor-panel`84（监控面板外壳：刷新 + 错误 + 摘要/对比网格 + 条目）/ `format`11（4 个类常量）。沿用「机械搬迁 + props 化外壳」：原 154-284（provision + 模板）、286-349（团队列表）、352-477（任务表单，仅 `selectedDispatchTeamIds.length` → `selectedTeamCount` 1 处替换）整块搬迁；监控区 480-841 拆为骨架 + 3 子组件，`comparisonRows`/`batchSummary` 计算下沉到 `monitor-panel`，`terminalRowsWithSummary`/`terminalRowsMissingSummary` 下沉到对比组件；入口仅保留 props 类型、页头与装配。等价性校验 `.tmp/verify-dispatch-split.mjs`：8 个区间逐行命中（含缩进与 1 处替换）+ 入口保留区间逐行 + 被移出内容零残留 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams.tsx src/components/workspace/runtime-teams/runtime-team-dispatch-panel.tsx src/components/workspace/runtime-teams/runtime-team-dispatch-panel` 0 问题；`npm run build` exit 0（1.04s，`runtime-team-dispatch-panel` chunk 25.99 kB）；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run src/components/workspace/runtime-teams` 2 文件 / 10 用例全绿；新模块全部 < 500 行（最大 `provision-section.tsx` 181），入口 846 → 180；`frontend/src` 内 > 500 行文件数 31 → 30 |
| P0-2 超长文件拆分（首批·第 14 项 `components/workspace/runtime-teams/runtime-team-details-panel.tsx`） | 已完成（2026-09-11） | `runtime-team-details-panel.tsx`(812) → 入口 147 行 + `runtime-teams/runtime-team-details-panel/` 10 个模块：`mailbox-section`229（信箱撰写表单 + 消息列表 + ack）/ `path-claims-section`203（冲突检查表单 + 结果卡 + 租约列表）/ `team-snapshot`164（团队页头 + meta pills + Tasks/Teammates/Task Graph 三卡 + detailsError）/ `roster-section`83（成员列表 + 能力徽标）/ `task-queue-section`75（任务队列）/ `types`69（props 与 section 类型）/ `timeline-section`66（事件时间线）/ `primitives`52（`TeamDetailsSection` 折叠外壳）/ `final-summary-section`38（终态总结）/ `format`24（4 个类常量 + `createInitialOpenSections`）。沿用「机械搬迁 + props 化外壳」：原 209-330 / 332-383 / 385-431 / 433-595 / 597-746 / 748-787 / 789-805 七块按 section 整块搬迁（仅 `open={openSections.x}` → `open={open}`、`onToggle={() => toggleSection("x")}` → `onToggle={onToggle}` 两处 props 化替换），section 内其余 JSX 与 `details.*` 引用逐行零改动；`primitives` 因 `react-refresh/only-export-components` 再拆出 `format`（常量/纯函数与组件分离）；入口仅保留 props 解构、`openSections` 状态、装配与导出壳。等价性校验 `.tmp/verify-team-details-split.mjs`：3 个类型/常量区间 + 7 个 section 区间逐行命中（含缩进）+ 入口 3 个保留区间逐行 + 被移出内容零残留 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams.tsx src/components/workspace/runtime-teams/runtime-team-details-panel.tsx src/components/workspace/runtime-teams/runtime-team-details-panel` 0 问题；`npm run build` exit 0（1.01s，`runtime-teams` chunk 22.75 kB）；`npm run test` 88 文件 / 459 用例全绿（JSON reporter：suites 217 / tests 459 / passed 459 / failed 0）；定向 `npx vitest run src/components/workspace/runtime-teams` 2 文件 / 10 用例全绿；新模块全部 < 500 行（最大 `mailbox-section.tsx` 229），入口 812 → 147；`frontend/src` 内 > 500 行文件数 30 → 29 |
| P0-2 超长文件拆分（首批·第 15 项 `components/workspace/settings/runtime-agent-routing-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-agent-routing-domain-editor.tsx`(807) → 入口 195 行 + `settings/runtime-agent-routing-domain-editor/` 9 个模块：`routing-difficulty-table`163（表头 + 四档路由行）/ `routing-toggles-section`122（继承开关 + 四宫格 + 提示）/ `routing-preview-section`106（预览表单 + 结果挂载）/ `routing-header`93（页头卡片 + 健康徽标 + scope 切换）/ `route-preview-result`92（预览结果卡片）/ `primitives`81（`PreviewValue` / `HealthBadge` / `ScopeButton` / `LabeledCell` 纯组件）/ `health-badges`81（`RouteHealthBadge` + `RouteHealthIssueText`）/ `routing-limits-section`66（并发 + 推理策略）/ `format`65（`reasoningPolicyValues` + `difficultyOptions` / `difficultyLabel` / `cloneRoutingConfig` / `previewTranslation` / `routeHealthIssueMessage`）。沿用「机械搬迁 + props 化外壳」：原 161-223 / 225-301 / 303-419 / 421-458 / 460-520 / 525-608 六块按 section 整块搬迁；原 56 / 610-619 / 621-629 / 631-699 / 701-713 / 715-778 / 780-792 / 794-807 八组 helper 收拢为 `format` / `primitives` / `health-badges`（跨模块复用的原私有 helper 就地补 `export` 但不上浮）；入口仅保留 props 解构、scope/health/预览状态与副作用、`updateConfig` / `updateProfile` / `runRoutePreview` 与装配壳，各 section 经显式 props 注入所需状态与回调，消费方 `backend-config-settings-page.tsx`（lazy import）零改动。等价性校验 `.tmp/verify-agent-routing-split.mjs`：10 个搬迁/保留区间逐行命中（允许补 `export` 与缩进归一）+ 入口 6 类搬迁残留零命中 + 行数门禁 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 新目录 + 入口测试 + utils 及其测试 + 消费方>` 0 问题；`npm run build` exit 0；`npm run test` 88 文件 / 459 用例全绿；定向 `npx vitest run runtime-agent-routing-domain-editor.test.tsx runtime-agent-routing-domain-utils.test.ts` 2 文件 / 16 用例全绿；新模块全部 < 500 行（最大 `routing-difficulty-table.tsx` 163），入口 807 → 195；`frontend/src` 内 > 500 行文件数 29 → 28 |
| P0-2 超长文件拆分（首批·第 17 项 `components/workspace/settings/runtime-rate-limit-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-rate-limit-domain-editor.tsx`(770) → 入口 152 行 + `settings/runtime-rate-limit-domain-editor/` 7 个模块：`overview-section`259（页头 + enabled 开关 + basicConfig + summary + default_limits + global_limits 三栏卡）/ `api-key-limit-dialog`127（API Key 规则对话框：错误提示 + 5 字段 + extraJson）/ `api-key-limits-table`118（API Key Limits 域表：摘要徽标 + 4 列 + 新建/编辑/删除）/ `path-limits-table`111（Path Limits 域表：摘要徽标 + requests_per_minute/burst 列 + burst 合计）/ `path-limit-dialog`105（Path 规则对话框：path/requests_per_minute/burst + extraJson）/ `format`86（`KNOWN_API_KEY_LIMIT_KEYS` / `KNOWN_PATH_LIMIT_KEYS` + `createApiKeyDraftInput` / `createPathDraftInput` / `stringifyEditableValue`）/ `types`27（`RuntimeRateLimitDomainEditorProps`）。沿用「机械搬迁 + props 化外壳」：原 144-367 / 369-455 / 457-534 / 536-629 / 631-702 五块按 section 整块搬迁（仅去 2 空格缩进）；原 38-46 常量、48-63 props 类型、707-770 草稿工厂收拢为 `format` / `types`（跨模块复用的原私有函数就地补 `export` 但不上浮）；props 名沿用原局部标识符（`t` / `apiKeyLimits` / `openCreateApiDialog` / `setApiDraft` 等），使搬迁区间逐行一致；入口仅保留 state、`totalPathBurst` memo、4 个 open 回调、2 个 save 回调与 5 个子组件装配，消费方 `backend-config-settings-page.tsx`（lazy import）零改动。脚本留档 `.tmp/split-rate-limit-editor.mjs`；等价性校验 `.tmp/verify-rate-limit-split.mjs`：8 个搬迁区间逐行命中 + 入口保留区间逐行 + 入口 15 类符号零残留 + 导出面不变 + 行数门禁 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 新目录 + 消费方>` 0 问题；`npm run build` exit 0（1.01s，`runtime-rate-limit-domain-editor` chunk 14.18 kB）；`npm run test` 88 文件 / 459 用例全绿（JSON reporter：suites 217 / tests 459 / passed 459 / failed 0）；定向 `npx vitest run runtime-rate-limit-domain-utils.test.ts runtime-rate-limit-domain-form-utils.test.ts` 2 文件 / 6 用例全绿；新模块全部 < 500 行（最大 `overview-section.tsx` 259），入口 770 → 152；`frontend/src` 内 > 500 行文件数 28 → 27 |
| P0-2 超长文件拆分（首批·第 18 项 `hooks/workspace/use-session-backtrack.ts`） | 已完成（2026-09-11） | `use-session-backtrack.ts`(744) → 入口 428 行 + `hooks/workspace/session-backtrack/` 4 个模块：`helpers`157（10 个纯函数：`resolveBacktrackEditPrompt` / `resolveSeededBacktrackEditPrompt` / `countUserTurnIndex` / `extractUserMessageText` / `extractUserMessagePreview` / `resolveUserTurnTargets` / `resolveInitialBacktrackNavigationId` / `moveBacktrackNavigationSelection` / `isEditableKeyboardTarget` / `resolveBacktrackMessageSelector`）/ `use-session-backtrack-keyboard`121（键盘导航副作用「Esc 进入/退出、↑↓/jk 循环、Enter 确认」抽为 `useSessionBacktrackKeyboard` 自定义 hook，`dialog.open` / `navigation.active` / `targets.length` 三个闭包变量 props 化为 `dialogOpen` / `navigationActive` / `targetCount`，依赖数组逐项不变）/ `format`76（`formatBacktrackApplyNotice` / `formatBacktrackAuditEntry` / `buildBacktrackRequestFields`）/ `types`65（3 个公开类型 + `UseSessionBacktrackOptions` + `initialDialogState` / `initialNavigationState`，跨模块复用的原私有声明就地补 `export` 但不上浮）。沿用「机械搬迁」：原 21-58 / 60-74 / 76-226 / 228-293 四块按职责整块搬迁（仅补 `export`）；原 634-723 键盘副作用以 props 化重命名整块迁入键盘模块；入口保留 hook 主体 295-631 与 return 725-744 逐行不变，仅在原副作用位置装配 `useSessionBacktrackKeyboard(...)` 一次；16 个公开导出符号（3 类型 + 12 函数 + `useSessionBacktrack`）经入口再导出保持不变，`hooks/workspace/use-session-backtrack.test.ts`、`pages/workspace-page.tsx`、`components/workspace/message-backtrack-dialog.tsx`、`components/workspace/workspace-shell.tsx` 等消费方 import 路径零改动。脚本留档 `.tmp/split-session-backtrack.mjs`；等价性校验 `.tmp/verify-session-backtrack-split.mjs`：6 个搬迁区间逐行命中（键盘区间含 3 处 props 化重命名）+ 入口 2 个保留区间逐行 + 20 类搬迁符号零残留 + `useSessionBacktrackKeyboard` 装配恰好一次 + 导出面 16 符号完全一致 + 再导出符号均可在模块中定位 + 行数门禁 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 测试 + 4 模块 + workspace-page + message-backtrack-dialog>` 0 问题（`workspace-shell.tsx` 存在基线既有 `react-hooks/set-state-in-effect` 报错，该文件未被本次改动且报错与本项无关）；`npm run build` exit 0（836ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `helpers.ts` 157），入口 744 → 428；`frontend/src` 内 > 500 行文件数 27 → 26 |
| P0-2 超长文件拆分（首批·第 19 项 `components/workspace/runtime-teams/use-runtime-team-dispatch.ts`） | 已完成（2026-09-11） | `use-runtime-team-dispatch.ts`(743) → 入口 470 行 + `components/workspace/runtime-teams/use-runtime-team-dispatch/` 4 个模块：`dispatch-monitor`133（`loadDispatchTeamReadiness` 就绪度巡检 / `buildDispatchMailboxPreview` 邮箱预览 / `buildDispatchMonitorEntries` 监控条目与事件·邮件匹配）/ `dispatch-requests`82（`buildDispatchTaskRequest` 草稿归一化 / `resolveSelectedDispatchTeamIds` 默认选择收敛 / `buildRoleAwareDispatchRequest` 角色化模板改写）/ `types`78（`UseRuntimeTeamDispatchOptions` / `DispatchTaskRequest` / `DispatchTaskDraftState` / `DispatchTeamIdentifier` / `UseRuntimeTeamDispatchReturn`，跨模块复用的原私有声明就地补 `export` 但不上浮）/ `use-dispatch-team-readiness`48（readiness 状态 + teams 变化巡检副作用抽为 `useDispatchTeamReadiness` 子 hook）。沿用「机械搬迁 + 外壳调用」：原 32-99 / 101-166 / 168-284 三块按职责整块搬迁（仅补 `export`）；原 294-297 状态与 327-353 副作用以 `useDispatchTeamReadiness(teams)` 装配一次；入口保留 hook 其余区间（286-293 / 298-326 / 354-743）逐行不变；5 个公开导出符号（`UseRuntimeTeamDispatchReturn` + `buildDispatchTaskRequest` / `resolveSelectedDispatchTeamIds` / `buildDispatchMailboxPreview` / `useRuntimeTeamDispatch`）经入口再导出保持不变，消费方 `runtime-teams.tsx` 与 `use-runtime-team-dispatch.test.ts` import 路径零改动。搬迁后子 hook 中的同步 `setState` 命中基线规则 `react-hooks/set-state-in-effect`（原实现因整文件体量未被编译器分析而静默通过，仓库另有同类基线报错在案；仓库既有 3 处 ignore 先例），为不改语义按规则加一行带说明的 `eslint-disable-next-line`。脚本留档 `.tmp/split-use-runtime-team-dispatch.mjs`；等价性校验 `.tmp/verify-use-runtime-team-dispatch-split.mjs`：5 个搬迁区间逐行命中 + 入口 3 个保留区间逐行 + 12 类搬迁符号零残留 + `useDispatchTeamReadiness` 装配恰好一次 + 导出面 5 符号完全一致 + 再导出符号均可在模块中定位 + 行数门禁 → EQUIVALENCE OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 4 模块 + 测试 + runtime-teams.tsx>` 0 问题；`npm run build` exit 0（961ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `dispatch-monitor.ts` 133），入口 743 → 470；`frontend/src` 内 > 500 行文件数 26 → 25 |
| P0-2 超长文件拆分（首批·第 20 项 `components/workspace/message-list.tsx`） | 已完成（2026-09-11） | `message-list.tsx`(719) → 入口 264 行 + `components/workspace/message-list/` 6 个模块：`types`33（`MessageBacktrackOptions` 对外类型 + `MessageListProps`，跨模块复用的原私有声明就地补 `export` 但不上浮）/ `segment-components`80（两个 `lazy` 富内容组件 + `StreamingMarkdown` + 两个 `role="status"` 占位组件，按 react-refresh 规则收敛为「只导出组件」模块）/ `segment-rendering`75（`renderMessageSegment` / `renderRelatedArtifactSection` 两个渲染辅助函数，只导出普通函数、不定义组件，导入路径对消费方不变）/ `user-message-bubble`214（user 气泡：内联编辑 / 回溯 / 键盘导航高亮 + `extractUserBubbleText`）/ `assistant-message-card`92（assistant 卡片：流式标记 / 推理与工具段 / 相关证据区）/ `history-context-message-card`109（system/tool 历史上下文折叠卡片）。沿用「机械搬迁 + props 化外壳」：原 32-41 / 53-78 / 80-89 / 266-416 / 428-484 / 506-624 / 626-719 七块按职责整块搬迁（仅补 `export`）；入口保留 43-51（`SCROLL_FOLLOW_THRESHOLD` / `PHASE_LABELS`）、91-264（滚动跟随 / 编辑态复位 / 列表外壳与每消息派生变量）、486-504（收尾与 `isResponding` 状态行）逐行不变，消息体按 user / history / assistant 三态替换为三个 shell 组件装配（16 + 7 + 7 个 props 全部沿用原局部标识符）；`MessageList` 与 `MessageBacktrackOptions`（入口再导出）导出面不变，消费方 `workspace-shell.tsx` 与 `message-list.test.tsx`（4 用例）import 路径零改动。搬迁后入口两个「重置编辑态」effect 命中基线规则 `react-hooks/set-state-in-effect`（原实现因整文件体量未被编译器分析而静默通过，仓库既有 3 处 ignore 先例），为不改语义按规则各加一行带说明的 `eslint-disable-next-line` | 脚本留档 `.tmp/split-message-list.mjs` + `.tmp/re-split-segment-rendering.mjs`；等价性校验 `.tmp/verify-message-list-split.mjs`：13 个搬迁区间逐字节命中 + 入口 20 类搬迁符号零残留 + 模块目录无多余文件 + 导出面一致 + 三个 shell 各装配一次且传入 props 均在子模块声明 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 6 模块>` 0 问题；`npm run build` exit 0（722ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `user-message-bubble.tsx` 214），入口 719 → 264；`frontend/src` 内 > 500 行文件数 25 → 24（首批 20 项全部完成） |
| P0-2 超长文件拆分（第 2 批·第 21 项 `components/workspace/settings/runtime-retry-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-retry-domain-editor.tsx`(715) → 入口 93 行 + `components/workspace/settings/runtime-retry-domain-editor/` 5 个模块：`types`33（`RetryRuleDraftInput` 对外类型 + `RuntimeRetryDomainEditorProps`，跨模块复用的原私有声明就地补 `export` 但不上浮）/ `draft-utils`71（`createRetryRuleDraft` 草稿构造 / `summarizeMatcher` 匹配器摘要 + 私有 `summarizeTextGroup`）/ `overview-section`239（主面板卡：启停开关 / 默认策略 / 摘要徽标 / `invalid_encrypted_content_recovery` 与 `enhanced_strategy` 两个子卡）/ `rules-table`156（规则表：4 列 + 上下移/编辑/删除动作 + 新增按钮）/ `rule-dialog`253（规则编辑弹窗表单：name / enabled / description / 重试策略 / matcher 字段）。沿用「机械搬迁 + props 化外壳」：原 39-52 / 54-64 / 112-315 / 317-429 / 431-648 / 653-715 六块按职责整块搬迁（仅补 `export`）；入口保留 66-73（组件签名）与 74-108（`useTranslation` / 4 个 state / `useMemo` 计数 / 3 个处理器）逐行不变，三块 JSX 替换为 `<RuntimeRetryOverviewSection>` / `<RuntimeRetryRulesTable>` / `<RuntimeRetryRuleDialog>` 装配（5 + 7 + 8 个 props 沿用原局部标识符，`t` 与派生态原样下传）；`RuntimeRetryDomainEditor` 与 `RetryRuleDraftInput`（入口再导出）导出面不变，消费方 `backend-config-settings-page.tsx`（lazy import + 草稿类型）import 路径零改动 | 脚本留档 `.tmp/split-runtime-retry-domain-editor.mjs`；等价性校验 `.tmp/verify-runtime-retry-domain-editor-split.mjs`：10 个搬迁区间逐字节命中 + 入口 15 类搬迁符号零残留 + 模块目录无多余文件 + 导出面一致 + 消费方仍走原路径且未出现子模块深路径 + 三个 shell 各装配一次且传入 props 均在子模块声明 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 5 模块>` 0 问题；`npm run build` exit 0（739ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `rule-dialog.tsx` 253），入口 715 → 93；`frontend/src` 内 > 500 行文件数 24 → 23（第 2 批 1/8 项完成） |
| P0-2 超长文件拆分（第 3 批·第 22 项 `components/workspace/workspace-shell.tsx`） | 已完成（2026-09-11） | `workspace-shell.tsx`(695) → 入口 358 行 + `components/workspace/workspace-shell/` 6 个模块：`main-section`331（`<section id="workspace-preview">`：topbar + Chat/Trajectory 视图切换 + MessageList/TrajectoryView + 新建会话 hero（4 条建议卡）+ 底部淡出层 + composer 覆盖层）/ `sidebar-section`125（`<WorkspaceSidebar />` 29 个 props 装配 + 3 个内联回调）/ `overlays-section`121（artifact 详情 / settings / backtrack 三个懒加载弹窗）/ `types`92（`WorkspaceShellProps` 就地补 `export`）/ `lazy-surfaces`52（4 个 `lazy()` + 3 个 loading fallback）/ `artifact-rail-section`42（右侧 artifact 栏 Suspense 容器）。沿用「机械搬迁 + props 化外壳」：原 47-66 / 68-145 / 360-400 / 402-603 / 605-616 / 618-662 / 667-695 七块按分区整块搬迁（仅 `density={settings.workspace.density}` → `density={density}`、`mobileOpen={mobileSidebarOpen}` → `mobileOpen={mobileOpen}` 与去缩进三处转写），分区内其余 JSX 与引用逐行零改动；`WorkspaceShellProps` 由 `types.ts` 导出（原文件未导出，入口导出面仍只有 `WorkspaceShell`），三个派生标签（`transportLabel` / `threadStatusLabel` / `threadSubtitle`）与 `messageListStyle` / `newThreadSuggestions` / `isCompact` / `liveTeamCount` / `viewMode`/`setViewMode` / `composerOverlayRef` 等本地派生值经显式 props 下传；入口仅保留 props 解构、14 个 state/ref、3 个副作用（ResizeObserver 测高 / Ctrl-, 快捷键 / autoOpenArtifacts 同步）、派生标签、两处处理器与四个分区装配，消费方 `pages/workspace-page.tsx` 零改动。搬迁后入口 `autoOpenArtifacts` 同步 effect 命中基线规则 `react-hooks/set-state-in-effect`（原实现因整文件体量未被编译器分析而静默通过，仓库既有同类 ignore 先例），为不改语义按规则加一行带说明的 `eslint-disable-next-line`。脚本留档 `.tmp/split-workspace-shell.mjs`；等价性校验 `.tmp/verify-workspace-shell-split.mjs`：6 个搬迁区间逐字节命中（允许 `export` 前缀与缩进归一）+ 入口保留区间逐行 + 入口 13 类搬迁符号零残留 + 模块目录无多余文件 + 导出面一致 + 四个分区壳 props 双向闭合（含字母序）+ 消费方 import 路径未变且模块目录仅被入口引用 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/workspace-shell.tsx src/components/workspace/workspace-shell` 0 问题；`npm run build` exit 0（766ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `main-section.tsx` 331），入口 695 → 358；`frontend/src` 内 > 500 行文件数 23 → 22（第 3 批 1/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 23 项 `components/workspace/runtime-teams.tsx`） | 已完成（2026-09-11） | `runtime-teams.tsx`(693) → 入口 401 行 + `runtime-teams/` 6 个模块：`directory-view`214（teams 分支：详情网格 + 侧栏详情面板装配，含全部草稿/派生态 props）/ `use-team-details-loader`197（详情加载副作用下沉为 hook：mailbox/tasks/task-graph/events/teammates/path-claims 扇出加载 + 取消语义 + 部分失败提示）/ `dispatch-view`146（dispatch 分支：`<Suspense>` + `RuntimeTeamDispatchPanel` 装配与 `Pick<UseRuntimeTeamDispatchReturn, …>` props 桥接）/ `summary-section`112（摘要栏 + 错误/加载/空态）/ `lazy-surfaces`26（两个 `lazy()` + `RuntimeTeamsPanelFallback`）/ `types`16（`RuntimeTeamsProps` / `RuntimeTeamsView` 就地补 `export`）。沿用「机械搬迁 + props 化外壳」：原 37-50 / 52-63 / 115-250 / 450-521 / 524-568 / 570-680 六个区间按行搬迁；详情加载 effect 整体下沉后 13 个本地 setter 与 `selectedTeamId` 显式 props 化（依赖数组保持 `[selectedTeamId]` 不变，因 setState 改为透传补一行说明性 `react-hooks/exhaustive-deps` ignore，原 `set-state-in-effect` ignore 保留）；dispatch 分支把 hook 返回的 16 个 `on*` setter/handler 以 `Pick<…>` + 重命名映射 props 化，团队列表与派生值（`summaryMap`/`teams`/`visible*`/`selectedSummary`/`selectedTeamId`）原样下传；摘要栏 JSX 因脱离 `<section>` 父级改用 `<>` 片段包裹（唯一结构性改动，其余 JSX 逐行零改动）；入口对外仍只导出 `RuntimeTeams`（before=after），消费方 `runtime-teams-dialog.tsx` 的 `lazy(() => import("@/components/workspace/runtime-teams"))` 路径零改动。 | 脚本留档 `.tmp/split-runtime-teams.mjs`；等价性校验 `.tmp/verify-runtime-teams-split.mjs`：入口 + 6 模块与原文 639 行代码行做多重集比对（仅 3 个多行 import 续行因 import 收敛为单行而差异）→ 代码行零丢失；入口被移出的 11 类标记零残留 + 导出面一致 + 6 个子模块均被入口/兄弟模块引用 + 消费方路径未变 + 行数门禁（原文 693 行在 `.tmp/runtime-teams.orig.tsx` 留档中核对）→ VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams.tsx src/components/workspace/runtime-teams` 0 问题；`npm run build` exit 0（727ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `directory-view.tsx` 214），入口 693 → 401；`frontend/src` 内 > 500 行文件数 22 → 21（第 3 批 2/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 24 项 `components/workspace/runtime-teams/team-details-panel.tsx`） | 已完成（2026-09-11） | `team-details-panel.tsx`(683) → 入口 105 行 + `runtime-teams/team-details-panel/` 9 个模块：`mailbox-section`211（信箱撰写表单 + 消息列表 + ack）/ `path-claims-section`188（冲突检查表单 + 结果卡 + 租约列表）/ `snapshot-section`148（团队页头 + Tasks/Teammates/Task Graph 三卡 + workspace/lead/strategy 行 + detailsError）/ `roster-section`66（成员列表 + 能力徽标）/ `task-queue-section`62（任务队列）/ `timeline-section`53（事件时间线）/ `types`44（`TeamDetailsPanelProps` 就地补 `export` + 依赖类型 import）/ `final-summary-section`36（终态总结）/ `format`12（detailsPanelClass / detailsCardClass / detailsInputClass / detailsPillClass）。沿用「机械搬迁 + props 化外壳」：原 118-231 / 233-280 / 281-324 / 326-480 / 482-626 / 628-662 / 664-680 七块按 section 整块搬迁（仅根 `<div className="rounded-[0.95rem] …">` 壳留在入口）；每个 section 以 `Pick<TeamDetailsPanelProps, …>` 声明所需 props，由拆分脚本按 section 内实际引用自动推导并生成，入口逐个显式下传，section 内 JSX 与标识符逐行零改动；`TeamDetailsPanelProps` 由 `types.ts` 导出，入口导出面仍只有 `TeamDetailsPanel`（before=after）。**核查发现**：该组件在全仓（frontend / backend / e2e / docs）零引用，自 2026-04-19 首次导入以来即为死代码；本次仍按计划机械拆分以保持「语义零改动」，死代码清理另议。 | 脚本留档 `.tmp/split-team-details-panel.mjs`（分区 props 自动推导）；等价性校验 `.tmp/verify-team-details-panel-split.mjs`：入口 + 9 模块与原文件 655 行代码行做多重集比对（仅 16 个多行 import 续行因 import 按模块重新分组而差异）→ 零丢失；入口 14 类标记零残留 + 导出面一致 + 9 个子模块均被入口/兄弟模块引用 + 全仓无消费方核验 + 入口装配 7 个分区 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams/team-details-panel.tsx src/components/workspace/runtime-teams/team-details-panel` 0 问题；`npm run build` exit 0（730ms）；`npm run test` 88 文件 / 459 用例全绿；新模块全部 < 500 行（最大 `mailbox-section.tsx` 211），入口 683 → 105；`frontend/src` 内 > 500 行文件数 21 → 20（第 3 批 3/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 25 项 `components/workspace/runtime-teams/shared.ts`） | 已完成（2026-09-11） | `shared.ts`(628) → 入口 57 行（纯再导出）+ `runtime-teams/shared/` 5 个域模块：`dispatch`338（12 个派发对比与结论叙事助手：`buildDispatchComparisonRows` / `summarizeDispatchBatch` / `sortDispatchComparisonRows` / `isTerminalDispatchStatus` / `normalizeDispatchOutcomeKey` / `formatDispatchOutcomeLabel` / `getDispatchOutcomeNarrative` / `shouldExpectDispatchOutcomeSummary` / `hasCreatedDispatchResults` / `shouldPollDispatchMonitor` / `countDispatchMonitorStatuses` / `resolveDispatchRolePlan`）/ `types`107（10 个团队详情与派发类型：`TeamDetailsState` / `ClaimCheckState` / `MultiTeamDispatchResult` / `DispatchTeamReadiness` / `DispatchMonitorEntry` / `DispatchComparisonRow` / `DispatchBatchSummary` / `DispatchOutcomeNarrative` / `DispatchTemplateMode` / `DispatchRolePlan`）/ `format`97（6 个展示格式化助手：`truncateIdentifier` / `statusTone` / `prettyEventType` / `describeMailboxRoute` / `summarizeConflict` / `describeEventPayload`）/ `sorting`63（6 个排序助手：任务 / 成员 / 事件 / 信箱 / 路径租约 / 派发监控）/ `details`62（6 个详情状态助手：`getSummaryCount` / `createEmptyDetails` / `isClaimActive` / `parsePathLines` / `uniqueStrings` / `normalizeTaskTitle`）。沿用「机械搬迁」：40 个导出声明按域整块搬迁、逐行零改写；顶部 runtime-api import 按各模块实际引用重写（`types` 7 个 / `sorting` 5 个 / `format` 3 个 / `details` 1 个 / `dispatch` 0 个，由脚本按引用自动推导）；跨模块依赖仅两条且无环（`details`/`sorting` → `types` 取类型，`dispatch` → `format` 取 `truncateIdentifier`/`prettyEventType`）。入口保持同名同路径（`shared.ts`），以显式 `export {}` / `export type {}` 再导出全部 40 个符号，**导出面 before == after**；35 处既有消费方（33 处 `@/components/workspace/runtime-teams/shared` + 2 处目录内相对 `./shared`，含直接消费本模块的 `shared.test.ts`）import 路径零改动。 | 脚本留档 `.tmp/split-runtime-teams-shared.mjs`（按顶层 `export type/function` 自动分块 + 引用驱动 import 生成 + 入口再导出自生成）；等价性校验 `.tmp/verify-runtime-teams-shared-split.mjs`：40 个声明块逐字节命中 + 入口/子模块行数门禁 + 入口再导出面 == 原导出面 + 入口无重复声明 + 模块声明集合与分配一致（40 个无重复）+ 35 处消费方文件仍存在 + 5 个模块 runtime-api 引用零缺失零冗余 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams/shared.ts src/components/workspace/runtime-teams/shared src/components/workspace/runtime-teams/shared.test.ts` 0 问题；`npm run build` exit 0（738ms）；`npm run test` 88 文件 / 459 用例全绿（`shared.test.ts` 经入口再导出消费，语义未变）；入口 628 → 57，最大子模块 `dispatch.ts` 338 行；`frontend/src` 内 > 500 行文件数 20 → 19（第 3 批 4/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 26 项 `components/workspace/message-markdown.tsx`） | 已完成（2026-09-11） | `message-markdown.tsx`(625) → 入口 185 行 + `components/workspace/message-markdown/` 5 个模块：`markdown-components.tsx`168（`LINK_CLASS_NAME` / `INLINE_CODE_CLASS_NAME` + `collectTextContent` / `getCodeLanguage` / `isInternalHref` / `renderMarkdownLink` + `createMarkdownComponents`（react-markdown `Components` 组件表）与 `inlineMarkdownComponents`）/ `streaming-blocks.tsx`164（`InlineMarkdown` + `StreamingBlockquoteParagraph` / `StreamingListItem` / `StreamingTableHeaderCell` / `StreamingTableRow` / `StreamingPlainFragment` 五个流式尾块子组件；`areStringArraysEqual` / `alignmentToClassName` 作为模块内私有助手保留、不导出）/ `streaming-tails.tsx`111（`renderStreamingStructuredTail` 结构化引用/列表/表格尾块渲染 + `renderStreamingPlainTail` 纯文本尾块渲染）/ `stable-fragment.tsx`29（`StableMarkdownFragment` 稳定区冻结 memo 片段，含原 JSDoc）/ `types.ts`9（`MessageMarkdownProps` 就地补 `export`）。沿用「机械搬迁」：21 个顶层声明整块搬迁、正文逐行零改写（唯一的文本处理是原块尾 JSDoc 按惯例归位到 `StableMarkdownFragment`）；跨模块复用的 11 个原私有声明就地补 `export`（不上浮入口），模块内私有助手 9 个保持不导出；import 清单按各模块实际引用自动生成（react / react-markdown / remark-gfm / remark-breaks / lucide-react / `@/components/ui/code-block` / `message-markdown-streaming` / `@/lib/utils` + 本地相对导入）。入口保持同名同路径（`message-markdown.tsx`），导出面仍只有 `MessageMarkdown`（before == after）；10 处消费方（含 `message-markdown.test.tsx` 的相对 `./message-markdown` 与 `message-markdown-streaming` 测试）import 路径零改动。 | 脚本留档 `.tmp/split-message-markdown.mjs`（顶层声明自动分块 + 引用驱动 import 生成 + 跨模块 export 就地提升 + 块尾 JSDoc 归位）；等价性校验 `.tmp/verify-message-markdown-split.mjs`：21 个声明块逐字节命中 + JSDoc 原样保留 + 入口/子模块行数门禁 + 入口导出面一致 + 5 个子模块导出面与设计一致 + 9 个模块内私有声明未被跨模块导入 + import 零冗余 + 10 处消费方路径仍存在 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/message-markdown.tsx src/components/workspace/message-markdown src/components/workspace/message-markdown.test.tsx src/components/workspace/message-markdown-streaming.ts` 0 问题；`npm run build` exit 0（736ms）；`npm run test` 88 文件 / 459 用例全绿（含 `message-markdown.test.tsx` 与流式渲染用例）；入口 625 → 185，最大子模块 `markdown-components.tsx` 168 行；`frontend/src` 内 > 500 行文件数 19 → 18（第 3 批 5/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 27 项 `components/workspace/artifact-panel.tsx`） | 已完成（2026-09-11） | `artifact-panel.tsx`(618) → 入口 243 行 + `components/workspace/artifact-panel/` 4 模块：`surface-tabs.tsx`246（`surfaceButtonClass` / `surfaceFromIndex` / `getEnabledTabIndices` / `handleHorizontalTabKeyDown` 四个原私有助手 + `ArtifactPanelSurfaceTabs`：面板头部 tablist（3 个 tab 按钮 + count Badge）与 sr-only 标题；`surfaceTabRefs` 由原 `useState<…>([])[0]` 改为模块内 `useRef<…>([])`，ref 赋值与 `refs:` 读取均走 `.current`，以适配拆分后生效的 `react-hooks/immutability` 基线）/ `artifact-list.tsx`184（`iconForArtifact` / `formatBytes` 两个原私有助手 + 模块内私有 `ArtifactList` 列表组件 + `ArtifactPanelArtifactSurface` 面板壳（空态文案 + 列表装配））/ `lazy-surfaces.tsx`37（checkpoint / plan 两个 `lazy()` 表面 + `ArtifactPanelCheckpointFallback` / `ArtifactPanelPlanFallback` 两个占位组件）/ `types.ts`23（`ArtifactPanelSurface` / `ArtifactPanelProps` 就地补 `export` + 新增 `ArtifactPanelSurfaceTabIds` 六 id 类型）。沿用「机械搬迁 + props 化外壳」：`renderArtifactList`（253-352）内嵌渲染函数下沉为 `ArtifactList` 组件（`items` / `onOpenArtifact` / `selectedArtifactId` 三个 props），`const artifactSurface`（354-368）下沉为 `ArtifactPanelArtifactSurface`（额外传 `artifacts` 以保持 `artifacts.length > 0` 空态判定不变），tablist（389-500）下沉为 `ArtifactPanelSurfaceTabs`（`activeSurface` / `artifactCount` / `backtrackCount` / `onSelectSurface` / `planIsActive` / `sessionId` / `surfaceTabDisabledStates` / `tabIds` / `titleId` 九个 props；JSX 逐行零改动，仅 `resolvedActiveSurface` / `setActiveSurface(` / `plan?.active` / `backtrackAuditEntries.length` / `artifacts.length` 与 6 个 useId 标识符按 props 改名）；入口保留 8 个 `useId`、`activeSurface` state、`useRuntimeCheckpoints` / `useRuntimePlanMode` 两个 hook、派生态与三个 tabpanel 装配（`Suspense` + 两个懒表面 + `ArtifactPanelArtifactSurface`），导出面仍只有 `ArtifactPanel`（before == after）；3 处消费方（`artifact-panel.test.tsx` 相对 `./artifact-panel`、`workspace-shell/lazy-surfaces.tsx` 与 `data/mock.ts` 的 `@/components/workspace/artifact-panel`）import 路径零改动。 | 脚本留档 `.tmp/split-artifact-panel.mjs`（区间切片 + 计数断言替换，防改名漏改）；等价性校验 `.tmp/verify-artifact-panel-split.mjs`：18 个搬迁区间归一化逐行命中（含 4 处按设计改写的期望替换）+ 入口 12 类搬迁痕迹零残留 + 入口导出面一致 + 4 个子模块导出面与设计一致 + 7 个模块内私有声明未被跨模块导入 + 3 处消费方路径仍存在 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/artifact-panel.tsx src/components/workspace/artifact-panel src/components/workspace/artifact-panel.test.tsx` 0 问题；`npm run build` exit 0（723ms）；`npm run test` 88 文件 / 459 用例全绿（含 `artifact-panel.test.tsx` 的 tab 切换与键盘导航用例）；入口 618 → 243，最大子模块 `surface-tabs.tsx` 246 行；`frontend/src` 内 > 500 行文件数 18 → 17（第 3 批 6/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 28 项 `components/workspace/artifact-panel-checkpoint-surface.tsx`） | 已完成（2026-09-11） | `artifact-panel-checkpoint-surface.tsx`(613) → 入口 104 行 + `components/workspace/artifact-panel-checkpoint-surface/` 4 模块：`checkpoint-detail-section.tsx`334（checkpoint 详情 section 原 338-610：预览 / 溯源 / 恢复 4 卡 + 恢复按钮 + 错误与通知条；props = `Pick<ArtifactPanelCheckpointSurfaceProps, …>` 14 项 + `checkpointDetailLoading` / `checkpointFilesForSelection`）/ `backtrack-audit-section.tsx`170（审计 section 原 119-248 + `selectedAudit` 派生原 106-107；props = 4 项 Pick + `onSelectAuditId` / `selectedAuditId`；`setSelectedAuditId(entry.id)` → `onSelectAuditId(entry.id)`）/ `checkpoints-section.tsx`124（restore points section 原 250-336：列表 + 空态 + 恢复状态徽标；props = 6 项 Pick）/ `types.ts`52（`CheckpointFileCode` / `ConversationSummaryMessage` / `ArtifactPanelCheckpointSurfaceProps` 就地补 `export` + 新增 `CheckpointFileSelection = RuntimeSessionCheckpointFile | RuntimeSessionCheckpointPreviewFile`）。沿用「机械搬迁 + props 化外壳」：4 个区间按行整块搬迁、JSX 逐行零改动；入口保留原 71-105（签名 + `useState` + `useEffect`，effect 前加说明性注释与 `react-hooks/set-state-in-effect` disable，与 `message-list.tsx` 既有先例一致）与原 108-115 派生态，仅装配三个 section；入口不再 import lucide-react / `@/components/ui/badge` / `@/components/ui/button` / `@/components/ui/code-block` / `message-markdown` / `formatCheckpoint` / `formatBacktrackAudit` / `formatRelativeTimestamp` / `cn`；导出面仍只有 `ArtifactPanelCheckpointSurface`（before == after）；消费方 `artifact-panel/lazy-surfaces.tsx` 的 `import("@/components/workspace/artifact-panel-checkpoint-surface")` 路径零改动。 | 脚本留档 `.tmp/split-checkpoint-surface.mjs`（幂等：首次备份原文到 `.tmp/artifact-panel-checkpoint-surface.orig.tsx`）；等价性校验 `.tmp/verify-checkpoint-surface-split.mjs`：8 个搬迁区间归一化逐行命中（含 1 处按设计改写的期望替换）+ 入口 15 类搬迁痕迹零残留 + 入口导出面一致 + 4 个子模块导出面与设计一致 + 3 个模块内私有 props 类型未被跨模块导入 + 入口三 section 装配各一次且传入 props 均已在对应模块声明 + 消费方路径仍存在 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/artifact-panel-checkpoint-surface.tsx src/components/workspace/artifact-panel-checkpoint-surface src/components/workspace/artifact-panel/lazy-surfaces.tsx` 0 问题；`npm run build` exit 0（736ms）；`npm run test` 88 文件 / 459 用例全绿；入口 613 → 104，最大子模块 `checkpoint-detail-section.tsx` 334 行；`frontend/src` 内 > 500 行文件数 17 → 16（第 3 批 7/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 6 项 `components/workspace/workspace-sidebar.tsx`） | 已完成（2026-09-11） | `workspace-sidebar.tsx`(1560) → 入口 449 行 + `components/workspace/workspace-sidebar/` 14 个模块：`types`87（入口 65-127 类型区就地补 `export` + `SidebarThread`/`SidebarThreadDescriptor`/`SidebarDirectoryGroup` 别名）/ `state-icon-utils`81（`getThreadWorkflowIcon`/`getSessionStatusIcon` 图标推导）/ `state-icons`21（`SidebarStateIcon`）/ `labels`39（`buildSidebarIconLabels`/`buildThreadSessionDetails` 包装原 464-478 两个标签表，新增 `ThreadSessionDetails`）/ `section-shell`62（`SidebarSection` 折叠壳）/ `sidebar-header`125（搜索与折叠头 657-743）/ `chats-section`109（752-815）/ `directories-section`343（817-1061）/ `sessions-section`284（1063-1266）/ `runtime-section`162（1268-1363，并把 `sessionRailSummary`/`liveTeamCount`/`hasRuntimeStats` 三个派生下沉，改为接收 `threads` prop）/ `runtime-teams-surface`60（1366-1379 装配 + 1397-1406 fallback，模块内 `lazy()` 自建 `RuntimeTeamsDialog`，props 改名 `open`/`error`/`isLoading`/`isRefreshing`/`onRefresh`/`summaries`/`teams`/`onClose`）/ `session-descriptor`22（`buildSessionDescriptorThread`）/ `session-item`146（会话行 + 重命名输入框 1425-1560）/ `use-sidebar-effects`101（480-548 三个 effect 下沉为 hook）。沿用「机械搬迁 + props 化外壳」：入口保留 props 解构与状态（274-436）、展示开关（438-454）、`useSidebarEffects` 调用、handler（550-635）与装配壳（637-656 / 744-751 / 1364-1366 + 1380-1393），其余整块下沉；各段自行调用 `buildSidebarIconLabels(t)`/`buildThreadSessionDetails(t)`，入口不再持有 `sidebarLabels`/`threadSessionDetails`；受 `react-refresh/only-export-components` 约束，非组件函数单独成 `state-icon-utils.ts`/`session-descriptor.ts`，含组件的模块只导出组件；入口对外仍只导出 `WorkspaceSidebar`（before == after）；消费方 `workspace-shell/sidebar-section.tsx`、`workspace-sidebar-responsive.test.tsx` 路径零改动。 | 脚本留档 `.tmp/split-workspace-sidebar.mjs`（幂等：首次备份原文到 `.tmp/workspace-sidebar.orig.tsx`，之后以备份为唯一真源重建）；等价性校验 `.tmp/verify-workspace-sidebar-split.mjs`：21 个搬迁区间归一化逐行命中（teams 装配按设计替换 8 个标识符）+ 入口 23 类搬迁痕迹零残留 + 入口导出面一致 + 14 个子模块导出面与设计一致 + 6 个段组件各装配一次且传入 props 均已在对应模块声明 + react-refresh 约束 + 消费方路径未变 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/workspace-sidebar.tsx src/components/workspace/workspace-sidebar` 0 error（`use-sidebar-effects.ts` 2 条 `react-hooks/exhaustive-deps` warning，与原实现同源）；`npm run build` exit 0（744ms）；`npm run test` 88 文件 / 459 用例全绿；入口 1560 → 449，最大子模块 `directories-section.tsx` 343 行；`frontend/src` 内 > 500 行文件数 16 → 15（第 3 批 8/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 30 项 `components/workspace/runtime-teams/dispatch-console.tsx`） | 已完成（2026-09-11） | `dispatch-console.tsx`(575) → 入口 132 行 + `components/workspace/runtime-teams/dispatch-console/` 8 个模块：`types`57（原 34-76 `DispatchConsoleProps` 就地补 `export` + 补齐 `@/lib/runtime-api` 与 `../shared` 类型 import）/ `provision-panel`138（原 135-225：建团队表单 + 派发表单）/ `template-panel`56（227-262：模板模式切换）/ `teams-selector`92（264-325：团队多选 + readiness 徽标）/ `task-composer`141（328-419：任务草稿表单）/ `dispatch-results`53（421-451：派发结果列表）/ `dispatch-monitor`159（453-571：监控列表 + 刷新按钮）/ `console-styles`13（22-32 四个类名常量就地补 `export`）。沿用「机械搬迁 + props 化外壳」：各段组件以 `Pick<DispatchConsoleProps, …>` 声明所需 props（按段内引用逐项列出，不新增子接口），入口逐个显式下传；段内 JSX 与标识符逐行零改动；`dispatchTaskResults.length > 0` 守卫在结果区与监控区各保留一处（monitor 因此接收 `dispatchTaskResults` prop）；入口对外仍只导出 `DispatchConsole`（before == after）。**核查发现**：该组件在全仓（frontend / backend / e2e / docs）零引用（运行时派发 UI 走 `runtime-team-dispatch-panel`），自存在以来即为死代码；本次仍按计划机械拆分以保持语义零改动，死代码清理另议。 | 脚本留档 `.tmp/split-dispatch-console.mjs`（幂等：首次备份原文到 `.tmp/dispatch-console.orig.tsx`，之后以备份为唯一真源重建）；等价性校验 `.tmp/verify-dispatch-console-split.mjs`：11 个搬迁区间归一化逐行命中 + 入口 6 个装配标记齐全 + 15 类搬迁痕迹零残留 + 8 个模块 <500 行且头注释齐全 + `dispatchTaskResults` 双守卫保留 + 行数门禁 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/runtime-teams/dispatch-console.tsx src/components/workspace/runtime-teams/dispatch-console` 0 问题；`npm run build` exit 0（788ms）；`npm run test` 88 文件 / 459 用例全绿；入口 575 → 132，最大子模块 `dispatch-monitor.tsx` 159 行；`frontend/src` 内 > 500 行文件数 15 → 14（第 3 批 9/10 项完成） |
| P0-2 超长文件拆分（第 3 批·第 35 项 `components/workspace/artifact-detail-dialog.tsx`） | 已完成（2026-09-11） | `artifact-detail-dialog.tsx`(534) → 入口 203 行 + `components/workspace/artifact-detail-dialog/` 6 个模块：`artifact-panes.tsx`211（五个内容卡：原 385-418 可渲染图片卡 / 420-452 图片不可内联卡 / 456-482 preview tabpanel / 483-506 source tabpanel / 509-525 独立源码阅读卡；分别以 `artifact` + `imageDetails` / `previewDocument` / `previewPanelId` / `previewTabId` / `sourcePanelId` / `sourceTabId` / `view` 窄 props 接收，JSX 逐行零改动）/ `dialog-helpers.ts`117（原 33-40 `surfaceButtonClass`、42-44 `getEnabledTabIndices`、46-88 `handleHorizontalTabKeyDown`、90-116 `buildMetaItems`（补 `ArtifactDetailView` 参数与 `ArtifactMetaItem[]` 返回类型）、118-133 `formatBytes`，均就地补 `export`）/ `reading-tabs.tsx`90（原 323-379 Preview/Source tablist；`viewTabRefs` 由入口 `useRef` 下沉为模块内 `useRef`（`react-hooks/immutability` 不允许改写 props），`setPreferredView` 以 `onSelectView` prop 传入）/ `dialog-header.tsx`70（原 244-282 头部：分类标签 / 标题 / 摘要 / 两个 Badge / 关闭按钮）/ `metadata-aside.tsx`46（原 292-319 左栏：artifact path 卡 + metaItems 卡列表）/ `types.ts`16（`ArtifactDetailDialogProps` 就地补 `export`，新增 `ArtifactDetailView` / `ArtifactMetaItem`）。沿用「机械搬迁 + props 化外壳」：入口保留 hooks（settings / 7 个 useId / `preferredView` state / 3 个 effect）、早退与派生态（`category` / `view` / `previewDocument` / `metaItems` / `isImageArtifact` / `isRenderableImage` / `imageDetails`）；`{artifact.previewHtml ? <ArtifactReadingTabs …/> : null}` 与 `{isImageArtifact ? isRenderableImage ? <ImageArtifactPane …/> : <ImageUnavailablePane …/> : artifact.previewHtml ? <ArtifactPreviewPane …/>+<ArtifactSourcePane …/> : <ArtifactSourceReaderCard …/>}` 两个守卫文本与分支顺序逐字保留；入口导出面仍只有 `ArtifactDetailDialog`（before == after）；消费方 `workspace-shell/lazy-surfaces.tsx` 的 `lazy(() => import("@/components/workspace/artifact-detail-dialog"))` 与同源测试 `artifact-detail-dialog.test.tsx` 路径零改动。原实现首个 effect 命中基线规则 `react-hooks/set-state-in-effect`（`eslint --stdin` 对照原文复现同一条 error），按仓库既有先例加一行带说明的 `eslint-disable-next-line`，语义不变；`react-hooks/exhaustive-deps` warning 与原实现同源保留。 | 脚本留档 `.tmp/split-artifact-detail-dialog.mjs`（幂等：首次备份原文到 `.tmp/artifact-detail-dialog.orig.tsx`）；等价性校验 `.tmp/verify-artifact-detail-dialog-split.mjs`：17 个搬迁区间归一化逐行命中（含 `buildMetaItems` 签名重排、缩进与 `setPreferredView`→`onSelectView` 改名）+ 入口 11 个装配标记齐全 + 13 类搬迁痕迹零残留 + 两个守卫文本保留 + 6 个模块集合/行数/头注释门禁 + 消费方路径未变 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/artifact-detail-dialog.tsx src/components/workspace/artifact-detail-dialog` 0 error（入口 1 条 `react-hooks/exhaustive-deps` warning，与原实现同源）；`npm run build` exit 0（877ms）；`npm run test` 88 文件 / 459 用例全绿（含 `artifact-detail-dialog.test.tsx` 4 用例）；入口 534 → 203，最大子模块 `artifact-panes.tsx` 211 行；`frontend/src` 内 > 500 行文件数 14 → 13（第 3 批 10/10 项完成） |
| P0-2 超长文件拆分（第 2 批·第 32 项 `components/workspace/settings/runtime-routing-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-routing-domain-editor.tsx`(565) → 入口 92 行 + `components/workspace/settings/runtime-routing-domain-editor/` 5 个模块：`types.ts`20（原 58-69 `RuntimeRoutingDomainEditorProps` 就地补 `export`）/ `draft-utils.ts`59（原 39-50 `KNOWN_ROUTE_KEYS` + 原 528-565 `createRouteDraftInput`，函数就地补 `export`）/ `overview-section.tsx`110（原 119-192 概览卡：图标标题 / 描述 / 三个摘要徽标 / `routing.strategy` 输入 / `routing.failover` 开关；props = `onChangeConfig` / `protocolCount` / `routeConfig` / `routes` / `t`）/ `routes-table.tsx`169（原 194-319 路由表：5 列 + 上移/下移/编辑/删除动作 + 新建按钮；props = `availableGroups` / `onDeleteRoute` / `onMoveRoute` / `openCreateDialog` / `openEditDialog` / `routeConfig` / `routes` / `t`）/ `route-dialog.tsx`247（原 321-523 编辑弹窗，原 52-56 `routeMatchTypeOptions` 随迁为模块内私有常量；props = `availableGroups` / `dialogError` / `dialogOpen` / `draft` / `editingIndex` / `handleSave` / `setDialogOpen` / `setDraft` / `t`）。沿用「机械搬迁 + props 化外壳」：五个区间按行整块搬迁（仅补 `export`），段内 JSX 与标识符逐行零改动（`setDialogOpen` / `setDraft` 以原 `useState` setter 直接下传，弹窗内 `onClose={() => setDialogOpen(false)}` 等写法保持原样）；入口保留原 71-79 签名与原 80-115（`useTranslation` / 4 个 state / `protocolCount` useMemo / 三个 handler）逐行不变，仅装配三个 section；入口不再 import lucide-react / `@/components/ui/badge` / `@/components/ui/select` / `config-domain-dialog` / `config-domain-table` / `config-form-field` / `editor-control-class` / `settings-action-group` / `settings-add-button` / `settings-badge-list` / `settings-dialog-footer` / `settings-notice-card` / `settings-panel-icon`；入口对外仍只导出 `RuntimeRoutingDomainEditor`（before == after）；消费方 `backend-config-settings-page.tsx` 的 `lazy(() => import("./runtime-routing-domain-editor"))` 与 `RouteDraftInput`（仍自 `runtime-routing-domain-form-utils`）导入路径零改动。 | 脚本留档 `.tmp/split-runtime-routing-domain-editor.mjs`（幂等：首次备份原文到 `.tmp/runtime-routing-domain-editor.orig.tsx`）；等价性校验 `.tmp/verify-runtime-routing-domain-editor-split.mjs`：9 个搬迁区间逐字节命中（含 2 处 export 前缀转写）+ 入口 21 类迁出符号零残留 + 入口导出面仅 1 项 + 5 个子模块导出面/头注释/行数门禁 + 三个 section 各装配一次且 22 个 props 均在对应模块类型中声明 + 消费方 lazy 路径与 `RouteDraftInput` 来源未变 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/settings/runtime-routing-domain-editor.tsx src/components/workspace/settings/runtime-routing-domain-editor` 0 问题；`npm run build` exit 0（806ms）；`npm run test` 88 文件 / 459 用例全绿；入口 565 → 92，最大子模块 `route-dialog.tsx` 247 行；`frontend/src` 内 > 500 行文件数 13 → 12（第 2 批 2/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 40 项 `components/workspace/settings/runtime-transformer-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-transformer-domain-editor.tsx`(508) → 入口 114 行 + `components/workspace/settings/runtime-transformer-domain-editor/` 6 个模块：`types.ts`26（原 47-63 `RuntimeTransformerDomainEditorProps` 就地补 `export`）/ `draft-utils.ts`43（原 40-45 `KNOWN_TRANSFORMER_MODIFIER_KEYS` + 原 479-508 `createTransformerModifierDraft` 补 `export` 与私有 `stringifyJsonObject`）/ `toggle-card.tsx`24（原 458-477 私有 `ToggleCard` 包装组件，随迁改名 `TransformerToggleCard` 并补 `export`；overview 4 处 + dialog 1 处调用点同名替换）/ `modifier-table.tsx`177（原 124-255 `renderModifierTable` 内联函数改造成 `RuntimeTransformerModifierTable` 组件，表体 JSX 逐行零改动；props = `description` / `items` / `onDeleteModifier` / `onMoveModifier` / `openCreateDialog` / `openEditDialog` / `scope` / `t` / `title`）/ `overview-section.tsx`98（原 261-328 `SettingsPanelCard` 概览：标题 / 图标 / 描述 / 三个状态徽标 + 4 个 `TransformerToggleCard`；props = `config` / `enabledModifierCount` / `onChangeConfig` / `t` / `totalModifierCount`）/ `modifier-dialog.tsx`149（原 344-453 规则编辑弹窗：错误提示 / type+enabled 行 / models / params JSON / extra JSON；props = `dialogError` / `dialogOpen` / `dialogScope` / `draft` / `editingIndex` / `handleSave` / `setDialogOpen` / `setDraft` / `t`）。沿用「机械搬迁 + props 化外壳」：入口保留原 65-73 签名与原 74-115（`useTranslation` / 5 个 state / `totalModifierCount` 与 `enabledModifierCount` 派生 / 三个 handler）逐行不变；原 `renderModifierTable(...)` 两处调用改写为 `<RuntimeTransformerModifierTable scope="request|response" …/>` 两次装配（title/description 的 `t(...)` 表达式原样移到调用点 props）；入口不再 import lucide-react / `@/components/ui/badge` / `config-domain-dialog` / `config-domain-table` / `config-form-field` / `editor-control-class` / `settings-action-group` / `settings-add-button` / `settings-badge-list` / `settings-dialog-footer` / `settings-notice-card` / `settings-panel-card` / `settings-panel-icon` / `settings-mini-toggle-card` / `runtime-provider-config-utils`；入口对外仍只导出 `RuntimeTransformerDomainEditor`（before == after）；消费方 `backend-config-settings-page.tsx` 的 `lazy(() => import("./runtime-transformer-domain-editor"))` 路径零改动。 | 脚本留档 `.tmp/split-runtime-transformer-domain-editor.mjs`（幂等：首次备份原文到 `.tmp/runtime-transformer-domain-editor.orig.tsx`）；等价性校验 `.tmp/verify-runtime-transformer-domain-editor-split.mjs`：9 个搬迁区间逐字节命中（含 3 处 export 前缀转写与 `ToggleCard`→`TransformerToggleCard` 改名）+ 入口 24 类迁出符号零残留 + 入口导出面仅 1 项 + 6 个子模块导出面/头注释/行数门禁 + 三个组件装配次数（overview 1 / table 2 / dialog 1）且 23 个 props 均在对应模块类型中声明 + 原 5 处 ToggleCard 调用点改名齐全 + 消费方 lazy 路径未变 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/settings/runtime-transformer-domain-editor.tsx src/components/workspace/settings/runtime-transformer-domain-editor` 0 问题；`npm run build` exit 0（795ms）；`npm run test` 88 文件 / 459 用例全绿；入口 508 → 114，最大子模块 `modifier-table.tsx` 177 行；`frontend/src` 内 > 500 行文件数 12 → 11（第 2 批 3/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 29 项 `components/workspace/settings/appearance-settings-page.tsx`） | 已完成（2026-09-11） | `appearance-settings-page.tsx`(596) → 入口 89 行 + `components/workspace/settings/appearance-settings-page/` 9 个模块：`options`117（4 个选项工厂 `buildAccentOptions` / `buildThemeOptions` / `buildFontFamilyOptions` / `buildCodeFontOptions` + 4 个 `ReturnType` 推导选项类型）/ `font-size-control-card`142（原 67-197 `FontSizeControlCard` + 私有 `clampFontSizeValue`）/ `theme-section`111（原 311-387 主题三选卡 + 当前主题信息卡）/ `font-family-section`89（原 435-492 内容字体 / 代码字体两栏选择）/ `accent-section`71（原 389-433 强调色三选卡）/ `preview-section`70（原 547-593 工作区与代码字体预览卡）/ `size-section`61（原 494-527 三个字号滑杆卡）/ `motion-section`39（原 529-545 减少动效开关）/ `font-choice-card`38（原 34-65 字体选择卡 + `FontChoiceCardProps`）。沿用「机械搬迁 + props 化外壳」：原 215-307 四个选项数组按「保留原变量名 + 补 return」整块迁入 `options` 工厂函数；原 311-593 六段 JSX 整块逐字节搬迁到 6 个 section 组件（props 名与原局部标识符一致，`settings` / `systemTheme` / `t` / `updateSection` 与选项数组经显式 props 注入，JSX 正文零改写）；入口仅保留 `useTranslation` / `useAppSettings` 解构、`currentFontStack` / `currentCodeFontStack` / `themeValueLabel` 派生与 6 个 section 装配，对外仍只导出 `AppearanceSettingsPage`（before == after）；消费方 `settings-dialog.tsx` 的 `import { AppearanceSettingsPage } from "./appearance-settings-page"` 零改动。 | 脚本留档 `.tmp/split-appearance-settings-page.mjs`（幂等：首次备份原文到 `.tmp/appearance-settings-page.orig.tsx`）；等价性校验 `.tmp/verify-appearance-settings-page-split.mjs`：13 个搬迁 / 保留区间逐行命中（含 2 处 export 前缀转写）+ 入口 25 类迁出符号零残留 + 入口导出面仅 1 项 + 9 个模块导出面 / 头注释 / 行数门禁 + 6 个 section 各装配一次且 props 均在对应模块声明 + 消费方 import 未变 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 9 模块 + 消费方>` 0 问题；`npm run build` exit 0（812ms）；`npm run test` 88 文件 / 459 用例全绿；入口 596 → 89，最大子模块 `font-size-control-card.tsx` 142 行；`frontend/src` 内 > 500 行文件数 11 → 10（第 2 批 4/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 31 项 `components/workspace/settings/harness-settings-page.tsx`） | 已完成（2026-09-11） | `harness-settings-page.tsx`(568) → 入口 157 行 + `components/workspace/settings/harness-settings-page/` 7 个模块：`workspace-section.tsx`74（原 118-157 工作区 section：状态徽标 + 刷新按钮 + 错误/缺失提示；props = `actionPending` / `displayError` / `loading` / `reload` / `setLocalActionError` / `statusLabel` / `t` / `workspacePath`）/ `permissions-section.tsx`132（原 159-259 权限总览 section：来源与版本信息卡 + allow/deny 徽标列表 + 规则表；props = `allowTools` / `denyTools` / `permissions` / `rules` / `t` / `workspacePath`）/ `grants-section.tsx`148（原 261-362 授权 section：授权表单 + 已记住授权列表与撤销；props = `actionPending` / `grantPattern` / `grantTool` / `grants` / `grantsStorePath` / `handleRememberGrant` / `revokeGrant` / `setGrantPattern` / `setGrantTool` / `setLocalActionError` / `t` / `workspacePath`）/ `memory-section.tsx`146（原 364-462 记忆 section：检索表单 + 记忆笔记列表；props = `actionPending` / `handleAppendMemory` / `loading` / `memoryNotes` / `memoryQuery` / `memoryTags` / `memoryText` / `searchMemory` / `setMemoryQuery` / `setMemoryTags` / `setMemoryText` / `t` / `workspacePath`）/ `plugins-section.tsx`138（原 464-565 插件 section：插件卡片列表 + trust/enable 操作；props = `actionPending` / `plugins` / `setLocalActionError` / `t` / `updatePlugin`）/ `types.ts`7（`HarnessSettingsPageProps` 就地补 `export`）/ `tags.ts`8（原 28-33 `splitTags` 就地补 `export`）。沿用「机械搬迁 + props 化外壳」：入口保留原 35-114（签名 / `useTranslation` / `useHarnessControlPlane` 解构 / 5 个 `useState` / `displayError` 与 `rules` / `denyTools` / `allowTools` 派生 / `statusLabel` memo / `handleRememberGrant` / `handleAppendMemory`）逐行不变，仅按原 DOM 顺序装配 5 个 section（props 名与原局部标识符一致，JSX 正文零改写）；入口不再 import lucide-react 与 `settings-*` 组件；对外仍只导出 `HarnessSettingsPage`（before == after）；2 处消费方（`settings-dialog.tsx` 与 `harness-settings-page.test.tsx` 的相对 `./harness-settings-page`）import 路径零改动。 | 脚本留档 `.tmp/split-harness-settings-page.mjs`（幂等：首次备份原文到 `.tmp/harness-settings-page.orig.tsx`）；等价性校验 `.tmp/verify-harness-settings-page-split.mjs`：入口导出面一致 + 7 个模块导出面 / 头注释 / 行数门禁 + 64 个 `t(...)` 调用键序列逐项一致 + 43 个 `className` 字面量序列逐项一致 + 92 个 JSX 标签序列一致（类型泛型已剥离）→ VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint src/components/workspace/settings/harness-settings-page.tsx src/components/workspace/settings/harness-settings-page src/components/workspace/settings/settings-dialog.tsx src/components/workspace/settings/harness-settings-page.test.tsx` 0 问题；`npm run build` exit 0（970ms）；`npm run test` 88 文件 / 459 用例全绿；入口 568 → 157，最大子模块 `grants-section.tsx` 148 行；`frontend/src` 内 > 500 行文件数 10 → 9（第 2 批 5/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 41 项 `components/workspace/settings/runtime-provider-queue-domain-editor.tsx`） | 已完成（2026-09-11） | `runtime-provider-queue-domain-editor.tsx`(504) → 入口 114 行 + `components/workspace/settings/runtime-provider-queue-domain-editor/` 6 个模块：`defaults-section.tsx`128（原 109-201 默认槽位卡：总开关 `SettingsMiniToggleCard` + `default_slot` 四字段；props = `config` / `onChangeConfig` / `providers`（页头 `badges.providerOverrides` 计数）/ `t` / `totalMaxConcurrency`）/ `overflow-heartbeat-section.tsx`144（原 203-320 两卡网格：`overflow`（`overflow.max_attempts` / `overflow.strategy`）与 `wait_heartbeat`（`interval` / `comment` / `max_wait_time`）各自的启用开关与三字段；props = `config` / `onChangeConfig` / `t`）/ `provider-table.tsx`117（原 322-401 `ConfigDomainTable`：标题 / 描述 / 概览徽标 / 新建按钮 + 3 列（provider / slot / actions），编辑与删除回调原样下传；props = `config` / `onDeleteProvider` / `openCreateDialog` / `openEditDialog` / `providers` / `t`）/ `provider-dialog.tsx`125（原 403-491 编辑弹窗：错误通知 + `provider` / `max_concurrency` / `queue_size` / `queue_timeout` 四字段 + 额外 JSON；props = `dialogError` / `dialogOpen` / `draft` / `editingProvider` / `handleSave` / `setDialogOpen` / `setDraft` / `t`）/ `types.ts`18（原 37-46 `RuntimeProviderQueueDomainEditorProps` 就地补 `export`）/ `draft-utils.ts`11（原 496-504 `stringifyExtraFields` 补 `export`）。沿用「机械搬迁 + props 化外壳」：入口保留原 48-105（签名 / `useTranslation("runtimeConfig")` / 4 个 `useState` / `totalMaxConcurrency` memo / `openCreateDialog` / `openEditDialog` / `handleSave`）逐行不变，仅按原 DOM 顺序装配 4 个 section（props 名与原局部标识符一致，JSX 正文零改写）；入口不再 import lucide-react / `@/components/ui/badge` / `config-domain-*` / `config-form-field` / `editor-control-class` / `settings-*`；对外仍只导出 `RuntimeProviderQueueDomainEditor`（before == after）；消费方 `backend-config-settings-page.tsx` 的 `lazy(() => import("./runtime-provider-queue-domain-editor"))` 路径零改动。 | 脚本留档 `.tmp/split-provider-queue-domain-editor.mjs`（幂等：首次备份原文到 `.tmp/runtime-provider-queue-domain-editor.orig.tsx`）；等价性校验 `.tmp/verify-provider-queue-domain-editor-split.mjs`：入口导出面一致 + 6 个模块导出面 / 头注释 / 行数门禁 + 32 个 `t(...)` 调用键序列逐项一致 + 17 个 `className` 字面量序列逐项一致 + 76 个 JSX 标签序列一致（双方对称剥离类型泛型）→ VERIFY OK | `npx tsc -b --force` 0 错误（首轮暴露的 `providers` 计数下传与入口未用类型导入已修正）；`npx eslint src/components/workspace/settings/runtime-provider-queue-domain-editor.tsx src/components/workspace/settings/runtime-provider-queue-domain-editor src/components/workspace/settings/backend-config-settings-page.tsx` 0 问题；`npm run build` exit 0（816ms）；`npm run test` 88 文件 / 459 用例全绿；入口 504 → 114，最大子模块 `overflow-heartbeat-section.tsx` 144 行；`frontend/src` 内 > 500 行文件数 9 → 8（第 2 批 6/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 38 项 `components/workspace/settings/runtime-agent-routing-domain-utils.ts`） | 已完成（2026-09-11） | `runtime-agent-routing-domain-utils.ts`(514) → 入口 barrel 31 行 + `components/workspace/settings/runtime-agent-routing-domain-utils/` 7 个模块：`types.ts`74（原 7-77：`agentRoutingDifficulties` 四档常量 + 10 个类型）/ `health.ts`207（原 193-224 `analyzeRuntimeAgentRoutingConfig` + 226-303 `analyzeRouteHealth` + 305-313 `routeHealth` + 315-340 `analyzeModelHealth` + 342-365 `resolveProviderReference` + 367-379 `providerAliases`）/ `summary.ts`87（原 79-94 `getRuntimeAgentRoutingSettings` + 436-453 `readRoutingSummary` + 455-462 `readRouteProfile` + 464-469 `readDifficulty` + 475-481 `readNumberText` + 501-514 `cloneRoutingSummary`）/ `build.ts`77（原 96-128 `buildRuntimeAgentRoutingRecord` + 130-140 `updateRuntimeAgentRoutingConfig` + 142-151 `updateRuntimeTeamRoutingInheritance`）/ `capabilities.ts`59（原 381-395 `modelMappingTargets` + 397-413 `findConfiguredModelCapability` + 415-424 `supportsReasoningEffort` + 426-434 `readCapabilityReasoningEfforts`）/ `options.ts`49（原 153-166 `providerModelOptions` + 168-191 `providerReasoningEffortOptions`）/ `text-utils.ts`24（原 471-473 `readText` + 483-486 `parseNonNegativeInteger` + 488-499 `setOptionalText`）。纯机械搬迁：跨模块复用的原私有 helper 就地补 `export`（`readText` / `setOptionalText` / `parseNonNegativeInteger` / `modelMappingTargets` / `findConfiguredModelCapability` / `supportsReasoningEffort` / `readCapabilityReasoningEfforts`），其余私有函数维持模块内私有；入口改为显式 barrel 后对外 18 个符号（10 类型 + `agentRoutingDifficulties` + 7 函数）与消费方（`backend-config-settings-page.tsx` / `runtime-agent-routing-domain-editor.tsx` / 其下级 7 个模块 / 2 个测试文件）import 路径零改动。 | 脚本留档 `.tmp/split-runtime-agent-routing-domain-utils.mjs`（幂等：首次备份原文到 `.tmp/runtime-agent-routing-domain-utils.orig.ts`）；等价性校验 `.tmp/verify-runtime-agent-routing-domain-utils-split.mjs`：25 个搬迁区间逐行命中（允许补 `export`）+ 原文非空行 0 未覆盖 + 入口导出面与原 18 个 public 符号逐一一致 + 入口零实现体残留 → PASS | `npx tsc -b --force` 0 错误（首轮暴露 `summary.ts` 漏引 `RuntimeAgentRoutingSettings` 已修正）；`npx eslint <入口 + 新目录 + 编辑器与页面消费方>` 0 问题；`npx vitest run runtime-agent-routing-domain-utils.test.ts runtime-agent-routing-domain-editor.test.tsx` 2 文件 / 16 用例全绿；`npm run build` exit 0（828ms）；入口 514 → 31，最大子模块 `health.ts` 207 行；`frontend/src` 内 > 500 行文件数 8 → 7（第 2 批 7/8 项完成） |
| P0-2 超长文件拆分（第 2 批·第 1 项 `components/workspace/settings/backend-config-settings-page.tsx`） | 已完成（2026-09-11） | `backend-config-settings-page.tsx`(3932) → 入口 135 行 + `components/workspace/settings/backend-config-settings-page/` 48 个模块（合计 4878 行）。支撑层：`types`（`Translator`/`EditorMode`）、`lazy-editors`（15 个懒加载域编辑器，动态 import 路径回退一层）、`mode-registry`（16 个模式菜单项）、`format`（7 个纯函数 + `countImpactPaths`/`buildSaveStatusMessage`）、`primitives`（`StatCard`/`SummaryPill`/`ControlPanel`/`MenuButton`/`ConfigEditorLoadingCard`）、`impact-panel`（`ImpactStat`/`ImpactPathList`/`RuntimeImpactPanel`）、`named-array-utils`；核心 hook 层：`use-config-state`460（state / 派生值 / 内建函数 / effects → `ConfigEditorState`）、`use-config-actions`254（`createConfigEditorActions(state)` 6 个动作 → `ConfigEditorActions`）、`use-config-core`（`{ ...state, ...actions }` → `ConfigEditorCore`）；`domains/` 15 个域工厂（`createXxxDomain(core)` 返回处理器对象 + `XxxDomain` 类型，函数体原样搬迁）；`sections/` 7 个块（`header-section`/`summary-grid`/`control-bar`/`impact-section`/`menu-panel`/`preview-section`/`unsaved-bar`）+ `sections/modes/` 16 个模式 section（原 16 个分支体逐块搬迁）。解耦策略：以 `ConfigEditorCore`（state + actions 合并对象）替代 props 爆炸，section 组件按需声明 `{ core: ConfigEditorCore }` 与 `{ domain: XxxDomain }`，域工厂在入口 `useConfigEditorCore()` 之后创建并按模式下传；JSX 正文零改写（仅去分支守卫行与缩进转写）。入口保留 core + 15 个域工厂 + 7 个 section + 16 个模式分支装配，对外仍只导出 `BackendConfigSettingsPage`，消费方 import 路径零改动。脚本留档 `.tmp/backend-config-split-lib.mjs`（分析库：import 表 / 作用域声明 / 切片引用）、`.tmp/split-backend-config-settings-page.mjs`（生成器，幂等：首次备份原文到 `.tmp/backend-config-settings-page.orig.tsx`，内置单模块 < 500 行门禁）、`.tmp/verify-backend-config-split.mjs`（保真校验） | 保真校验：原 3932 行逐行核对，未逐字命中 222 行**全部可解释**（147 import 重排 + 36 `export` 前缀 + 15 懒加载动态 import 路径 + 22 分支守卫 / `) : null}` 转写 + 2 处 `core.t(...)`），分类 other=0；`npx tsc -b --pretty false` 0 错误（首轮暴露的 props 解构命名、自身导入、动态 import 路径、模板字符串插值漏检均已修正）；`npx eslint src/components/workspace/settings/backend-config-settings-page.tsx src/components/workspace/settings/backend-config-settings-page` 0 问题（react-refresh：组件模块只导出组件，hook / 工厂归 `.ts`）；`npm test` 88 文件 / 459 用例全绿；49 个文件全部 < 500 行（最大 `use-config-state.ts` 460）；`frontend/src` 内 > 500 行文件数 7 → 6（第 2 批 8/8 项完成） |
| P0-2 超长文件拆分（第 4 批·第 33 项 `data/mock.ts`） | 已完成（2026-09-11） | `data/mock.ts`(539) → 入口 barrel 6 行 + `data/mock/` 3 个模块：`types.ts`114（原 1-112：`MessageSegment` 多态联合（text / reasoning / tool / code / checklist / receipt / callout / image / image-placeholder 等）+ `ChatMessage` / `Artifact` / `Thread`）、`artifacts.ts`145（原 114-256：`workspaceShellArtifact` / `routeContractArtifact` / `landingPreviewArtifact` 三个模板串常量，就地补 `export`）、`initial-threads.ts`291（原 258-539：`export const initialThreads: Thread[]`，新增 `./artifacts` 值导入与 `./types` 类型导入）。纯机械搬迁：原文件无 import、无 JSX（`WorkspaceShell` 等代码均位于模板串内，非真实导出），入口改为显式 barrel（`export type { Artifact, ChatMessage, MessageSegment, Thread }` + `export { initialThreads }`）后对外导出面不变，70 处 `@/data/mock`（含 `type` 前缀命名导入）消费方 import 路径零改动。 | 脚本留档 `.tmp/split-mock.mjs`（幂等：首次备份原文到 `.tmp/mock.orig.ts`，锚点校验 + 单模块 < 500 行门禁）；保真校验 `.tmp/verify-mock-split.mjs`：原 539 行非空行 0 未覆盖（missing=0）+ 入口导出面一致 + 4 个文件头注释与行数门禁 → VERIFY OK | `npx tsc -b --pretty false` 0 错误（首轮暴露 `initial-threads.ts` 漏引 `Thread` 已修正）；`npx eslint src/data/mock.ts src/data/mock` 0 问题；`npm test` 88 文件 / 459 用例全绿；`frontend/src` 内 > 500 行文件数 6 → 5（第 4 批 1/4 项完成） |
| P0-2 超长文件拆分（第 4 批·第 36 项 `hooks/workspace/use-runtime-sessions-data.ts`） | 已完成（2026-09-11） | `use-runtime-sessions-data.ts`(523) → 入口 267 行 + `hooks/workspace/runtime-sessions-data/` 7 个模块：`types.ts`23（原 13-31：`RuntimeSessionsDataOptions` / `RuntimeSessionsSummary` / `StoredRuntimeSessionsPayload`，`RuntimeSessionsDataOptions` 与 `StoredRuntimeSessionsPayload` 就地补 `export`）/ `normalize.ts`37（原 170-198 `normalizeRuntimeSessions` + `normalizeRuntimeSessionUsers`）/ `sorting.ts`41（原 134-168 `sortRuntimeSessions` + `summarizeRuntimeSessions`）/ `storage.ts`97（原 33-35 存储键常量 + 38-44 `getBrowserStorage`（补 `export`）+ 46-53 `buildStoredRuntimeSessionsKey` + 55-67 `readStoredRuntimeSessionUserId` + 69-79 `writeStoredRuntimeSessionUserId` + 81-101 `readStoredRuntimeSessions` + 103-122 `writeStoredRuntimeSessions`）/ `retry.ts`13（原 36 重试退避常量 + 124-132 `resolveRuntimeSessionsRetryDelay`）/ `user-selection.ts`56（原 200-249 `chooseRuntimeSessionUserId`）/ `loading.ts`61（原 251-274 `mergePinnedRuntimeSession` + 276-299 `loadRuntimeSessions`）。纯机械搬迁：跨模块复用的原私有 `getBrowserStorage` 与未导出类型就地补 `export`，其余保持模块私有；入口保留原 301-523 hook 本体逐行不变，仅改写 import 并显式 re-export 原 15 个对外符号（1 类型 + 14 函数），消费方（`pages/workspace-page.tsx`、`settings-dialog.tsx`、`workspace-shell/types.ts`、`workspace-sidebar/*`、`about-settings-page.tsx` 与 `use-runtime-sessions-data.test.ts`）import 路径零改动。 | 脚本留档 `.tmp/split-use-runtime-sessions-data.mjs`（幂等：首次备份原文到 `.tmp/use-runtime-sessions-data.orig.ts`，20 处行锚点断言 + 单模块 < 500 行门禁）；保真校验 `.tmp/verify-use-runtime-sessions-data-split.mjs`：原文非空行 0 未覆盖（missing=0；仅原 import 头 10 行按模块边界重写属预期差异）+ 入口 15 个导出符号逐一可见 → VERIFY OK | `npx tsc -b --force` 0 错误（首轮暴露入口漏引 `listRuntimeSessionUsers` 已修正）；`npx eslint src/hooks/workspace/use-runtime-sessions-data.ts src/hooks/workspace/runtime-sessions-data src/hooks/workspace/use-runtime-sessions-data.test.ts` 0 问题；`npx vitest run src/hooks/workspace/use-runtime-sessions-data.test.ts` 16 用例全绿；`npm run build` exit 0；入口 523 → 267，最大子模块 `storage.ts` 97 行；`frontend/src` 内 > 500 行文件数 5 → 4（第 4 批 2/4 项完成） |
| P0-2 超长文件拆分（第 4 批·第 37 项 `pages/cache-analytics-page.tsx`） | 已完成（2026-09-11） | `cache-analytics-page.tsx`(518) → 入口 255 行 + `pages/cache-analytics/` 6 个模块：`format.ts`24（原 497-518 `formatCacheNumber` / `formatCacheReportedNumber` / `formatCacheRatio` / `formatCacheTime`）/ `cache-status.ts`29（原 273-281 `cacheStatusTone` + 479-495 `CacheStatusI18nKey` 类型与 `cacheStatusKey`）/ `cache-metric.tsx`11（原 247-255 `CacheMetric`）/ `distribution-chip.tsx`19（原 257-271 `DistributionChip`）/ `requests-table.tsx`112（原 283-377 `RequestsTable`：分页控制 + 8 列明细表）/ `trace-sidebar.tsx`111（原 379-477 `TraceSidebar`：加载 / 报错 / 追溯三段）。纯机械搬迁：跨模块复用的原私有 helper 就地补 `export`（`cacheStatusTone` / `cacheStatusKey` / 4 个 format 函数 / 4 个组件），入口保留原 32-44（视图注释 / `requestPageSize` / `emptyDistribution`）与 46-245 `CacheAnalyticsView` 逐行不变，仅改写 import；对外仍只导出 `CacheAnalyticsView`，消费方 `pages/usage-analytics-page.tsx`（`lazy(() => import("@/pages/cache-analytics-page"))`）与 `cache-analytics-page.test.tsx` import 路径零改动。 | 脚本留档 `.tmp/split-cache-analytics-page.mjs`（幂等：首次备份原文到 `.tmp/cache-analytics-page.orig.tsx`，18 处行锚点断言 + 单模块 < 500 行门禁）；保真校验 `.tmp/verify-cache-analytics-page-split.mjs`：原文非空行 0 未覆盖（missing=0；仅原 import 头 29 行按模块边界重写属预期差异）+ 入口导出面仅 `CacheAnalyticsView` → VERIFY OK | `npx tsc -b --force` 0 错误（首轮暴露 `cache-status.ts` 内 `cacheStatusKey` 漏加 `export` 已修正）；`npx eslint src/pages/cache-analytics-page.tsx src/pages/cache-analytics src/pages/cache-analytics-page.test.tsx` 0 问题；`npx vitest run src/pages/cache-analytics-page.test.tsx` 4 用例全绿；`npm run build` exit 0；入口 518 → 255，最大子模块 `requests-table.tsx` 112 行；`frontend/src` 内 > 500 行文件数 4 → 3（第 4 批 3/4 项完成） |
| P0-2 超长文件拆分（第 4 批·第 39 项 `hooks/workspace/use-runtime-checkpoints.ts`） | 已完成（2026-09-11） | `use-runtime-checkpoints.ts`(509) → 入口 392 行 + `hooks/workspace/runtime-checkpoints/` 3 个模块：`types.ts`36（原 23-50：`UseRuntimeCheckpointsOptions` / `ShouldReloadRuntimeCheckpointsOptions` / `ShouldReloadBacktrackAuditOptions` / `ResolveCheckpointDetailStateOptions` 四组选项类型，就地补 `export`）/ `reload-keys.ts`76（原 52-61 `buildRuntimeEventReloadKey` + 63-91 `shouldReloadRuntimeCheckpoints` + 93-120 `shouldReloadBacktrackAudit`）/ `detail-state.ts`43（原 122-154 `resolveCheckpointDetailState`）。纯机械搬迁：跨模块复用的未导出类型就地补 `export`，原 import 表按模块归属拆分；入口保留原 156-509 hook 本体逐行不变，仅改写 import 并显式 re-export 原 5 个对外符号（4 助手函数 + hook），消费方（`components/workspace/artifact-panel.tsx`、`hooks/workspace/use-runtime-plan-mode.ts` 及 `use-runtime-checkpoints.test.ts` / `use-runtime-plan-mode.test.ts`）import 路径零改动。 | 脚本留档 `.tmp/split-use-runtime-checkpoints.mjs`（幂等：首次备份原文到 `.tmp/use-runtime-checkpoints.orig.ts`，10 处行锚点断言 + 单模块 < 500 行门禁）；保真校验 `.tmp/verify-use-runtime-checkpoints-split.mjs`：原文非空行 0 未覆盖（missing=0；仅原 import 头 20 行按模块边界重写属预期差异）+ 入口 5 个导出符号逐一可见 → VERIFY OK | `npx tsc -b --force` 0 错误；`npx eslint <入口 + 新目录 + 2 个消费方 + 2 个测试文件>` 0 问题；`npx vitest run use-runtime-checkpoints.test.ts use-runtime-plan-mode.test.ts` 2 文件 / 6 用例全绿；`npm run build` exit 0；入口 509 → 392，最大子模块 `reload-keys.ts` 76 行；`frontend/src` 内 > 500 行文件数 3 → 2（第 4 批 4/4 项完成，仅余 P0-6 承担的 i18n 双语字典） |
| P0-2 超长文件拆分（任务级收口） | 已完成（2026-09-11） | 41 项归属全部落定：P0-2 自行完成 **39 项**（首批 15 项 + 第 2 批 8 项 + 第 3 批 10 项 + 第 4 批 4 项 + 随源测试 2 项并入第 4/13 项行）；余 **2 项** i18n 双语字典（`zh-CN.ts` 2595 / `en-US.ts` 2536）按 §6.1 约定由 P0-6 以 namespace 拆分承接。39 项全部为纯机械搬迁：子模块首行固定注释、对外导出面不变、消费方 import 路径零改动、单模块 < 500 行。 | 每项留档 `.tmp/split-*.mjs`（幂等锚点断言）+ `.tmp/verify-*-split.mjs` 保真校验（missing=0 且导出面一致 → VERIFY OK）；逐项 `tsc -b --force` / `eslint` / 定向 `vitest` / `npm run build` 全绿，最近一次全量 `npm test` 88 文件 / 459 用例全绿；`frontend/src` 内 > 500 行文件数 41 → 2（余项均为 P0-6 承担），满足「M4 前 ≤ 10 且余项有排期」验收。 |

| P0-3 卡片/面板组件收敛（表面与槽位原语） | 已完成（2026-09-11） | 新增 `components/ui/surface-card.tsx`（cva 五轴显式枚举：`frame` outlined/dashed、`surface` solid/softer/panel/accent/warning/warning-soft、`radius` sm/md/lg/xl/panel、`density` snug/default/panel/compact/tight/roomy/wide、`state` disabled/hoverable）与 `components/ui/section-header.tsx`（`SectionHeaderRow` / `SectionHeaderMain` / `SectionAside` / `SectionTitleRow` / `SectionDescriptionText` / `SectionBody` 六个槽位）；`settings-panel-card` / `settings-info-card` / `settings-field-card` 改为原语组合，`settings-mini-card` / `settings-subsection-card` 维持既有薄封装（默认 `className` 覆盖语义不变）。非 div 容器（`button` / `label`）直接调用 `surfaceCardVariants(...)` 取类名，避免 `as` 多态。DOM 结构、元素标签与类名集合逐字保留（仅类名书写顺序可不同，twMerge 语义等价）。 | 保真探针 45 用例（静态渲染 + jsdom 双通道）规范化比对 diff=0（比对「元素/属性/类名集合/文本」四级，容忍类名顺序）；`npx tsc -b --force` 0 错误；`npx eslint <5 个改动文件>` 0 问题；`npm run build` exit 0；提交 `29fbd990` |
| P0-3 卡片/面板组件收敛（开关/选择/提示/空态 + 图标与复选框） | 已完成（2026-09-11） | 新增 `components/ui/panel-icon.tsx`（原 `settings-panel-icon` 与其在 `settings-toggle-card`、`workspace-settings-page` 的两处内联副本三合一）与 `components/ui/checkbox.tsx`（`h-4 w-4 accent-[var(--accent-primary)]`，`shrink` 可选）；`settings-toggle-card` / `settings-mini-toggle-card` / `settings-inline-toggle-card` / `settings-choice-card` / `settings-notice-card` / `settings-empty-state` / `ui/card` 改为表面变体组合，notice 四档 tone 以 `surface` + `density` + 文本类映射表表达。 | 探针首轮即抓出 `choice-card/inactive-disabled` 差异（原实现在 disabled 时仍保留 hover 类，语义不能丢），据此改为「hoverable 与 disabled 叠加」→ 复跑 diff=0；`tsc` / `eslint` / `build` 全绿；提交 `af783bc2` |
| P0-3 卡片/面板组件收敛（对话框外壳与生命周期） | 已完成（2026-09-11） | 新增 `components/ui/dialog-shell.tsx`（`DialogOverlay` 遮罩 + `DialogPanel` 面板，`elevation` md/lg 对应两处阴影）与 `components/ui/use-dialog-lifecycle.ts`（body 滚动锁定 + Esc 关闭，两份同构 effect 合一）；`settings-dialog` / `config-domain-dialog` 复用外壳，前者保持内联渲染、后者保持 `createPortal` 挂到 `document.body`，渲染位置与 DOM 均不变。 | 探针 config-dialog open/closed/width 三用例 diff=0；`npx tsc -b --force` 0 错误；`npx eslint <4 个改动文件>` 0 问题；提交 `ae057867` |
| P0-3 卡片/面板组件收敛（任务级收口） | 已完成（2026-09-11） | 19 个设置域组件收敛到 **6 个共享抽象**：`SurfaceCard`（+`surfaceCardVariants`）、`SectionHeader*` 槽位族、`PanelIcon`、`CheckboxInput`、`DialogOverlay`+`DialogPanel`、`useDialogLifecycle`（对齐 §6.2 目标 Card/PanelSection/EmptyState/Dialog/FieldRow/ActionGroup 的口径）。原 16 个具名枚举组件全部改为共享原语组合或保持薄封装（`settings-mini-card` / `settings-subsection-card`）；`settings-action-group` / `settings-badge-list` / `settings-section` / `settings-add-button` / `settings-dialog-footer` 为单职责薄组件、无重复表面，保持原样。对外 props、导出名、消费方 import 路径零改动。遗留（不阻塞）：设置域各编辑器内约 35 处内联 `<input type="checkbox">` 仍手写同一类名，已具备 `CheckboxInput` 原语，可作为后续机械替换项。 | 保真探针 45 用例（覆盖 `ui/card`、16 个设置组件与 config-dialog 三态）规范化比对 diff=0，探针为临时工具、已按要求在交付前删除（基线快照在 gitignored 的 `.tmp/`）；`npx tsc -b --force` 0 错误；`npx eslint <全部改动文件>` 0 问题；全量 `npm test` 88 文件 / 459 用例全绿（含探针时为 89 / 460）；`npm run build` exit 0；`frontend/src` 内 > 500 行的 ts/tsx 文件数保持 2（均为 P0-6 承接的 i18n 双语字典；样式表 `globals.css` 691 行不计入该口径），未新增超长文件 |
| P0-4 前置门槛：恢复 e2e 全绿（M0 门禁） | 已完成（2026-09-11） | e2e 5 个失败用例（G1/G2/G5/G8a/G8b）根因修复 + 跨 spec 状态泄漏治理：① `lib/thread-state/sessions.ts` 一个 runtime session 只保留一个 thread，本地在途线程优先于恢复占位（选中态不再落到空占位，正在流式的消息恢复渲染）；② `lib/thread-state/history-mapping.ts` 权威历史合并保留 live-only 段（tool/code），工具卡不再被历史投影抹掉；③ `lib/thread-state/history-artifacts.ts` 保留历史未落盘的 `interrupted/streaming` 消息并追加在权威历史之后，Ctrl+Enter 中止后的部分回答与 Stopped 标记不再丢失；④ `components/workspace/message-list.tsx` 贴底跟随覆盖「内容与 done 同批提交」的收尾帧并补一帧 rAF 贴底，同时保留 `isResponding` 语义（历史回放/回溯结束不拽底）、用户上滚仍暂停；⑤ `e2e/mock-server.mjs` 新增 `POST /api/_test/reset`（清 sessions / SSE 事件 / 坏事件流 / seq），4 个 spec 的 `beforeEach` 调用以清空跨用例残留（修复 `e2e-session-1` 历史累积导致时间线重复回答）；G1 定位由全局查询收紧为 `log` 角色内查询。随修补充测试：thread-state 历史投影 +2（tool 段透传、中断消息保留）、thread-selection +1。 | `npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿（54.6s，含 G1/G2/G5/G8a/G8b 与 P2-3 1000+ 事件虚拟化）；`npx eslint src/components/workspace/message-list.tsx` 0 问题；提交 `00ebed3c` |
| P0-4 三层设计 token 体系（第 1 步：样式表层） | 已完成（2026-09-11） | `globals.css` 重排为三层：**L1** `--primitive-*` 176 个（全仓唯一允许 `#hex`/`rgba()` 的层）；**L2** 语义别名——`:root` 暗色基准 118、`html[data-theme="light"]` 83、`html[data-accent-tone="cyan"\|"violet"]` 各 8（均为「引用 L1，不写字面量」）；**L3** 组合 token（字体角色 `--app-*-font-size-*`、`--shadow-lv1/2`、`--elevation-*`、`--scrollbar-*`、`--motion-duration-slow`）。`@theme inline` 新增 87 条 L2→Tailwind 语义类映射（`--color-<role>: var(--<role>)`，inline 保证主题/强调色切换即时生效），`@theme` 新增 9 档圆角阶梯（`--radius-chip/control/field/card/card-lg/panel/panel-lg/shell/hero`）。**landing.css 并入**：原 `:root` 暗色 20 变量、light 覆盖 10 变量与规则内内联字面量（eyebrow / grid-line / glow-gold / glow-violet）全部改为同层 token 引用，文件仅保留布局类与 keyframes，变量声明数 **0**；`prefers-reduced-motion` 的 `--animate-*` 覆盖块随 L3 迁入 `globals.css`（逐字保留语义，避免漏掉 `animate-[var(--animate-fade-up)]` 的两处消费方）。生成器 `.tmp/p0-4-build.mjs` 承担机械改写 + 守卫。 | 逐变量解析等价校验（脚本内建）：dark=118 / light=83 / cyan=8 / violet=8 四上下文全量比对，改写前后解析值 **0 diff**；`L1 之外出现字面量` 守卫通过（0 处）；`landing.css` 变量声明数 = 0（脚本断言）；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿；提交 `b0788488` |
| P0-4 三层设计 token 体系（第 2 步：颜色任意值收敛） | 已完成（2026-09-11） | 按 `@theme inline` 的 87 条映射，把 `text-` / `bg-` / `border`(+`-t/-r/-b/-l/-x/-y`) / `ring`(+`-offset`) / `divide-` / `accent-` / `caret-` / `outline-` / `decoration-` / `placeholder-` / `fill-` / `stroke-` / `from-` / `via-` / `to-` 前缀的 `[var(--token)]`（含 `[color:var(--token)]` 写法与 `/NN` 透明度修饰）机械改写为语义工具类，共 **1807 处 / 193 文件**；渐变、阴影、动效等无对应工具类的 token 原样保留，不作等价性存疑的猜测式替换。改写脚本 `.tmp/p0-4-codemod.mjs` 由 `@theme inline` **反向建映射**（避免手写清单随 token 漂移），并整体跳过 13 个当时在途的工作树文件（`pages/usage-analytics/**`、`cache-analytics-page`、`App.tsx`、i18n 双语字典），留待其自身提交后再收敛。 | 任意值计数 **2008 → 201（-90%）**，达成 §6.1「下降 ≥ 70%」验收（余量集中在上述在途文件与无工具类可用的渐变/阴影 token）；产物 CSS 冒烟对拍：`.focus\:ring-\[var\(--ring\)\]:focus,.focus\:ring-ring:focus{--tw-ring-color:var(--ring)}`、`.divide-\[var\(--border\)\]…,.divide-border…{border-color:var(--border)}`——旧任意类与新语义类被 Tailwind 合并为同一条声明，直接证明解析等价；`npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿；`npx eslint src` 无新增问题（存量 2 error / 4 warning 全部落在未改动文件）；提交 `dbed0cab` |
| P0-4 三层设计 token 体系（第 3 步：固定强调色 hex 收敛） | 已完成（2026-09-11） | 固定强调色三色 `#8fd0c6`(=L1 `--primitive-teal-300`)、`#f0c77b`(=L1 `--primitive-gold-200`)、`#f59e7d`(=L1 `--primitive-orange-150`) 在 L2 中均映射为**不随 light/dark/强调色切换变化**的常量别名（`--accent-teal` / `--accent-gold` / `--accent-orange`），因此「任意值里写字面量」→「引用 L2 语义工具类」解析值逐字等价；`globals.css` 的 `@theme inline` 补 `--color-accent-teal: var(--accent-teal)` / `--color-accent-orange: var(--accent-orange)` 两条映射（`--color-accent-gold` 已存在），`:root` L2 补 `--accent-teal: var(--primitive-teal-300)` / `--accent-orange: var(--primitive-orange-150)`（`--accent-gold: var(--primitive-gold-200)` 已存在），三者在全文件均只有 L2 一处定义、无 light / 强调色覆盖。改写脚本 `.tmp/p0-4-hex.mjs`（check / `--write` 双模式，仅白名单三色 + 色彩等值论证，沿用同一在途文件跳过逻辑）共改写 **370 处类名 + 1 处任意值内部**：类名按前缀分布 `bg-`129 / `border-`128 / `text-`96 / `from-`9 / `via-`3 / `accent-`2 / `to-`2 / `ring-`1（`/NN` 透明度修饰原样保留），目标 token 分布 accent-teal 159 / accent-gold 111 / accent-orange 100；任意值内部 1 处为 `bg-[linear-gradient(90deg,#8fd0c6,#f0c77b)]` → `bg-[linear-gradient(90deg,var(--accent-teal),var(--accent-gold))]`。 | 产物 CSS 对拍：`.bg-accent-teal,.bg-accent-teal\/8{background-color:var(--accent-teal)}`、`.border-accent-gold\/16{border-color:var(--accent-gold)}`、`.text-accent-teal{color:var(--accent-teal)}`、`.border-accent-orange\/18{border-color:var(--accent-orange)}`、`.from-accent-teal{--tw-gradient-from:var(--accent-teal);…}`，且旧任意类 `bg-[#8fd0c6]` / `bg-[#f0c77b]` 在产物 CSS 内 0 命中；`frontend/src` 内 `#8fd0c6` / `#f59e7d` 仅剩 L1 原语定义各 1 处，`#f0c77b` 仅剩 L1 定义 1 处 + mock 产物内容 1 处（`data/mock/artifacts.ts` 的 CSS 文本，属模拟用户产物内容、渲染于 iframe 内取不到应用 CSS 变量，故保留字面量）；有意保留待后续步骤：阴影 / 渐变内 `rgba(...)` 色标（三色共 29 处，含 L1 原语自身的 rgba 定义）；`npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿；`npx eslint src` 无新增问题（存量 2 error / 4 warning 全部落在未改动文件）；提交 `6f9d3f69` |
| P0-4 三层设计 token 体系（第 2 步收尾：在途文件余量收敛） | 已完成（2026-09-11） | 第 2 步改写时整体跳过的 6 个在途文件已由另一条线程提交（`604a6951`），对其余量复用同一 codemod（`.tmp/p0-4-codemod.mjs`，映射仍由 `@theme inline` 反向生成，语义零改动）完成收敛：**154 处 / 6 文件**——`App.tsx`、`pages/cache-analytics-page.tsx`、`pages/usage-analytics/{format,overview,primitives,sessions}.tsx`；按前缀 `text-`77 / `border-`41 / `bg-`33 / `ring-`2 / `divide-`1，按 token 前三位 `--muted-foreground`45、`--border`19、`--surface-softer`10，`--analytics-danger*` / `--analytics-warning*` 系列共 23。 | 产物 CSS 冒烟：旧任意类 `text-[var(--muted-foreground)]` / `bg-[var(--surface-softer)]` / `border-[var(--analytics-danger-border)]` 均已 0 命中，新语义类 `.bg-surface-softer`、`border-analytics-danger-border`、`divide-border`、`text-muted-foreground`（8 处合并写法）均在产物内命中；`npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿（用户本地 `pnpm dev` 常驻占用 5193 且 `strictPort: true`、playwright 侧 `reuseExistingServer: false`，故以临时未提交配置 `VITE_DEV_PORT=5321` 跑同一套用例，跑完即删）；`npx eslint src` 无新增问题；提交 `7e547076` |
| P0-4 三层设计 token 体系（内容 4 之圆角：硬编码圆角收敛） | 已完成（2026-09-11） | §6.1 内容 4「`rounded-[0.7rem]`/`rounded-[0.9rem]` … → token」的圆角部分：把与 L3 圆角阶梯值**逐字相等**的 `rounded-[..]` 改写为语义阶梯类（`rounded-chip` / `control` / `field` / `card` / `card-lg` / `panel` / `panel-lg` / `shell` / `hero`），阶梯由 `globals.css` 的 `@theme` 块**反查**得到（chip .5 / control .65 / field .7 / card .8 / card-lg .85 / panel .9 / panel-lg .95 / shell 1.5 / hero 1.8 rem），不做猜测式映射。脚本 `.tmp/p0-4-radius.mjs`（check / `--write` 双模式，含 git 状态跳过与未命中清单），改写 **378 处 / 129 文件**：card 104、field 59、control 58、panel 57、panel-lg 41、card-lg 39、chip 9、shell 6、hero 5；形态 `rounded-*` 376 + `rounded-r-*` 2。**有意保留**（需设计确认后再收敛，不计入本次）：未命中阶梯的 101 处硬编码圆角（`0.75rem` 62、`0.6rem` 9、`1rem` 6、`1.7rem` 5、`0.72rem` 4、`1.9rem` 3、`0.55rem` 3、`2rem` 2、`2.25rem` 2、`3px`/`2px`/`1.4rem`/`1.15rem`/`0.45rem` 各 1）与字号硬编码 67 处（`text-[13px]` 36、`text-[10px]` 16、`text-[11px]` 10 等）——后者因 `--app-*-font-size-*` 角色 token 会随用户字号设置缩放，替换属**行为变更**，需按站点逐个决策角色。 | 等价性：阶梯值均为 `@theme` 静态字面量（不随主题 / 强调色 / 字号变化），产物 CSS 对拍 `.rounded-card{border-radius:var(--radius-card)}`、`.rounded-r-card{border-top-right-radius:var(--radius-card);border-bottom-right-radius:var(--radius-card)}`、`.rounded-panel-lg{border-radius:var(--radius-panel-lg)}`，`--radius-card:.8rem` 等 9 档阶梯全部产出，旧任意类 `rounded-[0.8rem]` / `rounded-[0.7rem]` 在产物内 0 命中；`npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npm run test:e2e` 16 例 / 4 文件全绿（临时未提交配置 `VITE_DEV_PORT=5321` 规避用户常驻 dev server 的 5193 占用，跑完即删）；`npx eslint src` 与基线一致（6 problems：2 error / 4 warning，唯一落在改动文件内的是 `artifact-detail-dialog.tsx:50` 既存 `react-hooks/exhaustive-deps` warning，与本次单行类名替换无关）；提交 `2ed83b46` |
| P0-4 三层设计 token 体系（内容 4 验收：主题 × 强调色 × 字号 e2e） | 已完成（2026-09-11） | 补 §6.1 内容 4 的「主题 / 强调色 / 字号切换后无颜色回归（e2e 覆盖三档主题 × 两种强调色）」缺口：新增 `frontend/e2e/design-tokens.spec.ts`（3 用例 / 425 行）。矩阵驱动方式为 localStorage 种子（`ai-agent-runtime.workspace.settings` 的 `appearance` 补丁）+ `page.emulateMedia({ colorScheme })` + reload，覆盖 light / dark / system 三档主题 × cyan / violet 两种强调色共 6 组合；system 档显式模拟 `prefers-color-scheme: dark` 以验证 media 解析路径（显式 dark 档与 system 档的区分由 `data-theme-mode` 断言兜住）。断言链路为「L2 语义变量 → L3 工具类 → 真实元素计算样式」：用探针元素把 `var(--x)` 解析成浏览器规范化色值，要求 `.app-chat-input`（`text-foreground`）计算色 === `var(--foreground)` === `@theme` 别名 `var(--color-foreground)`，且 `var(--color-accent-primary)` === `var(--accent-primary)`；不做像素比对以防渲染抖动误报。跨组合不变量：同一主题下切换强调色不得改变 5 个中性色（background / foreground / muted-foreground / border / surface-solid）；同一强调色下 `--accent-primary` / `--accent-secondary` 不随主题变化（强调色板主题无关）；亮 / 暗主题前景背景相对亮度差 > 0.15 且方向正确；默认 gold 与 cyan / violet 主色三者互不相同、且 gold 下中性色与显式主题一致。字号用例：`textSize` / `chatTextSize` / `codeTextSize` = 18 / 17 / 14 与 16 / 15 / 13 两组，断言 `--app-root-font-size` 等 4 个内联变量、`--app-code-line-number-size` = max(10, code-1)、`.app-text-13` = 基准 × 0.8125、`.app-code-surface .app-text-12` = code × 0.9230769、`.app-chat-copy .app-text-13` = chat × 0.8666667，并断言调大基准后角色字号单调变大。顺带定位并固化一处既有级联事实（非 P0-4 引入）：未分层的 `button, input, textarea, select { font: inherit }` 优先于 `@layer base` 内的 `.app-chat-input { font-size: var(--app-chat-font-size) }`，故输入框字号实际跟随 `--app-root-font-size`；`git log -S` 显示 `@layer base {`、`font: inherit`、`--app-chat-font-size`、`.app-chat-input` 四处构造均出自初始提交 `aa42c0ff`，即该规则自始未生效——e2e 以注释 + 断言固化现状（防止无声变化），是否修复属行为变更、留待单独决策。 | `npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；`npx playwright test` **19 例 / 5 文件全绿**（原 16 例 / 4 文件 + 新增 3 例；临时未提交配置 `VITE_DEV_PORT=5321` 规避用户常驻 dev server 的 5193 占用，跑完即删）；`npx eslint src` 与基线一致（6 problems：2 error / 4 warning，全部落在未改动文件）；`npx eslint e2e` 0 问题；提交 `7d42b79b` |
| P0-1 目录与构建卫生 | 已完成（2026-09-11） | 删除工作区内全部遗留备份：清理前实测 **7 个 `.backups` 目录 / 11 个 `.bak` 文件**（`e2e/.backups` 1、`components/workspace/runtime-teams/runtime-team-details-panel/.backups` 2、`components/workspace/settings/.backups` 1、`i18n/resources/.backups` 2、`pages/.backups` 1、`pages/cache-analytics/.backups` 1、`pages/usage-analytics/.backups` 3；与方案盘点的 8 / 20 差额为此前步骤已清理部分），清理后两者均为 0；`git ls-files` 确认这些文件从未被跟踪，故清理不产生任何 tracked diff。`.gitignore` 复核：`frontend/.gitignore` 已含 `*.bak`、`.backups/`（并含 `test-results/`、`playwright-report/`），根 `.gitignore` 第 68 行 `.tmp/` 覆盖脚手架输出。`dist/` 外构建残留：清理 `frontend/.tmp/` 内本会话产物（`eslint-after-codemod.txt`、`vitest-report.json`）。调试用 e2e 已按方案由 `diag.spec.ts` 改名为 `diag.manual.ts`，由 `playwright.manual.config.ts` 承接按需运行（默认 `playwright.config.ts` 的 testMatch 不匹配 `*.manual.ts`，实跑 5 文件 / 19 例不含该文件）。 | `frontend` 递归 `*.bak` 计数 = 0、`.backups` 目录 = 0；`git status --porcelain -- frontend` 无 `.bak` 新增且无 tracked diff；`npx tsc -b --force` 0 错误；`npm run build` exit 0；`npm test` 89 文件 / 470 用例全绿；提交：无源码 diff（清理对象均为 gitignore 覆盖的未跟踪文件），实施记录见本行所在提交 |
| P0-2 超长文件拆分（按域注册表化） | 已完成（2026-09-11） | 分批策略落地后复核当前形态：`frontend/src` 内 > 500 行文件**仅剩 i18n 双语字典 2 个**（`i18n/resources/en-US.ts` 2587 行、`zh-CN.ts` 2528 行；方案在 P0-2 分批策略中已明确把这两项划给 P0-6 同批完成）；> 400 行文件共 9 个，其中最大非 i18n 文件为 `components/workspace/settings/backend-config-settings-page/use-config-state.ts` 445 行。首批 15 项均已落为目录/多文件骨架并低于 500 行，抽样：`backend-config-settings-page/`（原 3932 行单文件目录化）、`workspace-sidebar.tsx` 433、`lib/thread-state/*`（原 `workspace-thread-state.ts` 1815 行拆分）、`types/runtime/*`（原 `runtime.ts` 1692 行拆分）、`use-workspace-agent-chat-turn.ts` 420、`use-runtime-team-dispatch.ts` 432、`runtime-provider-groups-domain-editor/` 目录化。 | 41 项超长文件总量 **41 → 2**，达成「M4 前降至 ≤ 10」验收；2 个余项均有明确责任任务（P0-6）；`npx tsc -b --force` 0 错误（拆分后对外 API 不变）；`npm test` 89 文件 / 470 用例全绿（既有测试未改断言）；`npx playwright test` 19 例 / 5 文件全绿；`npx eslint src` 与基线一致（6 problems：2 error / 4 warning）；提交：本条为状态复核与记录（拆分本体在 P0-2 各批次提交中，无新增源码 diff） |
| P0-5 主题解析/应用分离与首屏防闪烁 | 已完成（2026-09-11） | 新增 `core/theme/`：`resolve.ts`（设置 + 系统主题 + 语言 → 不可变 snapshot：theme/themeMode/accentTone/density/reducedMotion/fontSizes/fontFamilies/locale，纯函数无 DOM）、`present.ts`（唯一 DOM 写入入口；写入集合显式登记，`dispose()` 只回收「本次写入且当前值仍等于写入值」的属性，外部覆写保留；重复 present 幂等）、`boot-script.ts`（`buildThemeBootScript()` 生成与 present 完全同表面的解析期脚本，常量全部取自 `settings/local`：storageKey/defaults/limits/legacyPresets/fontStacks）、`index.ts` barrel。接线：`main.tsx` 与 `SettingsProvider` 改走 `presentThemeSnapshot(resolveThemeSnapshot(...))`；`applyDocumentSettings` 保留为兼容入口（写入集合与顺序不变）；`vite.config.ts` 新增 `themeBootPlugin`（`transformIndexHtml` + `order: "pre"`）把脚本内联替换 `index.html` 的 `<!--theme-boot-script-->` 占位，位于 `<head>` 且早于模块入口。附带加固：历史预设查表统一 `hasOwnProperty`（避免 `"constructor"` 等原型链键产生 `NaNpx`）、`tsconfig.node.json` 补 `@/*` 路径映射（vite.config 需导入启动脚本生成器）。 | `npx tsc -b --force` 0 错误；`npx eslint src` 与基线一致（6 problems：2 error / 4 warning）；`npm test` **92 文件 / 495 用例**全绿（新增 3 文件 / 25 用例）；`npm run build` exit 0 且产物 `dist/index.html` 中启动脚本位于 `<head>`（boot 脚本 index 269，早于模块入口 5875）并内嵌真实 storage key；e2e（临时 5321 配置）**21 例 / 5 文件**全绿（新增 2 例：拦截 `/src/main.tsx` 后在 React 挂载前断言 dark 主题 + 字号 + lang + color-scheme，以及 `system` 档按 prefers-color-scheme 解析）；boot 脚本与 resolve+present 逐属性对拍覆盖 11 组存储形态（默认、自定义、历史预设、越界钳制、非法值、损坏 JSON、原型链键、中英文语言、存储抛错）；提交：`70058a2a`（代码）、`ee3e8acd`（测试） |
| P0-6 i18n 类型真源与硬编码门禁 | 已完成（2026-09-11） | 双语字典按 namespace 拆分：`zh-CN.ts`(2528 行) → `resources/zh-CN/` 24 个模块、`en-US.ts`(2587 行) → `resources/en-US/` 24 个镜像模块（7 个顶层 namespace：common / landing / workspace / runtimeConfig / settings / logs / usageAnalytics；workspace 再分 base / panels-shell / panels-messages / panels-artifacts / panels-teams-panels / panels-teams-dispatch，runtime-config 再分 page / editor-*），聚合 `index.ts` 保持原对象形状，`resources.ts` 导入说明符不变。类型真源：`resources/shape.ts` 的 `DeepStringShape<T>`（键树与嵌套必须一致、值放宽为 string）+ `en-US/index.ts` 的 `satisfies DeepStringShape<typeof zhCN>` 编译期对齐；`react-i18next.d.ts` 的 `resources` 指向 `resources["zh-CN"]`，`t()` key 同源。运行时回退链：`fallbackLng: "zh-CN"` + `fallback.ts`（`parseMissingKeyHandler` 回退 key 文本并 dev 告警、`collectMissingKeys` 缺口审计）、`languageChanged` 回写 `documentElement.lang`；`loaders.ts` 提供 `namespaceKeys` / `namespaceLoaders` / `loadNamespaces` / `preloadNamespaces` 按 namespace 动态补包（首屏仍同步注入全量资源以消除缺键闪烁，rolldown 的 INEFFECTIVE_DYNAMIC_IMPORT 提示属预期）。门禁：`scripts/verify-frontend-i18n.ts`（315 行；TS AST 扫描 JSX 文案与 aria-label/title/placeholder/alt/label/confirmLabel，排除 i18n/test/d.ts，80 文件防呆阈值可经 `I18N_LINT_MIN_FILES` 覆盖）接入 `npm run lint`，新增 `npm run lint:i18n`；`tsconfig.node.json` include 增补 `scripts/**/*.ts`。 | `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=504, violations=0）`；编译期对齐探针：临时删除 `en-US/common.ts` 的 `loading.page` 后 `npx tsc -b` 失败（TS2741 + TS2322，指向 `DeepStringShape`），恢复（SHA-256 比对一致）后 0 错误；`npx tsc -b --pretty false` 0 错误；`npx eslint src` 与基线一致（6 problems：2 error / 4 warning；期间消解 1 处新增 `react-refresh/only-export-components` 违规，经抽取 `subagent-session-target.ts`，该组文件属并行工作流随其提交）；`npm test` **101 文件 / 565 用例**全绿（i18n 新增 index / fallback / loaders 3 个测试文件 16 用例）；`npm run build` exit 0；`frontend/src` 内 > 500 行文件数 **2 → 0**（41 → 0）；提交：`c86d9cba`（174 个文件；与并行多智能体面板 / 右栏 / 会话用量工作共享的 15 个文件未混入本提交） |
| P0-7 测试基建三件套 | 已完成（2026-09-11） | ① 构建产物单一来源：`playwright.config.ts` 只跑 `dist/`，`requireDist()` 在缺 `frontend/dist/index.html` 时 fail-fast，`webServer` 以 `npx vite preview --host 127.0.0.1 --port <previewPort> --strictPort` 起预览并透传 `VITE_API_PROXY_PORT` 指向 mock；② 空闲端口：`e2e/probe-ports.mjs` 子进程向 OS 申请 mock/preview 各一个端口，并以 `E2E_MOCK_PORT` / `E2E_PREVIEW_PORT` / `E2E_BASE_URL` 回写（worker 再次求值配置时复用同一组；显式设置环境变量可复现固定端口）；③ 失败产物：`outputDir=.artifacts/playwright`、`trace: retain-on-failure`，`e2e/fixtures.ts` 在用例失败时经 `saveFailureShot()` 补全页截图（`.artifacts/e2e-failures/<用例名>-<时间戳>.png`）并挂 `full-page-screenshot` 附件，`.artifacts/` 入 `.gitignore`；④ 固定种子：`DEFAULT_LOCALE=en-US`（应用默认 `localization.locale:"system"` 按浏览器语言解析，固定后英文断言与 mock 文案不随机器漂移；中文场景用 `seedLanguage` 显式覆盖）、`DEFAULT_TIMEZONE=Asia/Shanghai`、`DEFAULT_VIEWPORT=1440×900`，`global-setup.ts` 与 config 同源；⑤ scaffold 收敛：`e2e/support.ts` 导出 `probeFreePort` / `requireDist` / `saveFailureShot` / `resetMockState` 与 seed API（`seedSession` / `seedRuntimeEvents` / `seedSettings` / `seedAppearance` / `seedLanguage` 为预导航注入，`applySettings` / `applyAppearance` / `clearStoredSettings` 为当前文档立即写入以支持同测多轮切换；storage key 对齐应用真源 `ai-agent-runtime.workspace.settings`），`mock-server.mjs` 的 `/api/_test/runtime-events` 支持批量注入（兼容单条）、`POST /api/runtime/sessions` 接受显式 id/title；5 个 spec 的 `beforeEach` 收敛到 `resetMockState` + seed API，删除各自的 `_test/reset` 直连与本地 seed 实现。 | 缺 dist 探针：临时移除 `dist/index.html` → `npx playwright test --list` 输出 `Error: 缺少 frontend/dist/index.html：e2e 基于构建产物运行。请先执行 npm run build（或 npm run test:e2e 前置构建）。` 且 exit 1（随后文件已恢复），不再是白屏超时；失败产物探针：临时 `zz-fail-probe.spec.ts` 故意失败 → 报告含 viewport 截图 + `full-page-screenshot` 附件（`.artifacts/e2e-failures/`，91,202 字节，已确认落盘）及 error-context/trace，探针已删除；`npm run test:e2e` **21 例 / 5 文件全绿**（55.6s；收敛过程中修复 locale 固定导致的英文断言失效、G2 `Running` 中间态窗口 400→900ms、P2-3 "Streaming" 选择器歧义）；`npx eslint e2e playwright.config.ts` 0 问题；`npx tsc -b --pretty false` 0 错误；`npm run lint` 与基线一致（6 problems：2 error / 4 warning，i18n 门禁通过）；提交：`1ce89c24`（12 个文件；并行工作流改动未混入本提交） |
| M0 门禁复核（P0-1 ~ P0-7 收口） | 已完成（2026-09-11） | 在 P0-7 收口后按 §9.1 M0 验收标准对当前工作树做一次性复核（frontend 工作区，顺序：`lint:i18n` → `build` → `vitest` → `playwright`）。 | ① `npm run lint:i18n` → `i18n lint OK（scanned=504, violations=0）`；② `npm run build` exit 0（3124 modules transformed，`dist/index.html` 6.33 kB，`tsc -b` 0 错误）；③ `npm test` **101 文件 / 565 用例全绿**（54.07s）；④ `npm run test:e2e` **21 例 / 5 文件全绿**（57.5s；webServer 实测分配 `127.0.0.1:53318` mock / `127.0.0.1:53319` preview，空闲端口探测-回写链路在真实运行中生效）；⑤ `git ls-files "*.bak" "*.orig"` 为空；`frontend/src` 下 10 个 `.backups/` 目录为就地编辑工具备份，命中 `frontend/.gitignore:34`（未跟踪、不入库）；⑥ `landing.css` 变量声明计数 0（正则 `^\s*--[A-Za-z0-9-]+\s*:`）。五项验收标准全部满足，M0 达成。 |
| P1-1 消息渲染投影模型与折叠策略 | 已完成（2026-09-11，两步：投影内核 `534ca637` + 渲染接线 `deebd764`） | **投影内核**（`534ca637`，7 文件 559 行）：`lib/chat-view/` 落 `types.ts`（Definition/Node/投影类型：`nodes` / `hiddenNodes` / `collapsed` / `summary` / `finalAnswerStart` / `systemPrompt` / `usage`）、`nodes.ts`（segment → 稳定 node key，工具段沿用 `getToolSegmentKey` 语义）、`collapse.ts`（final-answer 边界 + 过程证据摘要：工具数/带回复数/子代理数，零值段省略）、`project.ts`（三态投影入口：System prompt 行 / 已结束 Turn 折叠 / 可见）、`index.ts` barrel；Chat 与 Trajectory 各自消费自己的快照，不新建第二份权威状态。**折叠谓词**：`streaming !== true` 且 `finalAnswerStart > 0` 且未手动 `expanded` → 折叠过程证据；无 final answer 的 closed Turn 保全过程可见；流式期不折叠。**渲染接线**（`deebd764`，15 文件）：`assistant-message-card` 改为消费 `projectChatView`，折叠时渲染新组件 `process-collapse-row`（`aria-expanded` + sr-only 展开/折叠文案 + 摘要行），展开后用 `hiddenNodes` 原样重放过程节点（渲染参数与可见节点一致）；`history-context-message-card` 消费投影得到 `System prompt` 标题行与上下文折叠行（`panels.messages.systemPrompt.*` / `contextRow.*`），文案不再硬编码；`user-message-bubble` 不在本次范围（用户气泡无过程证据）。**Turn 用量行**：新增 `lib/turn-usage.ts`（provider 原样键 `prompt_tokens` 等 / runtime 前缀键 `usage_*` / camelCase 三路归一化，缺输入或输出任一 → `null`，总数缺失时按输入+输出回填；全零 → `null`），`finalize-turn` 从 `turnState.finalResult ?? payload.result` 读用量并保留旧值兜底，卡片仅在完整用量时渲染 `turnUsage.summary`（`toLocaleString()`），不完整整行隐藏。**i18n**：`zh-CN/workspace/panels-messages.ts` 真源新增 `collapsedSummary` / `systemPrompt` / `contextRow` / `turnUsage` 四组 key，`en-US` 同步。 | `npm run test` **105 文件 / 599 用例全绿**（63.1s）：新增 `lib/turn-usage.test.ts` 6 例（三路键名 / 字符串数字 / 缺项 → null / 全零 → null）、`message-list/collapse-rendering.test.tsx` 9 例（四类折叠结果 + 摘要零值省略 + 追加文本后行数稳定 + 流式不折叠 + 用量完整/不完整）、`chat-view/project.test.ts` 11 例（含 usage 透出与不完整隐藏）；`npx tsc -b --pretty false` 0 错误；`npm run lint:i18n` → `i18n lint OK（scanned=511, violations=0）`；`npm run build` exit 0；`npm run test:e2e` **21 例 / 5 文件全绿**（59.3s）——G2 新增断言覆盖「done 事件 `result.usage`（1234/567/1801）→ finalize 写入 → 投影透出 → DOM 显示 `1,801`」全链路，折叠接线未破坏既有 G1/G2/G5/G6/G8a/G8b 与 trajectory 5 例；`npx eslint .` **0 error / 4 warning**（顺带清掉基线遗留的 2 个 error：`trajectory-reducer.sequence.test.ts` 的 `prefer-const` 与 `use-reasoning-effort.ts` 的 `set-state-in-effect` 一次性收敛豁免，见 `c49b6980`）。 |

| P1-2 Markdown 增量渲染与视口懒高亮（三个子片：1/3 视口懒高亮 + 2/3 流式期引用/图片门控 + 3/3 前缀冻结 / 绝对 offset key / fence 双前沿） | 已完成（2026-09-11，子片 `29b336fd` / `afb09986` / `a303161c` / `c12e0cf4`） | **新增 `components/ui/use-viewport-activation.ts`**：模块级单例 `IntersectionObserver`（`sharedObserver` + `sharedObserverCtor` + `pendingActivations: Map<Element, () => void>`），`supportsViewportActivation()` 能力探测 + `useViewportActivation<T extends Element>(enabled)` 返回 `{ activated, targetRef }`；元素**首次相交即激活并永久离开观察集**（`releaseActivation` 以 `pendingActivations.delete(target)` 做幂等守卫，避免重复 `unobserve`），构造函数身份变化（测试桩 / HMR）时自动重建单例；observer 为**文档级共享**，每个代码块不各自持有。**`code-highlighting.ts`**：新增 `supportsHighlighting(language)` 前置门（别名表优先、`Prism.languages` 兜底、`text` 明确排除）与 `plainCodeLines(code, language)`（与 `highlightCode` 共用私有 `buildPlainLines` 的同一分行实现，两态行结构一致）。**`code-block.tsx`**：`highlightedLines` state → `prismReady` state + `useMemo`；未激活或 Prism 未就绪渲染 `plainCodeLines`，激活且就绪后切 `highlightCode`；根 surface 挂 `ref={targetRef}`；`supportsHighlighting === false`（text / 未知语言）完全跳过观察直接纯文本。**回退**：`IntersectionObserver` 缺失（SSR / 静态渲染 / jsdom）时立即激活，`renderToStaticMarkup` 输出不变。<br>**子片 2/3「流式期引用/图片门控」（`afb09986`，3 文件 +180/−24）**：`markdown-components.tsx` 的 `a`/`img` handler 增加 `streaming` 形参——流式期只渲染占位（无 `href` 的 `<a data-streaming-link="true">`；`role="img"` 的 `<span data-streaming-image="true">`，不发起请求），settled（`streaming=false`）后由同一 handler 重解析补齐，即「不烘焙 → 自愈」；链接判定改为白名单（`afb09986` 首版放行无 scheme 的相对引用，`a303161c` 按方案 §2②「禁用相对链接」收紧为仅绝对 `http(s)` / `mailto` / `tel` 与页内锚点 `#…` 可导航，相对路径（`/…`、`./`、裸相对路径）与协议相对 `//host`、`javascript:` / `data:` / `file:` 一律拦截；react-markdown 自带 `urlTransform` 仍是前置防线），图片仅放行绝对 http(s)，相对路径在 settled 后保持 `<span data-image-blocked="true">` 占位；`createInlineMarkdownComponents(streaming)` 与 `createMarkdownComponents(streaming)` 共用同一实现，删除仅供 settled 语义的 `inlineMarkdownComponents` 兼容导出；`streaming-blocks.tsx` 尾块 `InlineMarkdown` 改用 `createInlineMarkdownComponents(true)`（此前误用 settled 语义，是本次门控的最后一处漏点）。<br>**子片 3/3「前缀冻结 / 绝对 offset key / 非追加输入 generation 递增 / 未闭合 fence 双前沿」（`c12e0cf4`，5 文件 +512/−100）**：**`message-markdown-streaming.ts`**：新增 `UNSTABLE_TAIL_BLOCKS = 2`、`MarkdownBlockSpan`（`start`/`end`/`container`）与 `scanMarkdownBlocks()`——按空行切顶层块，并把同一容器（`list` / `quote`）跨空行合并为一块（松散列表、多段引用不被拆开），切割点取最后冻结块的 `end`，`stableContent` / `tailContent` 为逐字一致、无空洞的两段切片；`StreamingCodeFence` 增第二前沿 `stableCode`（以 `\n` 结尾的整行前缀）/ `partialLine`（最后一个 `\n` 之后的在增长行）；未闭合顶层 fence 的切割点取 fence 起点，跨 fence 起点的块截断到 `unclosedFenceIndex`（fence 紧随段落行时不丢文本），`tailBlocks` 为单块。**`message-markdown.tsx`**：冻结块与不稳定区首块合并为同一 keyed 列表 `settledBlocks = [...stableBlocks, ...leadingTailBlocks]`，key = `${generation}:${block.start}`（块绝对起始 offset），块滑入冻结前缀时 fiber 复用、memo 命中跳过重解析；只有最后一个尾块走流式解析（code fence / 结构化 / plain）；非追加输入使 `generation` 递增触发换代 remount。**`code-block.tsx`**：新增 `partialLine` prop（复制内容含 partial 行），`stableLines` 去掉 `stableCode` 尾随 `\n` 切出的空行，partial 行 `data-line-kind="partial"`、行号 = `stableLines.length + 1`，折叠判定统一改用 `totalLineCount`。 | `npx tsc -b --pretty false` 0 错误；`npx vitest run` **105 文件 / 602 用例全绿**（61.1s；`code-block.test.tsx` 增至 8 例，新增「懒激活并离开观察集」「两块共享单例」「无 IO 环境即时高亮」；`message-markdown.test.tsx` 的 token 类名断言在 jsdom（无 IO → 即时激活）下不受影响）；`npx eslint .` **0 error / 4 warning**（4 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`、`trajectory/projection.ts:173`，无新增）；`npm run lint:i18n` → `i18n lint OK（scanned=512, violations=0）`（本次未改文案）；`npm run build` exit 0（1.02s）；`npm run test:e2e` **21 例 / 5 文件全绿**（1.0m）——真实浏览器走 IntersectionObserver 路径（首屏代码块相交即激活），G1/G2/G5/G6/G8a/G8b 与 trajectory / design-tokens 断言无回归。<br>**子片 2/3 验证**：`message-markdown.test.tsx` 15 例（原「流式列表尾块 `href`」断言改为 `data-streaming-link`，新增「流式期不烘焙 href/src + settled 自愈」「settled 链接 scheme 白名单」）；`npx tsc -b --pretty false` 0 错误；`npx vitest run` **105 文件 / 604 用例全绿**（74.1s）；`npx eslint .` 0 error / 4 warning（与基线一致，无新增）；`npm run lint:i18n` → `i18n lint OK（scanned=512, violations=0）`；`npm run build` exit 0；`npm run test:e2e` **21 例 / 5 文件全绿**（1.0m）；`a303161c` 仅重跑定向用例（`message-markdown.test.tsx` 15 例）+ `tsc` 0 错误，全量门禁在子片 3 收口时统一重跑；**子片 3/3 验证**：`npx tsc -b --pretty false` 0 错误；`npm test` **105 文件 / 609 用例全绿**（JSON reporter 复核 numTotalTests 609 / numPassedTests 609）；`npm run lint` **0 error / 4 warning**（4 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`、`trajectory/projection.ts:173`）+ `npm run lint:i18n` → `i18n lint OK（scanned=512, violations=0）`；`npm run test:e2e` **21 例 / 5 文件全绿**（1.1m，在冻结代码上重跑）；性能验收（`message-markdown.test.tsx` 新增用例）：24 块 × 4 chunk 与 × 48 chunk 的 `normalizeMarkdown` 调用次数**均为 23**（≤ 块数 24，与 chunk 数无关；末尾 tail 块走 plain 路径不经 `normalizeMarkdown`），且「流式增量 settled 后与一次性全量解析」整树逐字节一致（含链接/图片自愈）。 |
| P1-3 滚动所有权契约 | 已完成（2026-09-11，`066fcd69`） | **新增 `lib/conversation-scroll.ts`**（纯函数 + 跨挂载记忆）：`SCROLL_FOLLOW_THRESHOLD=120`、`READING_LINE_RATIO=1/3`、`distanceFromBottom`/`isAwayFromBottom`/`maxScrollTop`/`clampScrollTop`、`resolveAnchorScrollTop`（锚点行视口偏移差 + clamp 收口）、`pickReadingLineIndex`/`pickConversationAnchor`、`rememberConversationAnchor`/`recallConversationAnchor`/`forgetConversationAnchor`（模块级 Map）。**新增 `hooks/workspace/use-conversation-scroll.ts`**（唯一滚动宿主）：`ResizeObserver` 同时观察语义行容器与滚动宿主，**贴底时持续钉底线**（替代原「一次性 layout effect + 单帧 rAF」，工具卡 / 异步回流 / 字体晚到不再漏跟）；**离开底部后按语义锚点保顶**——锚点 = reading-line 命中行的 `messageId + 视口偏移`，prepend/回流时按 `scrollTop += (currentTop − anchorTop)` 重算并 clamp；**锚点按 `memoryKey`（线程 id）记忆**，remount / 会话切回时先恢复阅读位置（锚点缺失即按贴底处理）；**reading-line（视口顶部 1/3）几何命中 active Turn**，语义行以 `data-message-id`、命中行以 `data-active-turn="true"` 暴露（语义行容器即 `role="log"`，其 `parentElement` 为滚动宿主）。**`message-list.tsx`**：删除 `SCROLL_FOLLOW_THRESHOLD` 常量、`userScrolledAway`/`userScrolledAwayRef`/`wasRespondingRef` 与一次性贴底 effect，改为 hook 接线（`revision=messages`、`suspended=backtrackNavigationActive`、`memoryKey=scrollMemoryKey`）；宿主补 `overflow-anchor: none`（原生 scroll anchoring 与语义锚点是两套所有权，统一收口）。**契约口径**：贴底意图一旦成立即持续有效（不再依赖 `isResponding` 的同帧特判），只有用户主动离开底部才让出；回溯导航期间**让出滚动所有权**（`scrollIntoView` 接管），解除时按几何重推贴底意图，避免用陈旧意图把视图拽走；effect 主体只同步写 DOM/refs，state 对账（`stickToBottom`/`activeMessageId`）走 rAF 回调，规避 `react-hooks/set-state-in-effect` 级联渲染。**类型/接线**：`message-list/types.ts` 增 `scrollMemoryKey`；`workspace-shell/main-section.tsx` 传 `selectedThread.sessionId ?? selectedThread.id`（线程身份即记忆键）。 | `npx tsc -b --pretty false` 0 错误；`npm test` **106 文件 / 617 用例全绿**（63.7s；新增 `conversation-scroll.test.ts` 8 例：阈值边界、clamp、锚点 delta、reading-line 三态与记忆读写）；`npm run lint` **0 error / 4 warning**（4 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`、`trajectory/projection.ts:173`，无新增）+ `npm run lint:i18n` → `i18n lint OK（scanned=514, violations=0）`（本次未改文案）；`npm run build` exit 0；`npm run test:e2e` **24 例 / 5 文件全绿**（1.3m）——新增 P1-3a「流式期间向上滚动：锚点行视口偏移 ±2px 不漂移、`data-active-turn` == reading-line 命中行、上滚期间不跟随」、P1-3b「语义行容器顶部前插 900px 行（等价历史前插，虚拟化中立）：锚点行 ±2px 不动、scrollTop 增大抵消前插、仍保持离开底部」、P1-3c「长内容后叠加工具回合：工具卡 Started→Running 与正文收尾时距底 ≤32px」；G5（跟随 ≤120px / 上滚暂停 ±60px / 回底恢复）与 trajectory / design-tokens 断言无回归。**偏差说明**：方案中的「ResizeObserver 观察输入座位高度」以观察滚动宿主落地（座位/悬浮引起的高度变化即宿主 `clientHeight` 变化），composer 预留高度变量归 P1-4/P1-5 的 composer 面。 |

| P1-4 Composer 输入面升级（子片 1：草稿按会话持久化 + 14 行封顶 + 回焦） | 已完成（子片 1，2026-09-11，`13cfeba2`） | **新增 `lib/composer-draft.ts`**（纯存储层，React/DOM 无关）：key 前缀 `aicli.workspace.composer-draft.v1:`（改结构可整体弃用不误读旧值）；会话键 sessionId 优先、线程 id 兜底、空白视为无键不持久化；`read`/`write`/`clear` 全容错（SSR / 隐私模式 / 配额满 / 值异常 → 降级内存态，绝不抛出；空白草稿即删条目避免残留空键）。**新增 `lib/composer-textarea.ts`**（几何纯函数与 DOM 度量分离，jsdom 无排版也能单测）：`COMPOSER_MAX_VISIBLE_LINES=14`；line-height 兜底链（CSS 值 → font-size×1.5 → 20px）；`resolveComposerTextareaLayout`（未超限贴内容高度且 `overflowY:hidden`，超限封顶 `lineHeight×14 + 上下 padding` 且 `overflowY:auto`）；`applyComposerTextareaLayout` 幂等写样式（1px 内不重写，避免 auto-grow 与 scrollHeight 互相反馈抖动）。**新增 `hooks/workspace/composer/use-composer-draft.ts`**：存储解析走 `useMemo`（渲染期零 ref 读写，满足 react-hooks v7 compiler 规则）；键变化时渲染期同步派生目标会话草稿（切会话无 effect 闪帧），键相同复用组件内状态避免每次渲染重读存储；`setDraft` 跟随当次渲染的会话键并写穿 localStorage，卸载/刷新/切会话三路径存储与界面一致。**接线**：`use-workspace-agent-chat-turn.ts` 由 `useState("")` 换为 `useComposerDraft({ thread: selectedThread })`（提交后 `setDraft("")` 写空即删条目，语义不变）；`message-composer.tsx` 增 `focusKey`（`workspace-shell/main-section.tsx` 传 `selectedThread.sessionId ?? selectedThread.id`）驱动挂载/切会话回焦，`useLayoutEffect` 按 draft/density/isNewThread 应用 14 行封顶、window resize 重度量，提交/停止后回焦。 | `npm run build`（`tsc -b` + vite build）exit 0；`npm test` **109 文件 / 639 用例全绿**（73.7s；新增 22 例：`composer-draft.test.ts` 8 + `composer-textarea.test.ts` 6 + `use-composer-draft.test.tsx` 5 + `message-composer.test.tsx` +3）；`npm run lint` **0 error / 4 warning**（4 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`、`trajectory/projection.ts:173`，无新增）+ `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=517, violations=0）`；`npm run test:e2e` **27 passed**（新增 `e2e/composer-input.spec.ts` 3 例：P1-4a 双会话草稿隔离与刷新恢复、P1-4b 14 行封顶内部滚动、P1-4c 挂载/切会话/提交回焦）。 |

| P1-4 Composer 输入面升级（子片 2：附件输入面） | 已完成（2026-09-11，`3873dc24`） | **新增 `lib/composer-attachments.ts`**（纯模型 150 行，React/DOM 无关）：`classifyComposerAttachment`（`image` / `file`，仅 `image/*` 生成缩略图）、`COMPOSER_ATTACHMENT_MAX_COUNT = 8`、`COMPOSER_ATTACHMENT_MAX_BYTES = 20 MiB`、`composerAttachmentIdentity`（名称+大小+最后修改+类型构成身份键，同批与跨批去重）、`planComposerAttachmentAdd`（一次受理多候选，返回 `accepted` 与逐项 `rejections`，原因分 `duplicate` / `too-large` / `over-limit`）、`formatComposerAttachmentSize`；`COMPOSER_ATTACHMENT_UPLOAD_READY = false` 显式登记「后端 `POST /runtime/uploads` 未就绪（§6.3 P2-1C）」，状态类型只有 `pending`，**无伪造已上传态**。**新增 `hooks/workspace/composer/use-composer-attachments.ts`**（owner hook 282 行）：file input / 粘贴 / 全视口拖放三入口汇入同一 add 管线（拖放按 dragenter/leave 计数判定进出，子元素抖动不熄灭「可放置」提示；drop 始终 `preventDefault` 防浏览器直接打开文件）；控制器按 `threadKey`（sessionId 优先、线程 id 兜底）隔离，切会话即清空；图片预览走注入式 `createObjectUrl`/`revokeObjectUrl`（不直接依赖 `URL` 全局，jsdom 可测），remove / clear / 卸载三路径回收 objectURL。**新增 `components/workspace/composer-attachment-rail.tsx`**（展示层 105 行）：图片缩略图 / 文件卡 + `pending`「待发送」标记 + 单项移除；拒收结果在同一轨道内提示。**接线**：`message-composer.tsx` 接管 input 事件、粘贴、drop 与轨道渲染（有附件时允许空文本提交、无附件保持原禁用语义），`workspace-shell` / `main-section` / `types` / `workspace-page` / `use-workspace-agent-chat-turn` 透传 owner 控制器；i18n `zh-CN`、`en-US` 的 `workspace/base.ts` 新增附件 key（待发送 / 移除 / 拒收原因 / 拖放提示）。 | `npm run build`（`tsc -b` + vite build）exit 0；`npm test` **111 文件 / 662 用例全绿**（66.9s；新增 23 例：`composer-attachments.test.ts` 11 + `use-composer-attachments.test.tsx` 8 + `message-composer.test.tsx` +4）；`npm run lint` **0 error / 4 warning**（4 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`、`trajectory/projection.ts:173`，无新增）+ `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=520, violations=0）`；`npm run test:e2e` **31 passed**（`e2e/composer-input.spec.ts` 7 例，其中新增 4 例：P1-4d 待发送附件阻止空文本提交、移除后恢复提交、P1-4e 粘贴文件入轨、P1-4f 全视口拖放入轨、P1-4g 重复选择仅提示不重复入轨）。 |
| P1-4 Composer 输入面升级（子片 3：触发器 / 命令 / 引用菜单） | 已完成（2026-09-11，`51970e84`；16 文件 +1981/−15） | **新增 `lib/composer-trigger.ts`**：光标处触发检测（`/` 仅行首（允许前导空白）、`@` 词边界），token 内 caret/click 重定位复用同一 key（不重复开菜单），`applyComposerTriggerInsertion` 只替换触发 token 并把光标落在插入文本末尾（不吞后续空格）。**新增 `lib/composer-menu.ts`**：三模式（`all` / `commands` / `references`）× root/group 两级；空分组一律剔除，root 显示组 launcher（`launcher:<groupId>`）并带 `--count`，drill 后只保留该组；`rankMatch` 前缀优先于子串、按 rank 排序并裁剪到每组上限。**新增 `lib/composer-commands.ts`**：`leadingInput` / `popupSelect` / `action` / `execute` 四分类注册表；非法名（`ComposerCommandNameError`）与同名冲突（`ComposerCommandConflictError`）**构造即抛**，不做静默覆盖；`parseComposerCommandLine` 只认行首 `/`；`classifyComposerSubmit` 返回 `empty / prompt / command / incomplete-command / unknown-command`，**命令行永不降级为 prompt**。**新增 `hooks/workspace/composer/use-composer-menu.ts`**（owner hook 357 行）：`+` 按钮 / `/` / `@` 三入口共用同一 snapshot 与派发路径；菜单打开时接管 Esc（关闭并回焦）、↑/↓（循环移动、`aria-activedescendant` 同步）、Tab（无高亮**放行原生焦点遍历**，有高亮时 launcher=drill、leaf=pick）、Enter（launcher 下钻 / leaf 补全）；提交按分类分流——`unknown`/`incomplete` → 可见提示 + 阻塞，`command` → `onCommand`，未接线时给 `no-executor` 提示（不伪装成功、不发送原文）。**新增 `components/workspace/composer-menu.tsx`**（展示层 173 行）：`role=listbox` + 组 `aria-label` + `role=option`/`aria-selected` + 计数 + 空态与按键提示；选项 `onMouseDown preventDefault` 保住输入框焦点。**接线**：`message-composer.tsx` 给 textarea 加 `role=combobox` / `aria-expanded` / `aria-controls` / `aria-activedescendant` 并新增命令行状态位与可关闭提示行，`+` 按钮改为菜单触发器；`main-section.tsx` 用当前线程交付物构造 `@` 文件组（`lib/composer-references.ts`，单组上限 12；会话/子代理分组待数据源就绪）。i18n `zh-CN` / `en-US` 各新增 menu / commands / references 三组 key。 | `npx tsc -b --force` **exit 0**；`npm test` **115 文件 / 701 用例全绿**（58.6s；新增 39 例：`composer-trigger.test.ts` 11 + `composer-menu.test.ts` 10 + `composer-commands.test.ts` 11 + `message-composer-menu.test.tsx` 7——覆盖「行首 `/` 才算命令行」「token 内重定位不重复触发」「launcher 先下钻、leaf 才补全」「Tab 无高亮放行原生焦点」「未知命令不发送原文」「同名命令构造即抛」）；`npm run lint` **0 error / 3 warning**（3 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`；顺带清掉 `trajectory/projection.ts:173` 的失效豁免，见 `5a85d8a2`）+ `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=526, violations=0）`；`npm run build`（`tsc -b` + vite build）exit 0；`npm run test:e2e` **32 passed**（新增 P1-4h：`+` 打开菜单并暴露 combobox 语义、Esc 关闭后回焦、行首 `/` 进入命令行并显示状态行、未知命令 Enter 与 Ctrl/Cmd+Enter 均不发送原文且给可见原因、提示可关闭、普通文本恢复可提交）。 |
| P1-6 工具行状态机与工具专属视图 | 已完成（2026-09-11，`1806c003`；20 文件 +1529/−85） | **新增 `lib/tool-row/` 数据层**：`state.ts`（状态机：kind → 图标/色调、失败态（error/cancelled）判定与输出块替换、`expandable` 折叠谓词（有输入/输出/错误才可展开）、摘要分段、`data-*` 语义属性）、`details.ts`（展示白名单键提取 + 截断 `argsSummary` 的补丁目标回退 `PATCH_TARGET_EMBEDDED_PATTERN`/`PATCH_FILE_KEYS`）、`kind.ts`（工具名 → 卡片类型 `read`/`diff`/`terminal`/`search`/`web`/`image`/`json`/`generic`，兼容 `mcp__*` 前缀）、`link.ts`（`FilePathLink` 契约 + `isExternalUrl`）、`artifact-links.ts`（`createArtifactFilePathLinkResolver`：无宿主目标时不渲染死链接，路径降级纯文本）、`index.ts` barrel。**新增 `components/workspace/tool-row/` 视图层**：`tool-row-summary.tsx`（状态徽标 + 工具专属图标 + 分段摘要 + 文件路径链接）、`tool-row-panels.tsx`（输入/输出/错误分块面板）。**重构 `message-tool-row.tsx`**：状态徽标 + 专属图标 + 折叠输入面板 + sr-only 状态播报（`aria-live`；i18n 双语）；**失败态错误块替换输出块**、路径保留禁用态链接（`data-tool-row-link-disabled="true"`）。**接线**：`message-list/{assistant-message-card,history-context-message-card,segment-rendering}.tsx`、`lib/thread-state/tools.ts`、`data/mock/types.ts`、i18n `zh-CN`/`en-US` `workspace/panels-messages.ts`。 | `npx tsc -b --force` **exit 0**；`npm test` **118 文件 / 727 用例全绿**（74.1s；较 P1-4 子片 3 的 115/701 新增 3 文件 / 26 例：`state.test.ts` 7 + `details.test.ts` 12 + `message-tool-row.test.tsx` 7——覆盖失败态替换、`expandable` 谓词、白名单键提取、截断 argsSummary 回退、无目标不渲染链接、sr-only 播报）；`npm run lint` **0 error / 3 warning**（3 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`）+ `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=534, violations=0）`；`npm run build` exit 0；`npm run test:e2e` **32 passed**（`workspace-chat.spec.ts` G2 与 P1-3c 因 sr-only 播报新增同名文本，查询收窄为 `getByText("web_search", { exact: true })`）。 |

| P1-7 审批与提问交互（统一 pending 生命周期） | 已完成（2026-09-13，`bff7de9c`；22 文件 +2314/−2） | **新增 `lib/pending-interaction/` 数据层**：`types.ts`（kind `approval` / `question` / `plan_review`；status `pending` → `resolving` → `resolved` / `cancelled` / `expired`；身份口径审批=`request_id`、提问=`question_id`、计划评审=`plan-review:<sessionId>`；`selectPendingInteraction` 本会话 pending 优先、回落 resolving，`isActionablePendingInteraction` 决定是否留在呈现位）、`events.ts`（纯函数事件归约：`approval_requested` / `approval_resolved`（`resolution="expired"` 为后端 30min 超时终态）/ `question_asked` / `question_answered` 及点号变体；`session_end` / `session_interrupted` / `session_closed` / `session_deleted` → 未决条目收敛 `cancelled`；`registerPendingInteraction` 同 id 幂等且保留注册序、缺 id 不建入口（不伪造身份）；`markPendingInteractionResolving` 防重复提交、`settlePendingInteraction` 回填、`failPendingInteractionResolve` 回退 `pending` 保留原因、`expirePendingInteractions` 按 `expires_at` 置 `expired`、`pendingInteractionsFromRuntimeEvents` 支持回放重建）、`plan-review.ts`（`pendingPlanReviewFromPlan`：计划模式 active 即条目、退出即消失，透出 `plan_path` / `notes`）、`index.ts` barrel。**新增 `hooks/workspace/use-pending-interactions.ts`**（owner hook）：事件归约全局累积、呈现按会话过滤（切换会话不误杀旧会话条目）；审批与提问各自 `AbortController`（可取消，卸载时全部 abort），提交成功**乐观收敛**（随后 `approval_resolved` / `question_answered` 按 id 幂等覆盖），网络失败回退 `pending` 且错误文案可重试；呈现优先级 = 真实交互 > 计划评审投影（计划激活期内的工具审批不被恒存投影遮蔽）；`converge(reason)` 供停止 / 取消主动收敛。**新增 `components/workspace/pending-interaction-bar.tsx`**（composer 上沿单卡片）：审批（工具名徽标 + 理由 + approve/deny）、提问（建议按钮 + 输入框 + 提交；子表单以 `key={interaction.id}` 挂载，身份切换即重挂载、草稿不串条目）、计划评审（备注 textarea + approve / request_changes / quit，与 artifact 面板共用 `planNotesDraft` / `planActionPending`）；暴露 `data-kind` / `data-status` / `data-interaction-id` 与 `aria-live="polite"`，resolving 期禁用重复提交并显示「提交中」。**接线**：`use-session-runtime-stream.ts` 新增 `onRuntimeEvent` 逐条投递；`workspace-page.tsx` 组合 `usePendingInteractions` + `useRuntimePlanMode`（计划评审与 artifact 面板同源，不建第二套 pending 判定），停止会话先 `converge("session_interrupted")` 再 stop；`workspace-shell/{types,workspace-shell,main-section}` 透传呈现位；`api/runtime/sessions.ts` 新增 `answerSessionQuestion` 与 `SessionRuntimeCommandOptions.signal`（`resolveSessionToolApproval` 支持取消），`api/runtime/index.ts` 补齐两条命令导出。**i18n**：`zh-CN` / `en-US` 新增 `workspace/panels-interactions.ts`（approval / question / planReview / submitting），由各自 `workspace/index.ts` 汇总。 | `npx tsc -b --pretty false` **exit 0**；`npm test` **121 文件 / 758 用例全绿**（63.8s；较 P1-6 的 118/727 新增 3 文件 / 31 例：`events.test.ts` 15 + `use-pending-interactions.test.tsx` 8 + `pending-interaction-bar.test.tsx` 5 + `sessions.test.ts` 3——覆盖缺 `request_id` 不建入口、同 id 重复注册幂等、`expired` 超时终态、`approval_resolved` 缺 id 时按会话保守清除（不误清其他会话）、resolving 失败回退 `pending`、`session_end` 收敛且不动已完成条目、会话过滤、计划 active 投影）；`npm run lint` **0 error / 3 warning**（3 条均为基线既有：`artifact-detail-dialog.tsx:50`、`use-sidebar-effects.ts:53/100`）+ `node scripts/verify-frontend-i18n.ts` → `i18n lint OK（scanned=540, violations=0）`；`npm run build`（`tsc -b` + vite build）exit 0；`npm run test:e2e` **35 passed**（32 既有 + 新增 `e2e/pending-interaction.spec.ts` 3 例：审批卡出现 → Deny → 断言投递 `approve_tool`（`request_id` / `allow:false`）且卡片离开呈现位；提问卡 → 输入答案提交 → 断言投递 `answer_question`；`session_interrupted` 把未决审批收敛、卡片不悬挂）。 |

| P0-2 回退修复（缺口 A2） | 已完成（2026-09-13；工作树，未单独 commit） | **三类超长文件拆分，行为中性（公共 API 不变、既有断言零改动）**：① `components/workspace/message-markdown-streaming.ts`(525) → 目录 barrel `message-markdown-streaming/{types 59, blocks 102, fence 93, tails 276, index 19}`（14 个导出逐个对拍，原 `.ts` 删除）；② `components/workspace/trajectory/subagent-session-dialog.test.tsx`(516) → 301 行主文件（12 个 `it` 标题 1:1 保留）+ `subagent-session-dialog.test-helpers.ts`(133) + `subagent-session-target.test.ts`(51) + `subagent-session-dialog-trajectory.test.tsx`(102)；③ `styles/globals.css`(1043) → 入口 17 + `styles/globals/{theme 110, tokens 335, themes 150, base 435}`（按 token/基础/组件层保序拆分；切割脚本内置字节级重组校验后才落盘）。 | `frontend/src` 内 > 500 行文件 **3 → 0**（`Get-ChildItem -Recurse` 实测）；构建产物 `index-B1wjI4k5.css`(129938 B) / `landing-page-D_9RRpVY.css`(1407 B) 与拆分前**同尺寸同 SHA256**；主树复跑：`pnpm lint` 0 error / 3 基线 warning（i18n scanned=555 / violations=0、备份门禁 777 文件 0 残留）、`pnpm test` **128 文件 / 778 用例全绿**（73.3s）、`pnpm test:e2e` **35 passed**（1.5m）、`pnpm build` exit 0 |
| P0-2 二次回退修复（缺口 A2 复检） | 已完成（2026-09-13；工作树，未单独 commit） | **两个「随 P2-1A 接线增量引入」的超长文件拆分，行为中性（公共 API 不变、既有断言零改动）**：① `pages/usage-analytics/quota.tsx`(559 非空行) → `quota.tsx` 429 + `quota-shared.ts` 74（常量 / 负载形状辅助 / 纯格式函数）+ `quota-atoms.tsx` 73（`PolicyBadge` / `StatRow` / `QuotaBar`；组件与纯函数分文件以过 `react-refresh/only-export-components`）；② `pages/workspace-page.tsx`(512) → 437 + `hooks/workspace/use-workspace-session-actions.ts` 133（重命名 / 归档 / 归档恢复 / Fork / 删除 / 目录内新建六个会话行动作收口为 hook，页面只保留组合与转发，页面内 `navigate` / `t` 随之下沉，`useNavigate` 导入收窄为 `useParams`）；③ **新增 `frontend/scripts/verify-max-lines.mjs` 门禁**：`frontend/src` 下 `.ts/.tsx` 非空行 > 500 即失败、i18n 词典（`src/i18n/resources/**`）整树豁免，并入 `npm run lint` 并新增 `npm run verify:lines`（501 行探针负路径验证 exit 1 后清理）。 | `npm run verify:lines` **exit 0**（扫描 831 个 `.ts/.tsx`、61 个词典豁免、0 个 > 500，最大 `components/workspace/workspace-sidebar.tsx`=483）；`npx tsc -b --force` exit 0；`npx eslint`（5 个改动文件）exit 0；`npm run lint` **0 error / 3 基线 warning**（i18n scanned=605 / violations=0、备份门禁 874 文件 0 残留）；`npm test` **156 文件 / 1026 用例全绿**（94.7s）；`npm run build` exit 0；`npm run test:e2e` **57 passed**（2.2m，含 `usage-quota.spec.ts`）。 |
| P2-1A 后台任务（Jobs）面板（缺口 C2 首项） | 已完成（2026-09-13 合入主树） | **后端零改动，纯前端接线**：① `src/types/runtime/jobs.ts`（+ barrel 导出）与 `api/runtime/jobs.ts`——`/api/runtime/background/jobs` 五端点（列表/详情/取消/output 分页/events）归一化，`background.Job` 无 json tag 故同时接受 PascalCase 与 snake_case；② `hooks/workspace/use-background-jobs.ts`——`enabled` 打开拉取、`job_*` runtime 事件触发的 800ms 合并刷新、live（pending/running）5s 轮询、取消后立即刷新且失败可重试；③ `hooks/workspace/use-elapsed-tick.ts`——仅 live 存在时以 1s 对齐计时（effect 内不同步 setState）；④ `components/workspace/jobs-panel.tsx` + `jobs-panel-shared.ts`——`role="dialog"` 弹层（`data-testid="jobs-panel"`）：live/settled 分区计数、命令/耗时/退出码/状态徽标、逐行输出按 `offset/limit=8192` 展开与「加载更多」、取消入口、Esc 与遮罩关闭、`useDialogLifecycle` + `useFocusRestore` 焦点还原；⑤ `workspace-shell-topbar.tsx` 会话头入口（仅非新会话 + 已附着会话渲染）与 `main-section.tsx` 挂载；⑥ 双语 `panels.jobs.*` + `workspace.base.topbar.jobs`；⑦ e2e `mock-server.mjs` 新增 jobs 五端点与 `POST /api/_test/jobs` 注入、`/api/_test/reset` 清空。 | `pnpm exec vitest run src/components/workspace/jobs-panel.test.tsx` **8 passed**（分区 / 空态 / 失败 / 输出分页 / 取消 / 关闭与 Esc / 关闭态不渲染）；`pnpm exec playwright test e2e/jobs-panel.spec.ts` **2 passed**；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=567 / violations=0、备份门禁 802 文件 0 残留）、`pnpm test` **136 文件 / 837 用例全绿**（80.1s）、`pnpm build` exit 0、`pnpm test:e2e` **40 passed**（1.6m） |
| P2-1A 运行时状态快照（缺口 C2「审批闭环」行与 P1-7 遗留项） | 已完成（2026-09-13 合入主树） | **关闭 P1-7 遗留的「重载 / 重连后未决审批与提问丢失」，后端零改动、纯前端接线**：① `src/types/runtime/session-runtime.ts`（`RuntimeSessionState` / `RuntimeSessionSnapshot`，+ barrel 导出）；② `api/runtime/session-runtime.ts`——`getSessionRuntimeState`（`GET /api/runtime/sessions/{id}/runtime`）：404 按空态、结构不满足抛错（**不伪造空态**）、snake_case / camelCase 双键兼容、id 编码；③ `lib/pending-interaction/snapshot.ts`——`pendingInteractionsFromRuntimeState` / `hydratePendingInteractions`：幂等重建、不回退 `resolving` 与终态、快照缺席结算收敛 `snapshot_absent`、`expires_at` 超时兜底；④ `hooks/workspace/use-session-runtime-state.ts`——按会话键存、切换 / 卸载 abort 在途请求、effect 同步段零 setState、暴露 `refresh`；⑤ 接线：`use-pending-interactions.ts` 水合 + `workspace-page.tsx` 组合两 hook；⑥ e2e：`mock-server.mjs` 新增「由事件存储推导未决项」的 `GET /api/runtime/sessions/{id}/runtime`（会话不存在 404），`pending-interaction.spec.ts` 新增 P1-7d「重载后由快照恢复（事件流为空）」。 | `pnpm exec playwright test pending-interaction` **4 passed**（P1-7a/b/c/d）；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=571 / violations=0、备份门禁 809 文件 0 残留）、`pnpm test` **139 文件 / 862 用例全绿**（78.1s；本次新增 3 个单测文件 / 25 用例：`api/runtime/session-runtime.test.ts` 9、`lib/pending-interaction/snapshot.test.ts` 10、`hooks/workspace/use-session-runtime-state.test.tsx` 6）、`pnpm build` exit 0、`pnpm test:e2e` **41 passed**（1.6m）；反向对照：临时摘除 mock 端点后 P1-7d 失败，证明卡片确由快照水合而非事件流（端点已还原）。 |
| P2-1A 用量 / 配额面板（缺口 C2「usage / 配额」行） | 已完成（2026-09-13 合入主树） | **后端零改动，纯前端接线；严格 token 口径，不推算货币成本**：① `src/types/runtime/usage.ts` + `api/runtime/usage.ts`——`/api/runtime/usage/stats|ledger|policy` 三端点归一化（键名固定 snake_case；缺 `usage` / `policy` / `records` 判结构失败并抛错，**不伪造正常空态**；`scope` 缺席即全局视图、`quota` 缺席为显式降级；403 / 503 保留 `status` 供上层分类）；② `hooks/use-usage-quota.ts`（+ 测试）——三段请求独立发起与独立错误（任一段失败不遮蔽其余段）、按作用域重取、筛选后重取账本、已知作用域在切到具体作用域（响应不再带 `scopes`）后仍保留供回选；③ `pages/usage-analytics/quota.tsx`（`UsageQuotaPanel`，+ 测试）——策略摘要、全局 / 作用域余量与生效来源、账本筛选（entrypoint / skill / success / limit ≤ 200）与 `N matched (limit M)` 计数、403 提示补 admin token、503 账本未配置提示、未配置上限的作用域显示「无配额快照」而非伪造余量；④ `pages/usage-analytics/overview.tsx` 挂载 + 双语 `usageAnalytics.quota.*`；⑤ e2e：`mock-server.mjs` 新增 usage 三端点夹具（全局带 `scopes`、指定作用域带 `scope + quota` 且不再带 `scopes`、`tenant-a` 未配置上限返回 `quota: null`、账本支持 entrypoint / skill / success / limit 过滤）、`usage-quota.spec.ts` 2 用例。 | `pnpm exec playwright test e2e/usage-quota.spec.ts` **2 passed**（全局聚合 → 账本筛选 → 作用域余量；403 时提示补 admin token 且不显示伪造数据）；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=575 / violations=0、备份门禁 817 文件 0 残留）、`pnpm test` **142 文件 / 891 用例全绿**（84.7s；本次新增 3 个单测文件 / 29 用例：`api/runtime/usage.test.ts`、`hooks/use-usage-quota.test.tsx`、`pages/usage-analytics/quota.test.tsx`）、`pnpm build` exit 0、`pnpm test:e2e` **43 passed**（1.7m） |
| P2-1A 会话元数据搜索（缺口 C2「sessions/search」行） | 已完成（2026-09-13 合入主树） | **后端零改动，纯前端接线**。交付：① `types/runtime/sessions.ts` 增补 `RuntimeSessionRecord` / `RuntimeSessionSearchFilters`（前端入参 camelCase）/ `RuntimeSessionSearchEcho`（后端 filters 回显 camelCase，与请求体 snake_case 不同）/ `RuntimeSessionSearchResponse`；② `api/runtime/session-search.ts`——`searchRuntimeSessions`：请求体 snake_case 且空 user/tags/state 一律省略（不让空串当筛选值）、`limit` 默认 50、offset 收口非负；响应 `sessions` 非数组即抛错（**不把结构异常伪装成空结果**），单条缺 `id` 只丢该条、不让整页失败；`isSessionSearchUnavailable` 把 404/405/501/503（含存储层不支持 ListAll 的 `STORE_UNAVAILABLE`）归类为「服务端检索不可用」；③ `hooks/workspace/use-session-search.ts`——idle → loading → ready/error 状态机：请求序号丢弃过期响应、新检索与卸载都 abort 在途请求（调用方 signal 与超时 signal 合并，超时保护不丢）、主动取消不落错误态；④ `components/workspace/session-search-dialog.tsx` + `session-search-shared.ts`——`role="dialog"` 弹层（`data-testid="session-search-dialog"`）：用户 / 状态 / 标签（逗号或顿号分隔、AND 语义）三组筛选、结果行（标题 / 会话 id / 标签 / 状态徽标 / 相对时间，无有效时间戳显示「时间未知」而非 Invalid Date）、`N session(s) matched` 计数与触顶 limit 提示、空态与不可用降级各有独立呈现（不可用时给状态码并说明「本地标题搜索仍可用」，不渲染伪结果）、`useDialogLifecycle` + `useFocusRestore` 保证 Esc / 遮罩关闭与焦点回位；⑤ 入口接线：`workspace-sidebar/sidebar-header.tsx` 触发按钮 + `workspace-sidebar.tsx` 打开即重挂载（不复用过期检索）、选中结果关闭弹层并切到该会话；⑥ 双语 `panels.sessionSearch.*`。验证：新增 2 个单测文件 / 17 例（`api/runtime/session-search.test.ts` 12、`components/workspace/session-search-dialog.test.tsx` 5）；`e2e/mock-server.mjs` 新增 `POST /api/runtime/sessions/search`（tags AND、state 全等、无 user_id 即不限用户，filters camelCase 回显）、seed 支持 `state/user_id/tags/metadata`、`/sessions/users` 改由 seed 派生；新增 `e2e/session-search.spec.ts` 3 例（服务端 AND + 状态过滤与请求体口径、按用户过滤后选中结果跳转、503 时如实降级且不出现结果行与空态）。全量门禁：`pnpm lint` **0 error / 3 warning**（i18n scanned=579 / violations=0、备份门禁 826 文件 0 残留）、`pnpm test` **144 文件 / 908 用例全绿**（98.4s）、`pnpm build` exit 0、`pnpm test:e2e` **46 passed**（1.8m） |
| P2-1A 运行时文件读取与预览（缺口 C2「fs/read-file」行） | 已完成（2026-09-13 合入主树） | **后端零改动、纯前端接线；内容唯一来源是运行时进程返回的字节，空 / 二进制 / 超限 / 读失败一律如实呈现，不做本地兜底缓存或占位文本**。交付：① `src/lib/file-preview/decode.ts`——base64 解码、含 NUL 字节或 UTF-8 解码失败 → binary、空内容 → empty、行数统计与 `formatByteSize`；② `src/types/runtime/files.ts`（`RuntimeFileReadResult` + barrel 导出）与 `src/api/runtime/files.ts`——`FILE_READ_PATH = "/api/runtime/fs/read-file"`、`FILE_PREVIEW_MAX_BYTES = 1_000_000`、`normalizeFileReadPayload`（缺 `file` / `path` / `data_base64` 即抛错；`byte_count` 缺失时按解码字节数收口）、`readRuntimeFile`、`isFileReadUnavailable`（404/405/501/503）；③ `src/hooks/workspace/use-file-preview.ts`——closed → loading → ready/error 状态机：请求序号丢弃过期响应、open / retry / close 均 abort 在途请求、AbortError 不落错误态、超限时 `tooLarge=true` 且 `body=null`；④ `src/hooks/workspace/use-focus-restore.ts`——打开记录 `activeElement`、关闭还焦；⑤ `src/components/workspace/file-preview-dialog.tsx`——portal + `role="dialog"` + `aria-modal` + `tabIndex={-1}` + 打开即聚焦（工具行触发按钮会吞掉 Escape 冒泡，靠面板自身键盘处理保证 Esc 可关），`data-testid` 家族 `file-preview-dialog\|resolved-path\|bytes\|lines\|loading\|error\|too-large\|empty\|binary\|text`，错误分「端点不可用（带状态码）」与「真实失败（展示后端 error 文本）」两类；⑥ 工具行接线：`components/workspace/tool-row/*` 的文件链接（`data-tool-row-file-link="true"`）→ 打开预览；⑦ 双语 `panels.filePreview.*`；⑧ `e2e/mock-server.mjs` 新增 `POST /api/runtime/fs/read-file`（未登记路径按后端口径返回 **500 + `no such file`**，不伪装 404）+ `read-file` 会话脚本，`e2e/support.ts` 新增 `seedRuntimeFiles`。 | `pnpm exec playwright test e2e/file-preview.spec.ts` **3 passed**；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=584 / violations=0、备份门禁 837 文件 0 残留）、`pnpm test` **147 文件 / 934 用例全绿**（82.05s；本次新增 3 个单测文件 / 26 用例：`lib/file-preview/decode.test.ts` 8、`api/runtime/files.test.ts` 9、`components/workspace/file-preview-dialog.test.tsx` 9）、`pnpm build` exit 0、`pnpm test:e2e` **49 passed**（1.9m；较上次 46 例新增 `e2e/file-preview.spec.ts` 3 例：工具行链接 → 预览内容 / 字节数 / 行数 + Escape 关闭、空文件与含 NUL 二进制如实呈现且不渲染文本、读失败显示后端 `no such file` 且无兜底内容可重试） |
| P2-1A 技能市场与热重载（缺口 C2「skills 市场 / 热重载」行） | 已完成（2026-09-13 合入主树） | **后端零改动、纯前端接线**：① `src/types/runtime/skills.ts`（+ barrel）——`RuntimeSkill` / `RuntimeSkillCatalog` / `RuntimeSkillSearchResult` / `RuntimeSkillStats` / `RuntimeHotReloadStats` / `RuntimeSkillMutationPolicy`；② `src/api/runtime/skills.ts`——八端点客户端与归一化：`skills` / `stats` 非数组即抛错（**不把结构异常伪装成空市场**）、单条技能缺 `name` 只丢该条、`count` / `total_skills` 保留后端上报值、`mutation_policy` / `embedding` 缺席置 `null`（UI 显示未知，不按 false 渲染）、检索 `limit` 默认 20 / 上限 200、`isSkillsUnavailable`（404/405/501/503）与 `isSkillsForbidden`（403）分类不混用、写操作 `X-Skills-Admin-Token`（空串不发头）；③ `src/hooks/use-skills-market.ts`——目录 / 统计 / 热重载三段独立请求与独立错误、检索「一份在途」+ 请求序号丢弃过期响应、主动 abort 不落错误态、写操作成功即以响应 stats 覆盖本地快照（**不以本地状态臆测 watching**）；④ `src/pages/skills-page.tsx` + `pages/skills/{catalog,detail,stats,hot-reload,shared}.tsx`——目录 / 检索（`resolved_mode` 与 `used_embedding` 原样展示：semantic 被降级为 lexical 也如实标注）/ 详情（选中后重新拉取，失败如实报错且不回退本地缓存）/ 统计（policy 标志三态 on/off/unknown）/ 热重载（503 不渲染伪 watching、403 提示补管理令牌、policy 禁用时按钮不可点）；⑤ 入口接线：`App.tsx` 路由 `/runtime/skills` + runtime-config 页与侧栏 runtime 区块入口；⑥ 双语 `skills.*`（zh-CN / en-US 逐键对齐）；⑦ `e2e/mock-server.mjs` skills 全族夹具（目录刻意含缺 `name` 的坏条目且 `count` 上报 3）+ `e2e/skills-market.spec.ts` 4 例。 | `pnpm lint` **0 error / 3 基线 warning**（i18n scanned=593 / violations=0、备份门禁 850 文件 0 残留）；`pnpm test` **148 文件 / 948 用例全绿**（101.94s；本次新增 1 个单测文件 / 14 用例：`api/runtime/skills.test.ts` 14）；`pnpm build` exit 0；`pnpm test:e2e` **53 passed**（2.2m，含新增 `e2e/skills-market.spec.ts` 4 例） |
| P2-1A 侧栏会话统计摘要（缺口 C2「`/sessions/stats`」行） | 已完成（2026-09-13 合入主树） | **后端零改动、纯前端接线；只呈现后端上报计数（不推算、不补零）**：① `src/types/runtime/sessions.ts` 增补 `RuntimeSessionStats` / `RuntimeSessionStatsResponse`；② `src/api/runtime/session-stats.ts`——`fetchRuntimeSessionStats`（`user_id` 空串不发查询参数）、`normalizeSessionStats`（`stats` 非对象即抛错，不伪装「0 个会话」；计数只收有限非负数取整；`tags` 只保留「字符串键 → 有限数」）、`isSessionStatsUnavailable`（404/405/501/503 判不可用，与真实失败分列）；③ `src/hooks/workspace/use-session-stats.ts`——idle → loading → ready/error + `refresh()`；AbortController + 请求序号（userId 变化 / 重跑 / 卸载中止在途、过期响应丢弃）；主动 abort 不落错误态；失败保留上次成功数据；④ `src/components/workspace/workspace-sidebar/session-stats-summary.tsx` + `session-stats-summary-shared.ts`——total 恒显、其余计数非零才显 chip（0 计数不渲染）；加载中不渲染骨架数值；不可用 / 失败各有独立呈现且可重试，**不回落成 0 计数**；⑤ 接线：`workspace-sidebar.tsx` 与侧栏用户筛选用同一 `userId`（统计与列表同口径）、`sessions-section.tsx` 渲染并透传 `refresh`；⑥ 双语 `workspace.base.sessionStats.*`。 | `pnpm exec vitest run`（本批 4 文件）**29 passed**；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=602 / violations=0、备份门禁 870 文件 0 残留）；`pnpm test` **156 文件 / 1026 用例全绿**（107.02s；本次新增 4 文件 / 29 例）；`pnpm build` exit 0；`pnpm test:e2e` **57 passed**（2.2m，含新增 `e2e/session-stats.spec.ts` 4 例） |
| P2-1A 子代理控制面（缺口 C2「subagent 控制面」行） | 已完成（2026-09-13 合入主树） | **后端零改动、纯前端接线；动作面只做「看 + 停 + 恢复」**（spawn / wait / events / input 仍由 agent 侧工具调用完成，UI 不代发）：① `src/types/runtime/agents.ts`——`RuntimeAgentStatus`（`active` / `stale` / `closed` / `unknown`）、`RuntimeAgentRecord`（身份路径 / 深度 / 类型 / workflow / team / 生效 provider / model / 时间戳 / `route_warnings`）、`RuntimeAgentCatalog`（`count` 保留后端上报值、`source` 仅作诊断）、`RuntimeAgentMutation`；② `src/api/runtime/agents.ts`——`listRuntimeAgents`（`root_session_id` / `session_id` / `path_prefix` 任意组合 + `include_closed`；`limit` 前端收口默认 200 / 上限 500）、`closeRuntimeAgent` / `resumeRuntimeAgent`（缺 `agent` 即抛错）、`normalizeRuntimeAgentStatus`（未知值一律 `unknown`）、`isAgentControlUnavailable`（404/405/501/503；500 按真实失败呈现）；`agents` 非数组抛错、单条缺 `agent_id` 只丢该条；③ `src/hooks/use-session-agents.ts`——两段式加载（`session_id` 定位本会话身份行 → `root_session_id` 拉整棵树）+ 纯派生 `pickCurrentAgent` / `buildAgentLineage` / `listAgentDescendants` / `deriveSessionAgentTree`；AbortController + 序号（切换 / 卸载 abort、过期响应丢弃、主动 abort 不落错误态）；**身份行缺失 ≠ 错误**（`hasIdentity=false` 如实呈现「未登记身份」）；`truncated` 用后端 `count` 与返回行数比较；close / resume 成功只以响应身份行覆盖本地该行，失败保留错误交 UI 分类（`pendingAgentId` / `actionErrorAgentId`）；④ `src/components/workspace/session-agents-panel.tsx` + `session-agents-panel-shared.ts`——portal 弹层（`role="dialog"`）：lineage 链（当前节点 `data-current`）+ 后代目录（运行 / 已结算分区、昵称→类型→id 回退命名、模型 / provider / workflow / team 元数据、触顶截断提示）；**动作面只对 `active` / `stale` 提供停止、只对 `closed` 提供恢复，`unknown` 不提供任何动作**；⑤ 会话头接线：`workspace-shell-topbar.tsx` lineage 面包屑（仅非新会话且链长 > 1 时渲染）+ 控制面入口（含后代计数）、`main-section.tsx` 装载 hook 与弹层；⑥ 双语 `panels.agents.*` + `workspace.base.topbar.agents` / `agentsBreadcrumb`。 | `pnpm exec vitest run src/components/workspace/session-agents-panel.test.tsx src/components/workspace/session-agents-panel-shared.test.ts` **20 passed**；全量门禁：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=598 / violations=0、备份门禁 861 文件 0 残留，含清理编辑期遗留的 4 个 `.backups/` 目录）；`pnpm test` **152 文件 / 997 用例全绿**（103.40s；本次新增 4 个单测文件 / 49 用例：`api/runtime/agents.test.ts` 17、`hooks/use-session-agents.test.tsx` 12、`session-agents-panel-shared.test.ts` 9、`session-agents-panel.test.tsx` 11）；`pnpm build` exit 0；`pnpm test:e2e` **53 passed**（2.2m，本批未新增 e2e 用例） |
| P2-9 子片 1：后台任务（Jobs）常驻状态条 | 已完成（2026-09-13；实现已入库，`frontend/` 工作树无未提交改动；本台账行尚未单独 commit） | **数据源单一事实源重构 + 顶栏常驻状态条；后端零改动**：① `hooks/workspace/use-background-jobs.ts` 新增 `liveCount` 派生（与弹层 live 分区共用 `isLiveJobStatus` 判据 → 计数结构性一致）并导出 `BackgroundJobsController`；② `components/workspace/jobs-panel.tsx` 由「自带 hook + `sessionId` / `lastRuntimeEventType` / `runtimeEventCount` 三 props」改为纯渲染 + `controller` 注入（取消动作仍转发给控制器）；③ `workspace-shell/main-section.tsx` 成为 shell owner 单例（`useBackgroundJobs` 在 shell 装载一次，向顶栏与弹层分发同一份数据，不做第二次拉取）；④ `workspace-shell-topbar.tsx` 新增 `liveJobsCount` prop 与 `data-testid="topbar-jobs-running"` 常驻状态条（仅 live > 0 渲染、`hidden sm:flex`、点击打开弹层）；⑤ 双语 `topbar.jobsRunning`（zh `{{count}} 个后台任务运行中` / en `{{count}} background job(s) running`）。**通知位约束（§10 风险表「通知位重复」）**：状态条只做计数与入口，完成 / 失败的一次性提示仍由 toast / 桌面通知负责，不新增通知源。 | `npx tsc -b --force` exit 0；`npx eslint`（9 个改动文件）exit 0；定向单测 **16 passed**（`jobs-panel.test.tsx` 8：controller 注入下的分区 / 空态 / 错误 / 输出分页 / Esc 关闭 / 取消转发；`jobs-status-bar.test.tsx` 2：同一 hook 驱动顶栏 + 弹层 → chip 文本 `2 个后台任务运行中`、面板 `进行中（2）` / `已结束（1）`、点击打开、无 live 不渲染；`use-background-jobs.test.tsx` 6：未启用不拉取、`{sessionId, limit:50}` 口径与 `liveCount` 派生、`job_*` 事件 800ms 合并刷新、live 时 5s 兜底轮询且全终态停止、取消失败暴露 error 不刷新）；工作区回归 `workspace-shell` + topbar 3 文件 / 9 用例；全量门禁：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=605 / violations=0、备份门禁 876 文件 0 残留（含清理本轮编辑产生的 3 个 `.backups/` 目录）、行数门禁 0 个 > 500，最大 `workspace-sidebar.tsx`=483）、`npm test` **158 文件 / 1034 用例全绿**（121.6s）、`npm run build` exit 0、`npm run test:e2e` **57 passed**（2.0m；`e2e/jobs-panel.spec.ts` 首个用例改为先断言并点击状态条，再校验面板分区与 `session_id` 口径）。**过程纠错**：e2e 只跑 `dist/` 产物（`playwright.config.ts` 的 `requireDist()` + `vite preview`），首轮未 `build` 即跑 e2e 命中的是旧产物（新状态条断言失败）；已按「先 build 再 e2e」复跑，并把该口径写入 §9.4 复测注记与缺口清单复现命令。 |
| P2-9 子片 2：子代理树（可折叠到末级 + 只读原因 + 记录跨度） | 已完成（2026-09-13；实现已入库，`frontend/` 工作树含本批改动；本台账行与缺口清单 §3 批次 10 同批回填） | **后端零改动、纯前端；层级 / 折叠只影响渲染不改数据，只展示数据能证实的结论**：① `components/workspace/session-agents-panel-shared.ts` 新增纯派生——`buildAgentForest`（父边解析顺序：`parentAgentId`（集合内）→ `agent_path` 最长前缀祖先回退（后端缺 `parent_agent_id` 时仍能还原层级）；两者都解析不到的孤儿行**保留为顶层而非丢弃**；环数据断开闭环边、不递归爆栈）、`flattenAgentTree`（折叠感知展开为行序列，行携带 `depth` / `childCount` / `hiddenDescendantCount`）、`agentDurationSpan`（终点 = `closedAt ?? updatedAt`；端点缺失 / 无法解析 / 终点早于起点一律 `null`）、`formatAgentDuration` + `formatAgentDurationExact`（判别联合逐档静态展开 i18n 键与插值：秒 → 分 → 时 → 天 → 天+时 → 月 → 月+天 → 年 → 年+月；精确档不足一天与紧凑档一致、≥ 一天补零到秒）、`agentReadOnlyReason`（只回答能证实的「已关闭记录」/「父代理不在线」，其余返回 `null`，不猜）；② 新增 `components/workspace/session-agents-tree.tsx`（`SessionAgentsTree` + 内部行组件）：`<ul aria-label>` + 每行 `role="treeitem"` / `aria-level` / `data-agent-id` / `data-depth` / `data-status`、分支行 `aria-expanded` + `data-testid="agent-branch-toggle"`（文案带 `{{name}}` 与 `{{count}}` 隐藏后代数，展开态图标旋转）、记录跨度 chip `data-testid="agent-duration"`（title 明说「记录跨度：from → to（创建 → 关闭/最后更新，**非活跃耗时**）」——后端未上报活跃时长，不借用目标实现的耗时说辞）、只读原因行 `data-testid="agent-readonly"`；折叠状态是本组件内的渲染态（`ReadonlySet<string>`），不改上游数据；③ `session-agents-panel.tsx` 行渲染整体下沉到树组件（面板只保留 lineage / lineage 计数 / 触顶提示 / 空态），后代目录由「运行 / 已结算两段平铺」改为**单棵层级树 + 运行 / 已结算计数摘要**（`agents-count-running` / `agents-count-settled`；状态仍在每行徽标与色调上，「运行 / 非运行」语义不丢）；④ 双语 `panels.agents.*` 新增 `treeAria` / `branchExpand` / `branchCollapse` / `durationLabel` / `durationExactTitle` / `duration.*`（9 档）/ `readonly.*`（2 条），zh-CN 与 en-US 逐键对齐。**如实降级**：层级派生失败不臆造父子（孤儿行如实置顶层）；端点缺失不补零、无依据的只读原因不渲染；`unknown` 状态仍不给动作。**遗留（后端字段缺口，已登记）**：`RuntimeAgentRecord` 无 token 字段，子代理 token 口径暂不渲染（禁止用会话级 usage 均摊推算），待后端上报后接入。 | `npx tsc -b --force` exit 0；`npx vitest run src/components/workspace/session-agents-tree.test.tsx` **4 passed**（乱序输入建树到末级 / 折叠只隐藏该节点后代 / 已关闭与父离线两类原因且无依据不渲染 / 跨度 chip 与缺端点不渲染）；`session-agents-panel-shared.test.ts` **23 passed**（本批 +14：跨度端点与倒挂、九档梯度与非法毫秒、精确档补零、建树 / 前缀回退 / 孤儿保留 / 环断边 / 折叠可见行、只读原因四类）；全量门禁：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=606 / violations=0、备份门禁 878 文件 0 残留（含清理本轮编辑产生的 1 个 `.backups/` 目录 / 2 个文件）、行数门禁 835 个 `.ts/.tsx` 中 0 个 > 500，最大 `workspace-sidebar.tsx`=483）；`npm test` **159 文件 / 1052 用例全绿**（98.7s；本批新增 1 个文件 / 18 例）；`npm run build` exit 0；`npm run test:e2e` **57 passed**（2.0m，未新增 e2e 用例——树交互 / 只读原因 / 跨度由组件与纯函数单测覆盖，e2e 仍按「先 build 再跑」口径执行）。 |
| P2-9 子片 3：会话目标（goal）四相指示 MVP | 已完成（2026-09-13；实现已入库，`frontend/` 工作树含本批改动；本台账行与缺口清单 §3 批次 11 同批回填） | **后端零改动、纯前端；只投影后端已上报的目标状态，不引入本地猜测**。**数据面前置复核（决定架构）**：goal 只在 chat SSE `tool_end.payload.tool.content` 完整到达——线程层 `getToolResultSummary` 截断到 240 字符、轨迹层 `toolResultSummaryOf` 不读 `tool.content`，直接解析二者会拿到半截 JSON；故唯一写入口放在 SSE 边界，不做轨迹回放、不做 REST 双写：① 新增 `lib/session-goal/derive.ts`——`parseGoalToolResult`（截断 / 非法 JSON / 无 goal 字段一律 `unknown`）、`parseSessionGoal` + `normalizeSessionGoalPhase`（四相 `active` / `paused` / `budget_limited` / `complete`；未知 status 不映射但 `statusRaw` 保真）、`deriveSessionGoal`（`unknown` 不覆盖既有结论、`absent` 如实清空）、`sessionGoalFromTrajectoryItems`（降级路径：摘要不完整即 `null`）、`formatGoalTokens`（999 / 1.2k / 20k / 123k / 1.5M；非法值空串）；② 新增 `lib/session-goal/store.ts`——按 `session_id` 的进程内只读投影：`recordGoalToolEnd`（非 goal 工具与无法判定的结果不写入、不覆盖）、`useSessionGoal`（`useSyncExternalStore`，投影引用稳定）；③ 捕获接线：`agent-chat-turn/stream-handlers.ts` 的 `onToolEnd` 增调 `recordGoalToolEnd(sessionId, payload)`（deps 新增可选 `sessionId`，新会话未登记时不误写），`use-workspace-agent-chat-turn.ts` 透传 `threadSnapshot.sessionId`；④ 新增 `components/workspace/session-goal-indicator.tsx`（`data-testid="topbar-session-goal"` + `data-phase` + 四相徽标 / 目标文本 / 用量子节点），无捕获数据不渲染、token 两侧都有才显示 `used / budget`（缺字段不补零）、title 含只读派生说明；⑤ 顶栏接线：`workspace-shell-topbar.tsx` 以 `selectedThread.sessionId` 订阅（未触碰并行工作流在改文件）；⑥ 双语 `topbar.goal.*`（phase 五键 + `objective` / `statusRaw` / `usage` / `usageUsed` / `completedBy` / `completionSummary` / `updatedAt` / `derivedNote`），zh-CN 与 en-US 逐键对齐。**如实降级 / 边界**：暂停 / 恢复 / 完成入口不渲染（后端未暴露 REST，见 §6.3 P2-1B，不放置死按钮）；刷新后 store 清空即不显示（不回落猜测值）；`goal_missing` 是合法空操作（清空投影，不当作失败）。 | `npx tsc -b --force` exit 0；定向 `npx vitest run src/lib/session-goal/session-goal.test.ts src/components/workspace/session-goal-indicator.test.tsx` **21 passed**（14 + 7：四相映射 / 未知状态保真 / 截断与非法 JSON 不猜 / `goal_missing` 清空 / token 不补零 / 订阅通知与引用稳定 / 指示条渲染与消失 / 非 goal 工具不渲染）；全量门禁：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=612 / violations=0、备份门禁 887 文件 0 残留、行数门禁 844 个 `.ts/.tsx` 中 0 个 > 500，最大 `workspace-sidebar.tsx`=483）；`npm test` **162 文件 / 1077 用例全绿**（123.8s；本批新增 2 文件 / 21 例，其余为并行工作流在库测试文件）；`npm run build` exit 0；`npm run test:e2e` **57 passed**（2.1m，未新增 e2e 用例——四相 / 降级 / 清空由纯函数与组件测试覆盖，e2e 按「先 build 再跑」口径执行）。 |
| P2-7 子片 2：内置命令清单与执行器（`/export` / `/rename`） | 已完成并提交（2026-09-13，`bdb3ed76`；提交只含本批 15 个文件——与并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底）同处一树，混合文件按「HEAD + 本批 hunk」入索引，未把在飞改动带入；本台账行与缺口清单 §3 批次 12 同批回填） | **后端零改动、纯前端；机制层沿用 P1-4 子片 3，不重复定义输入机制**。① 新增 `lib/composer-builtin-commands.ts`——`COMPOSER_BUILTIN_COMMANDS`（`export`=`action` `/export [--redact]`；`rename`=`execute` `/rename <title>`）+ `parseExportCommandArgs`（`--redact` 白名单；未知 flag / 多余位置参数判别联合前置失败）+ `parseRenameCommandArgs`（trim 后空标题前置失败，不产生空标题）+ `createComposerCommandRegistry`（沿用非法名 / 同名冲突「构造即抛」）；② 新增 `hooks/workspace/composer/use-composer-command-executor.ts`——`run` 同步派发返回 boolean（`true` 已认领 → 结果异步回填；`false` 未认领 → 交回 composer 的 `no-executor` 提示，**命令行绝不静默降级为 prompt**）、逐命令 `try/catch` 错误隔离、导出重入保护、通知模型 `{ tone, messageKey, values }` 只持 i18n key（文案在渲染层本地化）；**复用既有实现（单一事实源）**：`/export` → `lib/trajectory/export-session.ts` 的 `exportSessionTrajectoryJsonl(sessionId, { redact })`（与轨迹页导出同一分页拉取 → JSONL → 下载路径），`/rename` → 侧栏同一 `onRenameSession`（不另建重命名通道）；③ 新增 `components/workspace/composer-command-result-notice.tsx`（成功 `role="status"` / 失败 `role="alert"` 可关闭回执条，`data-composer-command-result` + `data-composer-command-result-dismiss`）与 `components/workspace/composer-status-row.tsx`（状态条抽出以过行数门禁：`message-composer.tsx` 507 → 达标）；④ 接线 `workspace-shell/main-section.tsx`（注入 `commands` + 执行器 + 通知映射）与 `message-composer.tsx`（已认领的命令提交后清空命令行、未认领 / 被阻塞保留草稿）；⑤ 双语 `composer.builtin.export.*`（9 键）/ `composer.builtin.rename.*`（6 键），回执条关闭按钮复用既有 `composer.commands.dismiss`。**如实降级**：非法参数、无会话、导出进行中、导出失败、重命名失败各自如实报错，不伪造成功、不补零。**提交口径**：菜单打开时纯 Enter 归菜单所有（选择 / 下钻 / 无候选阻塞提示），命令行提交走 Ctrl/Cmd+Enter 或发送按钮。**未交付**：`/feedback` 入口、`/model` 弹窗与命令专属 popupSelect 候选。 | `npx tsc -b --force` exit 0；定向单测 `npx vitest run src/lib/composer-builtin-commands.test.ts src/hooks/workspace/composer/use-composer-command-executor.test.tsx` **17 passed**；全量门禁：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=617 / violations=0、备份门禁 895 文件 0 残留、行数门禁 851 个 `.ts/.tsx` 中 0 个 > 500，最大 `workspace-sidebar.tsx`=483）；`npm test` **164 文件 / 1096 用例全绿**（含新增 `message-composer-menu.test.tsx` 2 例：已认领清空命令行 / 未认领保留草稿）；`npm run build` exit 0；`npm run test:e2e` **59 passed**（2.2m，含新增 `e2e/composer-commands.spec.ts` 2 例：`/` 菜单列出内置命令 + `/rename` 缺标题 alert、清空命令行并可按关；`/export --json` 前置失败不下载 + `/export` 真实下载 JSONL 并回报条数与文件名）；**提交态独立核验**（临时 worktree = `8833ceb6` + 本批文件，不改主工作树）：`npx tsc -b --force` exit 0、`npm run lint` 0 error / 3 基线 warning（i18n scanned=614 / violations=0、行数门禁 847 文件 0 超限）、`npm test` **163 文件 / 1092 用例全绿** |
| P2-7 子片 3：`/model` 会话模型切换与目录弹窗 + `/feedback` log-only 入口 | 已完成并提交（2026-09-13，`28546e71`；提交只含本批 23 个文件——18 个独占文件 + 5 个与并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底 / 右栏合并）共享的文件按「HEAD + 本批 hunk」入索引，未把在飞改动带入；本台账行与缺口清单 §3 批次 13 同批回填） | **后端零改动、纯前端；机制层与 `/export`、`/rename` 执行器沿用批次 12，不重复定义**。① 新增 `lib/composer-model-options.ts`——`GET /api/runtime/models` 目录 → provider 分组（保持目录顺序；同名模型跨 provider **不合并**，避免选错座位）+ 按名 / 按座位（provider + model）解析当前项；目录未就绪 / 为空 → 空集 + not-ready，**不伪造模型名、不猜默认值**；② 新增 `components/workspace/composer-model-dialog.tsx`（+ `composer-model-dialog.test.tsx`）——分组列表 + 当前项徽标 + loading / error / empty 三态 + Esc 关闭；选中调用常驻座位同一个 `onModelChange`（单一事实源）；③ 新增 `hooks/workspace/composer/use-composer-command-surface.ts`——命令清单、`/model` 候选与弹窗开合、执行器与回执文案收口为一个命令面，`main-section.tsx` 退化为接线；④ `/model`（缺参 / 目录未就绪前置失败、未知模型名报错并列出目录）与 `/feedback`（log-only：只写本地日志，回执明说本版本未接入上报渠道）；⑤ `workspace-shell.tsx` / `types.ts` / `pages/workspace-page.tsx` 透传 `runtimeModels`（与常驻座位同源）；⑥ 双语 `workspace.base.modelDialog.*` + `composer.builtin.model` / `composer.builtin.feedback`。**如实降级**：目录 loading / 失败 / 空各自如实呈现，不弹假候选、不假装已上报。**未交付**：命令专属 popupSelect 候选（§9.1 收敛表 5-12）。 | `npm run lint` **0 error / 3 基线 warning**（i18n scanned=617 / violations=0、备份门禁 897 文件 0 残留、行数门禁 852 个 `.ts/.tsx` 中 0 个 > 500，最大 `workspace-sidebar.tsx`=483）；`npm test` **165 文件 / 1117 用例全绿**；`npm run build`（`tsc -b && vite build`）exit 0；定向 e2e `npx playwright test e2e/composer-commands.spec.ts` **5 passed**（`/model` 打开目录弹窗并应用所选、无目录时 not-ready 且不伪造、`/feedback` log-only 回执）；**提交态独立核验**（临时 worktree 重置到 `28546e71`、`git status` 干净后重跑）：lint 0 error / 3 基线 warning、`npm test` **165 文件 / 1117 用例全绿**、`npm run build` exit 0、定向 e2e **5 passed** |
| P2-7 子片 4：命令专属 popupSelect 候选面（弹层自持焦点 + 本地检索 + 虚拟高亮） | 已完成并提交（2026-09-13，`82c81149`；提交只含本批 9 个文件，其中 2 个与并行工作流（P2-1B 技能面 / 轨迹 P4 历史兜底 / 右栏合并）共享的双语词典按「HEAD + 本批 hunk」入索引，未把在飞改动带入；本台账行与缺口清单 §3 批次 14 同批回填） | **后端零改动、纯前端；机制层（P1-4 子片 3）、内置执行器与 `/model` 弹窗分别沿用批次 12 / 13，不重复定义**。① `components/workspace/composer-model-dialog.tsx`——弹层**自持焦点**（打开即把焦点交给检索框，打字即检索）、↑/↓ 虚拟高亮 + Enter 应用（`preventDefault` 阻止行按钮默认激活，同一次按键不重复派发）、高亮行 `scrollIntoView({ block: "nearest" })` 滚入视口；外壳只持对话框生命周期，候选面状态随 `open` 挂载（重开即归零），高亮改为派生值（默认跟住当前座位，用户 ↑/↓ 或悬停后接管），以过 `react-hooks/set-state-in-effect` 门禁；② `lib/composer-model-options.ts` 增 `filterComposerModelGroups`——空 query 原样返回（不排序不裁剪）、大小写不敏感**子串**命中模型名或所属 provider 名、全部落空的 provider 分组不留空壳；③ **「检索无匹配」与「目录为空」是两种状态**，各自如实呈现——不补占位模型、不伪造默认选中；④ 目录晚到（先 loading 后到数据）时检索框更晚挂载：以「待聚焦」标志接住焦点，修复打开瞬间焦点落在遮罩上的真实缺陷；⑤ 双语 `workspace.base.modelDialog.noMatch` / `search.{aria,placeholder}`。**P2-7 四个子片至此全部落地。** | 主树复测：`npm run build`（`tsc -b && vite build`）exit 0、定向 vitest **4 文件 / 47 用例全绿**、定向 e2e `npx playwright test e2e/composer-commands.spec.ts` **7 passed**（新增 2 例：候选面本地检索 / 无匹配与空目录区分 / 虚拟高亮 + Enter 应用；菜单内下钻派发并应用）；**提交态独立核验**（临时 worktree 重置到 `82c81149`、`pnpm install --offline` 后重跑）：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=617 / violations=0、备份门禁 896 文件 0 残留、行数门禁 852 个 `.ts/.tsx` 中 0 个 > 500，最大 `workspace-sidebar.tsx`=483）、`npm test` **165 文件 / 1130 用例全绿**、`npm run build` exit 0、定向 e2e **7 passed**（主树同批 lint 仅因并行工作流遗留的 4 个 `.backups/` 被备份门禁阻断，非本批引入） |
| P2-6 子片 1：会话排序双模式与组内拖拽重排 | 已完成并提交（2026-09-13，`10627d34`；提交只含本批 18 个文件——16 个独占文件 + 2 个与并行工作流（右栏合并 / P2-1B 技能面）共享的双语词典按「HEAD + 本批 hunk」入索引，未把在飞改动带入；本台账行与缺口清单 §3 批次 15 同批回填） | **后端零改动、纯前端；分组（按工作目录）沿用批次 3 / P2-1A 既有口径，本批不改分组语义、不做跨组移动（不写假落点、不做假回滚）**。① 新增 `lib/workspace/session-order.ts`——两档排序口径与手动顺序账目的纯函数面（`updated` 恒派生、时间缺失 / 非法沉底 + 同刻按 id 升序稳定 tiebreak；账目 reconcile 忽略已消失 / 重复 id、未入账新会话按最近更新追加末尾；`moveSessionInOrder` 等价落点返回入参同一引用）；② 新增 `lib/workspace/session-order-store.ts`——版本化本地账目（只收字符串 id 数组；存储不可用 / 配额失败不抛错，不影响本会话排序）；③ 新增 `hooks/workspace/use-session-order.ts`——模式走设置域 `workspace.sessionOrder`，账目只在拖拽提交时写入（渲染期零写存储）；④ 新增 `components/workspace/workspace-sidebar/session-order-control.tsx`——`role="group"` + `aria-pressed` 双档控件，两个选项都是真实行为、无占位项；⑤ 新增 `components/workspace/workspace-sidebar/use-session-drag-reorder.ts`——组内拖拽编排（非手动模式整行不可拖、跨组 / 自落点不接管、落点按行高上下半判 `before` / `after`、行内移动不误清落点、等价落点不写账目也不播报、`dragend` 清态）；⑥ 接线 `workspace-sidebar` / `sessions-section` / `session-item`（分组内重排 + 控件 + `role="status"` 播报 + 行级 `draggable` 与落点指示线，缺省形态与不接线时一致）；⑦ 双语 `workspace.base.sidebar.sessionOrder.*` 7 键逐键对齐 + 设置域归一化（未知取值回落默认，不做隐式映射）。**未交付**：分组视图（工作区分组 / 平铺）切换、跨组移动与 Host 写回、组内展开 / 折叠、空白会话获首条消息后自动提升。 | 提交 `10627d34`（18 文件，+1734/−6）。主树复测：`npm run build`（`tsc -b && vite build`）exit 0、定向 vitest **4 文件 / 51 用例全绿**（session-order 21 / session-order-store 16 / use-session-order 7 / use-session-drag-reorder 7）、定向 e2e `npx playwright test e2e/session-order.spec.ts` **3 passed**、全量 `npm run test:e2e` **67 passed**。**提交态独立核验**（临时 worktree `ai-agent-runtime-v14` 检出到 `10627d34`、`git status` 干净、`frontend/node_modules` 以 junction 指回主树后重跑）：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=631 / violations=0、备份门禁 920 文件 0 残留、行数门禁 874 个 `.ts/.tsx` 中 0 个 > 500，最大 `src/lib/trajectory/timeline-window.ts`=494）、`npm test` **173 文件 / 1255 用例全绿**、`npm run build` exit 0、全量 `npm run test:e2e` **67 passed** —— 该 worktree 检出即提交树，即上述门禁即提交态门禁 |
| P2-6 子片 2：会话分组视图与跨组移动写回 | 已完成并提交（2026-09-13，`8520de33`；提交只含本批 27 个文件——23 个独占文件 + 4 个与并行工作流共享的文件（`workspace-shell.tsx` / `workspace-shell/types.ts` / 双语词典 `workspace/base.ts`）按「HEAD + 本批 hunk」入索引，未把在飞改动带入；本台账行与缺口清单 §3 批次 16 同批回填） | **后端零改动、纯前端；分组口径（按会话元数据的工作目录归属）沿用批次 3 / P2-1A 既有能力，本批只加「呈现形状」与「跨组移动」，不改分组判定、不新建第二套顺序账目**。① 新增 `lib/workspace/session-grouping.ts`——`SessionGroupingMode` = `directory \| flat` + `flattenSessionDirectoryGroups`（按「分组顺序 → 组内顺序」拼接平铺序列，两视图共用同一份组内账目，切换不让手动顺序分叉）；② 新增 `hooks/workspace/use-session-grouping.ts`——视图走设置域 `workspace.sessionGrouping`（渲染期只读、切换即写入；兜 `isSessionGroupingMode` 防脏存储透传）；③ 新增 `hooks/workspace/use-session-group-view.ts`——跨组移动编排（先本地乐观覆盖 `sessionId → 目标路径`；Host 快照已反映目标路径 / 会话消失时在渲染期归约回收覆盖，不在 effect 里 setState；失败即撤销覆盖并就地提示）；④ 新增 `components/workspace/workspace-sidebar/session-grouping-control.tsx`——`role="group"` + `aria-pressed` 双档控件（与排序控件同形，两个选项都是真实行为、无占位项）；⑤ 扩展 `use-session-drag-reorder.ts`——跨组落点（目标目录组头）接管：写回 `PATCH /api/runtime/sessions/:id` 的 `metadata.context.workspace_path`（用注册表原始路径；组头 title 仍为规范化路径），失败回滚原组、不写假落点；⑥ 下沉 `session-row-view-model.ts` / `session-user-menu.ts`（各自保持 < 500 行、行为不变）并接线 `workspace-sidebar` / `sessions-section` / `workspace-shell` / `workspace-page`（`onMoveRuntimeSession` 自页面透传；平铺视图不渲染目录组头——跨组移动不可表达），`workspace-sidebar-shared.ts` 把乐观覆盖的套用 / 回收做成纯函数（无覆盖返回入参同一引用）；⑦ 双语 `workspace.base.sidebar.sessionGrouping.*`（6 键）与 `sessionMove.*`（moved / failed）逐键对齐 + 设置域归一化（未知取值回落默认）。**未交付（子片 3+）**：组内展开 / 折叠（Show N more）、空白新会话在获得首条消息后自动提升。 | 提交 `8520de33`（27 文件，+2045/−218）。**提交态独立核验**（临时 worktree `ai-agent-runtime-v16` 检出到 `8520de33`、`git status` 干净、`frontend/node_modules` 以 junction 指回主树后重跑）：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=637 / violations=0、备份门禁 931 文件 0 残留、行数门禁 883 个 `.ts/.tsx` 中 0 个 > 500，最大 `src/lib/trajectory/timeline-window.ts`=494）、`npm test` **176 文件 / 1277 用例全绿**（本批新增 `session-grouping.test.ts` 3 例、`workspace-sidebar-cross-group-move.test.tsx` 2 例、`workspace-sidebar-shared.test.ts` 9 例）、`npm run build`（`tsc -b && vite build`）exit 0（`✓ built in 2.06s`）、全量 `npm run test:e2e` **70 passed**（新增 `e2e/session-grouping.spec.ts` 3 用例：拖到目标组头即乐观归属并写回 Host + 刷新仍在目标组；Host 拒绝写回即回滚原组并就地提示、刷新后不产生假落点；切平铺不渲染组头且不丢会话、切回按目录恢复）—— 该 worktree 检出即提交树，即上述门禁即提交态门禁 |
| P2-6 子片 3：组内展开 / 折叠（Show N more）与空白会话提升（P2-6 收口） | 已完成并提交（2026-09-14，`f2c9cf98`；提交只含本批 11 个文件——9 个独占文件 + 2 个与并行工作流（右栏合并 / P2-7 消息渲染）共享的双语词典按「HEAD + 本批 hunk」入索引（`git show f2c9cf98 -- <词典>` 仅含 `sessionGrouping.collapse` / `showMore` 4 行新增），未把在飞改动带入；本台账行与缺口清单 §3 批次 17 同批回填） | **后端零改动、纯前端；只影响呈现，不改分组判定、不新建第二套顺序账目、渲染期零写存储**：① `lib/workspace/session-grouping.ts`——新增 `SESSION_GROUP_VISIBLE_LIMIT`（5）与 `resolveSessionGroupVisibility`（纯判定：不超上限的组整体可见且不给控件；折叠态只渲染前 `limit` 个；`pinnedId`（当前选中会话）落在折叠区时自动展开——不出现「已选中却看不见」的假态）；② `lib/workspace/session-order.ts`——新增 `SessionBlankCandidate` / `isBlankSession`（只认权威消息计数 `metadata.totalTurns === 0`；字段缺失 / 非数字一律按非空白，宁可不提升也不猜）/ `promoteBlankSessions`（空白会话钉在组顶且彼此保持传入顺序、其余相对顺序不变；`anchoredIds`（该组手动账目里出现过的会话）不参与提升——显式意图优先于呈现层；无空白会话时返回入参同一引用）；③ 新增 `workspace-sidebar/use-session-group-visibility.ts`（展开态是会话期本地状态、按目录路径记忆）+ `workspace-sidebar/session-group-toggle.tsx`（真实按钮：`aria-expanded` +「展开其余 N 个会话」/「收起」）；④ `hooks/workspace/use-session-group-view.ts` 在排序结果之上套一层空白提升（`anchoredIds` 取该组手动账目）；⑤ `sessions-section.tsx` 组内渲染改走 `groupVisibility.visible`、当前选中会话作为 `pinnedId`；⑥ 双语 `workspace.base.sidebar.sessionGrouping.collapse` / `showMore` 逐键对齐。**P2-6 收口**：3-1 / 3-2 / 3-3（批次 15，`10627d34`）、分组视图 + 跨组移动与 Host 写回（批次 16，`8520de33`）、3-4 + 3-18（本批）、3-5 空态三态（批次 3，`c25a72e0`）全部落地，P2-6 项下无未交付子项。 | 提交 `f2c9cf98`（11 文件，+589/−4）。主树复测：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=647 / violations=0；备份门禁被并行工作流在飞残留 `frontend/e2e/.backups/support.ts.20260914-174146.bak`挡住——非本批产物、未清理他人未提交文件）；`npm test` **179 文件 / 1322 用例全绿**（含并行工作流在库文件）；`npm run build`（`tsc -b && vite build`）exit 0（`✓ built in 1.59s`）；定向 e2e `npx playwright test e2e/session-collapse.spec.ts` **2 passed**（10.4s，新增 2 用例：超过上限的组折叠为 5 个 + 控件展开 / 收起；空白会话钉在组顶且高于最近更新的会话）。**提交态独立核验**（临时 worktree `ai-agent-runtime-v17` 检出到 `f2c9cf98`、`git status` 干净、`frontend/node_modules` 以 junction 指回主树后重跑）：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=639 / violations=0、备份门禁 933 文件 0 残留、行数门禁 885 个 `.ts/.tsx` 中 0 个 > 500，最大 `src/components/workspace/workspace-sidebar/sessions-section.tsx`=497）；`npm test` **176 文件 / 1289 用例全绿**；`npm run build` exit 0（`✓ built in 1.42s`）；定向 e2e `npx playwright test e2e/session-collapse.spec.ts` **2 passed**（10.5s） |
| 侧栏「工作目录 / 会话」分区合并（并行方案 `workspace-sidebar-directory-session-merge-plan.md` 落地回填） | 已完成（**工作树未提交**；该方案状态「草案 → 已落地」，实施记录见其 §10） | **后端零改动、纯前端**：`directories-section.tsx`（474 非空行）成为合并段主体——目录为骨架、会话为叶子行，会话段 `sessions-section.tsx` 删除（「同一会话两行」的重复根因消除）；`SidebarSectionId` 去掉 `"sessions"`；抽件 `session-browser-toolbar.tsx` / `session-row.tsx` / `directory-group-header.tsx` / `directory-group-actions.tsx`，新增 `workspace-directory-manage-dialog.tsx`（平铺模式仍可管理目录，§3.5-D）；删除重命名 origin 单编辑源机制；段显示条件取原两段**并集**（§3.4）；检索时展开 `chats` + `directories`；i18n 删除 `sidebar.sections.sessions`，双语逐键对齐。**P2-6 能力零丢失**：排序双模式 / 分组视图切换 / 组内折叠 Show N more / 跨组移动落点（组头 `data-testid="sidebar-session-group-drop"`）/ 0 会话注册目录常驻均沿用。 | `npm run lint` **0 error / 3 基线 warning**（i18n scanned=661 / violations=0、备份门禁 978 文件 0 残留、行数门禁 926 个 `.ts/.tsx` 中 0 个 > 500，最大 `src/lib/trajectory/timeline-window.ts`=494）；`npm test` **195 文件 / 1478 用例全绿**；`npm run build`（`tsc -b && vite build`）exit 0；定向 e2e `npx playwright test e2e/session-collapse.spec.ts e2e/session-grouping.spec.ts` **5 passed**（24.1s）；全量 e2e **82 用例 80 passed / 2 failed**，两红项归因见该方案 §10.5（`connection-recovery.spec.ts` 为在飞新断言早于 dist 构建所致，重建后单跑通过；`live-delta.spec.ts` 在 HEAD `be1754c4` 独立 worktree 中同样失败 = 既有红项，非本批引入） |
| 侧栏样式优化（目录/会话行顶到最左、图标动作收进 hover 菜单、空间让给标题；同上方案 §11 落地回填） | 已完成（**工作树未提交**；该并行方案状态为「§10 分区合并 + §11 样式优化」） | **后端零改动、纯前端；只动渲染层与样式，能力与数据口径零变化**：① 「顶到最左」——删除目录内会话行的 `ml-4` 嵌套缩进，组头内边距 `px-1.5 → px-1`，组头与会话行共用同一左边界并贴齐段左缘；② 「图标操作收进菜单」——目录组头三个平铺图标（新建会话 / 重命名 / 移除）收敛为单个 `MoreHorizontalIcon` 菜单入口（`aria-haspopup="menu"` / `aria-expanded` / `ArrowUp`·`ArrowDown`·`Home`·`End`·`Escape`·`Tab` 键盘导航、`creating` 忙碌禁用 + 旋转图标，对外 props 不变）；会话行内铅笔按钮并入行菜单作为**首项**「重命名会话」（`SessionRowMenu` 新增 `onRename` / `renameLabel`）；③ 「hover 再显示」——两处入口都留在既有 `opacity-0 … group-hover/*:opacity-100 focus-within:opacity-100` 槽位内，默认不可见、hover 或键盘聚焦才显形（键盘可达性不退化）；④ 「把空间让给标题」——会话行右侧预留 `pr-16 → pr-8`（无菜单行 `pr-9 → pr-2`）、组头常驻占位从约 3 个图标宽收到 1 个。**不变式**：e2e 定位契约（`sidebar-session-group` / `sidebar-session-group-drop` / `sidebar-session-group-toggle`、会话行 `[role="treeitem"]` + `data-depth`）与动作回调语义零变化；i18n 新增 `sidebar.directories.actions`（目录操作 / Directory actions）双语逐键对齐 | `npm run lint` **0 error / 3 基线 warning**（i18n scanned=664 / violations=0、备份门禁 982 文件 0 残留、行数门禁 930 个 `.ts/.tsx` 中 0 个 > 500，最大 `src/pages/workspace-page.tsx`=497）；`npm test` **196 文件 / 1486 用例全绿**（新增 `workspace-sidebar/directory-group-actions.test.tsx` 6 例）；`npm run build`（`tsc -b && vite build`）exit 0；定向 e2e `npx playwright test e2e/sidebar-session-actions.spec.ts e2e/session-collapse.spec.ts e2e/session-grouping.spec.ts` **10 passed**（20.3s）。**踩坑留档**：e2e 只跑 `dist`（`vite preview`），改完源码必须先 `npm run build`——本轮首次定向 e2e 因旧 dist 出现「菜单项找不到」假红，重建后同用例 1.9s 通过 |
| P2-9 子片 4：composer 上方浮动「当前任务」Todo 面板 | 已完成（**工作树未提交**；方案 `docs/plan/frontend-composer-floating-task-panel-plan.md`，实施记录见其 §11；本台账行与缺口清单 §3 批次 18 同批回填） | **后端一处 additive 契约放行 + 纯前端投影；无新依赖 / 无新 SSE 通道 / 无新全局 store**：① `internal/toolprotocol/result.go`——`Result.EventMapWithMetadataKeys(scopedKeys ...string)` 在既有 thin allowlist 之外按「单次结果」放行工具作用域键（trim 判空、缺席 / nil 跳过、**不并入共享白名单** → 同名键不会被其它工具顺带泄漏），`eventMap(scopedKeys)` 收内部实现、`EventMap()` 委托 `eventMap(nil)`（既有语义零变化）；② `internal/agent/tool_runtime_events.go`——`attachProtocolResultToPayload` **仅**在工具名等价 `todos` 时把 `metadata["todos"]` 裁剪为 `protocol_result.metadata.todo_snapshot`（`{items:[{content,status,active_form}], session_id?, goal_id?}`；空 `content` / 未知 `status` 整条丢弃、全丢则键不出现；**不改写生产侧 metadata**，与 `compact_reconciliation` 读取的 `metadata["todos"]` 命名显式分离）；③ 前端新增 `lib/thread-state/todos.ts`（只认该键、无键即 `null`；按 `seq` 取最新；runtime 单调、历史不覆盖实时；与 todo 无关的事件**原样返回入参引用**，不打断下游 memo）、`hooks/workspace/use-session-todos.ts`（空列表不渲染 / 全部完成即隐藏、三态计数、当前项、折叠态）、`components/workspace/task-panel/*`（默认折叠单行条 + `aria-expanded`/`aria-controls`/`aria-live="polite"`、展开 `grid-rows-[1fr]↔[0fr]` + `motion-safe:`、`line-clamp-1` + `title` 全文、上限 8 项 + 页脚、宽度取 dock 轴）；`lib/thread-state/events.ts` 在 `applyRuntimeEventToThread` 内折叠调用、私有文本工具机械搬迁到新 `lib/thread-state/text-utils.ts`（语义不变，为过 ≤500 非空行门禁）；`lib/thread-state/history-artifacts.ts` 从历史工具消息 metadata 反扫最近一次快照（刷新 / 会话切换后恢复）；`workspace-shell/main-section.tsx` 在 `chatSurfaceVisible` 分支、`PendingInteractionBar` 之前挂载（复用既有 `absolute inset-x-0 bottom-0 z-30` 吸底 overlay，不新增定位体系）；双语 `panels.todos.*` 逐键对齐 + mock 类型补可选 `todoSnapshot`。**未交付（非阻塞）**：T4 `todos` 工具行摘要打磨留待独立排期。 | `npx tsc -b --force` exit 0；`npm run build`（`tsc -b && vite build`）exit 0；`npm run lint` **0 error / 1 基线 warning**（`artifact-detail-dialog.tsx:50` 既有 `react-hooks/exhaustive-deps`；i18n scanned=684 / violations=0、备份门禁 1035 文件 0 残留、行数门禁 973 个 `.ts/.tsx` 中 0 个 > 500（最大 `hooks/workspace/use-session-runtime-stream.ts`=500）、消息 token 门禁 37 文件 0 处）；`npm test` **216 文件 / 1669 用例全绿**（本批新增 30 例：`todos.test.ts` 17、`use-session-todos.test.tsx` 6、`task-panel.test.tsx` 7）；后端 `go build ./...` / `go vet ./...` / `go test ./internal/agent/... ./internal/toolprotocol/... ./internal/toolkit/...` 全绿（含「非 `todos` 工具不得出现该键 / 生产侧 metadata 不被改写」用例）；e2e 按方案 D3 **不新增用例**；基线既有红项已在干净 HEAD `2d6dd61c` 独立 worktree 复现（`live-delta.spec.ts:85` / `thread-link.spec.ts:52,116` / `trajectory.spec.ts:71`），`workspace-chat.spec.ts:205`（P1-3a）经 `--repeat-each=3` 判为时序 flaky（2026-09-14 起即有失败留图），均未纳入本批范围。 |
| 运行时快照端点空态语义修正（`GET /api/runtime/sessions/{id}/runtime` 由「一律 404」改为「已知会话 → 200 显式空态 / 未知会话 → 404 `SESSION_NOT_FOUND`」；本台账行同批提交——缺口清单 §3 无对应登记项，未改动） | 已完成（**本批提交**） | **根因**：`backend/internal/api/skills/session_runtime_handlers.go` 的 `GetSessionRuntimeState` 旧实现对「会话存在、但从未进入 durable session actor」（只经无状态 `POST /api/agent/chat` 的 web 会话，如 `session_20260915184532_0Q6l5uf6`）与「会话根本不存在」都返回 `404 session runtime state not found`；两种终态不可区分，调用方只能把 404 当常规空态吞掉，于是每个 web 会话都在控制台留下误导性 404（报错点 `src/api/runtime/shared.ts:247` 的 `performRuntimeJsonFetch`）。**后端**：`state == nil` 时先用 `sessionManager.GetSession` 判存在性（与 `/sessions/{id}`、`/sessions` 列表同源）——存在 → `200 { session_id, state: null }`（显式空态：不生成状态行、不伪造未决审批 / 提问）；不存在 → `404 SESSION_NOT_FOUND`（经 `writeSessionStoreError` 统一映射：锁定 / 超时为 503 / 504）；`sessionManager` 缺失时保持历史 404（无可判定依据时不把未知当空态）。**SDK**：`pkg/skillsapi/client.go` 的 `SessionRuntimeStateResponse.State` 改为 `*SessionRuntimeState` 并回显 `SessionID`，避免 `state: null` 被解析成零值状态而再次混淆两种终态。**前端**：`api/runtime/session-runtime.ts` 新增 `isEmptySessionRuntimeSnapshot`，`getSessionRuntimeState` 返回 `RuntimeSessionSnapshot \| null`（null = 显式空态；结构不满足仍抛错、不伪造空态）；`hooks/workspace/use-session-runtime-state.ts` 把 null 归入空态并清掉本会话陈旧快照（404 继续按空态处理，兼容旧后端）。 | 后端：`session_runtime_handlers_test.go` 新增 3 例（已知会话无 actor 状态 → 200 且显式 `state: null` / 未知会话 → 404 `SESSION_NOT_FOUND` / 有状态原样返回）、`pkg/skillsapi/client_test.go` 新增 1 例（`{"session_id":…,"state":null}` 判空）；`go build ./...` exit 0；`go test ./internal/api/skills/... ./pkg/skillsapi/...` 全绿（15.7s / 1.5s）。前端：`api/runtime/session-runtime.test.ts` 11 例、`hooks/workspace/use-session-runtime-state.test.tsx` 8 例、`lib/pending-interaction/snapshot.test.ts` 10 例全绿；`npx tsc -b` exit 0；`pnpm lint` **0 error / 1 基线 warning**（`artifact-detail-dialog.tsx:50` 既有 `react-hooks/exhaustive-deps`；i18n scanned=686 / violations=0、备份门禁 1039 文件 0 残留、行数门禁 977 个 `.ts/.tsx` 中 0 个 > 500、消息 token 门禁 37 文件 0 处）；`pnpm test` **218 文件 / 1695 用例全绿**；`pnpm build` exit 0 + 定向 e2e `pnpm test:e2e e2e/pending-interaction.spec.ts e2e/connection-recovery.spec.ts` **5 passed**（15.8s）。**未覆盖（如实登记）**：未跑真实后端 e2e，以 `httptest` 直连 handler 的契约测试替代；e2e mock 的 `GET /sessions/:id/runtime` 本就区分未知（404）与已知（200），未改动。 |

---

### 9.4 2026-09-13 复核与缺口登记

- **基线**：HEAD `41471f24`；复核时前端工作区无未提交改动；方法=计划文本逐条对拍 + 12 个声称 commit 校验 + 静态代码取证 + 本地重跑门禁。缺口明细、证据与处置进展见 `docs/plan/frontend-deepseek-harness-gap-list.md`。
- **门禁实测**：`npm run build` exit 0；`npm run test` 121 文件 / 758 用例通过；`npm run test:e2e` 35 通过；`npm run lint:i18n` scanned=540 / violations=0（§9.3 旧记录 534）。**P1-10 合入后复测**：`npm run lint` exit 0（i18n scanned=550 / violations=0、备份门禁 766 文件 0 残留）；`vitest` 126 文件 / 778 用例；`npm run build` exit 0。**A2 拆分后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=555 / violations=0、备份门禁 777 文件 0 残留）；`pnpm test` **128 文件 / 778 用例全绿**（73.3s；拆分类新增 2 文件、用例数不变）；`pnpm build` exit 0 且构建产物 CSS（`index-B1wjI4k5.css` 129938 B、`landing-page-D_9RRpVY.css` 1407 B）与拆分前同尺寸同 SHA256；`pnpm test:e2e` **35 passed**（1.5m）。**B2 合入后复测（2026-09-13）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=558 / violations=0、备份门禁 784 文件 0 残留）；`pnpm test` **131 文件 / 793 用例全绿**（65.8s）；`pnpm build` exit 0；`pnpm test:e2e` **36 passed**（1.5m，含新增 `e2e/connection-recovery.spec.ts` 断线恢复用例）。**B3 合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=560 / violations=0、备份门禁 789 文件 0 残留，含清理本次 e2e mock 编辑产生的 `e2e/.backups/` 后复跑 `verify:clean`）；`pnpm test` **133 文件 / 803 用例全绿**（73.1s）；`pnpm build` exit 0；`pnpm test:e2e` **38 passed**（1.6m，含新增 `e2e/sidebar-session-actions.spec.ts` 两用例：Fork 请求体带分支后缀、删除后行消失）。**P2-1A Jobs 合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=567 / violations=0、备份门禁 802 文件 0 残留）；`pnpm test` **136 文件 / 837 用例全绿**（80.1s；本次新增 3 个单测文件 / 34 用例）；`pnpm build` exit 0；`pnpm test:e2e` **40 passed**（1.6m，含新增 `e2e/jobs-panel.spec.ts` 两用例：会话头入口 + 分区/退出码、输出展开 + 取消转终态）。**P2-1A 运行时状态快照合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=571 / violations=0、备份门禁 809 文件 0 残留）；`pnpm test` **139 文件 / 862 用例全绿**（78.1s；本次新增 3 个单测文件 / 25 用例）；`pnpm build` exit 0；`pnpm test:e2e` **41 passed**（1.6m，含新增 `e2e/pending-interaction.spec.ts` P1-7d 重载恢复用例，并已做「摘除端点即失败」的反向对照）。**P2-1A 用量 / 配额面板合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=575 / violations=0、备份门禁 817 文件 0 残留）；`pnpm test` **142 文件 / 891 用例全绿**（84.7s；本次新增 3 个单测文件 / 29 用例）；`pnpm build` exit 0；`pnpm test:e2e` **43 passed**（1.7m，含新增 `e2e/usage-quota.spec.ts` 两用例：全局聚合 → 账本筛选 → 作用域余量、403 提示补 admin token 不伪造数据）。**P2-1A 会话元数据搜索合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 项 warning**（i18n scanned=579 / violations=0、备份门禁 826 文件 0 残留）；`pnpm test` **144 文件 / 908 用例全绿**（98.4s；本次新增 2 个单测文件 / 17 用例：`api/runtime/session-search.test.ts` 12、`components/workspace/session-search-dialog.test.tsx` 5）；`pnpm build` exit 0；`pnpm test:e2e` **46 passed**（1.8m，含新增 `e2e/session-search.spec.ts` 三例：服务端 AND + 状态过滤与请求体口径、按用户过滤后选中结果跳转、503 如实降级）。**P2-1A 运行时文件读取与预览合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=584 / violations=0、备份门禁 837 文件 0 残留）；`pnpm test` **147 文件 / 934 用例全绿**（82.05s；本次新增 3 个单测文件 / 26 用例：`lib/file-preview/decode.test.ts` 8、`api/runtime/files.test.ts` 9、`components/workspace/file-preview-dialog.test.tsx` 9）；`pnpm build` exit 0；`pnpm test:e2e` **49 passed**（1.9m，含新增 `e2e/file-preview.spec.ts` 三例：工具行文件链接 → 预览内容 / 字节数 / 行数 + Escape 关闭、空文件与含 NUL 二进制如实呈现且不渲染文本、读失败如实提示后端 `no such file` 且无兜底内容可重试）。**P2-1A 技能市场与热重载合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=593 / violations=0、备份门禁 850 文件 0 残留）；`pnpm test` **148 文件 / 948 用例全绿**（101.94s；本次新增 1 个单测文件 / 14 用例：`api/runtime/skills.test.ts` 14）；`pnpm build` exit 0；`pnpm test:e2e` **53 passed**（2.2m，含新增 `e2e/skills-market.spec.ts` 四例：目录 / 检索 / 详情 / 统计全链路且 semantic→lexical 降级如实标注、热重载未配置 503 如实呈现 → start / stop / reload 真实 POST 与请求体口径、写操作 403 提示补管理令牌且无伪状态、目录端点 404 不伪装空市场 + policy 禁用时按钮不可点）。**P2-1A 子代理控制面合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=598 / violations=0、备份门禁 861 文件 0 残留；首跑发现编辑期遗留 4 个 `.backups/` 目录，确认对应源文件齐全后删除并复跑归零）；`pnpm test` **152 文件 / 997 用例全绿**（103.40s；本次新增 4 个单测文件 / 49 用例：`api/runtime/agents.test.ts` 17、`hooks/use-session-agents.test.tsx` 12、`components/workspace/session-agents-panel-shared.test.ts` 9、`components/workspace/session-agents-panel.test.tsx` 11）；`pnpm build` exit 0；`pnpm test:e2e` **53 passed**（2.2m，本批未新增 e2e 用例）。**P2-1A 侧栏会话统计摘要合入后复测（2026-09-13，pnpm 口径）**：`pnpm lint` **0 error / 3 基线 warning**（i18n scanned=602 / violations=0、备份门禁 870 文件 0 残留，含清理编辑期遗留的 1 个 `.backups/` 目录后复跑）；`pnpm test` **156 文件 / 1026 用例全绿**（107.02s；本次新增 4 个单测文件 / 29 用例）；`pnpm build` exit 0；`pnpm test:e2e` **57 passed**（2.2m，含新增 `e2e/session-stats.spec.ts` 4 例：默认用户聚合与 seed 一致、切侧栏用户重取、503 如实降级且不渲染任何 chip 并在恢复后重试可见真实计数、500 按真实失败呈现）。**P2-9 子片 1（后台任务常驻状态条）合入后复测（2026-09-13，npm 口径）**：`npm run lint` **0 error / 3 基线 warning**（i18n scanned=605 / violations=0、备份门禁 876 文件 0 残留（含清理编辑期产生的 3 个 `.backups/` 目录）、行数门禁 0 个 > 500）；`npm test` **158 文件 / 1034 用例全绿**（121.6s）；`npm run build` exit 0；`npm run test:e2e` **57 passed**（2.0m）。**口径修正（本次踩坑）**：e2e 只跑 `dist/` 构建产物——`playwright.config.ts` 的 `requireDist()` 只校验 `dist/index.html` 存在、webServer 起的是 `vite preview`；改了前端源码但未 `build` 时，e2e 会在旧产物上运行（本轮首跑新状态条断言即因此失败，`build` 后复跑通过）。此后凡 e2e 验证必须「先 `pnpm build` 再 `pnpm test:e2e`」。
- **台账补记**：§6.2 已为 P1-5 / P1-8 / P1-9 / P1-10 增补状态行；P1-10、P1-8、P1-9 先后于 2026-09-13 完成并合入（§6.2 状态改为「已完成」，缺口清单 B4 / B2 / B3 关闭）；P1-6 / P1-7 以 §9.3 行为准。§6.3 自 2026-09-13 起启用「P2-1A 逐项」状态块：后台任务（Jobs）、运行时状态快照、用量 / 配额、会话元数据搜索、运行时文件读取与预览（`fs/read-file`）、技能市场与热重载（`skills/*` 八端点）、侧栏会话统计摘要（`/sessions/stats`）先后完成并合入（缺口清单 C2 的 jobs 项关闭、「审批闭环」行整行闭环、usage / 配额项关闭、sessions/search 项关闭、fs/read-file 项关闭、skills 市场 / 热重载项关闭、子代理控制面（AgentControl 身份图）项关闭、侧栏会话统计摘要（`/sessions/stats`）项关闭，余 9 行待排期）；逐项明细见 §6.3「状态（P2-1A 逐项）」及其续块（续 / 续 2 / 续 3 / 续 4 / 续 5 / 续 6 / 续 7）；同时**回填 §9.3 技能市场 / 热重载交付行与缺口清单 §3 批次 5 行**（该项此前只登记了状态块与门禁实测，缺 §9.3 / §3 台账行）；本批同样为该统计项回填 §9.3 交付行与缺口清单 §3 批次 7 行。
- **验收回退与漂移**：
  1. P0-1「`src` 下 `.bak`=0」在执行期被回退（复核时 `src` 下 52 个 `.bak` / 17 个 `.backups/`，`frontend/` 全域 72 / 19，全部为 2026-09-13 生成且被 `.gitignore` 隐形）。2026-09-13 已再次清理为 0 / 0，并新增 `frontend/scripts/verify-no-backups.mjs` 门禁（并入 `npm run lint`）。
  2. P0-2「> 500 行 = 0」复核时不成立：3 个文件超长（`styles/globals.css`=1043、`components/workspace/message-markdown-streaming.ts`=525、`components/workspace/trajectory/subagent-session-dialog.test.tsx`=516）；按 ts/tsx 口径 2 个，M4 阈值（≤ 10）仍满足。**2026-09-13 已修复**：三个文件全部拆分达标，`frontend/src` 内 > 500 行文件 **0 个**（证据见 §9.3「P0-2 回退修复」行与缺口清单 A2）。**2026-09-13 后续复检（P2-1A 第八项交付后）该指标再次不成立**：`frontend/src` 内 > 500 行文件 2 个（`pages/usage-analytics/quota.tsx`=559、`pages/workspace-page.tsx`=512），随 P2-1A 接线增量引入；该口径至今无门禁脚本看护（`frontend/scripts/` 仅 `verify-frontend-i18n.ts` / `verify-no-backups.mjs`），故回退未被拦截，处置见缺口清单 A2 复检记录。**2026-09-13 二次修复（A2 收口，npm 口径）**：`pages/usage-analytics/quota.tsx`(559) → 429 + `quota-shared.ts` 74 + `quota-atoms.tsx` 73；`pages/workspace-page.tsx`(512) → 437 + `hooks/workspace/use-workspace-session-actions.ts` 133（六个会话行动作收口，仅搬迁不改语义）；并新增 `frontend/scripts/verify-max-lines.mjs` 行数门禁（非空行 ≤ 500、i18n 词典整树豁免；501 行探针负路径验证 exit 1）并入 `npm run lint` / `npm run verify:lines`——该口径自此有门禁兜底。复测：`frontend/src` 内 > 500 非空行文件 **0 个**（最大 `workspace-sidebar.tsx`=483）；门禁四件套 `lint` 0 error / 3 基线 warning（i18n scanned=605 / violations=0、备份门禁 874 文件 0 残留）、`test` 156 文件 / 1026 用例全绿、`build` exit 0、`test:e2e` 57 passed（证据见 §9.3「P0-2 二次回退修复」行与缺口清单 §3 批次 8）。
  3. P0-4 数值口径漂移：`@theme` 映射 89（原记 87）、残留 `[var(--…)]` 任意值 22 行（原记 20）。
  4. 文档引用漂移：§5.5 A2 的 `logs-page.tsx:132-166,301-303` 在 P0-2 拆分（`76c2f5be`）后失效，连接 UI 现位于 `pages/logs-page/connection.tsx` / `logs-header.tsx`。
  5. 跨计划编号冲突：前端注释中的「P1-5 方案 1/2/4」指 `multi-agent-execution-optimization-plan.md` 的 P1-5，与本计划 P1-5（队列与 Steering）无关。
- **未开始任务**：P1-5（硬阻塞：后端无 queue/steering API）；P2 全量（P2-1 仅部分；**P2-7 四个子片已于批次 12–14 全部交付**——`/export` / `/rename` 内置执行器（批次 12）、`/model` 目录弹窗与 `/feedback` log-only 入口（批次 13）、命令专属 popupSelect 候选面（批次 14，`82c81149`），本计划内 P2-7 已收口（§9.1 收敛表 5-9 / 5-12）；**P2-6 已收口（三个子片 + 空态全交付）：排序双模式 + 手动顺序账目 + 组内拖拽重排（批次 15，`10627d34`）、会话分组视图（按目录 / 平铺）+ 跨组移动与 Host 写回（批次 16，`8520de33`，乐观归属 + 失败回滚）、组内展开 / 折叠（Show N more）+ 空白会话提升（批次 17，`f2c9cf98`）；空态三态由批次 3 交付（`c25a72e0`）**；其余 8 项零实施）。P1-8、P1-10、P1-9 已于 2026-09-13 完成。
- **范围外观察**：仓库内仍有 607 个前端域外 `.bak`（`backend/**/.backups`、`docs/**/.backups` 等，均未被 git 跟踪、被根 `.gitignore` 覆盖），建议各域自清。

---

## 10. 风险与依赖

| 风险/依赖 | 说明 | 缓解 |
|---|---|---|
| 后端接口缺口（Round 2 已收窄） | 硬前置仅剩 **队列/steering、附件上传、MCP 目录/schema、Artifacts 统一接口、货币成本**；审批、SSE 重放、子代理、Skills、搜索、用量、文件读写**均已就绪** | 按 §6.3 P2-1 三档推进：A 类先接、B 类并行小改、C 类 UI 以 `pending/占位` 呈现，不做假数据 |
| 折叠策略与现有回溯/恢复冲突 | 回溯（backtrack）会改写消息历史，折叠谓词需对「改写后的 Turn」重算 | 谓词纯函数化 + 回溯后强制重算投影；补回溯场景测试 |
| 样式重构视觉回归 | 三层 token 迁移可能造成颜色/间距偏差 | 先建立 golden 截图（P2-5 提前做最小集），迁移按页面分批 |
| 超长文件拆分影响既有 import | 大量模块可能直接深链 | 拆分时保留原路径 barrel re-export；一次性 codemod 更新 |
| SSE 重连与租约冲突 | 多人/多进程共享 runtime 时重连可能触发租约冲突 | 复用现有租约冲突标题映射；重连退避 + 会话级串行化 |
| i18n 门禁误报 | AST 扫描对非文案字符串可能误报 | 白名单 + 自然语言判定 + 最小扫描面；先以 warn 模式跑通再转 fail |
| 性能改造范围蔓延 | 虚拟化引入可能牵动消息列表整体结构 | 先度量（P2-4 基准），仅在不达标时引入，保持「先窗口化、后虚拟化」 |
| 大文件拆分与功能改造并发冲突 | 首批拆分的 `workspace-thread-state` / `use-workspace-agent-chat-turn` / `message-list` / 域编辑器，正是 P1-1~P1-4、P2-2 的改动对象；并行开工会造成大面积冲突 | 「先拆后改」固定顺序：命中批次的文件先做机械拆分（不改行为、barrel 保接口），再进行功能改造；功能任务开工前检查目标文件是否在拆分批次内 |
| 命令系统范围蔓延（P2-7） | `/` 命令易被扩展为全局面板或插件命令体系 | 明确非目标（§8）；首批限 3 条内置命令；与 P1-4 共用输入机而非另建一套 |
| 错误边界重试死循环（P1-10） | chunk 加载失败后无限重试或循环刷新 | 重试上限 + 退避 + 显式用户动作；测试必须包含「二次失败」用例 |
| 通知位重复（P2-9） | jobs 常驻状态条与既有 toast/桌面通知（A12）重复提示同一事件 | 单一通知位：状态条负责计数与入口，toast/通知只负责一次性完成/失败；同源事件去重 |
| 侧栏文件热点（P1-9 / P2-6） | `workspace-sidebar.tsx`(1560) 同时承载搜索/重命名/分组，且是第 3 批拆分对象 | 先按第 3 批拆出容器/行/菜单/搜索四块（行为不变），再在拆后结构上实现状态指示与组织功能 |

---

## 11. 附录

### 11.1 关键参考路径（目标项目）

- 消息与滚动：`packages/client/ui-chat/README.md`、`ui-conversation/src/client/contract/conversation.ts`、`ui-conversation/src/client/{conversation,input,queue,skeleton}/*`
- 增量 Markdown：`ui-primitives/src/markdown/{incremental.ts,MarkdownText.tsx,render.tsx,useViewportHighlighting.ts}`
- 工具行：`ui-tool/src/client/tool/components/ToolRow.tsx`
- 轨迹：`ui-trajectory/src/client/trajectory-virtual-rows.ts`、`ui-trajectory/README.md`
- 审批/提问/队列/steering：`ui-approval/src/client/index.ts`、`ui-user-questions/src/client/index.ts`、`input/submission-policy.ts`、`ui-chat/src/client/model/steering-history.ts`
- 设计体系：`ui-theme/src/styles/{base.css,design-platform.css,gradient-shadow-text.css,scrollbar.css,corner-shape.css}`、`ui-layout/src/client/{AppFrame.tsx,columns.ts,theme-presenter.ts}`、`docs/web-styling.md`
- 设置与 i18n：`ui-settings/src/client/schema.ts`、`ui-settings-plugins/src/client/{fields.tsx,card-form.ts}`、`locale/src/client/index.ts`、`scripts/verify-client-ui-i18n.ts`
- 质量保障：`apps/web/tests/{support.ts,scaffold.ts,chat-scroll-contract.e2e.ts,settings-chrome.e2e.ts,sidebar-right.e2e.ts}`

### 11.2 关键参考路径（当前项目，改造对象）

- 样式：`frontend/src/styles/{globals.css,landing.css}`、`frontend/src/core/settings/local.ts`
- 工作台：`frontend/src/components/workspace/{workspace-shell.tsx,workspace-sidebar.tsx,message-list.tsx,message-composer.tsx,artifact-panel*.tsx}`
- 轨迹：`frontend/src/components/workspace/trajectory/*`、`frontend/src/lib/trajectory/*`
- 数据层：`frontend/src/api/runtime/*`、`frontend/src/hooks/workspace/*`、`frontend/src/types/runtime.ts`
- 设置：`frontend/src/components/workspace/settings/*`（含 15 个 `runtime-*-domain-editor.tsx`）
- i18n：`frontend/src/i18n/resources/{zh-CN.ts,en-US.ts}`
- 测试：`frontend/src/**/*.test.ts(x)`、`frontend/e2e/*`

### 11.3 任务编号速查

| 阶段 | 任务 |
|---|---|
| P0 | P0-1 目录卫生 / P0-2 超长文件拆分 / P0-3 组件收敛 / P0-4 三层 token / P0-5 主题解析应用分离 / P0-6 i18n 真源与门禁 / P0-7 测试基建 |
| P1 | P1-1 消息投影与折叠 / P1-2 增量 Markdown / P1-3 滚动契约 / P1-4 Composer 升级 / P1-5 队列与 Steering / P1-6 工具行状态机 / P1-7 审批与提问 / P1-8 连接状态与断线恢复 / P1-9 会话列表状态与整理 / P1-10 错误边界与加载失败面 |
| P2 | P2-1 后端能力对接（就绪度驱动；子包 P2-1A/B/C 见 §6.3）/ P2-2 设置 schema 化 / P2-3 a11y 与响应式 / P2-4 性能预算 / P2-5 视觉回归 golden / P2-6 会话列表组织 / P2-7 命令系统 / P2-8 交付物呈现与降级 / P2-9 可观测面板增补 / P2-10 文件树只读浏览 / P2-11 右栏标签注册表 |

### 11.4 超长文件完整清单（> 500 行，41 项）

> 口径：`frontend/src/**`（排除 `.backups/`、`dist/`），行数 = 含空行编辑器口径（`@(Get-Content).Count`），2026-09-11 采集。39 个非测试 + 2 个测试。归属批次见 §6.1 P0-2「分批策略」。

| # | 行数 | 路径（相对仓库根） | 归属批次 |
|---|---|---|---|
| 1 | 3932 | `frontend/src/components/workspace/settings/backend-config-settings-page.tsx` | 第 2 批（设置域，按 section + 注册表） |
| 2 | 2595 | `frontend/src/i18n/resources/en-US.ts` | P0-6（按 namespace 拆分） |
| 3 | 2536 | `frontend/src/i18n/resources/zh-CN.ts` | P0-6（zh 为 key 真源） |
| 4 | 1815 | `frontend/src/lib/workspace-thread-state.ts` | 首批 |
| 5 | 1692 | `frontend/src/types/runtime.ts` | 首批 |
| 6 | 1560 | `frontend/src/components/workspace/workspace-sidebar.tsx` | 第 3 批（workspace 组件） |
| 7 | 1358 | `frontend/src/components/workspace/settings/runtime-provider-groups-domain-editor.tsx` | 首批 |
| 8 | 1337 | `frontend/src/hooks/workspace/use-workspace-agent-chat-turn.ts` | 首批 |
| 9 | 1187 | `frontend/src/components/workspace/settings/runtime-provider-domain-editor.tsx` | 首批 |
| 10 | 974 | `frontend/src/pages/usage-analytics-page.tsx` | 首批 |
| 11 | 893 | `frontend/src/pages/logs-page.tsx` | 首批 |
| 12 | 846 | `frontend/src/components/workspace/runtime-teams/runtime-team-dispatch-panel.tsx` | 首批 |
| 13 | 831 | `frontend/src/lib/trajectory/trajectory-reducer.ts` | 首批（仅机械整理，语义不动，见 A16） |
| 14 | 812 | `frontend/src/components/workspace/runtime-teams/runtime-team-details-panel.tsx` | 首批 |
| 15 | 807 | `frontend/src/components/workspace/settings/runtime-agent-routing-domain-editor.tsx` | 首批 |
| 16 | 800 | `frontend/src/lib/workspace-thread-state.test.ts` | 随第 4 项源文件同步拆分 |
| 17 | 770 | `frontend/src/components/workspace/settings/runtime-rate-limit-domain-editor.tsx` | 首批 |
| 18 | 744 | `frontend/src/hooks/workspace/use-session-backtrack.ts` | 首批 |
| 19 | 743 | `frontend/src/components/workspace/runtime-teams/use-runtime-team-dispatch.ts` | 首批 |
| 20 | 719 | `frontend/src/components/workspace/message-list.tsx` | 首批 |
| 21 | 715 | `frontend/src/components/workspace/settings/runtime-retry-domain-editor.tsx` | 第 2 批 |
| 22 | 695 | `frontend/src/components/workspace/workspace-shell.tsx` | 第 3 批 |
| 23 | 693 | `frontend/src/components/workspace/runtime-teams.tsx` | 第 3 批 |
| 24 | 683 | `frontend/src/components/workspace/runtime-teams/team-details-panel.tsx` | 第 3 批 |
| 25 | 628 | `frontend/src/components/workspace/runtime-teams/shared.ts` | 第 3 批 |
| 26 | 625 | `frontend/src/components/workspace/message-markdown.tsx` | 第 3 批 |
| 27 | 618 | `frontend/src/components/workspace/artifact-panel.tsx` | 第 3 批 |
| 28 | 613 | `frontend/src/components/workspace/artifact-panel-checkpoint-surface.tsx` | 第 3 批 |
| 29 | 596 | `frontend/src/components/workspace/settings/appearance-settings-page.tsx` | 第 2 批 |
| 30 | 575 | `frontend/src/components/workspace/runtime-teams/dispatch-console.tsx` | 第 3 批 |
| 31 | 568 | `frontend/src/components/workspace/settings/harness-settings-page.tsx` | 第 2 批 |
| 32 | 565 | `frontend/src/components/workspace/settings/runtime-routing-domain-editor.tsx` | 第 2 批 |
| 33 | 539 | `frontend/src/data/mock.ts` | 第 4 批（pages/data） |
| 34 | 537 | `frontend/src/lib/trajectory/trajectory-reducer.test.ts` | 随第 13 项机械整理同步拆分 |
| 35 | 534 | `frontend/src/components/workspace/artifact-detail-dialog.tsx` | 第 3 批 |
| 36 | 523 | `frontend/src/hooks/workspace/use-runtime-sessions-data.ts` | 第 4 批 |
| 37 | 518 | `frontend/src/pages/cache-analytics-page.tsx` | 第 4 批 |
| 38 | 514 | `frontend/src/components/workspace/settings/runtime-agent-routing-domain-utils.ts` | 第 2 批 |
| 39 | 509 | `frontend/src/hooks/workspace/use-runtime-checkpoints.ts` | 第 4 批 |
| 40 | 508 | `frontend/src/components/workspace/settings/runtime-transformer-domain-editor.tsx` | 第 2 批 |
| 41 | 504 | `frontend/src/components/workspace/settings/runtime-provider-queue-domain-editor.tsx` | 第 2 批 |

**批次合计**：首批 15（+1 源测试随拆）、第 2 批 8、第 3 批 10、第 4 批 4、P0-6 承担 2、测试随源 1 = 41。

> **拆分进度（2026-09-11）**：第 5 项 `types/runtime.ts`(1692)、第 6 项 `components/workspace/workspace-sidebar.tsx`(1560)、第 4 项 `lib/workspace-thread-state.ts`(1815) + 随源第 16 项测试(800)、第 13 项 `lib/trajectory/trajectory-reducer.ts`(831) + 随源第 34 项测试(537)、第 10 项 `pages/usage-analytics-page.tsx`(974)、第 8 项 `hooks/workspace/use-workspace-agent-chat-turn.ts`(1337)、第 7 项 `components/workspace/settings/runtime-provider-groups-domain-editor.tsx`(1358)、第 9 项 `components/workspace/settings/runtime-provider-domain-editor.tsx`(1187)、第 11 项 `pages/logs-page.tsx`(893)、第 12 项 `components/workspace/runtime-teams/runtime-team-dispatch-panel.tsx`(846)、第 14 项 `components/workspace/runtime-teams/runtime-team-details-panel.tsx`(812)、第 15 项 `components/workspace/settings/runtime-agent-routing-domain-editor.tsx`(807)、第 17 项 `components/workspace/settings/runtime-rate-limit-domain-editor.tsx`(770)、第 18 项 `hooks/workspace/use-session-backtrack.ts`(744)、第 19 项 `components/workspace/runtime-teams/use-runtime-team-dispatch.ts`(743)、第 20 项 `components/workspace/message-list.tsx`(719)、第 21 项 `components/workspace/settings/runtime-retry-domain-editor.tsx`(715)、第 22 项 `components/workspace/workspace-shell.tsx`(695)、第 23 项 `components/workspace/runtime-teams.tsx`(693)、第 24 项 `components/workspace/runtime-teams/team-details-panel.tsx`(683)、第 25 项 `components/workspace/runtime-teams/shared.ts`(628)、第 26 项 `components/workspace/message-markdown.tsx`(625)、第 27 项 `components/workspace/artifact-panel.tsx`(618)、第 28 项 `components/workspace/artifact-panel-checkpoint-surface.tsx`(613)、第 30 项 `components/workspace/runtime-teams/dispatch-console.tsx`(575)、第 35 项 `components/workspace/artifact-detail-dialog.tsx`(534)、第 32 项 `components/workspace/settings/runtime-routing-domain-editor.tsx`(565)、第 40 项 `components/workspace/settings/runtime-transformer-domain-editor.tsx`(508)、第 29 项 `components/workspace/settings/appearance-settings-page.tsx`(596)、第 31 项 `components/workspace/settings/harness-settings-page.tsx`(568)、第 41 项 `components/workspace/settings/runtime-provider-queue-domain-editor.tsx`(504)、第 38 项 `components/workspace/settings/runtime-agent-routing-domain-utils.ts`(514)、第 1 项 `components/workspace/settings/backend-config-settings-page.tsx`(3932)、第 33 项 `data/mock.ts`(539)、第 36 项 `hooks/workspace/use-runtime-sessions-data.ts`(523)、第 37 项 `pages/cache-analytics-page.tsx`(518)、第 39 项 `hooks/workspace/use-runtime-checkpoints.ts`(509) 已完成拆分（→ `types/runtime/` 14 个域模块；`lib/thread-state/` 10 个职责模块 + 4 个同源测试 + 共享夹具；`lib/trajectory/trajectory-reducer/` 4 个层次模块 + 3 个同源测试 + 共享夹具；`pages/usage-analytics/` 4 个模块；`hooks/workspace/agent-chat-turn/` 10 个模块；`components/workspace/settings/runtime-provider-domain-editor/` 6 个模块；`pages/logs-page/` 5 个模块；`components/workspace/runtime-teams/runtime-team-dispatch-panel/` 8 个模块；`components/workspace/runtime-teams/runtime-team-details-panel/` 10 个模块；`components/workspace/settings/runtime-agent-routing-domain-editor/` 9 个模块；`components/workspace/settings/runtime-rate-limit-domain-editor/` 7 个模块；`hooks/workspace/session-backtrack/` 4 个模块；`components/workspace/runtime-teams/use-runtime-team-dispatch/` 4 个模块；`components/workspace/message-list/` 6 个模块；`components/workspace/settings/runtime-retry-domain-editor/` 5 个模块、`components/workspace/workspace-shell/` 6 个模块、`components/workspace/runtime-teams/` 6 个模块、`components/workspace/runtime-teams/team-details-panel/` 9 个模块、`components/workspace/runtime-teams/shared/` 5 个域模块、`components/workspace/message-markdown/` 5 个模块、`components/workspace/artifact-panel/` 4 个模块、`components/workspace/artifact-panel-checkpoint-surface/` 4 个模块、`components/workspace/workspace-sidebar/` 14 个模块、`components/workspace/runtime-teams/dispatch-console/` 8 个模块、`components/workspace/artifact-detail-dialog/` 6 个模块、`components/workspace/settings/runtime-routing-domain-editor/` 5 个模块、`components/workspace/settings/runtime-transformer-domain-editor/` 6 个模块、`components/workspace/settings/appearance-settings-page/` 9 个模块、`components/workspace/settings/harness-settings-page/` 7 个模块、`components/workspace/settings/runtime-provider-queue-domain-editor/` 6 个模块、`components/workspace/settings/runtime-agent-routing-domain-utils/` 7 个模块（类型 / 摘要读取 / 写入构建 / 能力探测 / 选项 / 健康分析 / 文本工具）、`components/workspace/settings/backend-config-settings-page/` 48 个模块（core 三层 hook / 15 个域工厂 / 7 个 section / 16 个模式 section / 7 个支撑模块）、`data/mock/` 3 个模块（类型 / 模板串资产 / `initialThreads` 夹具）、`hooks/workspace/runtime-sessions-data/` 7 个模块（类型 / 归一化 / 排序与摘要 / 存储键与缓存读写 / 重试退避 / 用户选择 / 加载合并）、`pages/cache-analytics/` 6 个模块（数字格式化 / 缓存状态映射 / 指标卡 / 分布徽标 / 请求明细表 / 追溯侧栏）、`hooks/workspace/runtime-checkpoints/` 3 个模块（选项类型 / 重载键判定 / 详情派生状态），均见 §9.3 实施记录）；表中行数为拆分前快照，其余 2 项待推进（首批 20 项全部完成；第 2 批 8 项全部完成；第 3 批 10 项全部完成；第 4 批 4/4 项完成；余下 2 项为 P0-6 承担的 i18n 双语字典）。

### 11.5 Round 2 功能点处置矩阵（89 项逐条）

> 编号沿用 `.tmp/frontend-opt-analysis/round2-target-shell-features.md`；任务 ID 见 §6，保留资产见 §5.5，不采纳/暂缓见 §8，设计决策见 §7。本表与 §5.6 汇总口径一致。

#### 1-x 外壳与启动（15）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 1-1 | 单页 HTML 骨架与固定挂载点 | 已具备（`#root`；P0-1 保持） |
| 1-2 | PWA manifest 引用 | §8 不采纳 |
| 1-3 | HTML `lang` 静态 en | P0-6（运行时按 locale 回写 `document.lang`） |
| 1-4 | 入口硬失败保护 | P1-10 |
| 1-5 | AppWebEntry 组合入口 | P1-10（bootstrap 顺序化，不引入插件体系） |
| 1-6 | 框架无关启动页与进度弧 | P1-10（首屏骨架 + 失败面） |
| 1-7 | 逐插件加载状态投影 | P1-10（映射为路由/chunk 加载状态） |
| 1-8 | 启动失败报告面 | P1-10 |
| 1-9 | 启动完整性审计 | P1-10（构建资源/路由完整性校验） |
| 1-10 | 客户端 HMR（开发期） | §8 不采纳 |
| 1-11 | fixture 查询开关 | §8 不采纳（e2e 用 route mock） |
| 1-12 | 无客户端路由（单页单视图） | 不采纳（保留 react-router 多路由） |
| 1-13 | 多窗口/多标签同步 | §8 不采纳（单窗口；并行会话走 Fork/多会话列表） |
| 1-14 | 浏览器鉴权与登录入口 | §8 暂缓（按权限模型接后端授权端点） |
| 1-15 | 明文 HTTP 读取信任提示 | §8 暂缓（P3，连接设置文案） |

#### 2-x 布局与侧栏（15）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 2-1 | 三栏几何解算 | P2-3（纯函数 + 边界单测） |
| 2-2 | 侧栏宽度拖拽 clamp | P2-3（持久化偏好） |
| 2-3 | 窄屏自动折叠（1024px） | P2-3（auto 与 user 两态分离） |
| 2-4 | 右栏比例布局 | P2-3 + P2-11 |
| 2-5 | 主题呈现器（DOM 应用层） | P0-5（单一 presenter 所有权 + 可回收） |
| 2-6 | 框架级通知/状态位 | P2-9（单一通知位约束见 §10；目标项目该位仅声明未实现，见 §11.6 C-6） |
| 2-7 | 侧栏壳与全局面板导航 | P1-9/P2-11 扩展（现状部分具备；不引入 slot 体系） |
| 2-8 | 右栏面板 + 头部展开按钮 | P2-11 |
| 2-9 | 右栏标签类型注册（两段式） | P2-11（「注册即生效」为验收项） |
| 2-10 | 右栏历史与资源打开 | P2-11 |
| 2-11 | 工作区文件树浏览 | P2-10 |
| 2-12 | 文件树错误分类 | P2-10（越界为安全语义） |
| 2-13 | 文档预览（含 PDF 失败面） | 部分：文本预览 P2-10；PDF/加密失败面 §8 暂缓 |
| 2-14 | 目录选择：应用内浏览对话框 | 已具备（§5.5 A10） |
| 2-15 | 目录选择：原生 OS 选择器 | §8 不采纳（可选增强） |

#### 3-x 会话列表与工作区（19）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 3-1 | 分组视图（工作区/平铺） | P2-6 |
| 3-2 | 双排序（手动/最近更新） | P2-6 |
| 3-3 | 拖拽排序（组内 + 跨组） | P2-6（乐观更新 + 失败恢复） |
| 3-4 | 组内展开/折叠（Show N more） | P2-6 |
| 3-5 | 空态（无会话/无匹配） | P2-6 |
| 3-6 | 新建会话 | 已具备 |
| 3-7 | 新增工作区 | 已具备（A10） |
| 3-8 | 重命名工作区/会话 | 已具备（A13） |
| 3-9 | 删除工作区（非破坏 + pending） | P1-9（复核非破坏语义与 pending） |
| 3-10 | 归档会话 | P1-9（后端 archive/activate 就绪） |
| 3-11 | 会话 Fork | P1-9 |
| 3-12 | 操作菜单与 aria | P1-9 + P2-3 |
| 3-13 | 会话状态指示（7 态） | P1-9 |
| 3-14 | 定时任务指示 | §8 暂缓（后端证据不足） |
| 3-15 | 相对时间与创建时间浮层 | P1-9 |
| 3-16 | 会话搜索（名称+内容，降级提示） | P2-1A（受 A3 约束：不重写轨迹搜索） |
| 3-17 | 可见性过滤与记账槽 | P2-6（列表正确性） |
| 3-18 | 空白会话自动提升排序 | P2-6（后置项） |
| 3-19 | 多选/批量、未读、置顶（未发现项） | §8 不采纳（需求未验证；Round 3 目录级复核零命中，§11.6 C-3；P1-9 先做等待态指示） |

#### 4-x 设置体系（12）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 4-1 | 设置域基础服务与单一读取 | P2-2 |
| 4-2 | 设置壳独立包 | §8 不采纳（保留现有设置页结构） |
| 4-3 | 设置扩展槽位组 | P2-2（schema + 注册表） |
| 4-4 | 设置触发入口 | 已具备 |
| 4-5 | Onboarding 设置槽 | §8 暂缓 |
| 4-6 | 设置 schema 服务 | P2-2 |
| 4-7 | 设置作用域绑定器 | P2-2（scope 优先级与不可变快照） |
| 4-8 | 模型设置分区 | 已具备（A6）+ P2-2 增量 |
| 4-9 | 插件配置分区表单状态机 | P2-2（dirty/saving/error/readonly） |
| 4-10 | 插件配置项（Shell/loop/Web search/Subagent） | P2-2 首批三项（Shell 超时/并行度/Web search key） |
| 4-11 | 插件清单页 | P2-1A（花名册）+ P2-2 |
| 4-12 | 语言切换入口 + 回退链 | P0-6 + P2-2（General 语言行） |

#### 5-x 运行时功能包（14）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 5-1 | 目标（Goal）生命周期控件 | P2-9（四相指示；权威源见 P2-1B） |
| 5-2 | 子代理会话树查看器 | P2-9 |
| 5-3 | 子代理元信息格式化 | P2-9（共享纯函数 + i18n 单位） |
| 5-4 | 后台任务（Jobs）状态条 | P2-9（与 toast 去重） |
| 5-5 | 工作流运行视图 | §8 暂缓（状态词汇表先并入 P2-9） |
| 5-6 | 技能（Skill）工具行 | P1-6（ToolRow 状态机）+ P2-1A（技能面板） |
| 5-7 | 交付物呈现与宿主动作 | P2-8（能力降级 + 三态） |
| 5-8 | 交付物打开深链 | P2-8（后置；与路由 query 设计同行） |
| 5-9 | 消息反馈（Feedback） | P2-7（收敛为 `/feedback` 命令入口） |
| 5-10 | 模型选择器（Composer 内） | 已具备（A5/A6；P2-2 不得回退） |
| 5-11 | Agent 预设切换（会话级） | §8 暂缓（待后端预设版本） |
| 5-12 | 命令系统（输入触发式） | P2-7 |
| 5-13 | 资源模型（统一资源打开） | P2-11 |
| 5-14 | 轻量状态引擎（store）契约 | 部分采纳：§7.9 两条纪律，不引入自研 store |

#### 6-x 横向交互（14）

| 编号 | 功能点 | 本方案处置 |
|---|---|---|
| 6-1 | Composer 提交键位（Ctrl/Cmd+Enter） | P1-4 |
| 6-2 | 组件级 keydown 监听生命周期 | P1-4（挂载/卸载配对，并入 §7.9 纪律） |
| 6-3 | 无全局快捷键体系/命令面板 | §8 不采纳 |
| 6-4 | aria 标签覆盖 | P2-3 |
| 6-5 | 状态提示可访问性（role=status） | P2-3 + P2-6（拖拽宣告） |
| 6-6 | 虚拟高亮下拉（输入内） | P2-7 |
| 6-7 | 骨架屏/占位行 | P1-10 + P2-4 |
| 6-8 | 槽位级错误边界 | P1-10（面板层边界） |
| 6-9 | 错误重试模式（显式 retry） | P1-10 + P2-1C（各面板 retry 三态） |
| 6-10 | 订阅者/监听器错误隔离 | §7.9 + P1-10 |
| 6-11 | 离线/在线与连接恢复 | P1-8 |
| 6-12 | 遥测/埋点 | §8 不采纳 |
| 6-13 | 密钥与敏感输入（password） | P2-2（表单规范） |
| 6-14 | 路径隐私与显示缩写（`~`） | P2-10（文件树）+ P1-6（工具行路径） |

**统计核对**：落地 63 项 / 部分采纳 2 项 / 已具备 8 项 / 暂缓 6 项 / 不采纳 10 项 = **89 项**，与 §5.6 汇总一致；无悬空功能点。

---

### 11.6 目标项目证据缺口复核记录（Round 3）

> 来源：`.tmp/frontend-opt-analysis/round3-gap-a.md`（C-1/C-3/C-6）与 `round3-gap-b.md`（C-2/C-4/C-5/C-7），方法为对 deepseek-harness 源码直读 + 目录级 grep。证据路径除注明 `apps/web` 外均相对 `packages/client/`；标「未核验」的子项不得作为定稿依据。

| 缺口 | 复核结论 | 关键证据（deepseek-harness） | 对方案的影响 / 未核验子项 |
|---|---|---|---|
| C-1 侧栏主体与会话列表装配 | 侧栏无独立几何：列宽常量、断点、三列求解均在 `ui-layout`；折叠为 150ms slide+crossfade 并冻结展开宽度；会话列表入口为 `ui-workspace` 的 `WorkspaceBrowser` | `ui-layout/src/client/columns.ts:13-19,20-23,50-56`；`ui-layout/src/client/AppFrame.tsx:160-164,193-196,206-209`；`ui-sidebar/src/client/SidebarRoot.tsx:30-31,100-119,166-179`、`SidebarRoot.module.css:45-86,418-429`（reduced-motion 全关）；`ui-workspace/src/client/index.ts:141-150`、`rows/WorkspaceBrowser.tsx:839,1256-1326` | 2-1/2-2/2-3 的几何参数与 2-7 职责划分有据可依；P1-9 扩展点落在 `WorkspaceBrowser` 拆分后的结构上。未核验：`SidebarRoot` 密度/主题变量细节 |
| C-2 设置壳槽位与导航 | 设置 SHELL 确在 `ui-settings-general`；`sidebar.settings` 注册时一次声明 6 个子槽；导航不是独立数据表，而是 `settings.section` 槽条目按 order 投影；4-4 触发入口与 4-5 Onboarding 槽均真实存在 | `ui-settings-general/src/client/index.ts:100-125,146-182`；`SettingsRoot.tsx:47-101,138-144,182-193,217-221`；`ui-settings/src/client/contract/slots.ts:14-90,104-136` | P2-2 注册表模型与 4-3/4-5 对照以该包为准；**更正**：契约槽位共 8 个（6 个 owner props 类型 + action/close 复用 Header props），P2-2 措辞已同步。未核验：`navIcon(id)` 映射、`shell-contract.ts` 字段全集、实际注册 onboarding 的功能包清单 |
| C-3 多选/未读/置顶负向结论 | 两目录逐文件复核确认：多选/批量、未读标记、置顶均不存在；唯一行隐藏机制是 archive | `ui-workspace/src/client`（13 文件）与 `ui-sidebar/src/client`（5 文件）全量关键词 grep 零命中（`selectedIds` 仅视图选项菜单 `WorkspaceBrowser.tsx:187`；`pin*` 均为菜单布局/固定类注释） | 3-19 不采纳与 §8 行升级为「目录级复核」证据；P1-9 明确不引入多选/未读。未核验：两包 tests/、`Menu.selectedIds` 的多选语义 |
| C-4 `/export` 命令 | 词条与 builtin 映射在 `ui-commands`；执行落在 `session-log-export` 包（GET/HEAD `/api/session.export`），范围=根会话+子会话+附件，fflate 流式 Zip（默认级别 6）；已读范围未见内容级脱敏 | `ui-commands/src/client/locales.ts:17,23,29`、`resolution.ts:12`；`session-log-export/src/index.ts:42-43,79-101,113-166`、`archive.ts:1-15,16-18,24,100-136,186-196` | 命令系统（5-12/6-6）实现参照明确；若未来做导出，脱敏需自定。未核验：`archive.ts` 中段、`controller.ts` 内部控制流 |
| C-5 `apps/web` 构建与静态资源 | `base:'./'`；无 server/proxy/HMR 配置（独立 serve 被插件显式拒绝）；PWA 可安装静态元数据链完整且被 e2e 锁定；fixture 查询开关仅连接与上传两处 | `apps/web/vite.config.ts:31-38,146-150`；`apps/web/index.html:6-7`、`apps/web/public/manifest.webmanifest:1-16`、`apps/web/tests/pwa-manifest.e2e.ts:8-35`；`connection/src/client/index.ts:190-194`、`file-upload/src/client/runtime.ts:314-319` | 1-2 维持不采纳（属产品取向取舍，非能力缺失）；1-11 维持不采纳（开关面窄、生产误开风险）。未核验：Service Worker/离线运行时、fixture 差异行为 |
| C-6 全局通知位 | `shell.overlay` 仅为 ui-layout 声明的通用 list 槽（click-through），全仓无任何包注册 toast/status 条目；无 stacking、优先级、去重、自动消失、`role=status`/`aria-live` 实现 | `ui-layout/src/client/index.ts:81-91,151-155`；`ui-layout/src/client/AppFrame.tsx:200,230-232`、`AppFrame.module.css:90-99`；跨包 grep 仅注释与示例命中，该位静态为空 | P2-9「单一通知位」为本项目自建能力（无可移植实现），不得引用目标实现作参照；2-6 维持 P2-9。未核验：slot registry 内部排序规则 |
| C-7 审批与会话运行时状态 | 审批卡渲染/提交/abort、会话级 pending 注册表、运行态词汇与工具行状态渲染均已核验；**目标项目无审批超时实现**（取消经 AbortSignal） | `ui-approval/src/client/ApprovalPanel.tsx:12-54`、`contract/slots.ts:68-159`、`index.ts:35-92`；`ui-session/src/client/index.ts:69-102,304-323,366-386`；`ui-conversation/src/client/locales.ts:98-103,145,162-168`、`ui-tool/src/client/tool/components/ToolRow.tsx:90-109,197` | P1-7 交互与状态表达对齐有据；`expired` 30min 超时为本项目新增能力（后端已就绪，§6.3 P2-1A）。未核验：ui-chat 上层是否另有运行态入口、completed 显式分支 |

**复核后仍存的未核验子项**（均不影响任务划分与排期）：`navIcon` 映射、slot registry 内部排序、`apps/web` 的 SW/离线运行时、fixture 差异行为、`archive.ts`/`controller.ts` 中段控制流、ui-chat 上层运行态入口。本轮未修改 deepseek-harness 任何文件；两份报告含完整文件清单与关键词 grep 记录，可复查。
