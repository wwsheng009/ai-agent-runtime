// 由 components/workspace/settings/harness-settings-page.tsx 机械拆分而来（P0-2），仅搬迁不改语义。

import { type RuntimeClientIdentity } from "@/lib/runtime-client";

export type HarnessSettingsPageProps = {
  runtimeClient: RuntimeClientIdentity;
};
